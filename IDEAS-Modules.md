# IDEAS-Modules — 远期假说（北极星，非实验）

状态：**远期假说档案，2026-09-16 归档**。
用途：记录长期方向性思考，**不参与任何当前实验的设计、判据或结论登记**。
纪律：任何实验的预注册、DESIGN、判据文本中**不得引用本文件**作为依据；
      除非用户显式拍板"启用某条假说为当前实验方向"，本文件的内容不进入主线。
触发：2026-09-15/16 用户与外部 AI 推演所得的远期图谱。

> **【2026-09-16 纪律强化】本文件内容绝对禁止直接作为任何实验（含 E43）的实验假设。**
> E43 的预注册必须严格围绕"抑制控制 = 负值注入对轨迹的压制效果"，
> 遵循先判据后代码的铁律——与本文档的远期假说无关。

---

## 0. 一句话

从"共现绑定 + 激活扩散"两条原语出发，**如果底层坐标轴本身具有正负对称性、
如果引入时间延迟与多步交叉、如果多项动态平衡共存**，系统可能自发分化出
更高级的认知结构。这是**假说**，不是**结论**；是**北极星**，不是**路线图**。

---

## 1. 四步推演

### 1.1 第一步：正负坐标轴对称 → 有向力场（因果与归宿）

**假说**：底层同一坐标轴（正/负）的演化，可能产生"有向力场"——
方向性不再外加（如 L1 的方向计数），而是从坐标轴本身的不对称性涌现。

**与已有边界的关系**：B12（非负权重无 WTA）、B33（max 吸收 + 种子固定下到达值层
造不出互斥）说的是**非负权重**与**到达值算式**；本假说说的是**正负都有**且**坐标轴
本身的演化**。两者不矛盾，本假说是更远一层的假设。

**未测**：如何定义"坐标轴的不对称"？"有向力场"的可判定形式是什么？

### 1.2 第二步：时间延迟 + 多步交叉 → 动态平衡/神经振荡

**假说**：若允许激活在多步之间交叉（非单次扩散）、且引入时间延迟，
系统可能产生**周期性动态平衡**（类似脑电波 / 神经振荡）。

**当前状态**：L0 扩散是单次 BFS；E25 模块级动力学是**静态收敛**，不是周期；
振荡需要时间轴（当前 L0 无时间轴，只有 last_seen 时间戳）。

**未测**：延迟参数从哪来？多步交叉是否算改架构（L0-SPEC §1）？

### 1.3 第三步：多项动态平衡共存 → 认知模块分化

**假说**：若多个动态平衡可以**同时共存**（不是 E25 的"每个模块各有两稳态"，
而是**多个独立振荡在系统里共存**），系统可能自发分化出不同认知模块——
如"逻辑推理" vs "直觉情绪"这样的分野。

**当前状态**：E25 已证模块级双稳存在；E24/E29 已证模块可被谱聚类读出；
但"多个动态平衡共存产生认知模块分化"是**量的飞跃**（从"结构模块"到"功能模块"）。

**未测**：共存如何定义？"逻辑 vs 直觉"的可判定形式是什么？

### 1.4 第四步：底层同源、上层异构的动态路线网络

**假说**：系统的最终形态是**底层同源**（同一套共现 + 扩散原语）+ **上层异构**
（不同上层回路处理不同模态/任务）的动态路线网络。

**当前状态**：BOUNDARY §8 已有"底层同源，上层异构"的工作假说；
E23（组合律）已证 L1/L2/L4 可共存且可分别归因。本步是既有工作假说的**远期延伸**
（不止 L1/L2/L4，还有未来的层）。

---

## 2. 与当前工作的关系：北极星 vs 探照灯

- **北极星**：给出长期方向，**不指引具体路径**；
- **探照灯**：当前实验（E0–E41）照亮的是脚下几米。

**纪律**：

1. **不介入当前实验**：任何实验的预注册、DESIGN、判据**不得引用本文件**。
2. **不追溯适用**：本文件不改变任何已登记条目的范围限定。
3. **不主动排期**：除非用户显式拍板，本文件的内容不成为 E42+ 的候选方向。
4. **可被修正**：本文件是假说，允许将来被推翻、修正或替换。

---

## 3. 用户原话（时间戳：2026-09-16）

> 底层同一坐标轴（正负）的演化，可能会产生"有向力场"（因果与归宿）。
> 如果引入多步交叉与时间延迟，可能会产生"动态平衡"（类似于脑电波/神经振荡）。
> 多相位/多项动态平衡共存，可能会自发分化出不同的认知模块（逻辑推理 vs 直觉情绪）。
> 当前任务：把 E42 的"组合符号"跑通，先把物理坐标系的微观规律彻底搞清楚。

---

## 4. 核心理论闭环（2026-09-16 补）

昨晚的推演在 E42 收尾时被彻底证实。三条路径共享同一个 L0 底座，
仅由注入方向、带值规则和采样方式决定"时间感"与"认知模块"的分化：

· 正向传播（+1）= 未来 / 推断 / 演绎（逻辑向外扩散）
· 反向传播（-1）= 过去 / 解释 / 溯因（寻找因果源头）
· 全局采样（能量势）= 现在 / 想象（跨越逻辑闭包的概率坍缩）

【纪律】以上为北极星级远期假说，**绝对禁止直接作为 E43 的实验假设**。

---

## 5. 突破泛化瓶颈的路径（2026-09-16 补）

· 前人的教训：试图让底层承担泛化，最终陷入容量爆炸或恒等式陷阱
· 我们的解法：底层只负责闭包与记忆，泛化由上层（L5 组合符号）完成
· E42 实现了"结构泛化"的第一步（无中生有组合概念 C）
· 下一步关键：泛化必须依赖抑制控制（反向传播 / 负值注入），
  这是凸显抽象概念（C）、压平背景噪声的物理前提

---

## 6. 远期模块分化假说（2026-09-16 补）

· 多相位、多频率的正负反馈循环（动态平衡 / 神经振荡），
  可能自发分化出"逻辑推理"、"直觉情绪"、"工作记忆"等不同认知模块
· 底层同源，上层异构
