# E27 设计：上下文窗口边界（B01 正式化，k_min = k*）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-13。
依赖：L1 `order_layer.py`（相邻方向计数）、L2 `context_layer.py`（双前缀三元组）。
**无 numpy 依赖**（纯计数）；**不改任何层文件**。

---

## 0. 前置决定（三条 + 两处补充）

### 决定一：必须有阳性对照（本实验最容易翻车的地方）

本实验的结论是**阴性**的（现有层分不开长上下文）。阴性结论最大的风险不是机制，
**是工具坏了给出一个看起来合理的数**（B17/B18 的教训）。所以判据里必须有阳性对照：

```text
k*=2 语料上 L2 决定度 = 1.00；
对每个 k*，probe-k（k >= k*）决定度 = 1.00。
阳性对照不过 ⇒ 判为"实验工具坏"，不许把阴性当结论。
```

### 决定二：probe-k 是**实验侧只读探针**，不是新层

k>=3 的前缀计数只在实验脚本里现算现用，**不写入任何层文件、不在 LAYERS 里登记为 L3**。
沿用 E14 的先例（"不改层文件，只在实验里做只读比较"）。
否则 E27 就从"量化边界"变成"偷偷加能力"，违反 BOUNDARY §1。

### 决定三：决定度口径写死

口径不预先定死，就会重演预检第一版的错误（把两个不同键的计数都当成 100，
对比度全 0——**不报错，给出看起来合理的数**）。预注册如下：

```text
键 (key)       = 目标之前最近的 k 个 token（k = 1..5）
语料构造规则   = 两条场景的中间 token 完全相同，只有最远那一个 token 不同
决定度         = 给定键之后，正确目标的概率（0.50 = 纯歧义；1.00 = 完全确定）
对比度         = |a(C) - a(D)| / (a(C) + a(D))；分母为 0 时记 0
```

### 补充一（用户）：P2 的"严格 0"要给容差

浮点下 `0.4500 - 0.4500` 未必精确为 0，判据写成 `|对比度| < 1e-9`，
与 E26 的零耦合判据同口径。

### 补充二（用户）：补 k*=1 语料，覆盖律的最小端

**但语料要改一处**：`B C` / `B D` 实测在**任何 k 下都是 0.50**——它没有判别 token，
是"纯分叉对照"，不是 k*=1。k*=1 的正确语料是 `uX C` / `uY D`
（判别 token 与目标相邻，**L1 就该够用**）。
两条都保留：`uX C / uY D` 作律的最小端，`B C / B D` 作律之外的**纯分叉对照行**。

---

## 1. 要解释的现象（宪章四问）

1. **基底参与的步骤**：绑定（共现 / 相邻方向 / 三元组计数）与传播（L1 激活）。
2. **需要外部模块吗**：不需要。
3. **结论归谁**：归"**现有层（L0/L1/L2）的能力上限**"；probe-k 的能力**不归基底**，
   它是实验侧的读数工具。
4. **做到哪一步算停**：算到"决定度阶梯 + k_min = k*"就停，**不实现任何新层**。

**要解释的现象**：判别性上下文离决策点越远，需要多长的窗口才能消歧？
现有层的天花板在哪里？

**可证伪预期**：若在 k* >= 3 的语料上，L1 或 L2 能给出 >0.5 的决定度，
则 B01 的表述需要改写（现有层天花板不止 2）。预检提示不会发生，但判据照留。

---

## 2. 语料（受控阶梯）

```text
k* = 1 : [uX, C]                 vs [uY, D]
k* = 2 : [uX, s1, C]             vs [uY, s1, D]
k* = 3 : [uX, s1, s2, C]         vs [uY, s1, s2, D]      <- 你最初给的语料同型
k* = 4 : [uX, s1, s2, s3, C]     vs [uY, s1, s2, s3, D]
k* = 5 : [uX, s1, s2, s3, s4, C] vs [uY, s1, s2, s3, s4, D]
对照   : [B, C]                  vs [B, D]               <- 无判别 token（纯分叉）
每条场景 100 次；两场景之间除"最远 token"与目标外完全对称
```

符号定义（补充，避免读设计的人要猜）：

```text
uX / uY      = 判别 token（两条场景不同；uX 属场景 X，uY 属场景 Y）
s1 / s2 / …  = 共享 token（两条场景完全相同；数量 = k* - 1）
C / D        = 目标 token（场景 X 的目标是 C，场景 Y 的目标是 D）
键长不足时    = 若目标之前的可用 token 少于 k 个，键退化为"全部可用 token"，
               并在 JSON 里标 degenerate=true（k*=1 时 k>=2 会命中这条）
```

对称性由构造保证 ⇒ L1 激活对比度在 k* >= 2 时**精确为 0**。

---

## 3. 预检结果（已执行，2026-09-13，只读即算即弃）

| k* | 场景 X / Y | probe-1 | probe-2 | probe-3 | probe-4 | probe-5 | L1 对比度 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `uX C` / `uY D` | **1.00** | 1.00 | 1.00 | 1.00 | 1.00 | **1.0000** |
| 2 | `uX s1 C` / `uY s1 D` | 0.50 | **1.00** | 1.00 | 1.00 | 1.00 | 0.0000 |
| 3 | `uX s1 s2 C` / `uY s1 s2 D` | 0.50 | 0.50 | **1.00** | 1.00 | 1.00 | 0.0000 |
| 4 | + s3 | 0.50 | 0.50 | 0.50 | **1.00** | 1.00 | 0.0000 |
| 5 | + s4 | 0.50 | 0.50 | 0.50 | 0.50 | **1.00** | 0.0000 |
| 对照 | `B C` / `B D` | 0.50 | 0.50 | 0.50 | 0.50 | 0.50 | 0.0000 |

**律**：`决定度 = 1 当且仅当 k >= k*`（k* ∈ {1..5}）；无判别 token 时恒 0.50。
**现有层天花板 = 2**（L2）。k*=3 语料上的精确数字：L0 0.4500/0.4500、
L1 0.9000/0.9000，对比度都 = 0.0000（不是"差一点"，是逐位对称）。

**k*=1 的层归属（补充）**：k*=1 时 **L0 就够**了——`uX–C` 共现 100 次、`uX–D` 共现 0 次，
所以从 `uX` 出发 L0 给出的 C/D 对比度已是 1.0；L1 只是给出**同样的**结果
（方向计数 `uX→C` = 100、`uX→D` = 0）。因此**律的最小端由 L0/L1 共同封住**，
不能写成"L1 才够"。

---

## 4. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1 | 语料构造核查：两场景中间 token 逐位相同、只有最远 token 不同；对照行无判别 token | 通过 | 语料 bug，改语料，不算结论 |
| P2 | **阴性复现（B01）**：k*=3 语料上 L1 与 L2 的 C/D 对比度 | `\|对比度\| < 1e-9` | 记录负结果并复查语料对称性 |
| P3 | **阳性对照**：k*=2 上 L2 决定度 = 1.00；且对每个 k*，probe-k（k >= k*）= 1.00 | 通过 | **判为工具坏**，阴性结论作废 |
| P4 | **定量律**：k* ∈ {1,2,3,4,5} 上 `k_min = k*`（k<k* 时 `\|决定度 - 0.50\| < 1e-6`；k>=k* 时 `决定度 >= 1 - 1e-6`）；对照行所有 k 都满足 `\|决定度 - 0.50\| < 1e-6` | 通过 | 记录反例；若 k_min ≠ k* 则 B01 表述要改写 |
| P5 | 只读：不改 L0/L1/L2 层文件、不新增层文件；跑前跑后节点/连接/计数一致 | 通过 | 直接 FAIL |
| P6 | 登记：写入 C27，并把 B01 升级为带数字版本（文本见 §7） | 通过 | — |
| 辅助 | **不平衡暴露下的决定度偏置**（k*=3、k=2：场景 X:Y 暴露比 1:1 / 1:2 / 1:5） | 仅记录 | — |

**关于辅助曲线（替换说明）**：原设想是"样本量递减 100→1/5/20 看决定度偏离多少"，
但本实验的决定度是**计数精确量**——语料构造确定时，重复次数怎么减，决定度都精确是
0.50 或 1.00，那条曲线**按构造恒无信息量**。改成"不平衡暴露"才有读数：
两个场景对**同一个键**的贡献比，直接决定决定度（1:1 → 0.50；1:2 → 0.33；1:5 → 0.17），
说明"决定度 = 0.50"这个歧义值的前提是**两条场景暴露均衡**。
它只是算术性质，仅记录，**不参与判据**，随时可删。

---

## 5. 预注册参数

```text
每场景重复 = 100 次              决定度取值 = {0.50, 1.00}（精确）
k* 阶梯 = 1 / 2 / 3 / 4 / 5 + 纯分叉对照（无判别 token）
键长扫描 k = 1..5                P2 容差 = 1e-9（与 E26 零耦合判据同口径）
L1 hop_decay = 0.9（默认）       K（成块阈值）不参与本实验
探针实现 = 实验脚本内现算的只读计数（不进层文件）
```

---

## 6. 产出物

- `experiments/exp27_context_window.py` + `outputs/exp27_context_window.json`；
- 通过或失败都登记 CLAIMS-REGISTRY；
- 不改 L0/L1/L2；不新增层；不引入语义/stub/奖励。

---

## 7. 拟登记的表述（用户给定）

```text
C27：在"中间 token 共享、只有最远 token 不同"的受控语料族下，
现有层（L0/L1/L2）的最大可用消歧前缀长度 = 2；
当判别上下文到决策点的距离 k* >= 3 时，决定度恒为 0.50；
最小可消歧前缀长度 k_min 精确等于 k*（k* ∈ {1,2,3,4,5} 上验证）。
阳性对照：k*=2 时 L2 决定度 = 1.00。

B01（升级）：现有层最大前缀 = 2；k* >= 3 时决定度恒为 0.50；
k_min = k*；k* >= 3 的上下文消歧需要最小窗口 k* 的新层。
```

---

## 8. 边界与不做

- **不实现新层**（不做 L3 / k-前缀层）——本实验只量化边界；
- probe-k 的结果**不得**写成"基底具备长上下文能力"；
- **`k_min = k*` 是律，`天花板 = 2` 是当前层集合的现状**：加一层新层
  （最小窗口 = k*）后天花板会跟着移动，**但那不属于 E27 的范围**——
  本实验只给出"现在到哪儿为止"，不给出"能到哪儿";
- 不声称任何"理解上下文"的语义结论（B07）。
