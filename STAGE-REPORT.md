# STAGE-REPORT — 当前架构的能/不能边界（阶段收束）

日期：2026-09-16。状态：**阶段报告**，非实验，非规格。
用途：E0–E48 的收口文档——把"这套架构做了什么、做不了什么、为什么做不到"
集中成一份，给论文写作 / 新接手人 / 未来开新架构时用。
与 [HANDOFF.md](./HANDOFF.md) 的分工：HANDOFF 是"入口"（怎么接手），
本文件是"边界"（哪里到头了）。

---

## 0. 一句话

```text
L0 是"无状态、无时间轴、不写回"的纯函数层——
这既是它 45 个编号全部实验可复现的原因，也是它机制探索的边界所在。
```

---

## 1. 规模与状态

| 项 | 数量 / 状态 |
| --- | --- |
| 实验 | E0–E48（**45 个编号 / 46 个模块**，含 E6b）——全部 PASS |
| C 条目 | 42 条：C01–C31 + C33–C43（C32 未立，历史空号） |
| B 条目 | 31 条：B01–B21 + B23/B24/B26/B30 + B32–B37 |
| 未触发编号 | 6 个：B22 / B25 / B27 / B28 / B29 / B31（预注册纪律的副产品） |
| 方法学类别 | 6 类（"不报错的坏结果"）+ 1 个流程条目（口径预检） |
| 纪律 | 十条 + 提交规范（HANDOFF §8） |
| L0 实现版本 | v1 冻结（`cf99a07a0e60fd4d`）/ v2（`ee6df589eb2f6983`）/ v3（`085be73a06f0694c`） |
| 上层 | L1 方向 / L2 三元组 / L4 组块 / L5 组合符号（`81e350497e9e9960`） |
| 规格 | L0-SPEC / LAYERS-SPEC / BOUNDARY / NEXT-PLAN |
| 归档 FAIL | exp32 v1 / exp33 v1 / exp39 v1 / exp40 v1 |
| 三次封箱（结构性不可能） | E44（自动选注入点）/ E45（L5 自动建边）/ E46（时间序）—— 均只读预检否掉，不占实验号 |
| 第四次封箱（可行但无新内容） | **E47（分类 + 值序组合态）**—— 组合输入 = 一个单独输入在另一个单独输入集合上的**投影** ⇒ 简单叠加，不占实验号 |
| E48 已落 | 组合的嵌套（`combine(C, R) → D`）—— 登记 **C43**；并答掉"路线②是否必须开"（**本命题上不必开**，见 §5 生成） |

---

## 2. 能做什么

### 2.1 基础能力（C01–C31）

```text
· 链自组织 / 频率调制 / 阈值可访问性           C01–C02
· 双向等强（无向）；方向不对称来自 L1 计数        C03–C04
· 分支诚实歧义；上下文偏置；情景互译             C05–C07
· 密集脉冲 / 枢纽 / 补全 / 衰减 / 干扰            C08–C12
· 组块与双写还原 / 重复标记 / 双前缀消歧          C13–C16
· 角色绑定 / 叠词外推（外部模块）/ 情绪调制       C17–C19
· 消融 / 规模 / 统计                             C20–C22
· 组合律（多层共存且可归因）                     C23
· 模块可谱聚类读出                              C24
· 模块内双稳 + 滞回                             C25
· 模块间协调（EI 剂量响应）                     C26
· 上下文窗口边界（k_min = k*；天花板 = 2）       C27
· max 与 sum 是读取端两种选择                    C28
· 多尺度读出（层级 = k 的函数）                  C29
· 闭包是设计不变量                              C30
· 自动尺度（弱版本，BIC 3/3）                    C31
```

### 2.2 四块骨头

```text
基础设施（跨块）：
  C37  L0 v2 带值种子入口（E41 值序 / E42 组合 均建立其上）

生成：
  C33（条件采样：max 给 1 个、采样给 4 个）
  C39（无条件采样：8 状态全开，5 个 max 到不了）
C41（组合符号：景观外新对象）
C43（组合的嵌套：符号本身可再当模式，层级不膨胀，L0 可及强度按 0.9^d）

工作记忆：
  C34（跨次携带）
  C35（多步串联 = 载体）
  C36（顺序标记：分类可测）
  C40（值序可测）

抑制控制：
  C38（可及性压制：reach 7→1）
  C42（轨迹压制：割点注入负值 ⇒ 分支下游 ≤0）

多模态：
  已决不做（见 §5）
```

---

## 3. 做不了什么——按根因分组

**不是按实验编号，是按"为什么"。**

### 根因一：非负权重 + max 吸收（数学层）

```text
B12  非负 L0 权重下无 WTA 互斥切换
B33  竞争性抑制在到达值算式层不可实现（max 吸收 + 种子固定）
     · 构造性上界：单跳到达 ≤ 0.9 < 1.0；任何 ≤0 抑制项只会更小

⇒ 两条的共同点：max 吸收 + 非负权重决定了"竞争"在数学上无法表达。
  要互斥，必须改吸收判据（L0-SPEC §2 ⑥ 明写"必须保留 max 吸收"）⇒ 越界。
```

### 根因二：L0 是静态图，`activate()` 是它的确定函数且不写回（信息层）

```text
B20  谱聚类需外部给定 k；层数分辨率有上限
B23  eigengap 在嵌套语料上系统性偏向粗层
B24  可读性不能由 r_j 完全预测；随嵌套深度急剧下降
B36  块状语料上，负值注入的有效性不由任何标准中心性量预测
     （同度数两点效果差 6 倍；真实决定因素是"弱边入口"）

⇒ 结构论证（E44/E45）：
  · connections 就是边表 ⇒ "从图算"是平凡
  · activate() 是图+种子的确定函数，且【不写回】（clock 不变、connections 不变、
    无激活历史字段）⇒ 任何对 activate() 输出的统计量都是图的函数，不含图外信息

⇒ "自动化"类命题（自动选点 / 自动建边）在信息层不可能非平凡。
```

### 根因三：粗粒化 / 读出层级的边界（跨层信息丢失）

```text
B32  块均值的模块级 W3 的 Boltzmann 概率不是节点级分布对模块符号的正确边缘化
     （偏移 d = +0.059548；由跨模块耦合产生；置 0 跨模块后精确塌成 0.25 均匀）

⇒ 与根因二不同：B32 是"同一份统计的两种读出层级不等价"，
  不是"行为不含图外信息"。这类边界在项目里目前只有 B32 一条。
```

### 根因四：基底无时间轴（时间层）

```text
C35 的边界（"无时间顺序"，E46 后升级为结构定理）：
  · 基底输入空间是 {node: value} ⇒ 时间不经过这个字典
  · activate() 是无状态纯函数 ⇒ 两次相同输入必然相同输出
  · 同集合、同值、不同时间 → 差异 = 0（实测）
⇒ "时间序"若要可测，只能编进值（= C40 值序）或集合（= C36 分类）——
  两者已覆盖，没有"只属于时间"的通道。
```

### 根因五：采样器 vs 目标分布（方法学层，不是架构限制）

```text
B26  抽样覆盖不足——单点 Gibbs 在低温弱耦合下不混合，
     1000 样本只覆盖 1 个状态且不报错
     · 解法：并行回火（E40 已用）；报告抽样覆盖
```

### 根因六：实现陷阱（已修，不是架构限制）

```text
B14  收敛判据实现缺陷（比较同一数组对象）
B17  EI 状态索引约定错位（块对角假阳性 1.82）
B18  TE 边际化遗漏（负 TE）
B19  簇编号任意性（ARI=1.000 却算出 6/15）
B21  失败语义层间不统一（KeyError / {} / None / ValueError）
```

### 根因七：L5 是单向压缩器（**存储层**，不是 L0）

```text
B37  组合符号对源模式不可分解（coactivate 只存成员并集；符号 id 只由成员集决定）
     · 单符号 79 种有序 (P,Q) → 同一符号；歧义封闭形式 = 2^|P△Q|
     · 跨符号（三角形）只把 79 → 63（布尔 OR 不可逆），且须外部告知配对结构
⇒ 与根因二的区别：根因二是"L0 不写回 ⇒ 行为不含图外信息"（信息从未被记录）；
  B37 是"L5 在【写入时】就丢弃源模式"（信息曾经存在，但没存）。
⇒ 形态一句话：L5 是单向压缩器 —— 可迭代（E48），不可逆（B37）。
```

### 【其余 17 条 B 条目说明】

```text
本节只列【有结构性根因】的 B 条目（14 条）。
其余 17 条（B01–B11、B13、B15、B16、B30、B34、B35）为：
  · B01–B07：第一轮旧边界（OVERVIEW §4.1 标为"已全部清算"；B01 已被 E27 升级、
    B05 已并入 C30）
  · B08–B11：E23/E24 的方法学边界（ICA/LCA/稳定性）
  · B13 / B15 / B16：E25/E26 的探索性与结构性边界
  · B30 / B34 / B35：E37/E40/E42 的语义与结构边界
    （v2 常量语义 / 基态流形分支 / L5 last-write-wins）
  · B02 / B05：指针条目（已并入 C28 / C30）
分类与全文见 CLAIMS-REGISTRY。
```

---

## 4. 三条结构性论证

E44/E45/E46 三次封箱的净收益——**不是"我们没做出来"，是"在该架构下不可能"**。

```text
论证一（E44/E45）：L0 是静态图，activate() 是它的确定函数且不写回
  ⇒ 任何"从行为推断结构"的路线不可能带来新信息。
  （实测：行为统计检出 31 对，全部 ⊂ 静态边集 34 条，行为独有 = 0）

论证二（E46）：基底输入空间是 {node: value}，无时间维度
  ⇒ 时间不经由任何通道进入；两次相同输入必然相同输出。
  （实测：同集合、同值、不同时间 → 差异 = 0）

论证三（C35 升级）：项目里"顺序"已有的两种可测形式都要求顺序【学进基底】
  · L1 OrderLayer：有向相邻计数（学习顺序进了图）
  · L4 ChunkLayer：chunks[cid] 有序成员表（块内顺序）
  —— W 的【痕迹顺序】不属于任何一种。
  ⇒ C35 的"无时间顺序"从经验观察升级为结构定理。
```

**三条合起来一句话**：

```text
L0 是"无状态、无时间轴、不写回"的纯函数层——
这既是它 45 个编号全部实验可复现的原因，也是它机制探索的边界所在。
```

---

## 5. 已决事项

```text
多模态：已决【不做】。
  理由：在 L0 架构下只能提升工程学角度（能接图像/声音），
       不能提升认知/科学角度（不带来新的机制发现）。
  若将来项目转入"工程化"阶段，可重启评估；
  当前阶段（找机制规律）不列入候选。

抑制控制：已收口。
  能做的做完（C38 + C42）；做不到的钉死（B33 + B36）。
  残留项：真互斥（WTA）需架构级变更；注入点手工选——B36 证明这不是懒，是没有可靠替代。

工作记忆：已收口。
  分类（C36）+ 值序（C40）两轴可测；时间序经 E46 证明在现有架构下不可测。
```

---

## 6. 未触发的预留号

```text
B22 / B25 / B27 / B28 / B29 / B31
来源：E30 / E33 / E34 / E35 / E36 / E37 各预留一个 FAIL 分支编号，
     实验通过则落 C，编号空着。
性质：预注册纪律的副产品，不是登记表失控。
唯一来源：CLAIMS-REGISTRY 的"未触发编号总表"（不再用编号扫描推断）。
```

---

## 7. 门槛：若要继续机制探索，必须改什么

**本节只点出门槛位置，不展开设计。**（开新架构属下一阶段决定，不在本报告范围。）

三次否证的共同根因指向同一堵墙：

```text
L0 的三条架构属性：
  ① 无状态（activate 不写回）
  ② 无时间轴（输入空间 {node: value}）
  ③ 非负权重 + max 吸收

要突破，至少需要改动其中一条：
  · 改 ① → 给基底加写回（架构级）
  · 改 ② → 给基底加时间轴（架构级）
  · 改 ③ → 改吸收判据（L0-SPEC §2 ⑥ 明写禁止）

三者都在 L0-SPEC §1 的架构边界之外。
⇒ 当前架构的机制探索已收满；下一步若要继续，是"开新架构"级别的决定。
```

---

## 8. 怎么用这份文档

```text
· 论文写作：§2（能做什么）+ §3（按根因分组的不可能）+ §4（结构性论证）
· 新接手人：本文件 + HANDOFF（入口）+ CLAIMS-REGISTRY（唯一结论来源）
· 开新架构前：§7（门槛在哪）+ L0-SPEC §5（改 L0 的五步）
· 复现：见 HANDOFF §7 的运行环境与校验命令
```
