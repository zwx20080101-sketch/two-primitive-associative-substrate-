# E37 设计：v2 带值种子入口（常量语义）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：`L0-SPEC.md`（架构/实现之分、v1/v2 版本纪律）、`DESIGN-E37-IMPACT.md`（影响评估）、
E1 的 26 字母链语料、E27/E30 的只读口径。**不修改 v1 文件；v2 是平行分支。**

---

## 0. 四个已定死的前置

```text
① 语义 = 常量：种子值在传播中【不被抬高、也不被降低】；
② v2 是平行文件（synapse_net_v2.py），v1 一字不动；
③ 不混用：一个实验要么在 v1 上跑、要么在 v2 上跑；
④ 只读记两个哈希：synapse_net.py（v1，cf99a07a0e60fd4d）与 synapse_net_v2.py（v2，首记录）。
```

## 0.1 实现形态的说明（**偏离"复制 v1 + 改 6–10 行"**，待确认）

```text
本设计采用【子类覆盖】：synapse_net_v2.py 里 class SynapseNet(_V1.SynapseNet)，
只覆盖 activate()，其余（learn / strength / effective_weight / state_snapshot）全部继承。

理由（两条，都是本项目一贯的取舍）：
  ① 避免"第二份实现"：复制 170 行会产生两份 learn/strength，一旦漂移就是 B17 那类问题；
  ② 改动面可见：v2 的 diff 就是"种子入口"这一段，符合 L0-SPEC §2 对第①条的定位。
代价：v2 依赖 v1 文件存在（v1 必须保持冻结——本来就要求）。
若更想要自包含副本，改成复制版即可（改动面一样，但多 170 行重复代码）。
```

---

## 1. 命题

```text
v2 的带值种子入口保留种子值（常量语义），改变传播路径的【绝对水平】；
名字列表形式向后兼容。
```

---

## 2. 判据（预注册）

### P1a 文件与接口

```text
v1 文件 synapse_net.py 不动：sha256 = cf99a07a0e60fd4d（跑前跑后一致）
v2 文件 synapse_net_v2.py 新建：sha256 记录在案（首记录）
v2 的 activate 接受三种输入：str | list[str] | dict[str, float]
```

### P1b 常量语义（核心；**已修正为可分辨判据**）

```text
【原判据是空过的】activate({"A":0.5,"B":0.5})：B→A 到达值 = 0.5×0.9 = 0.45 < 0.5，
浮动/下界实现也会通过 → 测不出常量语义。
【修正后】用"能抬得动"的配置（A 与 B 在链上相邻）：
  配置甲 {"A":0.5, "B":0.9}：B→A 到达 0.81 > 0.5 → 常量下 A 仍 0.5（下界/浮动会变 0.81）
  配置乙 {"A":0.9, "B":0.5}：A→B 到达 0.81 > 0.5 → 常量下 B 仍 0.5
判据：两组配置下 A、B 都精确等于传入值（容差 1e-12）
```

### P2 值保留（主判据）

```text
语料：E1 的 26 字母链（A–Z 相邻对 ×100）
输入：{"Z":1.0, "A":1.0, "B":0.9, "C":0.81}
v1 对照：A=1.0, B=1.0, C=1.0（全部被抹平）
v2 期望：A=1.0, B=0.9, C=0.81（值被保留）
判据：v2 下 B=0.9、C=0.81（容差 1e-12），且与 v1 相比 B、C 均不同
```

### P3 排序差异（v2 越过 C36 的边界）

```text
对照配置（节点集合相同 {A,B,C}，仅值的对应关系翻转）：
  配置 1：{"Z":1.0, "A":1.0,  "B":0.9, "C":0.81}
  配置 2：{"Z":1.0, "A":0.81, "B":0.9, "C":1.0}
判据：两配置输出至少有一个节点差异 > 1e-12
     （v1 下两配置完全相同——值被抹平；这是 C36 在 v1 下测不到的）
```

### P4 向后兼容

```text
activate(["Z","A","B","C"]) 在 v2 下的输出 = 在 v1 下的输出（逐位相同，容差 1e-12）
覆盖：第一轮 11 处多节点种子调用全部用名字列表 → 语义不变
```

### P5 整表比较（预检预测的正式验证）

```text
语料：26 字母链；输入：{"Z":1.0, "A":1.0, "B":0.9, "C":0.81}
预检预测（v2 相对 v1）：变化 13 个（B,C,D,E,F,G,H,I,J,K,L,M,N）；不变 13 个（A 与 O..Z）
判据：E37 输出完整 26 节点比较表；变化数 = 13 且不变数 = 13
若不符 → 记录偏差 + 诊断（v2 实现是否与预检的 activate_v2 有差异）
```

### P6 只读（两哈希口径）

```text
synapse_net.py    = cf99a07a0e60fd4d（前后一致）
synapse_net_v2.py = <首记录>
E37 的 JSON 同时记两个哈希，与 E27–E36 口径一致
```

### P7 双向结局

```text
通过（P2 + P3 + P4 全过）→ C37
不通过 → B31（v2 实现与预期不符；记录哪条挂、实测值、诊断三步：
         先查 v2 实现 → 再查预检对照 activate_v2 → 再谈机制）
B30【无论通过与否都登记】：常量语义的直接后果——种子节点在传播中"冻结"，
         不会被任何路径强化（即使某条路径的值更高）。这是语义选择，不是 bug。
```

---

## 3. 预注册参数

```text
语料：A–Z 相邻对 ×100（26 节点 / 25 边）
v2 语义：常量（种子值不可变）；名字列表 → 1.0（向后兼容）
容差：1e-12（逐位）          只读：v1 哈希不变 + v2 哈希首记录
运行环境：PYTHONHASHSEED = 0
```

---

## 4. 双向结局（跑前写死）

```text
C37：L0 的种子入口支持带值 mapping（v2 分支）。种子值在传播中保持【常量】
     （不被抬高或降低）；名字列表形式向后兼容（仍全部 1.0），第一轮 11 处
     多节点种子调用语义不变。
     带值种子改变传播路径的【绝对水平】：26 字母链上，v2 与 v1 相比有 13 个节点值变化
     （A 侧半条链 B..N），13 个不变（A 与 Z 侧 O..Z）。因此"传播路径与种子入口无关"
     不成立——路径的绝对水平由种子值决定。
     "排序差异"在 v2 下可测（配置 1 与配置 2 输出不同），这是 v1（值抹平）下做不到的
     （C36 的边界被 v2 越过）。
     范围限定：v2 分支；v1 冻结不变（E0–E36 全部有效）。

B30（与 C37 同时登记，不是失败分支）：v2 的常量语义导致种子节点在传播中"冻结"——
     种子节点不会被任何传播路径强化（即使某条路径的值更高）。这是常量语义的直接后果，
     不是 bug。范围限定：v2 分支。
```

---

## 5. 产出物

- `synapse_net_v2.py`（平行分支，不动 v1）；
- `experiments/exp37_seed_values.py` + `outputs/exp37_seed_values.json`；
- 登记：**C37 + B30**（B30 无条件登记）；
- **不改任何 v1 文件**；不新增层；不引入 stub/语义/奖励。

---

## 6. 边界与不做

- **不改 v1**（L0-SPEC §5 第 2 步：新建平行文件）；
- **不做"种子可被抬高/降低"的变体**（下界/浮动）——那是另外的语义分支，需另立实验；
- **不把 v2 当成 v1 的替代**：v2 是分支，v1 仍是 E0–E36 的证据基座；
- 不声称任何"意识/理解"类结论（B07）。
