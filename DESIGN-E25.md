# E25 设计：S2 模块内动力学（平均场 / Hopfield / 双稳态）

状态：**设计稿（待实现）**。依赖 E24：S2 的模块划分来自 E24 的谱聚类读出。
三个前置决定在实现前定死（见第 1–3 节），跑后不得更改。

## 1. 决定一：双稳态怎么判（主判据 = 行为，不靠拟合）

动力学模型（模块级平均场）：

```text
dV_i/dt = -V_i + tanh( β · ( Σ_j W_ij V_j + θ_i + I_i ) )
```

- **主判据（硬）**：多初值长时间积分，收敛到**恰好两个吸引子簇**（低态/高态），
  且两簇盆地各占 ≥20% 初值；再做**准静态输入扫描**：I 从低到高（上扫）与
  从高到低（下扫），转变点之差（滞回宽度）≥ 输入范围的 10%。
- **对照（硬）**：单稳模块（无自兴奋）必须**不出现滞回**（滞回宽度 ≤2% 范围）。
- **辅助（不单独作为 PASS）**：拟合 `dV/dt = A V² + B V + C` 并记录判别式
  B²−4AC；只作旁证，避免"拟合出的双稳"变成数据噪声的产物。

## 2. 决定二：谁的状态做动力学、用什么权重

- **主口径**：状态 = **模块平均激活 V_i**（由 L0 激活矩阵按 E24 模块取平均）；
  权重 `W_ij` = 模块间 **L0 共现计数的块均值**（对称化、按最大块均值归一化）。
- **次口径（稳健性，不参与主判据）**：把 `W_ij` 换成 **L1 方向计数对称化**后的
  块均值，重跑同一套判据；若结论与主口径不同，记录为**方向依赖性**，
  不作为主判据失败。

理由：L0 是唯一冻结基底，S2 是它的模块级宏观动力学；L1 是上层统计，
其权重非对称，与 Hopfield 型对称能量不天然兼容，因此只做旁证。

## 3. 决定三：E24 的划分怎么喂给 E25（接口预注册）

```text
同一批经验事件
  → L0 激活时间序列 X (节点 × T)
  → 按 E24 谱聚类划分，对每个模块取节点均值
  → V_i(t)
```

不重新用 L1 方向计数计算主口径（L1 只出现在次口径）。
**依赖规则**：若某语料上 E24 的划分不稳定（多种子 ARI < 0.8），
E25 对该语料不启动，记为"依赖阻塞"，不算机制失败。

## 4. 语料

| 语料 | 用途 | 期望 |
| --- | --- | --- |
| C1 植入模块（E24 的 3 组） | 主测试 | 模块自兴奋足够时出现双稳与滞回 |
| C2 单稳对照（无自兴奋/同质模块） | 对照 | 无双稳、无滞回 |
| C3 互抑变体（两模块强互抑） | 切换测试 | 出现两个稳定切换态（赢者通吃） |

## 5. 指标

- 吸引子簇数（多初值收敛后的聚类数，容差 0.05）；
- 盆地占比（低态/高态的初值比例）；
- 滞回宽度（上扫/下扫转变点之差，按输入范围归一化）；
- 对照滞回宽度；
- 主/次权重口径的结论对比；
- L0/L1 只读检查（跑前跑后节点/连接/计数一致）。

## 6. PASS / FAIL（预注册）

| # | 判据 | 阈值 |
| --- | --- | --- |
| P1 | 接口：模块划分来自 E24、覆盖全部节点、V 为模块均值 | 覆盖 100% |
| P2 | 双稳：C1 恰好两个吸引子簇，盆地各 ≥20% | 通过 |
| P3 | 滞回：C1 滞回宽度 ≥10% 输入范围；C2 ≤2% | 通过 |
| P4 | 切换：C3 出现两个稳定切换态 | 通过 |
| P5 | 次级口径：L1 对称化权重的结论如实记录（可不同） | 记录 |
| P6 | 无越权：L0/L1 只读；S2 只做宏观动力学分析 | 通过 |

## 7. 预注册参数

```text
β = 2.0    dt = 0.01    steps = 5000    多初值 = 200 个
吸引子聚类容差 = 0.05     I 扫描 = [-2, 2]，步长 0.05
滞回判据阈值 = 输入范围的 10%    对照阈值 = 2%
```

## 8. 产出物与边界

- `experiments/exp25_module_dynamics.py` + `outputs/exp25_module_dynamics.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C25 / 负结果 B12 起）；
- 不改 L0/L1；不引入语义/情绪/奖励；不把模块动力学称为"意识/理解"。
