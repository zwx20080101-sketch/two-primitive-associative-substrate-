# E25 设计：S2 模块内动力学（平均场 / Hopfield / 双稳态）

状态：**已实现（7/7 PASS，2026-09-11）**。依赖 E24：S2 的模块划分来自 E24 的谱聚类读出。
三个前置决定在实现前定死（见第 1–3 节），跑后不得更改。

## 1. 决定一：双稳态怎么判（主判据 = 行为，不靠拟合）

动力学模型（模块级平均场）：

```text
dV_i/dt = -V_i + tanh( β · ( Σ_j W_ij V_j + θ_i + I_i ) )
```

**外部控制参数（预注册）**：`I_i` 是外部施加的控制参数，不是从语料统计里
算出来的（扫描 I∈[-2,2] 是人为实验手段）；`θ_i = 0`，无外部偏置。

- **主判据（硬）**：多初值长时间积分，收敛到**恰好两个吸引子簇**（低态/高态），
  且两簇盆地各占 ≥20% 初值；再做**准静态输入扫描**：I 从低到高（上扫）与
  从高到低（下扫），转变点之差（滞回宽度）≥ 输入范围的 10%。
- **对照（硬）**：单稳模块（无自兴奋）必须**不出现滞回**（滞回宽度 ≤2% 范围）。
- **辅助（不单独作为 PASS）**：拟合 `dV/dt = A V² + B V + C` 并记录判别式
  B²−4AC；只作旁证，避免"拟合出的双稳"变成数据噪声的产物。

**准静态扫描协议（预注册，跑后不改）**：I 在 [-2, 2] 上以步长 0.05 扫描；
每个 I 点**从上一个 I 点的稳态出发**，积分 1000 步，取**最后 200 步的平均**
作为该点稳态；转变点定义为稳态值越过中点（0.5）时的 I 值；
滞回宽度 = 上扫转变点 − 下扫转变点，再按输入范围（4.0）归一化。
辅助拟合得到的 A、B、C 与判别式 B²−4AC 一律写入输出 JSON（旁证数据）。

## 2. 决定二：谁的状态做动力学、用什么权重

- **主口径**：状态 = **模块平均激活 V_i**（由 L0 激活矩阵按 E24 模块取平均）；
  权重 `W_ij` = 模块间 **L0 共现计数的块均值**（对称化、按最大块均值归一化）。
- **次口径（稳健性，不参与主判据）**：把 `W_ij` 换成 **L1 方向计数对称化**后的
  块均值，重跑同一套判据；若结论与主口径不同，记录为**方向依赖性**，
  不作为主判据失败。
- **归一化（预注册）**：`W_ij` 按最大块均值归一化，使 `max W = 1`；
  β = 2.0 作用于**归一化之后**的 W。
  操作化补充（实现前定稿）：**归一化尺度统一取 C1 的最大块均值**（=130），
  C2/C3 用同一尺度，否则单模块语料归一化后恒为 1，单稳对照失去意义。

理由：L0 是唯一冻结基底，S2 是它的模块级宏观动力学；L1 是上层统计，
其权重非对称，与 Hopfield 型对称能量不天然兼容，因此只做旁证。

## 3. 决定三：E24 的划分怎么喂给 E25（接口预注册）

```text
同一批经验事件
  → L0 激活时间序列 X (节点 × T)
  → 按 E24 谱聚类划分，对每个模块取节点均值
  → V_i(t)
```

不重新用 L1 方向计数计算主口径（L1 只出现在次口径）。
**依赖规则（预注册）**：

- C1/C3 的"划分稳定性"用**多种子划分之间的一致性**计算（10 个随机种子的
  两两平均 ARI），不依赖真值标签；C1 额外报告与真值的 ARI 作参考。
  若某语料多种子一致性 ARI < 0.8，E25 对该语料不启动，记为"依赖阻塞"，不算机制失败。
- **C2 不走 E24**：C2 是单稳对照，其模块划分**人工指定为单模块**
  （全部节点归一个模块），不调用谱聚类，因此不会触发依赖阻塞规则。

## 3.5 实现前诊断：W 矩阵（已执行，2026-09-11）

用 E24 的 C1 划分取模块块均值并归一化（max=1）：

```text
W_diag = [0.769, 0.692, 1.000]      模块内自兴奋
W_offdiag_max = 0.002               模块间耦合（基本为零）
ρ = max(W_offdiag) / max(W_diag) = 0.002
```

结论与预注册分支：ρ 远小于 0.5 → **弱耦合分支**。此时各模块独立双稳，
200 个初值应收敛到 **2^N = 8 个联合吸引子**（每个模块两态），
而不是 2 个。因此 P2 采用下面的**双分支预注册判据**（由静态 W 诊断决定分支，
不依赖动力学结果，避免事后挑选）。

## 4. 语料

| 语料 | 用途 | 期望 |
| --- | --- | --- |
| C1 植入模块（E24 的 3 组） | 主测试 | 模块自兴奋足够时出现双稳与滞回 |
| C2 单稳对照（单模块，人工指定） | 对照 | 无双稳、无滞回 |
| C3 互抑变体（两模块强互抑） | **探索性观察，不参与 PASS/FAIL** | 记录结果；因 L0 权重非负，预期不出现"赢者通吃"互斥吸引子 |

说明（预注册）：L0 的共现次数恒 ≥0，块均值非负，对称化后仍非负；
Hopfield 型"赢者通吃"需要负的互抑权重，这在主口径下**原理上造不出来**。
因此 C3 只作探索性观察；若未来需要互斥切换，须另立实验并显式声明负权重来源
（例如上层调制），不得悄悄越权到 L1/L2 或改 L0。

关于同步分支的语料：当前 C1 的 ρ=0.002（弱耦合），只覆盖独立分支。
若将来要覆盖 ρ≥0.5 的同步分支，需要另设"模块间强耦合"语料
（例如所有模块共享一个高权重枢纽），并沿用上面预注册的 P2 同步分支判据；
该语料属于后续工作，本轮不实现。

## 5. 指标

- 吸引子簇数（多初值收敛后的聚类数，容差 0.05）；
- 盆地占比（低态/高态的初值比例）；
- 滞回宽度（上扫/下扫转变点之差，按输入范围归一化）；
- 对照滞回宽度；
- 未归类初值比例（振荡/发散/未收敛）；
- E24 划分的多种子一致性 ARI（C1/C2/C3 都用；C1 另报真值 ARI）；
- 拟合得到的 A、B、C 与判别式 B²−4AC（旁证，写入 JSON）；
- 主/次权重口径的结论对比；
- L0/L1 只读检查（跑前跑后节点/连接/计数一致）。

## 6. PASS / FAIL（预注册）

| # | 判据 | 阈值 |
| --- | --- | --- |
| P1 | 接口：模块划分来自 E24、覆盖全部节点、V 为模块均值 | 覆盖 100% |
| P2 | 双稳（**双分支，由 ρ 决定**）：ρ≥0.5 → C1 恰好 2 个吸引子簇且盆地各 ≥20%；ρ<0.5 → C1 每个模块两态且联合吸引子数 = 2^N | 通过 |
| P3 | 滞回：C1 滞回宽度 ≥10% 输入范围；C2 ≤2% | 通过 |
| P4 | 未归类：未归类初值比例 ≤20% | 通过 |
| P5 | 划分依赖：C1/C3 多种子一致性 ARI ≥0.8；C2 人工单模块（不检查） | 通过 |
| P6 | 次级口径：L1 对称化权重的结论如实记录（可不同） | 记录 |
| P7 | 无越权：L0/L1 只读；S2 只做宏观动力学分析 | 通过 |

## 7. 预注册参数

```text
β = 2.0（作用于归一化后 W，max W = 1）    dt = 0.01
多初值单点积分 = 5000 步（判吸引子）    多初值 = 200 个
初值分布 = V0 ~ Uniform(-1, 1)（对称；原稿未指定初值域，实现前定稿并固定；
若用 U(0,1)，高态盆地会一边倒，属初值域设定问题而非机制问题）
联合吸引子数记录口径 = 每模块低/高（≥0.5）的**二值模式数**（弱耦合下预期 ≤2^N）
C3 的"赢者通吃"判据 = 存在互斥模式且该簇 ≥5% 初值（修正了原先的计数阈值笔误）
吸引子聚类容差 = 0.05     I 扫描 = [-2, 2]，步长 0.05
每个 I 点：从上一点稳态出发，积分 1000 步，取最后 200 步平均
滞回判据阈值 = 输入范围的 10%    单稳对照阈值 = 2%
未归类比例上限 = 20%    划分一致性 = 10 种子的两两平均 ARI ≥ 0.8
θ_i = 0（无偏置）    I 为外部控制参数（非语料统计）
W 诊断分支阈值：ρ = max(W_offdiag)/max(W_diag)，ρ ≥ 0.5 走同步分支，
否则走独立分支（当前 ρ=0.002，走独立分支，预期 2^N 个联合吸引子）
```

## 8. 产出物与边界

- `experiments/exp25_module_dynamics.py` + `outputs/exp25_module_dynamics.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C25 / 负结果 B12 起）；
- 不改 L0/L1；不引入语义/情绪/奖励；不把模块动力学称为"意识/理解"。

## 9. 实测结果（2026-09-11，收敛判据订正后的正式跑数）

> 订正说明：首轮运行的数值（盆地 0.555/0.445、细簇 136）来自一处**收敛判据实现缺陷**，
> 该批数字**作废**，下表为订正后重跑的结果。缺陷与修法见 §10（已登记为 B14）。

| 项 | 结果 |
| --- | --- |
| ρ（W 诊断） | 0.0019（弱耦合 → 独立分支，W_diag=0.769/0.692/1.000） |
| P1 接口 | 3 模块，覆盖 15/15 节点 |
| P2 双稳（独立分支） | 每模块低/高盆地：0.49/0.51、0.54/0.46、0.53/0.47；联合二值模式数 = 8 = 2³（细簇同为 8，无伪影） |
| 两态实际取值（辅助） | 模块1 ±0.872、模块2 ±0.806、模块3 ±0.958；最靠近判定线 \|V\|=0.80（远离 0.5，二分不是硬切） |
| P3 滞回 | C1 宽度 0.150（转变点 ±0.30，阈值 10%）；C2 单稳对照 0.012（= 1 个扫描步长 0.05/4.0 的离散化伪影，非真实滞回） |
| 数值稳健性（辅助） | 每 I 点积分步数 ×5 → C1 宽度仍 0.150，非积分不足造成 |
| P4 未归类 | C1/C2 = 0.000（阈值 ≤0.20）；不动点残差 <1e-6；独立复算 20 万步参考积分残差 <1e-15 |
| P5 划分依赖 | C1 多种子一致性 ARI = 1.000 |
| P6 次级口径 | 成对事件下 L1 对称化方向计数 = L0 共现计数（方向信息为零，如实记录） |
| P7 无越权 | L0/L1 跑前跑后 (15,34) 一致 |
| 辅助拟合 | A=−35.10, B=0.46, C=34.93, B²−4AC=4903.17（仅旁证） |
| C3 探索性 | 联合二值模式 **2**（非 4：正耦合把两模块锁在一起，同高同低）、每模块盆地 0.485/0.515、滞回 0.163、赢者通吃 = False |

## 10. 实现订正记录（B14）

首轮实现里 `attractor_scan` 的收敛判据写成：

```python
V = V0
for _ in range(STEPS):
    Vn = V + DT * (-V + tanh(β(W @ V)))
    if converged(Vn, V): V = Vn; break
    V = Vn
if converged(Vn, V, tol=5e-3):   # ← 循环结束后 V 与 Vn 是同一个数组对象，恒为真
    finals.append(V)
```

循环结束时 `V is Vn`，比较同一对象差值恒为 0，于是：

1. **P4 空过**——"未归类比例"恒为 0，与机制无关；
2. 未收敛的轨迹被当成吸引子混进统计，于是出现细簇 136、盆地 0.555/0.445 这类伪影。

修法（跑前订正，不改任何判据、阈值、参数）：改用**不动点残差**判据
`r = max|−V + tanh(β(WV))| < 1e-6`，不收敛的初值计入未归类。
订正后所有收敛态都落在 ±(0.80~0.96)，两态分离明确，细簇正合 2³=8。

**结论未变**：C25 的双稳 + 滞回结论在订正前后都成立；变化的是数值精度——
订正前的盆地数字不可信（偏高），订正后的盆地更均衡（0.46–0.54）。
该缺陷已登记 CLAIMS-REGISTRY 的 **B14**。
