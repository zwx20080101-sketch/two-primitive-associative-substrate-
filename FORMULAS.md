# Synapse-Net 公式冻结（FORMULAS）

冻结日期：2026-09-06　依据：代码实现 + E0–E19 实验 + BOUNDARY.md 决策

冻结规则：本文件是各层公式的**唯一权威来源**。任何公式改动必须
先更新本文件（经用户确认），再改代码；`synapse_net.py`（L0）继续冻结不改。

---

## 0. 全局不变量

1. **闭包**：激活输出 ⊆ 曾 learn 过的节点集合；未知节点激活 → KeyError。
2. **记忆永存**：连接本体（N、last_seen、方向计数）永不删除、永不衰减；
   一切"衰减"只发生在检索时派生的有效强度上。
3. **不判对错**：基底不做价值/语义/正确性判断；判断属于外部消费模块。
4. **无写侧调制**：情绪、奖励等反馈不得改写基底连接强度；
   强反馈只经"输入密度"或"输入标记+外部读取端"两条路径生效（E19 拍板）。
5. **传播规则冻结为 max**；多路求和(sum)仅作为读取端可选模式（E14 拍板）。

---

## 1. L0 共现基底（`synapse_net.py`，冻结）

### 1.1 学习（共现绑定）

输入：事件 = 同一窗口内的同步节点集合 `E = {x1..xk}`（内部去重）。

```text
节点: 首次出现则创建; 观测元数据 first_seen/last_seen/appear_count 更新
连接: 对每个无序对 (u,v), u≠v:
      N_uv += 1
      last_seen_uv = t            (t = 显式时间戳或内部时钟递增)
```

事件内全部两两成连（完全绑定）；**没有自环**（u≠v）；连接无向。

### 1.2 检索时派生的有效强度

```text
W_uv(t) = N_uv · exp(-λ · max(0, t - last_seen_uv))
```

- `λ` = 时间衰减率，默认 0（W=N）；E10 用 0.005 验证；
- 时间只影响检索强度，不影响 N 与连接存在性；
- Δt 由连接自己的 last_seen 决定，与节点无关。

### 1.3 传播强度（每源 max 归一化）

```text
s(u→v; t) = W_uv(t) / max_{w ∈ N(u)} W_uw(t)      (无边时为 0)
```

- 分母是 u 的全部邻居中最大的有效强度；
- 由于分母以 u 为准，s(u→v) 与 s(v→u) 可以不相等（非对称读取）；
- 连接本身无向，方向性只出现在读取归一化上。

### 1.4 激活扩散（max 吸收，best-path）

```text
种子: a(seed) = 1.0
传导: arrival = a(u) · s(u→v) · hop_decay
吸收: a(v) = max(a(v), arrival)；仅当严格提升时才再次发射
```

等价语义：`a(v) = max over 种子到 v 的简单路径 of ∏(hop_decay · strength)`
单调、无状态、必然终止；自环不可能增强激活（E13/E14 验证）。

默认 `hop_decay = 0.9`。

---

## 2. L1 方向/顺序层（`order_layer.py`）

### 2.1 学习（有序相邻对，含自转移）

输入：有序事件 `[x1..xk]`（相邻到达顺序）。

```text
对每个相邻对 (x_i, x_{i+1}):
      n(x_i → x_{i+1}) += 1      (允许 x_i == x_{i+1}: 重复标记, E13)
```

- 计数按方向分开：n(A→B) 与 n(B→A) 各自累计，互不假定相等；
- `n(汪→汪)` = "同节点相邻再现"的重复标记，纯时间性、无语义；
- 长度 k 的事件产生 k−1 个相邻转移（[汪,汪,汪] → n=2）。

### 2.2 方向强度（软反向）

```text
raw(u→v)      = n(u→v) + floor
strength(u→v) = raw(u→v) / max_{w 与 u 相邻} raw(u→w)
```

- `floor` 默认 1.0：从未发生过的方向保留弱可逆保底（倒背"可能但费力"）；
- 邻居集合含双向边（u 可沿两个方向出发），但强度按各自方向的计数归一化。

### 2.3 激活扩散

与 L0 相同的 max 波前（种子 1.0、hop_decay 0.9、严格提升再发射）。

注意：L1 没有时间衰减参数——方向计数不衰减；新旧只由 L0 的 W 承担。

---

## 3. L2 上下文/三元组层（`context_layer.py`）

### 3.1 学习（滑动相邻三元组）

输入：有序事件 `[s0..sk]`。

```text
对 i = 0 .. k-3:
      n((s_i, s_{i+1}) → s_{i+2}) += 1
```

- 只计相邻三元组，非相邻不计数（滑动窗口规则已锁定，E15）；
- 前缀长度固定 = 2（双前缀）；3 前缀以上 N 元依赖不在本层范围。

### 3.2 查询（候选强度分布）

```text
给定前缀 (a, b):
      raw(c) = n((a,b) → c)
      strength(c) = raw(c) / max_c raw(c)         (前缀未知 → 空)
```

只输出候选分布，不选句。

---

## 4. L4 组块层（`chunk_layer.py`）

### 4.1 块的形成

输入：带边界的单元（词/短语），按序去重前不丢失重复位置。

```text
unit_count(seq) += 1
当 unit_count(seq) == K 时创建块:
      chunk_id = CH_n
      record = list(seq)             # 顺序记录, 保留重复(双写 l)
之后每次该单元出现:
      member_bind[(chunk_id, m)] += 1, 对每个去重成员 m
```

`K` 默认 5。块名 CH_n 是机器号，不携带语义。

### 4.2 拼写/回放（外部动作模块使用）

```text
spell(chunk_id) = 顺序成员表(含重复位置)      # 未知块 → KeyError
```

### 4.3 块级序列与激活

块可作原子参与块级序列：相邻无序对计数（规范序，同 L0 绑定）；
块级激活使用与 L0 相同的 per-source max 归一化 + max 波前（hop_decay=0.9）。

---

## 5. 读取端工具（不属于基底）

### 5.1 阈值过滤

```text
attention_filter(acts, threshold): 保留 activation >= threshold 的节点
attention_filter(acts, top_k):     按 (-activation, label) 取前 k
threshold 语义: 作用于 [0,1] 激活标度, 默认不设
```

### 5.2 走读器（实验侧）

每步：激活当前节点 → 取激活最强的未访问邻居 → 继续。读取工具，不改基底。

### 5.3 sum 读取端候选模式（E14，非底层规则）

```text
单次发射: 节点出队时冻结激活, 只发射一次;
累加: a(v) = min(1.0, Σ 已发射邻居贡献); 已发射节点不再接收贡献;
封顶: cap = 1.0
```

注意：sum 的遍历拓扑 ≠ 底层原始无向图（带访问标记的遍历），
输出不能与 max 直接做底层机制对比；≥2 路时封顶饱和，>2 路梯度消失
（未来需归一化变体）。

---

## 6. 外部 stub 语义（占位，可替换，不冻结）

| stub | 输入证据 | 占位规则 | 真实模块 |
| --- | --- | --- | --- |
| 语言（`language_stub.py`） | R 绑定词集合、F 特征集合、n(w→w) | 规则已学 + 有资格 + 未叠过 → 输出 ww | 需从反馈经验学习规则 |
| 动作（`motor_stub.py`） | 块记录 spell | 逐字母渲染字符串 | 需真实书写/发音 |
| 情绪（`emotion_stub.py`） | 激活图 + 强度标记 | fear = a(疼)·gain(标记); avoid 阈值 | 增益表需从身体反馈学习 |

---

## 7. 参数速查（详见 PARAMETERS.md）

| 参数 | 默认 | 域 | 归属 |
| --- | --- | --- | --- |
| hop_decay | 0.9 | (0,1] | L0/L1/L4 扩散 |
| λ | 0.0 | ≥0 | L0 检索衰减 |
| K | 5 | ≥2 | L4 成块 |
| floor | 1.0 | >0 | L1 软反向 |
| 归一化 | per-source max | — | L0/L1/L4 |
| sum cap | 1.0 | >0 | 读取端候选 |
| L2 前缀长度 | 2 | 固定 | L2 |
