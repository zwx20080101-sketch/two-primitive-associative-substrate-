# E24 设计：S1 模块分化（ICA 主口径 + LCA 第二分析）

状态：**已实现（2026-09-11）**：主判据 8/8 PASS，但发生了一次
"预注册方法失败 → 最小方法修订"，细节见第 8 节。依据：NEXT-PLAN §6 已确认的三条前置约定。

## 0. 定位与边界

S1 是**读侧分析层**：只读 L0 的共现统计，回答"L0 的统计能否被分解成少量可分离模块"。
它**不修改 L0**、不产生语义/情绪，产出的只是"模块归属"标注；
若日后要让模块反过来影响输出，那是一个独立的新实验（另立编号）。

## 1. 四个语料（含真值与对照）

| 语料 | 构造 | 真值/用途 |
| --- | --- | --- |
| C1 planted | 3 组 × 5 节点；组内两两共现 ×100；组间少量噪声 ×5 | 真值组 = 3；主判据 |
| C1-hub | 在 C1 上加一个枢纽 H，与各组各节点共现 ×30 | 检验枢纽：单独成团 or 被吸入某组（预注册二选一，报告实际） |
| C2 chain | 26 字母链，窗口 w=3 的共现（相邻 ±1..3，计数 3/2/1） | 对回：模块应为链上连续区段 |
| C3 fork | 共享前缀 + 两条分支：主链 A..Z 窗口 w=3，另加分支 q>B>x、t>E>w | 对回：两条分支的独有节点应分开 |
| C4 null | 同密度随机共现（无植入结构） | 负对照：不应恢复出干净模块 |

说明：C2/C3 用窗口 w=3 而非仅相邻对，是为了让结构在**共现统计**中可辨识
（本实验测的是共现统计的可分性；相邻对语料下多远共现为 0，属语料设定问题）。

## 2. 指标

- **ARI**：恢复的模块归属 vs 真值组的调整兰德指数（实现于 numpy）。
- **团间相似度**：各成分向量两两 |cos| 的最大值。
- **稳定性**：10 个随机种子的 ICA，两两 ARI 的均值（并匹配成分编号）。
- **模块数**：恢复出的成分中"主导节点 ≥2"的数量。
- **连续度（链）**：模块按平均链位排序后，各模块节点在链上是否连续；
  连续模块比例 ≥0.9，且区段顺序与链顺序单调（Spearman=1）。
- **分支分离指数（分叉）**：两条分支的独有节点被分到不同模块的比例（≥0.9）。
- **LCA（第二分析，不计入主判据）**：收敛步数、稀疏度（活跃比例）、跨种子稳定性，
  以及与 ICA 归属的一致性（ARI）。

## 3. 先查语料信号（P1）

任何算法结论之前先算：

- C1 的**组内/组间共现强度比**（≥5 才认为信号足够）；
- 置换检验：打乱节点真值标签后重算 ARI 的零分布，确认观测 ARI 显著高于零分布
  （≥95 分位）；
- 若信号不足：**先判语料问题**，不得据此说机制失败（与 E23 符号撞车同类的坑）。

## 4. 主判据（P2–P6，ICA 共现矩阵）

| # | 判据 | 阈值 |
| --- | --- | --- |
| P2 | C1 恢复 | K'=3 时 ARI ≥0.8；恢复模块数 =3 |
| P3 | 团间可分 + 稳定 | 团间 |cos| ≤0.3；10 seeds 两两 ARI 均值 ≥0.8 |
| P4 | 负对照 | C4 上 best ARI（K'=2..5）≤0.3，且无可复现的 3 模块结构 |
| P5 | 链对回 | 连续模块比例 ≥0.9；区段顺序单调（Spearman=1） |
| P6 | 分叉对回 | 分支分离指数 ≥0.9；共享前缀节点的归属按预注册二选一报告 |

预注册的共享前缀归属：**允许"成员跨多团"或"单独成团"两种结果之一**，
跑前登记该二选一为可接受结果；硬判据只压在"分支独有节点分离"上。

## 5. 第二分析（LCA，不计入主判据）

在 C1 的激活时间序列上跑 LCA（Rozell 动力学，字典 = 激活数据的 PCA 前 K 主成分）：

- 只验证：能收敛、稀疏（活跃比例 ≤0.3）、跨种子稳定；
- 再与 ICA 的模块归属做一致性（ARI ≥0.6 作为支持性证据）；
- 若 LCA 不稳或与 ICA 冲突：如实记录，不改主判据结论。

## 6. 无越权检查（P7）

- S1 只读 L0（共现矩阵）；跑完前后 L0 的节点/连接/计数必须逐项一致；
- 不引入语义/情绪/奖励；模块归属只是标注；
- 若未来要让模块影响输出，另立实验与判据。

## 7. 产出物

- `experiments/exp24_module_differentiation.py`（numpy 自实现 FastICA + LCA + ARI）；
- `outputs/exp24_module_differentiation.json`；
- 结果如实登记进 CLAIMS-REGISTRY（通过或失败都要记）。

## 8. 实现过程中的失败与修订（诚实记录）

### 8.1 预注册方法 ICA：失败（记为 B09）

- 现象：植入语料上 ARI≈0、恢复模块数=1、10 种子归属稳定性 ARI=0.165。
- 排查后确认不是实现 bug（合成 ICA 问题 ARI=1.0 验证实现正确），而是**方法不适用**：
  1. 共现矩阵零对角 → 线性混合模型被对角线样本特有项破坏；
  2. 模块质量（组大小×强度）若相等 → 特征值简并 → 原理上不可辨识；
  3. 去均值会减掉共享 DC 方向，K 个簇只剩 K−1 维；
  4. ICA 成分在算法内天然正交，|cos| 恒为 0，不能再当"团间相似度"用。
- 处置：记录为负结果（B09），不改 L0、不改判据阈值，改用**最小方法修订**。

### 8.2 最小方法修订：谱聚类读出（通过全部判据）

- 预处理（预注册）：对角线用行最大值插补；不做去均值；
- 读出：归一化拉普拉斯前 K 特征向量 + k-means（10 次重启）；
- 结果：植入语料 ARI=1.000（3/3 模块）、稳定性 ARI=1.000；
  随机负对照模块富集度 1.14（植入 555.4）；链呈 3 个连续段且不交错；
  分叉分支分离指数=1.00（共享前缀节点跨模块，非单独成团）。

### 8.3 负对照判据的修正

谱聚类在随机图上也会给出"稳定"的划分（稳定性 ARI=1.0），
**稳定性不能作为无结构判据**；改用**模块富集度**（组内均值/组间均值），
随机语料≈1、植入语料≫1（记录为 B11）。

### 8.4 LCA 第二分析：失败（记为 B10）

当前配置下 LCA 活跃比例=1.0（不稀疏）、未正常收敛、与谱聚类归属一致性≈0，
作为旁证不成立；按约定不影响主判据，但必须如实记录。

### 8.5 语料修正

分叉最初用相邻对（q>B>x）导致分支独有节点彼此不共现、信号不可辨识；
改为窗口事件 [q,B,x]×20（[t,E,w] 同理）后分离指数=1.0。
