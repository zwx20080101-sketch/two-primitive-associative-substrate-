# 网页玩具 v3 简版设计方案（"人脑形网络 + 批量时间输入"）

目标：把网络做得**像人脑那样好看**（肉粉/奶油、不刺眼），画布占主屏；
不做花哨装饰性预测；新增**带时间戳的批量输入接口**，
支持"先后 / 同时 / 隔断"写法，粘一段就一键复现到图上。
底层仍 L0，上层仍是"统计 + 反馈"，所有层标注清晰。

状态：**已冻结 v3（2026-09-06）**。页面即当前实现；"分层影响/组合律"
这一未覆盖缺口记录在 [OPEN-QUESTIONS.md](./OPEN-QUESTIONS.md)，本轮不做。

## 1. 视觉（"人脑形"）

- **画布占主屏**（约 70-80vh），底部 / 侧栏折叠次要面板，顶部只有一条极简工具栏；
- **配色（肉色系，防反胃）**：背景为深灰蓝（暗）或暖米色（浅）；
  节点用柔和腮红粉/奶杏，连接用灰紫/淡玫瑰；激活用更亮一点的玫瑰金并带微光；
  边缘描边用低对比灰，避免刺眼高饱和；
- **结构像脑叶**：布局用"聚类力"把同层/同情景的节点拢成几个团块，
  团块之间留沟——远看像大脑皮层的叶回；
- **不做的装饰**：不画预测弧、不发散无意义的粒子云；只保留
  有意义的视觉：连接粗细=共现次数、方向粗细=先后强度（L1 弱=细灰）、
  激活→波浪扩散、块=更大节点、stub=右侧小卡片。

## 2. 激活波浪（保留，但简洁）

- 点种子后从该节点发出**向外一圈圈衰减的波纹**，依次扫过逐跳邻居；
- 被扫到的节点在到瞬间闪亮一次并保持亮度；激活边在波经过时被点亮；
- 遵循系统"减少动态"自动静帧；速度一个滑块即可。

（波浪是"激活沿连接扩散、有先后"的可视化隐喻，不宣称真实脑电动力学。）

## 3. 批量输入接口 + 带时间戳的格式（重点）

在"经验输入"旁加一个**批量框（textarea）+ 载入按钮**；
支持粘一段含时间戳的大文本，点一下全部处理并复现到图上。

**格式规范（详见 INPUT-FORMAT.md）**：

```text
# 注释
@1.0 [A B]        # 同步窗口：同一时间点 A、B 同时出现 → L0 两两绑定
@2.0 A>B>C>D      # 有序序列：A 先、B 后、C 再后 → L0 相邻对 + L1 方向
@5.0 {H e l l o}  # 带边界单元（词）→ L4 成块
@1.2 A            # 单个符号出现
```

- `;` 或换行 = 事件之间隔断；
- `@数字` = 事件时间戳（缺省则上一个事件 +0.1）；
- `[ ]` = 同一时间；`>` = 谁先谁后；`{ }` = 一个整体单元。

解析后直接按规则喂给对应层，并**记录时间戳**（每个节点/连接的
first_seen / last_seen），"公式态"行显示最近共现时间 t。
这样"现实世界的因果 = 时间先后"就有了载体；λ 仍为预设 0（W=N），
但时间信息已存好，为后续使用 λ 打底。

## 4. 分层映射（最小化，不去装饰）

| 层 | 显示 | 对应实验 |
| --- | --- | --- |
| L0 | 连接粗细=次数；节点激活波浪 | E1/E4/E2/E9 |
| L1 | 边的方向粗细（正向粗亮、反向细灰） | E5（正走顺、倒走弱） |
| L4 | 更大节点（块）+ spell 文字 | E12/E18 |
| 外部 stub | 右侧小卡片（语言/动作/情绪）输出文字 | E17/E18/E19 |
| 时间 | 每个节点/连接带 first_seen/last_seen；公式态显示 t | E7/E10 打底 |

L2（三元组）先用批量格式的 `>` 事件自动记录，但**先不做预测弧那种装饰**，
只在"三元组"面板以文字/条形展示（可开关）。

## 5. 实施阶段（简化）

| 阶段 | 内容 | 出口 |
| --- | --- | --- |
| v3.1 | 脑形布局 + 肉粉配色 + 画布主屏 + 聚类力团块 | 画面"像人脑不反胃" |
| v3.2 | 激活波浪扩散（简洁版） | "从种子荡开" |
| v3.3 | 批量输入解析（时间戳/先后/同时/隔断）+ 一键载入 | AI/用户可批量写实验 |
| v3.4 | 记录并显示时间戳（first_seen/last_seen）；stub 小卡片 | 因果论载体 + 外部接口 |

## 6. 边界（仍写明）

- 波浪是隐喻；不宣称脑电/时间动力学真实；
- 上层/stub 不冒充基底；参数（hop_decay、λ=0、K、threshold）均预设；
- 不做真实多模态与无监督自组织宣称。
