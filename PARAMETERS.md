# Synapse-Net 全参数表（PARAMETERS）

冻结日期：2026-09-06　权威来源：代码签名 + FORMULAS.md + INTERFACE.md

说明：本表集中记录所有可调参数/常量。凡标"冻结"者，改动必须先做实验并经确认；
"占位"者为外部 stub 的临时常量，真实模块可整体替换。

---

## 1. 核心参数（基底与各层）

| 参数 | 默认 | 域 | 所在位置 | 作用 | 实验出处 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `hop_decay` | 0.9 | (0,1] | L0/L1/L4 的 `activate(..., hop_decay)` 参数（每次调用可传） | 每跳脉冲传导损耗：arrival = a×strength×hop_decay | E1/E4/E5/E7/E8 等全部扩散实验 | 冻结默认值；单次调用可覆盖 |
| `λ` (time_decay_lambda) | 0.0 | ≥0 | L0 构造函数 `SynapseNet(time_decay_lambda=...)` | 检索衰减 W=N·e^(−λΔt)；只影响检索强度，不影响 N | E10（用 0.005） | 冻结默认 0；激活值按实验指定 |
| `K`（成块阈值） | 5 | 整数 ≥2 | L4 构造函数 `ChunkLayer(k=...)` | 带边界单元出现 ≥K 次自动成块并绑定成员 | E12/E17/E18（用 100 次经验，K=5） | 冻结默认 5；可调但需标定实验 |
| `floor`（软反向保底） | 1.0 | >0 | L1 构造函数 `OrderLayer(floor=...)` | raw=n+floor：从未发生过的方向保留弱可逆性 | E5/E13 | 冻结默认 1.0 |
| L2 前缀长度 | 2 | 固定 | `ContextLayer`（无参数） | 双前缀→下一个的三元组；3 前缀以上不在范围 | E15 | 冻结（扩展需新层/实验） |
| 归一化规则 | per-source max | — | L0/L1/L4 强度计算 | strength = W/该源最大邻居 W | E0–E19 全部基线依赖 | **冻结**（改动=推翻全部基线） |
| 吸收规则 | max | — | L0/L1/L4 激活 | best-path 语义；多路不叠加 | E14 拍板 | **冻结** |
| 种子激活值 | 1.0 | 固定 | 各层 activate | 种子强度 | 全部 | 冻结常量 |
| 时间戳 t | None→时钟+1 | float | 各层 learn(t) | last_seen 记录与 λ 检索 | E7/E10 | 约定：driver 统一传 |

---

## 2. 读取端参数

| 参数 | 默认 | 域 | 所在位置 | 作用 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `threshold` | None（不过滤） | (0,1] | `attention_filter(acts, threshold=...)` | 保留 activation≥threshold 的节点；语义=可访问性旋钮 | 读取端，纯函数 |
| `top_k` | None | 整数 ≥0 | `attention_filter(acts, top_k=...)` | 按 (−activation, label) 取前 k | 读取端，纯函数 |
| sum 封顶 cap | 1.0 | >0 | 读取端候选模式（E14，未落库） | 多路求和封顶；≥2 路饱和 | 候选，非底层 |
| sum 遍历策略 | 单次发射 | 固定 | 同上 | 节点出队冻结、只发射一次；已发射节点不再接收 | 候选，非底层 |

---

## 3. 内部常量（不对外暴露为参数）

| 常量 | 值 | 作用 |
| --- | --- | --- |
| EPS | 1e-12 | 激活严格提升判断、浮点容差 |
| 种子强度 | 1.0 | 扩散起点 |

---

## 4. 外部 stub 占位常量（可替换，不冻结）

| stub | 常量 | 默认 | 说明 |
| --- | --- | --- | --- |
| 语言 `ReduplicationStub` | 规则标记 R / 特征 F | `"R"` / `"F"` | 占位标签，真实模块自行学习规则 |
| 情绪 `EmotionStub` | 增益表 | `{剧:3.0, 轻:1.0}` | 占位；真实模块应从身体反馈经验学习该表 |
| 情绪 `EmotionStub` | 躲避阈值 | 2.0 | 占位决策阈值 |
| 动作 `MotorStub` | — | — | 无参数；spell→逐字母渲染 |

---

## 5. 变更纪律

1. 冻结参数（归一化、max 吸收、hop_decay 默认、K 默认、floor 默认、λ 默认）
   的改动 = 先出实验与判据 → 经用户确认 → 先改 FORMULAS/PARAMETERS → 再改代码；
2. 每次调用可覆盖的参数（hop_decay）不视为全局改动；
3. 外部 stub 占位常量替换真实实现时，只改 stub 文件，不碰层文件。
