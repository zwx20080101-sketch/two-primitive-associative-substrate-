# 摘要与第 1 章 引言（中文草稿）

## 摘要

本文提出并实现了一个极简的联想记忆基底参考系统：仅以"共现绑定"
与"激活扩散"两条规则作为核心，在其上叠加方向、上下文与组块等
上层表示层，并配置只读的读取端工具与外部功能模块。
在 22 组受控实验（E0–E22）中，系统在预先注册的判据下稳定复现了
联想链自组织、检索强度随频率与时间变化、方向不对称、共享情景绑定、
组块与词内重复位置记录、角色标记区分、部分输入补全、重复标记等行为；
消融、规模一致性与随机输入族抽样（Wilson 95% 置信区间）提供了
对照证据。所有结论均限定于所报告的受控输入族；本文不作关于
人脑或生物神经系统的等价论断，也不将结果外推到真实感知输入
与大规模语言模型场景。

## 1.1 背景与问题

联想式记忆是认知系统的基础能力之一：把共同出现的经验关联起来，
并在后续输入中唤醒相关经验。现有实现多内嵌于任务导向的大型系统，
或直接以规模与参数取胜；对其"最小可运行、可判据、边界清晰"
的基础原型，公开且可复现的实现仍然稀缺。

本文尝试回答的问题是：极简的"共现绑定 + 激活扩散"机制，
配合显式的上层表示层与外部读取层，能在受控输入族内
稳定复现哪些联想、时序、语言式与价值式行为？

## 1.2 相关工作缺口（浅度说明）

扩散激活、关联记忆与序列统计（n-gram 类）机制在文献中已有长期研究，
但其公开实现往往面向特定任务或依赖较大规模参数。本文不进行
系统性文献综述，只指出一个具体缺口：缺少一个以
"两条原生规则 + 明确分层边界 + 预先注册判据"为设计的
最小参考实现与配套数据集。

## 1.3 本文工作

本文工作包括：

1. 实现双原语联想基底（L0），并叠加方向（L1）、双前缀上下文（L2）、
   组块（L4）三层上层表示与只读读取端工具；
2. 定义并实现语言、动作、情绪三类外部模块（stub），
   明确其不属于基底原生能力；
3. 运行 E0–E22 共 22 组带预先注册判据的实验，
   覆盖联想、时序、语义绑定、组块、角色与价值反馈场景；
4. 提供消融对照、规模一致性与随机抽样统计三类附加证据；
5. 开放全部代码、数据与校验脚本，确保可复现与可核对。

## 1.4 核心边界声明

- 输入为受控符号事件流，不涉及真实感知与连续信号；
- 结论限定于报告所列输入族，见第 5 章边界与第 7 章局限性；
- 叠词规则外推、动作渲染与情绪读取由外部 stub 完成，
  不归入基底能力（见 4.7）；
- 本文不作出该机制与人脑或生物神经系统功能等同的论断。

## 正文顺序说明

完整报告章节顺序为：摘要 → 第 1 章 引言 → 第 2 章 系统架构与数学定义
→ 第 3 章 实验方法 → 第 4 章 结果（4.1–4.8）→ 第 5 章 边界与负结果
→ 第 6 章 讨论 → 第 7 章 局限性 → 附录/复现说明。
