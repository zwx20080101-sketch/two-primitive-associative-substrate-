# 后续开发计划（NEXT-PLAN）

日期：2026-09-11　依据：仓库现状（E0–E22 + 报告 + 网页玩具 v3 已提交）
+ 用户提供的外部建议文档（ICA/LCA、平均场/Hopfield、自由能/EI）
+ 我们自己的缺口记录 [OPEN-QUESTIONS.md](./OPEN-QUESTIONS.md)。

## 1. 现状盘点（已冻结）

- E0–E22 全部 PASS；公式/接口/参数/基准四件套；报告与数据已提交（v0.9.0 系列）。
- 网页玩具 v3 已提交：可复现 E1/E2/E4/E5/E6a/E15/E16/E7(近似)/E12/E18/E17/E19，
  含逐跳波纹激活、因果 Token 序列、能量流、批量时间戳输入、配置/副本保存。
- 已覆盖：**单层能力目录**（每层各自的现象）+ 边界 B01–B07 + 消融 E20 / 规模 E21 / 统计 E22
  + 系统级 stub 的功劳记账（M2）。
- 已记录未覆盖：**多上层同时返回数据如何合成一次输出**（"分层影响/组合律"）。

## 2. 缺口清单（三处来源合并）

| 来源 | 缺口 | 状态 |
| --- | --- | --- |
| 我们自己（OPEN-QUESTIONS） | 分层影响 / 组合律：L1+L2+L4 同时开启时，各层返回量如何合成同一输出 | 未做 |
| 外部建议文档 | S1 模块分化（ICA / LCA 稀疏竞争） | 未做（仅提议） |
| 外部建议文档 | S2 模块内动力学（平均场 / Hopfield / Boltzmann 双稳态） | 未做（仅提议） |
| 外部建议文档 | S3 模块间协调（变分自由能 / 有效信息 EI 因果涌现） | 未做（仅提议） |
| 既有边界驱动 | B01 三前缀之外；max 不叠加的读取端扩展；L2/L4 同口径基准 | 未做 |
| 远期 | 真实多模态输入、递归/组合泛化 | 远期 |

**命名冲突必须先解决**：建议文档的 L1/L2/L3 与仓库已有 L1（方向）/L2（三元组）/L4（组块）
撞名。统一改称 **S1 / S2 / S3**（Sparse competition / mean-field / free-energy coordination），
不占用 L 编号。

## 3. 对三条建议的评估（科学严谨性）

| 建议 | 性质 | 与边界是否一致 | 主要风险 / 必须修正 | 拟编号 |
| --- | --- | --- | --- | --- |
| S1 ICA + LCA | **新机制层**（稀疏竞争/侧抑制编码） | 不改 L0 一致 | 需引入字典 Φ 与侧抑制，属"新增机制"而非自然涌现，必须单层实验 + 功劳记账；判据阈值（模块数 < 节点/10、余弦低）需预设并做灵敏度分析；必须做消融（去掉 S1 后现象是否退化） | E24 |
| S2 平均场/Hopfield/Boltzmann | **模块级动力学模型** | 不改 L0–L1 一致 | Hopfield 依赖对称权重（L0 对称可用，L1 方向不对称 → 需显式定义模块级对称权重）；二次模型 dV/dt=AV²+BV+C 需拟合优度检验；它**不是**替代我们的扩散规则，而是叠加的模块级模型 | E25 |
| S3 自由能 / EI | **分析指标**（变分自由能、因果涌现） | 不改任何层一致 | EI 在小图上估计方差大，需 bootstrap 置信区间；"macro > micro" 是统计判据不是口号；"生成模型"须限定为统计预测，不得解释为"理解/意图" | E26 |

三条共同的越权检查（与 BOUNDARY 一致）：不修改 L0–L4；不引入语义/情绪/奖励；
若产生新结构（如字典原子/模块节点），必须像 L4 块节点那样**显式声明为新增结构**。

## 4. 推荐执行顺序（一次一层，先判据后实验）

| 阶段 | 内容 | 出口 |
| --- | --- | --- |
| P0 整理（**过渡**） | 统一命名 S1–S3；把三条建议登记为 candidate layers；补 L2/L4 同口径基准 | 文档一致、无撞名 |
| P1 = **E23 组合律**（第二轮） | 26 链 + 分叉 + 块化单元，2³ 全因子开关；逐层记录"返回了什么→如何影响"，与综合输出对比（详细判据见 [DESIGN-E23.md](./DESIGN-E23.md)） | **已完成（7/7 PASS，exp23_composition）**：三层现象共存且可归因 |
| P2 = **E24 模块分化（S1）**（第二轮） | 生成含 K 个隐模块的语料 → L0 统计 → ICA/LCA；判据：模块数 ≈ K、模块间相似度低、消融后检索退化 | **已完成（8/8 PASS，含方法修订）**：ICA 失败→谱聚类读出通过；LCA 旁证失败（均记录 B09/B10/B11） |
| P3 = **E25 模块内动力学（S2）**（第二轮） | 模块平均激活 V(t) 的动力学：双稳（两吸引子 + 盆地）+ 滞回扫描；C3 互抑体作探索性观察（非负 L0 权重原理上造不出赢者通吃）。判据与协议见 [DESIGN-E25.md](./DESIGN-E25.md) | **已完成（7/7 PASS）**：独立分支 8 个二值模式、盆地均衡、C1 滞回 0.150、C2 无双稳；C3 探索性如实记录 |
| P4 = **E26 模块间协调（S3）**（第二轮） | 三前置问题已拍板（合作型主口径 / 划分沿用 E24 接口 / 必须非成对事件）；主指标 = EI 整合量 + 剂量响应；判据见 [DESIGN-E26.md](./DESIGN-E26.md) | **已完成（7/7 PASS）**：10/10 种子剂量单调；零耦合与消融精确归零；P6 落在"宏观不劣于微观"；B15–B18 已登记 |
| P5 = **集成发布（网页）**（**展示层**） | 通过的层加入网页玩具：**P5a 已完成**——S1–S3 只读分析面板（快照回放 + 字段级自检 + commit 溯源，V1–V6 全过，`verify_playground.py` 7/7） | **已完成**；**P5b（模块实验模式 / 页内试算）已决定不做**，见 [DESIGN-P5.md](./DESIGN-P5.md) §7 |

轮次划分与产物编号见 [README.md](./README.md)「两轮实验阶段」：
**P0 为过渡，P1–P4 属第二轮（E23–E26），P5 为展示层**；第一轮为 E0–E22（单层能力目录）。

**为什么先做 E23 组合律**：在知道"多层返回怎么合成"之前，S1–S3 的贡献都无法被归因——
组合律是给所有后续层做功劳记账的前提。

### 4.1 候选补丁（不影响任何已验收范围，按需排期）

| 项 | 内容 | 归属 |
| --- | --- | --- |
| **E25 快照扩展** | 把 I 扫描的**完整轨迹**写进 `outputs/exp25_module_dynamics.json`，让网页 S2 面板能画真实滞回曲线（现在是由两个转变点**重建的示意**，面板上已如实标注） | 论文前候选（P5b 已取消，此项解耦） |
| 运行确定性 | **已纳入（E28 起）**：README 运行说明与本节纪律 ⑧ 都要求 `PYTHONHASHSEED=0`。理由：字符串哈希随机化会让遍历顺序类结论不可复现（exp14 的 JSON detail 抖动就是它），而 E28 要测的正是"顺序无关/顺序依赖" | 已完成（政策） |
| **方法学小节** | 把 B17/B18/B19 合并成论文方法学一节："三类不报错错数 + 对应退化解自检（块对角零 / 非负性 / ARI≠逐位一致）" | 论文前候选 |
| **变量命名陷阱** | E1 的 26 字母链用 **A–Z 单字母做节点名**；实验代码若用单字母做变量名会撞车（E42 实测：`Z = 1.0` 与节点 `"Z"` 冲突，dict 字面量 `{Z: Z}` 把 1.0 当成了 key → `KeyError`）。**约定：该语料族下变量名长度 ≥ 2，或加下划线前缀（`_Z`）。** 任何新实验评审时核对一次 | 已记（约定，非排期项） |
| **节点命名同样要避雷**（同上一条的延伸，E43 预检补） | 反向也成立：**节点名不要取与常用标识符同形的词**。E43 实测两轮：`seed` 作裸标识符 **114 处**（`spectral(..., seed=)`、`fastica` / `gibbs_module` 的 RNG 种子、exp28 的 `SEED = "A"`、exp27 的 `seed = sX[-2]`）；改用 `src` 后**仍撞 9 处**（exp23 的 `def hops(adj, src, dst)`、exp39 的 `def clone_into(src, g)`、`make_pdf.py` 的 `SRC` 常量）。**两层防护（缺一不可）**：① **避雷名** —— 节点名避开 `seed` / `src` / `hub` / `root` / `s0` / `id` / `type` / `list` 等内置或高频词；② **常量引用** —— 节点名字符串只在**一个**常量定义处出现（如 `START_NODE = "start_node"`），代码其余部分只引用常量、不写裸字符串。新实验评审时逐条核对这两层 | 已记（约定，非排期项） |
| **B32 跨划分稳定性检查** | B32 已 scoped 到"C1 的唯一划分"（种子轴退化：10 个 k-means 种子只给一个划分）。**若将来要引用 B32 并做推广**（换语料 / 加噪声 / 边 dropout / 换块均值口径），必须补一次"跨划分稳定性"实验——本项是那道门 | 引用 B32 推广时的入口（不排期） |

## 7. E26 前置约定（2026-09-11 拍板，写代码前先看这里）

外部评审提出的三个问题，已在 [DESIGN-E26.md](./DESIGN-E26.md) §0 拍板：

1. **协调的定义**：主口径 = **合作型耦合**（非负权重下唯一合法形态）；
   另设第二口径 = **共享下游仲裁**（两源模块投射同一目标，按强度分先后，非负可行）；
   **竞争型互斥（赢者通吃）不做**——需负权重，来源只能是外部调制，
   引入即越权（E25 的 C3 已在非负权重下实测无 WTA，记 B12）。
2. **模块划分**：沿用 E24/E25 的**同一套接口与算法**（L0 统计 → 对角填充 → 归一化拉普拉斯
   谱聚类 → 模块宏观状态 V），但在 **E26 自己的语料上重算划分并冻结**；
   用 E24 语料做**回归校验**（必须重现 ARI=1.000、ρ=0.0019），证明接口未被改动。
3. **语料**：**必须含窗口/序列事件**（C1'/C3'），耦合由语料植入、不得由算法制造。
   理由：E25 的 P6 已实测"成对事件下 L1 方向信息为零"，成对语料下任何方向性协调
   都不可能被观测到——是语料没给信息，不是机制不行。

## 5. 每个实验的固定纪律（沿用宪章）

① 要解释的现象；② 可证伪预期；③ 归属哪一层；④ PASS/FAIL 判据；
⑤ 失败后的最小机制改动。通过才进下一层；L0 继续冻结；
凡"新机制"必须显式标注，不得伪装成 L0 自然涌现。

**E26 追加的两条（2026-09-11，来自 E26 的复盘）**：

⑥ **预注册里不写"方向倾向"。** 双向判据就写"方向待定，双向都算完成"，
   跑完再如实记录落在哪一侧。理由：E26 的 P6 按预检（6 种子、简化构造）推测
   "倾向 B 侧"，正式跑数（10 种子、完整构造）落在 C 侧。**预检方向不可外推**——
   写了反而给"跑完再解释"留了口子。

⑦ **预检观察 ≠ 结论文，不得直接登记为 B 条目。** 预检构造与正式语料可能给出
   相反结果。B 条目只写正式跑数支持的结论；仅来自预检的观察必须标注
   "预检观察，未在正式语料复现"。理由：E26 的 w=91 饱和区在预检非单调
   （0.0447<0.0609），正式语料 10/10 种子却单调 → B16 已按此降级为结构性部分。

⑧ **只读类判据默认用"层文件 sha256 前后对比"**（自 E28 起）。比"节点/连接计数不变"
   更硬：它能抓到"计数没变但代码被改写"这类情况。E27 的 P5 是第一个这么做的
   （synapse_net.py / order_layer.py / context_layer.py 三个文件哈希 before==after）。
   同时要求：无数据的字段写 `null` 并给 `reason`，**不许省略字段**——
   省略会让下游脚本把"缺字段"误判成数据损坏。

⑨ **预检里的"形容词"不得写进预注册的结论性文本或拟登记表述**（自 E31 起）。
   预检的形容词（"两处""会掉到""倾向""显著""大幅"等）只能出现在辅助记录里，
   写成"预检观察：X；正式跑实测：Y"的**两栏对照**。
   判定性表述（如"两处静默"这种会进 C/B 文本的）**必须留到正式跑之后写**。

   依据（同型事故 ×3）：

   | 实验 | 预检里的形容词/方向 | 正式跑实测 | 后果 |
   | --- | --- | --- | --- |
| E26 P6 | "倾向 B 侧" | 落在 C 侧 | 预注册方向写反 |
| E29 k=5 | "会掉到随机（≤0.2）" | 0.928（超分，ARI 对细化不敏感） | 期望写错 |
| E30 B21 | "**两处**静默" | 只有 1 处（L4 的 None 与块 id 可区分） | 拟登记文本写错，已更正 |
| E32 v1 | "L=4 最细层可读出"（单种子扫参看到 1.000） | 10 种子中位 0.805、L=3 最小值 0.778 | 前提不成立，实验改版（v2） |

   规律：**预检里的数字通常可靠，形容词/方向/数量词最容易错。**
   补充（E32 v1）：**单种子/单次跑出来的漂亮数字也不可靠**——
   v1 用单种子扫参看到 L=4 的 ARI=1.000，10 种子中位数只有 0.805。
   凡涉及随机初始化（k-means、采样）的量，必须报**中位数 + 最小值**，不能报单次。

⑩ **区分"架构约束"与"实现约定"**（自 E37 起）。冻结前逐条列出：哪些是架构、哪些是实现。

   ```text
   架构约束 = 只做【存储（共现绑定）+ 激活（扩散）】——不可破；
   实现约定 = 种子抹平值 / 只接 list / 共享一次 BFS / dict 静默接受 ——可破，但要评估代价。
   ```

   改实现约定的五步（见 [L0-SPEC.md](./L0-SPEC.md) §5）：判性质 → 新建平行文件 →
   写影响评估 → 更新版本表 → 在新文件上跑并记录哈希。**不许原地改冻结文件**。

   依据：**E33–E36 三连撞墙，撞的全是实现约定，不是架构**——
   如果当初把"哪条不能改"写成规格，这三轮里至少两轮不必重跑。

## 6. E24 前置约定（2026-09-11 已确认，写代码前先看这里）

对 S1 模块分化（ICA/LCA），在实现前先定死三件事，并追加三条约束：

1. **成功标准**：埋组还原（还原出的组数 = 埋入组数 ±1）+ 团间相似度低 +
   多种子一致（≥10 个随机种子结果一致）。
   **追加**：若埋组还原不出来，先检查语料里这几组的共现统计是否足够强
   （语料 bug 优先于机制结论）——与 E23 的"分叉符号撞车"是同一类坑。
2. **数据口径**：主口径 = 共现矩阵（节点×节点）+ ICA；主判据只认这一条。
   **追加**：LCA 的时序口径只做第二分析，先只验证"能跑通、结果稳不稳"，
   不参与主判据；等主口径跑出结果后再做交叉验证。
3. **对回已知结构**：先定义"对上"再跑，不许跑完靠嘴说。
   - 字母链"对上"的定义：把各模块按其节点在链上的平均位置排序后，
     模块应构成**连续区段**（每个模块的节点占据链上连续区间），
     且区段顺序与链顺序一致；判据：连续度 ≥0.9 且顺序单调（Spearman=1）。
   - 分叉"对上"的定义：两条分支的**分支独有节点**应落在不同模块中
     （分支分离指数 ≥0.9）；共享前缀节点需**预注册**其期望归属方式之一
     （二选一：单独成团 / 成员跨多团），跑前选定，跑后不得更改。
   **追加**：若对不上，判定为"与 L0 结构无关的噪声"，如实记录。

---

## 8. 远期方向记录：从"检索"到"生成"（2026-09-13，**仅记录，不排期**）

**前提：不动 L0。** 本节只把未来的"动力来源"候选写下来，不改变任何已验收范围、
不排期、不承诺实现。E31 之后 E32+ 再按当时的证据决定。

### 8.1 问题陈述

L0 现在靠**共现统计**驱动、用**激活扩散**做事，能做的是**检索**——给定输入返回关联。
生成需要的不是"换轮子"（车还是那辆车），而是**换动力来源**：从"检索已有的"变成"合成新的"。
这是发动机级别的事。

### 8.2 已有的埋点：E25 就是能量模型的雏形

E25（模块内动力学）本质上是一个**能量模型**——它证明了本系统有**能量景观**
（模块状态可收敛到局部极小，即双稳）。这正是生成的物理基础：

```text
检索 = 在能量景观里找【已有】的局部极小
生成 = 从能量景观里【采样】出新的状态
```

所以 E25 不只是"模块内双稳"，它是**生成能力的埋点**。

### 8.3 候选动力来源与兼容度（按与底盘的兼容度排序）

| 候选 | 机制 | 与 L0 的兼容度 | 要新增什么 |
| --- | --- | --- | --- |
| **EBM（能量模型）** | 定义能量函数；生成 = 找/采样低能态 | **最高**（E25 已有能量景观） | 加"采样"算子 |
| **预测编码（PC）** | 预测误差驱动迭代逼近 | 高（激活扩散本质是一次预测） | 把"一次扩散"改成"多轮修正" |
| 扩散模型（DLM） | 从噪声迭代去噪（并行、任意顺序、可自修正） | 中（要加噪声调度器 → 属改底盘结构） | 噪声调度器 |
| SSM（Mamba 类） | 压缩的连续状态随时间长演化、线性复杂度 | 低（L0 是离散图） | 先建连续状态层 |
| HDC（超维计算） | 高维向量表示符号与运算 | 待评估 | 编码层 |
| SNN（脉冲网络） | 离散脉冲传递、低能耗 | 待评估 | 时间轴 + 脉冲编码 |

### 8.4 最可能的组合路径

```text
EBM 提供【目标】（能量最低态 = 最合理的输出）
预测编码提供【机制】（预测误差驱动迭代逼近）
激活扩散提供【实现手段】（现有的传播规则）
```

扩散模型与 SSM 作为备选；前两条路走不通再上。

### 8.5 与已有条目/边界的关系（前置约束，写进未来实验的预注册）

- **B12（非负权重造不出互斥）**：能量模型若需要负权重，**来源必须显式声明**（外部调制），
  不得悄悄越权——与 E25 的处理一致；
- **B20 / B23（尺度必须外部给定；单准则不可靠）**：生成若涉及"选哪一层/哪个尺度"，
  必须先解决尺度选择（B23 已证明嵌套结构上 eigengap/modularity 偏向粗层）；
- **闭包硬判据（BOUNDARY / C30）**：生成必须区分"**旧零件的新组合**"与"**凭空发明**"。
  未来所有生成实验的前置约束：输出节点集合仍须 ⊆ 曾学过集合；"新组合"要成为记忆，
  必须作为一次新经验再 `learn` 一次。

### 8.6 排期

**不排期。** 等 E32+ 之后再按当时的证据决定是否进入。

### 8.7 三种生成定义的分野（预备稿，2026-09-13；E33 的入口）

**E33 走定义 A（采样）**；B 是第二步，C 最后（或不做）。

| | **A 采样** | **B 组合** | **C 插值** |
| --- | --- | --- | --- |
| 定义 | 在 E25 的模块级能量景观上按 Boltzmann 分布采样，得到系统当前未处于的状态 | 两个已激活模式在某个上层被共现绑定成"组合符号"，该符号可像节点一样被再次激活 | 在节点表征空间取中间点（测地线/线性插值） |
| 与 L0 冻结 | **兼容**（只读景观） | 兼容（新层加在**上面**） | **冲突**（要先给节点加嵌入 = 改 L0 接口） |
| 功劳账 | 不需要重画 | **需要重画**（组合符号是谁造的？） | 需要（且最大） |
| 闭包（C30） | 天然满足 | 需显式声明组合符号来源 | **不满足**（中间点不是已学节点） |
| 靠现有实验 | E25 直接可用 | E29/E23 可用，但要先区分"组合 vs 组块" | 无现成基础 |
| 预注册难度 | 中 | 高 | 高 |
| 建议顺序 | **1** | 2 | 3（或不做） |

**E33（定义 A）的三个前置条件（预注册级，写设计稿时必须落进去）**：

```text
① "新状态"判据分两层（不能用"生成集合 ∩ max 可达集合 = ∅"——那太严，会误杀真生成）：
   【必满足·硬线】采样状态不违反闭包（输出节点 ⊆ 已学节点，C30）；
   【可证伪】采样分布【统计上偏离】max 传播的确定性结果：
       对同一种子：M = max 传播输出集合；S = 1000 次采样输出集合的分布
       判据：P(S ∈ M) < 0.5（一半以上概率落在 max 输出里 = 没生成）
             且 S 的熵 > H(M)（采样分布比 max 输出更多样）
   ——"偏离"是【统计上可区分】，不是"完全不重叠"。

② 语料必须先满足谱聚类可读性（**B24 的直接约束，是前置条件不是"已知限制"**）：
   可读 = 中位数 ARI >= 0.99 且 最小值 >= 0.90；E33 语料用【单层或 L=2】，
   否则"生成失败"与"读不出"不可区分。

③ β 是【预设参数】（与 λ/hop_decay/K 同级），必须写死默认值并做 β 扫描；
   不得把"某个 β 下看起来不错"当成涌现。
```

**定义 B 的关键判定规则（若走 B，必须先写死）**：

```text
组合符号（E33-B 候选）= 由两个已激活模式【共现绑定】产生的新符号
组块（L4 的 CH_n）    = 由同一单元【重复出现】产生的聚合符号

判定规则：
  新符号的创建由"重复次数超过阈值 K"触发 → 归为【组块】（E12/E18）
  新符号的创建由"两个模式同时激活"触发   → 归为【组合符号】（E33-B）
  两者可以共存，但【功劳各自记账】。
——这条不写死，E33-B 跑完分不清"组合成功了"还是"E12 的组块又长出来了"。
```

**明天入口**：E33 = 生成机制（采样）；上面三个前置条件已经想清楚，直接落成 DESIGN-E33。

---

## 8.8 远期假说指针（2026-09-16）

远期假说（北极星级）见 [IDEAS-Modules.md](./IDEAS-Modules.md)。
**不参与任何当前实验的设计、判据或结论登记**。
E42+ 的方向仍由用户从当前候选里拍板，不自动取自本文件。
