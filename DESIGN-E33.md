# E33 设计：从模块级能量景观采样，能否给出 max 传播之外的联合状态？

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-14。
依赖：E25 的模块级 W（块均值、最大块均值归一化）与 β=2.0；E24 的谱聚类划分；
E30 的 L0 签名口径；NEXT-PLAN §5 纪律 ⑥⑦⑧⑨。**有 numpy 依赖；不改任何层文件。**

---

## 0. 前置分析（E33 预检诊断，2026-09-14；**这是本实验的第一个产出**）

语料：E25 的 C1（`corpus_planted()`：15 节点 / 3 模块 / 4-5-6，ρ=0.0019）。

```text
① 可读性（B24 前置条件）：k=3 谱聚类 ARI（10 个 k-means 种子）中位 1.0000、最小 1.0000 → 通过；
② 模块级能量景观：E 范围仅 0.007；β∈[0.5,8] 上 H(S) 恒为 2.000 bit（= log2 4），
   P(S⊆M_strong) 从 0.2497 变到 0.2455 —— 该空间扫不动（β·ΔE_max = 8×0.007 = 0.056）；
③ 节点级：E 范围 33.0，β 能扫（H 从 6.181 降到 1.737；有效支撑 86 → 4），
   但 β≥1.5 后塌到 4 个状态，且这 4 个状态 = 模块级 4 个自由度（两者是粗粒化关系）；
④ M 的定义：M_all（阈值 1e-9）使 P(S⊆M) 恒为 1.0000（零区分度）；
   M_strong（阈值 0.5）才有非平凡值，但它随 β 非单调（0.1777 → 0.2379 → 0.1557，峰在 β≈1）。
```

**诊断**：在 E25 的 C1 语料上，能量景观在可读层只有 **4 个有效状态**；
"从能量景观采样出新状态"这条路径，在这个语料上受限于景观本身（ρ=0.0019 → 模块间几乎不耦合）。
本实验因此**不追求"采出新状态"**，而把命题收缩到可证伪的一步（见 §1）。

### 0.1 那 4 个状态是什么（预检实测，种子 g0_0，种子模块 m1 固定 +，β=2）

| 模块符号 (m0,m1,m2) | E | p(β=2) | 二值化后节点集合 |
| --- | --- | --- | --- |
| (+,+,+) | 0.000000 | 0.252411 | 全部 15 节点 |
| (+,+,-) | 0.005769 | 0.249516 | m0+m1（9 节点） |
| (-,+,+) | 0.006410 | 0.249196 | m1+m2（10 节点） |
| **(-,+,-)** | 0.007051 | **0.248877** | **m1（4 节点）← 与 max 传播的二值化输出相同** |

```text
max 传播（种子 g0_0，阈值 0.5）：+1 节点 = {g0_0..g0_3} = 模块 m1 → 对应符号 (-,+,-)
⇒ 4 个主导联合状态中，max 传播只能给出 1 个；
   另外 3 个（(+,+,+)、(+,+,-)、(-,+,+)）在 β=2 时合计概率 = 0.252411+0.249516+0.249196 = 0.751123
```

---

## 1. 命题（严格限定）

```text
E33 = 在 E25 的 C1 语料上，模块级能量景观的 4 个主导联合状态中，
      有 3 个是 max 传播给不出的；采样能否把【显著概率质量】分配给这 3 个状态？
```

**与 E25（C25）的区别（必须写在结论里，否则就是重复计数）**：

```text
E25 说：这 4 个（乃至 8 个）联合二值模式【存在】（枚举出来的）。
E33 说：这 4 个状态【可以被采样出来】，且采样分布把质量分配给了 max 给不出的那 3 个
       （β=2 时 0.751），并给出该质量随 β 的变化。
⇒ E33 的新内容 = 【采样分布 + 与 β 的关系】，不是"发现了新状态"。
```

**不声称**：不声称"系统能想象新状态"、不声称"这就是生成能力"（闭包 C30 仍成立：
采样状态的所有节点都是已学节点）。

### 1.1 与 E25 的 2³ = 8 个状态的关系（必须写清，否则会被读成矛盾）

```text
E25（C25）：不加钳制时枚举，联合二值模式数 = 2³ = 8（三个模块各 ±）。
E33（本实验）：把**种子所在模块 m1 钳制为 +1**，剩下 m0、m2 各 ±
              ⇒ 有效状态空间 = 2² = 4，是【条件分布】下的状态数。
两者不矛盾：E33 测的是"种子已知时的采样"（条件采样），不是无条件枚举。
```

---

## 2. 定义（全部写死）

```text
能量        : E(s) = -1/2 · s^T W s，s ∈ {±1}^n
模块级 W    : E25 口径 —— 块均值矩阵 / 最大块均值（max = 1）
节点级 W    : Mraw / max(Mraw)（Mraw = L0 共现矩阵，15×15，对角为 0；max = 130）
条件（clamp）: 种子节点固定为 +1（模块级则为"种子所在模块固定为 +1"）
二值化阈值  : 0.5（用于把 max 传播的激活值转成 ±1，与 E25 的模块二值化同阈值）
分布        : Boltzmann p(s) ∝ exp(-β E(s))；β 为【预设参数】（与 λ/hop_decay/K 同级）
状态空间    : 模块级 2^3（clamp 后 4）；节点级 2^15 = 32768（clamp 后 16384）
```

**两种算法同时报（互相校验）**：

```text
精确：枚举支撑内全部状态，直接算 p(s)（本规模完全可行 —— 见 E26 的做法）
采样：Gibbs（单点更新，P(s_i=+1) = σ(2β·h_i)，h_i = Σ_j W_ij s_j）；
      burn-in 2000 sweeps + 1000 个样本（每 20 sweeps 取一个，thinning）
```

---

## 3. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **精确分布的数值一致性**：对每个 β，精确 Boltzmann 分布满足 Σp = 1（容差 1e-12），且 4 个状态的概率全部 > 0 | 通过 | 精确枚举实现有误，先修 |
| P1a' | **（辅助，不参与 PASS/FAIL）Gibbs 采样覆盖诊断**：在 β ∈ {0.5, 2.0} 上与精确分布的 KL < 0.01；在 β ∈ {4.0, 8.0} 上记录"不同状态数"与"是否全部相同" | 仅记录 | — |
| P1b | **主判据：max 给不出的状态拿到了质量** —— 4 个主导状态中，max 给不出的 3 个的概率之和 S_3 = 1 − P(max 状态) | **每个扫描 β 上** S_3 ≥ 0.5（预检最小 0.751）；**不许只报某一个 β** | 记录负结果 → B25 |
| P1c | **空间结构**：节点级的**有效支撑**（定义见 §4：p > 1e-3 的状态数）随 β 单调不增，且 β≥1.5 时 = 4（= 模块级自由度数） | 单调 + 收敛到 4 | 记录反例 |
| P2 | 只读：层文件 sha256 + L0 签名（边集合排序逐项比较） | 通过 | 直接 FAIL |
| P3 | 双向结局：P1b 通过 → **C33**（严格限定）；不通过 → **B25** | 两分支都合格 | 不许跑完再挑解释 |
| 辅助 | 每个 β 的：H(S) 精确值、S_3、有效支撑、4 个状态各自的 p；精确 vs 采样的对比 | 仅记录 | — |

**P1c 的说明（与最初骨架的差异）**：骨架原写"4 个联合状态的相对概率与 β 单调相关"。
预检显示模块级的 β 效应只在小数第 4 位（0.2497 → 0.2455，幅度 0.4%），
把它当判据等于**测浮点噪声或跑一条恒等式**（精确枚举下 p ∝ exp(−βE) 的排序是构造性的）。
因此 P1c 换成**有效支撑随 β 的单调收敛**——这是景观的真实结构量（预检 86 → 52 → 21 → 4）。

### 3.1 v1 → v2 的改版说明（P1a 口径变更，2026-09-14）

**v1 的结果**：P0 ✓、**P1a ✗**（β=8 时 Gibbs vs 精确 KL = 27.51）、P1b ✓（S_3 ∈ [0.7503, 0.7545]）、
P1c ✓（支撑 86 → 4）、P2 ✓ → 整体 FAIL。v1 的 JSON 归档为
`outputs/exp33_landscape_sampling_v1_FAIL.json`。

**诊断**：不是实现 bug，是**单点 Gibbs 在低温柔性下不混合**——
模块级 W 对角 [0.7692, 0.6923, 1.0]、非对角最大 0.001923，翻转概率 `σ(−2βW_ii)` 在 β=8 时
只有 4.5e-06（2 万次尝试期望翻转 0.09 次）。实测访到的不同状态数：
β=0.5/2.0 → 4；β=4.0 → 2；β=8.0 → **1**（两种初始化结果相同）。

**改版（本版）**：

```text
① 主对象换成【精确 Boltzmann 分布】——E33 的核心命题是分布陈述
   （"0.751 的质量给了 max 给不出的 3 个状态"），不需要 MCMC 来证明；
   模块空间只有 4 个状态，精确枚举是平凡的（E26 先例）。
② P1a 改成"精确分布的数值一致性"（归一化 + 无零概率状态）；
③ Gibbs 降为【辅助记录】（P1a'），保留它在 β∈{0.5,2.0,4.0,8.0} 上的覆盖诊断，
   不参与 PASS/FAIL；
④ P1b / P1c / P2 的数字与口径不变（它们本来就基于精确分布）。
```

---

## 4. 预注册参数

```text
β 扫描 = 0.5 / 0.75 / 1.0 / 1.5 / 2.0 / 3.0 / 4.0 / 6.0 / 8.0（9 点；主判据取 β=2.0 处）
种子 = nodes[0]（g0_0，属于模块 m1）        二值化阈值 = 0.5
Gibbs：burn-in 2000 sweeps，样本 1000，thinning 20 sweeps，随机种子固定
有效支撑 = |{ s : p(s) > 1e-3 }|（阈值 1e-3，与预检口径一致）
P1a 阈值 = KL < 0.01（比特），在 β ∈ {0.5, 2.0, 8.0} 三点各跑一次
P1b：S_3 = 1 − P(max 状态)，在**每个**扫描 β 上算（判据 = 全部 ≥ 0.5）
只读 = 层文件 sha256 + L0 签名（边集合排序逐项比较）
运行环境：PYTHONHASHSEED = 0
```

---

## 5. 双向结局（跑前写死，两分支文本）

```text
分支 C33（P1b 通过 —— S_3 >= 0.5）：
C33：从 E25 的模块级能量景观采样，4 个主导状态中 **max 传播只给出 1 个**；另外 3 个在 β=2 时
     合计概率 **0.751（≥ 0.5）**，且该合计在扫描的每个 β 上均 ≥ 0.5（见辅助记录）。
     **结构关系**：节点级有效支撑从 **86（β=0.5）单调降至 4（β≥1.5）**，与模块级的 4 个自由度一致
     —— **节点级与模块级是粗粒化关系**。
     **范围限定**：① 这是**已知种子（种子模块钳制为 +1）下的条件采样**（2² = 4 个状态），
     不是无条件的"新状态发现"（E25 无条件枚举为 2³ = 8）；② 这 4 个状态在 E25（C25）中已被
     枚举为存在的联合二值模式——E33 的新内容是【它们可被采样、且采样分布把质量分配给了
     max 给不出的 3 个】，不是"发现新状态"；③ 不声称"系统能想象新状态"、"这等于生成能力"。

分支 B25（P1b 不通过 —— S_3 < 0.5）：
B25：在 E25 的 C1 语料上，Boltzmann 采样的质量集中在 max 传播能给出的联合状态上
     （S_3 = <实测值> < 0.5），因此"从模块级能量景观采样出 max 之外的状态"在该语料上不成立。
     与 §0 诊断一致：该景观的可读层只有 4 个有效状态，模块间耦合 ρ=0.0019 太小。
```

---

## 6. 产出物

- `experiments/exp33_landscape_sampling.py` + `outputs/exp33_landscape_sampling.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C33 / 负结果 B25）；
- 不改任何层文件；不新增层；不引入 stub/语义/奖励。

---

## 7. 边界与不做

- **不与 E25 重复计数**：E25 是"状态存在"，E33 是"状态可采样 + 质量分配"（见 §1）；
- **不做"更深景观"**：提高模块间耦合会改变 E25 的语料前提（C25 的结论建立在弱耦合上），
  属于另一个实验（需重新预注册），不在 E33 内；
- **不做预测编码路线**：作为 E33 之后的分支 B 候选，单独登记（不在本实验）；
- 不把采样结果说成"想象/理解/意识"（B07）；闭包（C30）仍然成立。
