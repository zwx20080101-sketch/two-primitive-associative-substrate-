# 未覆盖 / 待探索问题（记录）

状态：**仅记录，不在本轮实现**。完成"中文报告 + git 提交 + 网页玩具 v3"后
自查发现的理论缺口，先记下，待后续实验。日期：2026-09-06。

## 缺口：分层影响 / 组合律（上层建筑返回的数据如何合成一次输出）

**问题**：
当 L1（方向）、L2（三元组）、L4（组块）**同时开启**时，
它们各自返回的统计/结构如何叠加、竞争或分工，
最终**合成同一个读取输出**？"上层靠返回的数据控制下面的输出"
这个因果过程，能否被**拆解验证**（即：综合输出是否能由各层的返回量复合出来）。

**为什么之前没覆盖**：
E0–E22 做的是"**一次一层、每层一个现象**"的自下而上能力目录（E1/E4→L0，
E5→L1，E15→L2，E12→L4，E17/E19→stub），外加消融（一次关一个）、规模、统计
与边界 B01–B07。只做了"**单开 / 单关**"，没有做"**多上层同时读取时的组合分解**"。
LAYERS.md 总原则第 3 条（"上层可以观测、过滤、组合 L0/L1 的输出，绝不改动下层记忆"）
是**设计原则**，尚未被做成可证伪实验。

**理论位置**：介于"每层各自的现象"与"合成出一个系统行为"之间，
是"控制 = 返回数据"这一论断的关键检验。

**未来实验设想（未实施）**：

- 场景：26 字母链 + 按组块化的单元（如 ABC / DEF / …），L1+L2+L4 全开；
- 面板逐层显示：该层"返回了什么 → 如何影响 → 单独贡献"；
- 再加一行"综合输出"，检验能否由各层返回量精确复合；
- PASS：综合输出可分解为各层返回量的组合；
  FAIL：出现不可分解的结果，或发现某层悄悄改动底层 N。

**引证**：LAYERS.md 总原则 3；CLAIMS-REGISTRY.md C01–C22（单层现象 + 边界 + 方法）。
