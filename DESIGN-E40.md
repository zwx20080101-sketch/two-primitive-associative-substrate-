# E40 设计：无条件采样（解开种子模块钳制，8 状态全开）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：E33（C33 / B26，本实验是其**无条件版本**）、E25（C25，8 状态存在性已登记）、
E38（B32 / C33 限定）、E24（C24 语料）、E30（C30 闭包）、L0-SPEC。
**不改任何层文件（0 行改动）；不引入 stub / 语义 / 奖励。**

---

## 0. 概念基础

### 0.1 并行回火（PT）是本实验新增的**读取端工具**

```text
PT 与 E33 的 boltzmann() 并列，同属【读取端工具】——不写回基底、不改层文件、不进 LAYERS。
理由（B26 的直接后果）：单点 Gibbs 在 8 状态、β ≥ 4 时不混合（预检实测 β=8 → 1/1 覆盖），
  而 E40 的判定点包含 β=8。PT 实测在全部判定 β 上覆盖 8/8（中位与最小）。
基础 Gibbs 保留为对照与 B26 复现点（不是主口径）。
```

### 0.2 生成的定义式（P7a）与它和 E33 判据的层级关系

```text
P7a：生成集合 = 采样可达（8 个模块状态） ∖ 单种子 max 可达（3 个），按【集合差】定义。
     可判定性：两边都是 8 状态枚举的子集，逐元素比较；不依赖事后挑选。

与 E33 判据的层级：
  E33：P(S ∈ M) < 0.5   —— 【概率层】（采样有多大概率落在 max 输出里）
  E40：集合差           —— 【集合层】（存在 5 个状态：采样到、max 到不了）

【P7a 比 E33 判据强，但不能单独立住】：若采样的 0.99 质量仍落在 max 可达的 3 个状态里，
  集合差仍成立、而生成是空的。故 **P7a（存在）与 P7c（质量分配）必须联合**。
```

### 0.3 v1 的教训：预检数字不可跨参数外推（纪律⑨的新样本）

```text
v1 正式跑的唯一 FAIL 是 P1 的【对照条款】，不是主判据。根因是【DESIGN 内部不一致】：

  DESIGN 把 Gibbs 对照的期望写成"β ≤ 2 → 8/8；β = 8 → 1/1" ——
  那是从【n=1000 的预检】得来的；
  而 DESIGN 把运行参数定为【n = 5000】。
  样本翻 5 倍后实测：β=4 的覆盖从中位 5 变为 8/8；β=8 从"1"变为"中位 2 / 最小 1 / 最大 2"。

判据 FAIL 的原因【不是机制】，是判据条款把对照的期望写成了精确值，而该期望与运行参数不同源。

修订方向（v2）：
  P1 只判【主口径】（PT 覆盖）；对照条款降为 P1a'（只报告，不判定）。

一般化（写进纪律⑨的同族）：项目已有"预检形容词不可进预注册"；
  这次是【预检数字不可跨参数外推】—— 任何预检数字进入预注册前，
  必须核对【参数（n / β / 种子数 / 语料）是否与运行一致】。
  v1 的 FAIL JSON 归档为 exp40_unconditional_sampling_v1_FAIL.json。
```

---

## 1. 预检摘要（2026-09-15，只读，未动任何文件）

### 1.1 对照三口径

```text
(a) 单种子 max 传播 → 模块级符号（阈值 0.5，15 个节点逐一当种子）：
      (1,-1,-1) ← 模块 0 的 5 个种子      (-1,1,-1) ← 模块 1 的 4 个      (-1,-1,1) ← 模块 2 的 6 个
    ⇒ 单种子可达 3 / 8。每个种子只点亮它【自己所在的模块】（模块内 strength = 1.0 → 全模块 ≥ 0.5）
(b) 单种子输出集合（节点级）的并集 = 15 / 15 —— 作为对照【无区分力】，已排除
(c) 允许多种子（每模块取一个节点当种子）：8 个目标命中 7 个；唯一不可达 = (-1,-1,-1)
    原因：任何非空种子集都会点亮其所在模块 ⇒ "全关"在检索逻辑下封闭
    ⇒ (c) 作【辅助报告】，不参与主判据（主对照用 (a)，与 E33 同层级：单节点输入）
```

### 1.2 8 状态的质量分配：近乎均匀，β 几乎不起作用

```text
β=2.0 完整分布：0.126206 / 0.126206 / 0.124758 / 0.124758 /
                0.124598 / 0.124598 / 0.124438 / 0.124438

      β     单种子可达 3 态    ("全关"态)     m1=+1 四态总    最大单态
     0.5    0.374699          0.125301       0.500000       0.125301
     1.0    0.374398          0.125602       0.500000       0.125602
     2.0    0.373794          0.126206       0.500000       0.126206
     4.0    0.372581          0.127419       0.500000       0.127419
     8.0    0.370132          0.129868       0.500000       0.129868

β 从 0.5 到 8（16 倍），单种子可达 3 态的质量只从 0.3747 动到 0.3701（差 0.0046）。
⇒ E33 赖以立论的"β 扫出确定性 → 随机性过渡"，在无条件 8 状态下【没有对应物】。
```

### 1.3 B26 在 8 状态下更严峻（1000 样本覆盖的不同状态数，10 个 RNG 种子）

```text
β      单点 Gibbs      多起点(10链)     并行回火
0.5    8 / 8           8 / 8           8 / 8
1.0    8 / 8           8 / 8           8 / 8
2.0    8 / 8           8 / 8           8 / 8
4.0    5 / 4           8 / 7           8 / 8
8.0    1 / 1           6 / 4           8 / 8        ← 上限 8

E33（4 状态）时 β=8 覆盖 1 个；8 状态下仍是 1 个 ⇒ 复现且更严峻（模式翻倍、低温更难跨越）。
覆盖问题集中在 β ≥ 4；β ≤ 2 基础采样器已够。
```

### 1.4 与 E33 条件采样的精确对照

```text
E33 的条件 4 状态（m1 钳制 +1）在【无条件 8 状态】分布里的质量：
  [0.124438, 0.124758, 0.124598, 0.126206]，总计 = 0.500000（精确）
  归一化后 = [0.248877, 0.249516, 0.249196, 0.252411] = E33 的 p3（逐位相同）

⇒ 条件化 = 限制 + 重归一化，精确成立。E40 相对 E33 的"新内容" = 另外 4 个状态（m1=−1），
  携带 0.500000 的质量；原 4 态的相对质量【完全不变】，没有被稀释。
```

### 1.5 PT 参数与样本数的实测（n 必须从 E33 的 1000 提高）

```text
β     n=1000 KL中位/最大    n=5000 KL中位/最大      n=20000 KL中位/最大    覆盖(全部 n)
0.5   0.00481 / 0.01358 ❌  0.00089 / 0.00160 ✓    0.00023 / 0.00029 ✓   8/8
2.0   0.00422 / 0.00847 ✓  0.00121 / 0.00169 ✓    0.00023 / 0.00075 ✓   8/8
8.0   0.02257 / 0.04651 ❌  0.00356 / 0.00913 ✓    0.00079 / 0.00111 ✓   8/8

n=1000 时连 β=0.5 都 FAIL（最大 0.0136）；n=5000 时三个判定 β 的最大值都 < 0.01；
n=20000 余量约 9 倍但纯 Python 下约需 7 分钟。取 n=5000（余量问题见 §2 已知风险）。
交换接受率（阶梯 β·[0.25,0.5,1,2,4]）：β=0.5 → 0.999、β=2 → 0.997、β=8 → 0.989。
```

### 1.6 基态简并（B34 的措辞依据）

```text
(-1,-1,-1)  E = -1.235577   p(β=2) = 0.126206
( 1, 1, 1)  E = -1.235577   p(β=2) = 0.126206     ← 同能量（s → −s 全局对称）

⇒ 基态流形有【2 个分支】，检索只覆盖 1 个（(1,1,1) 侧），(-1,-1,-1) 侧在任何种子集下封闭。
  "全关态是唯一能量最优"的说法【不成立】，措辞必须按分支讲。
```

---

## 2. 预注册参数（跑前定死，跑后不改）

```text
语料：E24/E25 的 C1（corpus_planted()，15 节点 / 3 模块）—— 与 E33 同
模块级：W3 = block_matrix(Mraw, assign, scale)（E25 口径；assign 用 spectral seed = 20260911）
状态空间：S8 = all_states(3) = 8 个（无条件）；对照用的 S3 = all_states(3, {seed_mod: 1}) 仅用于与 E33 比

【采样器】
  主口径：并行回火（PT）
    阶梯   = β·[0.25, 0.5, 1, 2, 4]（5 个温度，目标 β 取中间那个）
    交换   = 每扫一次尝试一对相邻交换（接受率实测见 §1.5）
    burn-in = 2000 扫；采样间隔 = 20 扫
    样本数 n = 【5000】；RNG 种子 = 5000..5009（10 个）
  对照口径：单点 Gibbs（同 burn-in / 间隔 / n），复现 B26 在 β=8 的 1/1 覆盖

【β】
  判定点 = {0.5, 2.0, 8.0}（0.5 高温端 / 2.0 与 E33 同点 / 8.0 低温端）
  辅助扫描 = {1.0, 4.0}（补全曲线，不判定）

【辅助 β 的样本数 = 3（预设值，跑后不改）—— 硬线】
  判定点保持 10 个种子；辅助 β {1.0, 4.0} 降到 3 个种子（RNG 5000..5002）。
  报告里【必须显式标注 n=3】，不得与判定点的 n=10 混淆；3 个种子的"中位数 + 最小值"
  统计意义单薄，不标样本量会让后人误以为也是 10 个种子（纪律⑨：标注样本量）。
  辅助 β 的任何数字【不进入任何判据】，只作曲线补全。

【阈值】
  KL 阈值 = 0.01（判定 β 上取 10 个种子的【最大值】）
  生成质量阈值 = 0.5（5 个生成状态的总质量）
  reach/二值化阈值 = 0.5（与 E33 的 BIN_THR 一致）

【已知风险（风险预告，不是 caveat）】
  n=5000 时 β=8 的 KL 最大值实测 0.00913，距阈值 0.01 只有约 9% 余量。
  若正式跑出现任意一个种子 > 0.01 → P7c 在 β=8 上 FAIL，如实记录，不得用"中位数过了"
  或"余量偏薄"解释（纪律⑨：跑后不改判据）。

运行环境：PYTHONHASHSEED = 0
```

---

## 3. 判据（预注册）

### 3.0 判据分层（跑完读结果时按此归类，避免"报告项异常"被误读成"判据 FAIL"）

```text
· 实质判据（可 FAIL）      ：P1 采样覆盖 / P2 支撑差异 / P3 生成质量 / P7c KL
· 形式审计（预期恒成立，仍必须报违例数）：P4（节点级闭包）/ P4b（状态空间闭包）/ P5 / P6
· 辅助（不参与 PASS/FAIL） ：P1a'（基础 Gibbs 的 B26 复现记录）
· 定义式（按定义给出，不单独判定）：P7a
· 报告项                  ：P7b 分层
· 只读                    ：P8
```

### P0 前置：语料可读性

```text
C1 的 k=3 ARI 中位数 = 1.0000、最小值 = 1.0000（E38 已实测确认，同语料、同 assign 种子 20260911）。
本实验【不重测】（引用 E38 的数值即可）；若为审计需要，重跑一次作为报告项。
```

### P1 采样覆盖

```text
【v2 修订】本判据【只判主口径】—— PT 在判定 β ∈ {0.5, 2.0, 8.0} 上覆盖 8 / 8（中位与最小都是 8）。
  报告：每个 β 的覆盖数（中位 + 最小 + 最大）。
【v1 的挂点】原判据把基础 Gibbs 的对照期望"β=8 → 1/1"也写成了判据条款，
  见 §0.3（预检 n=1000 的期望被搬到 n=5000 的运行上）⇒ 该条款降为下面的 P1a'。
```

### P1a' 辅助（**不参与 PASS/FAIL**）：基础 Gibbs 的 B26 复现记录

```text
基础 Gibbs（n=5000，10 个种子）：β ≤ 4 覆盖 8 / 8；β = 8 覆盖 中位 2 / 最小 1 / 最大 2。

【P1a' 依据说明】预检在 n=1000 下记录 Gibbs β=8 覆盖 = 1；正式跑 n=5000 时该值变为
  中位 2 / 最小 1 / 最大 2。两值均正确，差异来自【样本数】——
  【预检数字不可跨参数外推】（纪律⑨）。本项只报告，不判定。

【B26 的样本数限定（不单独占编号）】B26 的量化数字必须与 n 一起引用 ——
  「β=8 覆盖 1」在 n=1000 下成立、「β=8 覆盖中位 2」在 n=5000 下成立；
  脱离 n 引用会误读。这是对 B26 表述的一次强化。
```

### P2 支撑差异（主判据之一）

```text
max 对照 (a)（单节点种子）给出 3 / 8 个模块状态；采样给出 8 / 8。
⇒ 生成集合大小 = |采样可达 ∖ 单节点 max 可达| = 5。
辅助：对照 (c)（任何种子集）给出 7 / 8，唯一不可达 = (-1,-1,-1)。【不参与主判据】
报告：三个集合都列出（3 个可达 / 5 个生成 / 8 个采样可达）。
```

### P3 生成质量

```text
5 个生成状态的总质量 > 0.5，且在【每个扫描 β 上】成立。
预检实测：β=0.5 → 0.625301、1.0 → 0.625602、2.0 → 0.626206、4.0 → 0.627419、8.0 → 0.629868。
```

### P4 闭包硬线（C30）

```text
所有采样输出 ⊆ 已学节点。本实验在模块级采样，故逐项审计"每个模块状态映射回的节点集合
是否 ⊆ 已学的 15 个节点"（预期恒成立，仍必须报违例数 = 0）。
```

### P4b 状态空间闭包（与 P4 并列的第二条硬线）

```text
采样得到的模块状态集合 ⊆ E25 无条件枚举的 8 个状态（S8）。
这是【状态空间闭包】而非节点闭包 —— 比 P4 更贴近本实验的层级（E25 的直接口径）。
预期恒成立（E25 已枚举 8 状态），仍必须报【越界状态数 = 0】。
【与 P4 的关系】P4 保节点级闭包（C30 的直接口径），P4b 保状态空间闭包（E25 的直接口径）；
  两条一起，硬线才完整。不做"节点级展开"审计 —— 一个模块状态对应一族节点配置
  （B32 量化的那件事），节点级展开无法逐位比较，与 E40 的模块级层级不匹配。
```

### P5 与 E33 一致性

```text
条件分布（m1=+1 的 4 状态，用 S3 的 Boltzmann）= 无条件分布限制在 m1=+1 后重归一化，
逐位相同（容差 1e-12）。预检已确认：限制后总计 = 0.500000，归一化 = E33 的 p3 逐位相同。
```

### P6 全关的物理地位（B34 的支撑）

```text
(-1,-1,-1) 在【每个扫描 β 上都是最大单态】（0.125301 → 0.129868），
同时是【任何种子集都不可达】的唯一状态。
并须报告基态简并：(-1,-1,-1) 与 (1,1,1) 的 E 相同（-1.235577），β=2 时质量相同（各 0.126206）。
```

### P7a 集合差定义式

```text
生成集合 = 采样可达（8） ∖ 单节点 max 可达（3），【按定义】给出，逐元素比较。
不依赖事后挑选；不引入任何"看起来像生成"的启发式。
```

### P7b 分层报告（**必须分层，不合并**）

```text
5 个生成状态 = 4 个"单节点不可达、多种子组合可达" + 1 个"任何种子集都不可达（全关）"。
【措辞】报告中给的是【示例种子集】—— 一个模块状态对应一族节点级配置与一族种子集，
  判据是"存在一个种子集能到达"，示例只取"每个 + 模块各一个节点"的那一组。
预检实测的示例（每模块第一个节点）：
  (1,1,-1) ← [g1_0, g0_0]      (1,-1,1) ← [g1_0, g2_0]
  (-1,1,1) ← [g0_0, g2_0]      (1,1,1)  ← [g1_0, g0_0, g2_0]
  (-1,-1,-1) ← 不存在
```

### P7c KL 检查（= §2 的 n=5000 与阈值，不重复定义）

```text
判定 β（0.5 / 2.0 / 8.0）上，10 个种子的 KL(精确 ‖ 采样) 【最大值 < 0.01】。
【硬线】β=8 的 9% 余量偏薄，故判据必须严格：任意一个种子 > 0.01 即 FAIL，
  不得事后用"中位数过了"或"余量偏薄"解释（纪律⑨：跑后不改判据）。
报告：每个判定 β 上 KL 的【中位数 + 最小值 + 最大值】——判据依赖最大值，必须报。
```

### P8 只读

```text
六文件逐一实测哈希（synapse_net.py / v2 / v3 / order_layer / context_layer / chunk_layer）：
  跑前 = 跑后；synapse_net.py = cf99a07a0e60fd4d、v2 = ee6df589eb2f6983、v3 = 085be73a06f0694c（均不动）
L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后。
注：E40 只对 v1 的 net 做 max 对照、对模块级 W3 做采样，v2/v3 的字段保留 + reason（同 E38/E39 处理）。
```

---

## 4. 双向落点（跑前写死）

### 通过 → C39（生成 = 采样覆盖检索逻辑封闭外）

```text
C39：解开种子模块钳制后（8 状态全开），并行回火采样覆盖全部 8 个模块状态；
     其中 5 个是【任何单节点种子下 max 传播都到不了】的（集合差定义），
     它们在 β ∈ [0.5, 8] 上共携带 0.6253–0.6299 的质量（判据 > 0.5）。
     分层：4 个可由【多种子组合】到达、1 个（全关 (-1,-1,-1)）在任何种子集下都封闭。
     范围限定：① 主对照是【单节点种子】（与 E33 同层级）；多种子组合属 E34–E36 的读取端能力，
     不计入"基底自然扩散"；② 这 8 个状态的存在性已由 E25（C25）无条件枚举登记 ——
     E40 的新内容是【采样可及性 + 质量分配】，不是"发现新状态"；③ 不声称"系统能想象新状态"。
     语料限于 E24/E25 的 C1；v1/v2/v3 冻结不变。
```

### 通过 → B34（与 C39 同时登记）

```text
B34：max 传播的可达集在【全局自旋翻转】下不对称 —— 能量景观有两个简并基态
     (1,1,1) 与 (-1,-1,-1)（E 相同 = -1.235577，β=2 时各 0.126206），基态流形有 2 个分支；
     但 max 传播（任何种子集）只能到达 (1,1,1) 分支，(-1,-1,-1) 分支在检索逻辑下封闭
     （机制：任何非空种子集都会点亮其所在模块 —— 种子语义的直接推论）。
     采样覆盖两个分支且质量相同。
     范围限定：限于 E24/E25 的 C1 语料（3 模块 / 块均值口径）；
     这是"检索可达集 ⊊ 基态流形"的一个实例，不是"检索总是不对称"的全称断言。
```

### 不通过 → 如实记录

```text
不登记。记录哪条判据挂、实测值多少；若挂的是 P7c（尤其 β=8），
按 §2 的硬线如实记 FAIL，不得改阈值或改判据。
```

---

## 5. 影响评估

```text
(a) 与 E33 / C33 的关系
    E40 是 E33 的【无条件版本】。C33 的范围限定明写"已知种子下的条件采样（2²=4 状态）"，
    不覆盖 E40 ⇒ E40 必须独立登记，不改 C33 的文本。

(b) 与 E38 / B32 的关系（P5 的旁证价值）
    B32 的偏移（节点级 0.311960 vs 模块级 0.252411）是在【条件 4 状态】上算的。
    E40 的 P5 证明：条件分布 = 无条件分布的限制 + 重归一化（逐位相同）
    ⇒ 该偏移【不随条件化 / 无条件化改变】，是 B32 的独立旁证（不是新结论，不新起条目）。
    语料同源（同为 C1），故 B32 的范围限定无需修改。

(c) 读取端工具的定位
    PT 是 E40 新增的读取端工具（与 E33 的 boltzmann() 并列）；基础 Gibbs 保留为对照。
    两者都不写回基底、不触层文件 ⇒ v1/v2/v3 三份实现全部不动。

(d) 不做的事
    不改任何层文件；不新增层；不引入 stub / 语义 / 奖励；不声称"理解 / 意识"（B07）。
```

---

## 6. 产物与不做

**产物**

- `experiments/exp40_unconditional_sampling.py` + `outputs/exp40_unconditional_sampling.json`；
- 登记（双向，跑前写死）：通过 → **C39 + B34**；不通过 → 如实记录（不占编号）。

**必报清单**

```text
① 对照三口径：(a) 3 个可达状态 + (b) 并集 = 15（无区分力，仅记录）+ (c) 7/8
② 各 β 的采样覆盖数（PT 与基础 Gibbs 各报 中位 + 最小）
③ KL（每个判定 β 上报 中位数 + 最小值 + 【最大值】；判据依赖最大值）
④ 5 个生成状态的逐状态质量（全 β）+ 总质量（全 β）+ 分层标注（4 + 1）
⑤ 示例种子集（4 个多种子可达的状态）
⑥ 基态简并：两个基态的 E 与 β=2 质量
⑦ 闭包审计：违例数（预期 0）
⑧ 六文件哈希（前后）+ L0 签名（前后）
⑨ 预检 vs 正式跑【两栏对照】（纪律⑨）
⑩ 【采样 − 精确】的生成质量差（只报告，不判定）：每个判定 β 上 10 种子的
   差值【中位数 / 最小值 / 最大值】，并给出该量在 n=5000 下的标准误估计（约 0.0068）。
   v1 实测：β=0.5 +0.0021 / β=2.0 +0.0023 / β=8.0 +0.0100（符号一致，随 β 增大）。
   【本实验不下结论】+0.010 约 1.5 SE，单点在噪声内；"符号一致"是提示不是证据。
   若将来要判是噪声还是偏差，需先定阈值、需更大 n、需独立实验 —— 都是新命题，不属 E40。
⑪ B26 的样本数限定：β=8 的覆盖数必须与 n 一起引用（n=1000 → 1；n=5000 → 中位 2）。
```

**不做**

- 不改层文件、不新增层（PT 与 Gibbs 都是读取端）；
- 不把多种子组合当作"基底自然扩散"的对照（那是 E34–E36 的读取端能力）；
- 不用 (b) 作对照（无区分力，预检已判）；
- 不声称"能想象新状态"、"等于生成能力"；
- 预注册阈值跑后不改（KL 0.01、生成质量 0.5、n 5000、β 判定点、PT 阶梯与 burn-in / 间隔）。
