# Synapse-Net V0 参考实现 —— 实验设计

版本：v0.1（设计定稿）　日期：2026-09-06　状态：已实施（L0 冻结，E0–E22 全部 PASS）

前置依据：理论基线讨论 + V0 字母实验方案（共现绑定 + 激活扩散，两条公理，无 LLM / Embedding / 分类器 / Reward）。

## 冻结状态

V0 底层 (`synapse_net.py`) 已冻结, 之后任何实验**不得修改该文件**;
新能力一律以"上一层"形式添加, 层次规划见 [LAYERS.md](./LAYERS.md)。
第一层 L1(方向/顺序层) 已实现于 `order_layer.py`。

---

## 1. 一句话定位

从一个完全空白的网络出发，只实现 `learn`（共现绑定）与 `activate`（激活扩散）两条公理，用一组可重复、可证伪的字母实验回答三个问题：

1. 关联结构能否**仅凭经验共现**形成；
2. 一次激活能否**沿历史经验形成的结构**传播；
3. “记忆存在”与“当前能不能被想起”是否是**两个独立问题**。

---

## 2. 范围与边界

### V0 做

- 无类型 Node 的自动创建与观测；
- 无向共现连接的累计（`N_ab` 与 `last_seen_ab`，连接本体不衰减）；
- 全量激活扩散（弱连接也必须出现在输出里）；
- 纯函数读取过滤器 `attention_filter`，只作为实验观测工具，不属于底层。

### V0 冻结不做

- Embedding、LLM、分类器、Reward、情绪、人格、语言等任何上层模块；
- 注意力作为底层机制（注意力只存在于读取端）；
- 遗忘 / 删除节点或连接；
- 时间衰减生效（保留 λ 参数，默认 λ=0，留给 V2）；
- τ 滑动共现窗口（V0 约定：一次 `learn` 调用内的节点视为同一时间窗，τ 的细化留给 V2）；
- 方向性连接与事件内时序（共现本身对称，顺序信息由事件的先后次序编码）。

失败路线约定：任何实验 FAIL → 冻结现场 → 记录失败现象 → 只做“最小机制修改”并重新验证；不允许为了结果好看而提前往底层塞模块。

---

## 3. 本体定义

### Node

无类型、无内置语义、无属性。仅有观测元数据（不参与计算）：

```text
label, first_seen, last_seen, appear_count
```

### Connection（连接本体 = 永久记忆）

每条无向边记录两件事：

```text
N_ab        —— A、B 历史共现次数（永久累计，永不衰减、永不删除）
last_seen_ab —— 最近一次共现的时刻
```

### Effective Weight（检索时才派生的有效强度）

```text
W_ab(t) = N_ab · exp(-λ · Δt_ab)      Δt_ab = t - last_seen_ab
```

V0 默认 λ=0，故 W = N。这样同时满足：连接本体永久存在（N），时间只影响“当前检索到的强度”（W），与理论基线中的数学修正一致。

### Edge Strength（传播时使用的归一化强度）

从节点 u 出发看邻居 v：

```text
strength(u→v) = W_uv / max_{w ∈ N(u)} W_uw
```

含义：v 相对 u 的**最强邻居**的强度。例：A 的邻居 B（N=100）与 X（N=2）→ strength = 1.0 与 0.02。

注意：`strength(u→v)` 与 `strength(v→u)` 各自相对于自己的最强邻居归一化，允许非对称读取；传播本身仍走同一条无向边。

---

## 4. 两条公理的精确规则

### 4.1 learn(event, t=None)

```text
1. 若未提供 t，内部时钟 +1；
2. event 内每个节点不存在则创建（first_seen = t）；
3. event 内所有无序对 (u,v)：
      N_uv += 1
      last_seen_uv = t
4. 更新相关节点的 last_seen / appear_count（仅观测）。
```

一次 `learn` 调用 = 一个共现窗口：`[A, B, C]` 会产生 A-B、A-C、B-C 三条边（完全绑定），这正是“同时出现的东西被关联了”，不作任何顺序假设。

### 4.2 activate(start, hop_decay=0.9)

```text
1. 种子节点激活值 = 1.0；
2. 沿连接传播，每次传导：
      arrival = a(u) × strength(u→v) × hop_decay
3. a(v) = max(a(v), arrival)；仅当激活值严格提升时才再次发射；
4. 直到队列为空，返回 {node: activation} —— 全量，不截断。
```

由于每跳乘 ≤ 1 的因子，传播单调有界，必然终止；环不会产生增益（设计决策 D2）。

激活语义等价于：`a(v) = 所有从种子到 v 的简单路径中，hop_decay 与各边 strength 乘积的最大值`（best-path）。

单链 A–B–C–D（所有 count=1）：

```text
a(A)=1.0
a(B)=0.9
a(C)=0.81
a(D)=0.729
```

这里 `hop_decay`（本次脉冲的传导损耗）与连接记忆强度 `W` 严格分离：

```text
signal_next = signal_current × ConnectionStrength × hop_decay
```

---

## 5. 读取层（不属于底层）

```python
attention_filter(activations, threshold=None, top_k=None)
```

- 纯函数，对网络状态**零副作用**；
- threshold 作用于 [0,1] 激活标度，返回激活值 ≥ threshold 的节点；
- 语义：高阈值 = 专注，低阈值 = 发散 / 梦境 / 灵感；网络本体完全不变。

这使“底层输出全部结果、上层决定哪些进入当前认知”成为可执行实验，而不是口头架构边界。

---

## 6. 默认超参数

| 参数 | 默认 | 含义 | 何时启用 |
| --- | --- | --- | --- |
| `hop_decay` | 0.9 | 每跳脉冲传导损耗（纯传播层） | V0 生效 |
| `λ`（time decay） | 0.0 | 时间衰减率 | V2 |
| `τ`（共现窗口） | = 单次 learn 事件 | 窗口内节点视为同步 | V2 |
| 种子激活值 | 1.0 | 启动强度 | V0 |
| `threshold` | 实验自定（0.5 / 0.1 / 0.001 等） | 读取端过滤 | 仅观测 |

说明：若想与“B=1.0 / X=0.02”那类示例数字严格对齐，可把 hop_decay 视为读取刻度；默认 0.9 会等比例缩放一跳邻居，不改变相对排序（B/X 仍为 50:1）。

---

## 7. 实验注册表（先写预期，再执行）

所有实验从空网络开始；结果记录到 `outputs/`；断言只检查结构与序关系，数值记录到报告。

### E0 冒烟：空网络与单事件绑定

```text
learn([A, B])
```

预期：nodes=2；存在 A-B 边 count=1；不存在 A-Z 边；`activate(A)` 返回 {A:1.0, B:0.9}。

### E1 V0 主实验：26 字母链（纯机制验证）

```text
learn([A,B]) → learn([B,C]) → … → learn([Y,Z])，各 1 次
```

预期：

- nodes=26，edges=25；
- A-Z 之间不存在连接（除非经验中真正共现过）；
- `activate(A)` 全部 26 个节点可达，且激活值沿 A→Z 严格单调递减：

```text
A: 1.000000
B: 0.900000
C: 0.810000
…
Z: 0.9^25 ≈ 0.071790
```

这验证：Node Creation、Co-occurrence Binding、Activation Diffusion、Signal Decay —— 顺序信息经由事件流编码，而不是节点属性。

### E2 重复经验强度（N 是否改变后续传播）

场景：`[A,B]×100 → [B,C]×100 → [C,D]×100`，外加弱经验 `[A,X]×2`。

预期结构：

```text
A ===== B ===== C ===== D      (N=100)
 \
  = X                          (N=2)
```

`activate(A)` 全量输出必须**同时包含** B 与 X：

```text
A: 1.0      B: 0.9      C: 0.81     D: 0.729
X: 1.0 × (2/100) × 0.9 = 0.018
```

然后演示读取端：

```text
attention_filter(·, threshold=0.5)  →  A, B, C, D     （X 被过滤）
attention_filter(·, threshold=0.01) →  A, B, C, D, X  （X 可访问）
```

X 在底层输出中始终存在 —— “想不起 X” ≠ “X 不存在”。

### E3 阈值状态开关（底层不变，认知内容剧变）

对 E1 的同一份全量激活结果分别过滤：

```text
threshold = 0.5  → 约前 7 个节点可见（A…G）
threshold = 0.1  → 约前 22 个节点可见（A…V）
threshold = 0.001 → 全部 26 个节点可见
```

约束：三次过滤之间网络状态逐字节一致（读取不改变底层）。

### E4 逐对强化 + 链式走读（实验侧交互协议）

教师侧交互（第 1 轮增量，之后强化 N-1 轮）：

```text
发 A → 回复 A → 把 [A, B] 一起发回（绑定, N(A,B)+=1）
发 B → 回复 B → 把 [B, C] 一起发回
… → [Y, Z]
```

预期：

- 底层从 0 nodes / 0 connections 起步，字母只存在于实验侧；单字母事件只创建/刷新节点，不产生连接；
- 强化后每条相邻边 N = 轮数（“被一起提起的次数”），非相邻对 N=0；
- 直接打 A：信号沿链到达末端 Z（a(Z)=0.9²⁵）；
- 在末端打回 Z：信号反向到达起点 A；
- 实验侧走读器（不属于底层）每步取“最强未访问邻居”继续，完整输出 A..Z（反向输出 Z..A）。

### E5 L1 方向层：HELLO 正走 vs 倒走（顺序只来自经验统计）

反复"说出 HELLO" 100 次: 相邻字母对按先后喂给 L0 与 L1。

- L0(冻结) 只记录 H-e、e-l、l-o 的无向共现, 完全对称;
- L1 记录 H->e、e->l、l->o 各 100 次, 反向 0 次 + floor 保底。

预期与 PASS 判据:

- 从 H 打: 正向一路顺, o ≈ 0.9³ (每跳只损耗 hop_decay);
- 从 o 打: 第一跳 l 还容易(0.9), 再往深处每跳只剩 ~1/101 保底强度,
  H 的激活 < 0.0001 —— "倒背越走越费劲";
- threshold=0.5 时倒背只见 o、l; threshold=1e-5 才倒完整 o-l-e-H;
- 冻结的 L0 从 o 打, H 仍然 0.729 —— 顺序不对称只来自 L1, 不污染 L0。

已知折叠: HELLO 双写 l 在节点层是同一个节点, 链为 H-e-l-o;
拼写中重复位置的精确保留属于未来 L4 组块层。

### E6a 分叉与上下文：底层不判对错，只给强度分布

设计红线（经讨论确认）：底层不知道"哪个结尾是对的"；蛇与恐惧的例子里，
底层只存"蛇-咬-疼-恐惧"共现路径，恐惧是外部模块消费激活的结果。
因此所有判据都是相对强度，不是"答对/答错"。

四个子场景，各从全新空网络开始：

| 场景 | 经验 | 预期 |
| --- | --- | --- |
| S1 | [A,B,C]×100 + [A,B,D]×100（无上下文） | A,B 处 C 与 D 等强（诚实歧义） |
| S2 | [A,B,C]×100 + [A,B,D]×10 | C:D ≈ 10:1（重复连接强度决定偏置） |
| S3 | [X1,A,B,C]×100 + [X2,A,B,D]×100 | X1 下 C 占优，X2 下 D 占优（上下文=同窗共现） |
| S4 | 同一批经验只喂 L1 相邻顺序 | X1 在场，B 分叉处 C/D 仍等强 → L2 候选 |

实现: `experiments/exp6_fork_context.py`。

### E6b 翻译/同义：互译、经情景聚类、因果联想链

场景(只用冻结的 L0, 不加新层)：

| 场景 | 经验 | 预期 |
| --- | --- | --- |
| S1 符号-读音 | [A,唉]×100、[B,必]×100 | 打 A 出 唉、打 唉 回 A；A 不串到 B/必 |
| S2 同义经情景 | 组A: 共享 S 的 [S,P,Q,R]/[S,X,Y,Z]×100；组B: 无 S | 组A: 打 P 经 S 取到 X(0.81)；组B: X 完全不出现 |
| S3 因果联想链 | [加速,摔倒]×100、[摔倒,疼]×100 | 打 加速 → 摔倒 0.9 → 疼 0.81 顺出 |

原则：底层不存"翻译/因果"概念，只存共现；反馈(疼)只是链条上的普通节点。

实现: `experiments/exp6b_translation_synonym.py`。

### E7 密集瞬间：一次摔倒为什么记得住（带时间维，不同时注入）

模型：平日 100 天每天一次正常跑步（t=1..100）；摔倒那天按时间轴注入
约 1.5 秒内 400+ 带递增时间戳的脉冲（失衡前兆→失衡→空中→撞击→痛风暴→站起），
每个脉冲单独 learn，不做整窗同时注入。对照组：轻碰×1。

| 判据 | 结果 |
| --- | --- |
| 摔倒前打"快" | 只有日常邻居，无 摔/疼（路径尚不存在） |
| 疼 枢纽度 | 5（摔、地面、快、加速、站起） |
| 打 摔 → 疼 | 0.9（密集共现） |
| 打 空中 → 疼 | 0.81（第二跳预警，threshold 0.7 可见） |
| 打 快 → 疼 | 0.36（可达但弱） |
| 轻碰对照 | 仅 1 条边，无枢纽无预警 |
| 闭包 | 输出全部为学过节点 |
| 时间维 | last_seen(快,风)=100.0，last_seen(摔,疼)≈101.29 |
| L1 顺序链 | 快→不稳→失衡→空中→摔→疼 分向计数成立 |

结论：密度 + 时间轴即可产生强连接、枢纽与预警，不需要情绪乘数；
"一次 vs 一次"的区别在网络宽度与路径数，不在单条边的强度。

实现: `experiments/exp7_dense_episode.py`。

### E8 连接是图不是线：共享枢纽支撑多条句子

训练（每句 100 次，只喂相邻对/顺序对）：

```text
狗咬猫   -> [狗,咬] [咬,猫]
狗追老鼠 -> [狗,追] [追,老鼠]
猫追老鼠 -> [猫,追] [追,老鼠]
```

| 判据 | 结果 |
| --- | --- |
| S1 结构 | 5 nodes/5 edges；N(追,老鼠)=200（共用）；N(狗,猫)=0、N(咬,老鼠)=0（未共现无直连） |
| S2 枢纽 | degree(狗)=2、degree(追)=3 |
| S3 打狗 | 咬=追=0.9，猫=老鼠=0.81（两句候选并行，不选边） |
| S4 打咬 | 猫 0.9 > 老鼠 0.729（本从句更近） |
| S5 打老鼠 | 追 0.9；狗=猫=0.405（主语诚实歧义） |
| S6 L1 | 顺向计数与经验一致，反向=0 |
| S7 组合 | 走读器跨枢纽走出 狗咬猫追老鼠（旧零件新排列）；无新节点（闭包） |

结论：连接是图不是线；共享节点给出并行候选；组合可达但无直连
（N(咬,老鼠)=0 仍能从枢纽走通）——旧零件的新排列，不是学过的句子。

实现: `experiments/exp8_hub_graph.py`。

### E9 部分输入补全：缺口回填，不许无中生有

训练：五连链 A-B-C-D-E，相邻对各 100 次。

| 判据 | 结果 |
| --- | --- |
| 打 A | B=0.9、C=0.81、D=0.729、E=0.6561（全链回忆） |
| 打 A+C+E | 缺口 B、D 补出（0.9），五节点全在，无新节点 |
| 打 A+E | 中间 C=0.81（两端会合，V0 max 规则不叠加——记录） |
| 闭包 | activate(F) 对未学过的 F 直接 KeyError |
| 取回 | 输出全 ⊆ 训练节点集合 |

结论：补全 = 沿已存路径取回已存节点；缺口能被填，但填进去的只能是记忆里有的东西。

实现: `experiments/exp9_partial_completion.py`。

### E10 新旧程度：同样次数，更久没见 → 检索变弱但记忆永存

训练：[A,B]×100(t=1..100)、[A,C]×100(t=901..1000)、[A,D]×100(t=1901..2000)。
三条边 N 全等，唯一变量是 last_seen。λ 首次从 0 激活到 0.005。

| 判据 | 结果 |
| --- | --- |
| S1–S2 | N=100×3；last_seen=100/1000/2000 |
| S3 λ=0 | B=C=D=0.9（新旧一样强） |
| S4 λ=0.005 | D=0.9 > C≈0.006 > B≈7e-5（越旧越难想起） |
| S5 阈值 | 0.5 只见 D；1e-6 时 B/C/D 全在（弱但存在） |
| S6 λ 调回 0 | 同一网络 B/C/D 立刻回 0.9 |
| S7 存储 | N 仍=100，4 nodes/3 edges 未删 |

结论：时间衰减只发生在检索时刻（W=N·e^(−λΔt)）；连接本体永久。
"细胞新旧" = 检索强度，不是删除。

实现: `experiments/exp10_recency_lambda.py`。

### E11 多词干扰：共享词多了，单句还能否干净取回

训练：L1/L2/L5 三组（狗 分别挂 1/2/5 个句子，相邻对 ×50）；
带情景网络：5 句各配唯一 X_i 整窗共现 ×50。

| 判据 | 结果 |
| --- | --- |
| 拥挤度 | 打狗并行 0.9 候选 = 句子数（1/2/5），无偏好 |
| 独特动词 | 打"抓"，0.85 只见 {抓,狗,鸟}（本句独占） |
| 无情景 | 单给狗，5 候选全亮（歧义在共享枢纽） |
| 带情景 | X_i 下 0.85 只放出 {X_i,狗,V_i,O_i}，五句互不串扰 |
| 存储 | N 仍=50，节点/边不丢 |

结论：干扰不是 bug——歧义集中在共享枢纽，独特处依然干净；
要从枢纽选句，必须有情景这类额外共现结构（若无情景仍可分离，才需要 L2/读取端抑制）。

实现: `experiments/exp11_interference.py`。

### E12 组块与嵌套（L4 组块层）

L4 最小机制（`chunk_layer.py`）：

1. 同一带边界有序单元重复 ≥K 次（默认 K=5）→ 自动成块；
2. 块内保存顺序记录（允许重复元素，双写 l 在此还原）；
3. 块可当普通原子参与块级序列共现；块名为机器号 CH_n，不携带语义。

子场景与结果：

| 子场景 | 结果 |
| --- | --- |
| S1 HELLO | 100 次成块；spell=[H,e,l,l,o]；平面 L1 链只能 H-e-l-o |
| S1 原子性 | 打"课堂" → 块整体 0.9 唤起 |
| S2 块复用 | 红苹果块被"我喜欢…/他买了…"共用，degree=2，spell=[红,苹果] |
| S3 嵌套 | 咬猫成块 C1；块级序列 [C1,的,狗,追,老鼠] 中 狗-追 相邻 0.9；从句首走通整句 |
| S3 对照 | 平面链无块节点，"咬"仅 1 度，展开不出独立"咬猫" |

结论：同一"共现绑定"规则换尺度即成块；块=重复整体+顺序记录+可作原子。
嵌套目前依赖关系标记（的/that）与块级顺序，自由嵌套/角色绑定仍开放。

实现: `experiments/exp12_chunk_nesting.py`。

### E13 基底重复标记：同一节点短窗内二次激活的表示

机制：L1 打开有序自转移 n(汪→汪)，含义严格为"汪 之后紧跟着又响了一次汪"，
纯时间性标记，不附带语义。L0 不动。

| 判据 | 结果 |
| --- | --- |
| [汪,汪]×100 | n(汪→汪)=100 |
| 分开两次 [汪]（t=1、t=100） | n(汪→汪)=0（分散出现不算重复标记） |
| [汪,汪,汪] | n(汪→汪)=2（按相邻转移计数） |
| 打汪 | 仍只有 {汪:1.0}，无"强调"节点自动冒出 |
| 不串扰 | 未学过的 人→人 =0 |
| 回归 | E5 判据更新（HELLO 含 l→l 重复标记），全量 E0–E13 PASS |

结论：基底现在能表示"重复发生"这一事实；"两次=强调"的语义外推仍属语言模块（M2）。

实现: `experiments/exp13_repeat_marker.py`。

### E14 多路证据：max vs sum 会合规则对比

纯实验层对比（不改任何层文件）：

| 规则 | 单路 T | 双路 T | 比值 |
| --- | --- | --- | --- |
| max（现状，L0 同款） | 0.81 | 0.81 | 1.0（会合无增益） |
| sum（单次发射+封顶 1.0） | 0.81 | 1.0 | ≈1.23（会合有增益） |

- 循环安全：sum 采用"单次发射、冻结于出队时刻、已发射节点不再接收"，
  三角形环上终止且封顶；
- 成本：全局换 sum 需重基线 E9（会合点）、E8（并行候选）、E6a-S3（上下文+双路）；
- 建议：基底保持 max；若将来需要"多路佐证更强"，把 sum 做成读取端可选模式。

实现: `experiments/exp14_multi_path_evidence.py`（实验层，不改层文件）。

拍板结论：基底传播永久冻结 max；sum 保留为读取端可选模式（单次发射+封顶 1.0），
不全局替换底层公式，存量基线不改。遗留风险：sum 遍历拓扑≠底层无向图；
封顶 1.0 使 ≥2 路饱和，>2 路证据需归一化变体扩展。

### E15 高阶顺序：L2 三元组上下文（双前缀 → 下一个）

L2 最小实现（`context_layer.py`，独立外挂，不动 L0/L1）：
滑动相邻窗口 n(a,b,c)（非相邻不计数）；查询前两项返回下一候选强度分布
（计数 max 归一化）；只出分布、不选句。

| 判据 | 结果 |
| --- | --- |
| 滑动窗口锁定 | 只计 (s0,s1,s2)、(s1,s2,s3)；非相邻 = 0 |
| S1 上下文分叉 | [X1,B]→只有 C；[X2,B]→只有 D；L1 只看 B 时 C=D 等强 |
| S2 前缀泛化 | [A,B]→C=D、E=0；[Q,B]→E |
| S3 能力边界 | X/Y 在 3 前缀外 → [A,B] 仍 C=D 等强（需 3 前缀，不在 E15） |
| S4 存储观测 | 三元组种类随样本量单调不减且有界（结构化语料饱和 16，上限 512） |
| S6 基线回归 | L0/L1 行为未变 |

结论：L2 三元组补齐双前缀上下文；更长的 N 元时序依赖与 L1/L2 融合属未来课题。

实现: `experiments/exp15_higher_order_context.py`。

### E16 角色标记归属判定：语序/格标记能否区分 狗咬猫 vs 猫咬狗

训练：L1/L2 学 [狗,咬,猫]×100 + [猫,咬,狗]×100（语序编码）；
L2 另学 [狗,把,猫,咬]×100 + [猫,把,狗,咬]×100（"把"=受事标记，输入侧提供）。

| 判据 | 结果 |
| --- | --- |
| 方向分账 | n(狗→咬)=n(咬→猫)=n(猫→咬)=n(咬→狗)=100 |
| 整句取回 | 打狗→咬→猫；打猫→咬→狗（语序够用） |
| 中位词歧义 | 只打"咬" → 狗≈猫 等强（诚实边界） |
| L2 前缀消歧 | [狗,咬]→猫；[猫,咬]→狗 |
| 格标记 | [狗,把]→猫；[猫,把]→狗；[把,猫]→咬；[把,狗]→咬 |
| 闭包/基线 | 无新节点；标记来自输入侧；L0/L1/L2 未改 |

判定结论：角色可由"输入中的语序/格标记" + 基底绑定来区分，
无需把树结构下沉进基底；标记系统本身归语言模块（输入侧）。

实现: `experiments/exp16_role_markers.py`。

**M1（基底缺口实验）至此全部完成。**

### E17 M2-A 叠词外推（系统级）：基底 + 语言规则 stub → "人人"

训练输入：

```text
教过的叠词: [天,天]×100、[夜,夜]×100      → L1: n(天→天)=n(夜→夜)=100
规则反馈:   [天,R]、[夜,R] ×100            → R=语言模块打的"这是叠词"标记
资格特征:   [天,F]、[夜,F]、[人,F] ×100    → F=名词类特征
反例:       [的]×100                       → 的 无 F 绑定
```

流程与结果：

| 步骤 | 谁干的 | 结果 |
| --- | --- | --- |
| 重复标记 | 基底 L1 | n(天→天)=n(夜→夜)=100 |
| 规则标记 R | 语言模块注入 + 基底绑定 | R 绑 天/夜，不绑 的 |
| 特征检索 | 基底 L0 | 打 F → 天/夜/人 全在 |
| 外推前 | 基底 | n(人→人)=0（没人教过就无标记） |
| 外推判定/渲染 | 语言 stub（外部） | 人→人人；的→拒绝 |
| 回写记忆 | 输出听回 + 基底 | n(人→人)=1 |
| 最终"人人" | 系统整体 | 基底+stub 相辅相成，闭包不破 |

实现: `language_stub.py` + `experiments/exp17_system_reduplication.py`。

### E18 M2-B 拼写输出（系统级）：块记录 → 动作 stub → 听回闭环

流程：

```text
1) HELLO 带边界单元 ×100        → L4 成块 CH_HELLO（spell=[H,e,l,l,o]）
2) 打"课堂"                     → 块整体 0.9 唤起
3) MotorStub 读块               → 逐字母 H,e,l,l,o → "Hello"
4) 输出听回 L1 ×100             → n(H→e)=n(e→l)=n(l→l)=n(l→o)=100
```

| 判据 | 结果 |
| --- | --- |
| 块记录 | spell=[H,e,l,l,o]（5 位置） |
| 语境唤起 | 课堂 → CH_HELLO 0.9 |
| stub 输出 | H,e,l,l,o → "Hello"（双写还原，大小写按 token） |
| 听回闭环 | L1 补齐 n(l→l)=100——E5 缺的双写由自己输出闭合 |
| 闭包 | 读未知块 CH_NOPE → KeyError |
| 功劳记账 | 块记录/检索/听回=基底；渲染=动作 stub；整体=系统 |

实现: `motor_stub.py` + `experiments/exp18_spelling_output.py`。

### E19 M2-C 情绪调制（系统级）：强反馈是否需要独立调制轴

构造（次数全等，只差输入标记）：

```text
蛇链: [蛇,剧疼]×100 + [剧疼,剧]×100
棍链: [棍,轻疼]×100 + [轻疼,轻]×100
密度复核: 蛇×300 vs 棍×60（公共邻居 草丛×100）
```

| 判据 | 结果 |
| --- | --- |
| 基底不分轻重 | a(剧疼|蛇)=a(轻疼|棍)=0.9（密度相同则无差异） |
| 情绪 stub | fear(蛇)=2.7 > fear(棍)=0.9；躲避仅蛇触发 |
| 密度路径 | 300 vs 60 → 基底 0.9 vs 0.54（E7 复核） |
| 拍板 | 基底无需写侧调制轴；强反馈 = 输入密度 或 输入标记+外部读取增益 |

实现: `emotion_stub.py` + `experiments/exp19_emotion_modulation.py`。

**M2（系统级场景）至此全部完成。**

### E20 V1.1 消融/对照套件：效果是否来自机制本身

统一指标：每个现象在 机制ON / OFF / 随机或纯计数 / 标签置换 下取值。

| 现象 | ON | OFF | 基线 |
| --- | --- | --- | --- |
| A 方向 | 正/倒比值 10201 | ≈1 | 随机高比值率 0.00 |
| B 上下文 | 正确-备选=1 | =0 | — |
| C 翻译 | 命中 1 | =0 | 标签置换率 1.0（内容无关） |
| D 补全 | 命中 1 | 纯计数=0 | 标签置换率 1.0 |
| E 双写 | spell 精确 1 | 平面链=0 | — |
| F 角色 | 正确-备选=1 | =0 | 随机≈0.33（机会水平） |
| G 叠词 | stub 产出 1 | 基底单独=0 | — |
| H 情绪 | fear 差 1.8 | =0 | — |

结论：每个主假设都有量化消融证据；机制 ON 有效、OFF 退化、
随机近机会、纯计数不可达、机制与具体 token 无关。

实现: `experiments/exp20_ablation_suite.py`，数据 `outputs/exp20_ablation.json`。

### E21 V1.2 规模一致性：六关键现象 1x/10x/100x

六个现象（方向/翻译/枢纽/补全/双写/角色）在共享同层、标签互不冲突的网络上
累加 1/10/100 个实例，指标 = 判据通过率。

| 现象 | 1x | 10x | 100x |
| --- | --- | --- | --- |
| 方向 / 翻译 / 枢纽 / 补全 / 双写 / 角色 | 1.0 | 1.0 | 1.0 |

附章（L2/L4 快基准）：L2 10k 单元 learn 14ms、query 10ms；L4 5k 词成块 19ms。

结论：冻结公式在共享网络小→中规模无退化；成本近线性可忽略。

实现: `experiments/exp21_scale_consistency.py`，数据 `outputs/exp21_scale.json`。

### E22 V1.3 统计口径：随机输入族成功率 + Wilson 95% CI

六现象（方向/翻译/枢纽/补全/双写/角色）各抽样 60 个随机输入族
（随机 token + 随机重复次数 30–80）。

| 样本 | 结果 |
| --- | --- |
| 正样本（机制 ON） | 六现象率全部 1.0，CI [0.9398, 1.0] |
| 负样本（零效应） | 方向/翻译/补全/双写 0.0（CI [0, 0.060]）；枢纽 0.017（CI [0.003, 0.089]） |
| 负样本（二分机会） | 角色 0.567（CI [0.441, 0.684]，覆盖 0.5） |

结论：机制在随机输入族上稳定有效（率=1.0、CI 下限 0.94）；
负样本近零或处于机会水平——主证据具备统计口径。

实现: `experiments/exp22_sampling_statistics.py`，数据 `outputs/exp22_stats.json`。

---

## 8. V0 PASS 清单

| # | 判据 |
| --- | --- |
| P1 | 启动 0 nodes / 0 connections；输入 26 字母经验后得到恰好 26 nodes |
| P2 | `learn([A,B])` 产生 A-B；A-Z 不存在，除非经验中真正共现过 |
| P3 | `[A,B]×100` 后 `N_AB = 100`，且其有效强度大于只出现 1 次的连接 |
| P4 | 学习 A-B-C-D 后 `activate(A)` 能沿链传播到 D；且 `a(v) = a(u) × strength × hop_decay`（float 容差内成立） |

全部 PASS → V0 完成；任一 FAIL → 冻结输出、记录失败现象，进入最小机制修改流程。

---

## 9. 代码结构（核心预计 100–160 行）

```text
synapse-net-v0-embedding-python-ai/
├── DESIGN.md
├── synapse_net.py      # SynapseNet：learn / activate / snapshot
├── attention.py        # attention_filter（读取层纯函数）
├── main.py             # 运行 E0–E3，控制台表格 + outputs/ JSON 报告
├── experiments/
│   ├── __init__.py
│   ├── exp1_chain.py
│   ├── exp2_strength.py
│   └── exp3_threshold.py
└── outputs/            # v0_report.json（每次运行覆盖，确定性内容）
```

接口草案：

```python
class SynapseNet:
    def __init__(self, time_decay_lambda=0.0): ...
    def learn(self, event, t=None): ...          # 共现绑定
    def activate(self, start, hop_decay=0.9): ... # 全量激活扩散
    # snapshot / stats 只读接口，供断言与报告

def attention_filter(activations, threshold=None, top_k=None): ...
```

---

## 10. 决策记录

| 决策 | 理由 |
| --- | --- |
| D1 连接无向 | 共现本身对称；顺序信息由事件先后编码（V1+ 再讨论方向性） |
| D2 单次事件 max 吸收、严格提升才再发射 | 确定性、单调、防环，best-path 语义清晰 |
| D3 记忆强度与脉冲损耗分离 | signal = a × W × hop_decay，两参数可独立实验 |
| D4 每源 max 归一化 | 相对竞争可解释（B:1.0 / X:0.02）；原始 N 保留在 snapshot 中可随时读取 |
| D5 λ=0 | V0 冻结时间效应，避免“连接衰减”与“永久记忆”的数学冲突 |
| D6 注意力不进入底层 | “记忆存在性”与“可访问性”是两个独立实验变量 |

---

## 11. 后续路线（本次不做）

> 注：以下旧路线（V1–V5）已被 LAYERS.md 与 STATUS.md 的层次计划取代，
> 仅作历史参考。

| 阶段 | 内容 | 要回答的问题 |
| --- | --- | --- |
| V1 | 重复经验强度扫描 | 传播结果是否稳定随 N 变化 |
| V2 | λ 时间衰减 + τ 滑动窗口 | 旧经验检索强度下降但记忆永存 |
| V3 | 部分输入 → 联想恢复 | 残缺事件能否补全 |
| V4 | 竞争路径实验 | 多路径如何取舍（best-path 是否够） |
| V5 | 多模态节点 | 跨模态共现能否形成语义拓扑 |
