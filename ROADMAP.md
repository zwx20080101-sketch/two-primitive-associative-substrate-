# Synapse-Net 拆分路线图（按边界宪章）

日期：2026-09-06　约束：[BOUNDARY.md](./BOUNDARY.md)

## 0. 路线总目标

1. 把待验证问题按归属拆清：**基底 / 外部模块 / 系统整体**；
2. 做完拆分后的实验，得到一套**可复用系统**（公式冻结 + 结构稳定 + 接口明确）；
3. 只有当"上量 gate"通过、且上量要解决的问题被明确写出时，才上量
   （类似训练大模型，但绝不默认"规模会自动涌现新能力"）；
4. 在此之前，凡是没调查清、归属存疑、或理论上有问题的点，先做实验与调整。

## 1. 里程碑

| 里程碑 | 内容 | 出口标准 |
| --- | --- | --- |
| M0 | 边界宪章 + 历史功劳重标 | 已完成（BOUNDARY.md） |
| M1 | 基底缺口实验：重复标记、多路证据、高阶顺序表示、角色标记归属判定 | **已完成（四个问题均有结论）** |
| M2 | 系统级场景（外部模块以 stub 出现）：叠词外推、拼写输出、情绪调制 | **全部完成**（功劳记账写清基底/外部各自贡献） |
| M3 | 冻结可复用系统：公式清单 + 接口契约 + 参数表 + 复杂度基线 | 四件套已交付；L1 索引待办已清 |
| V1 | 科学验证补强：消融/对照套件 + 规模一致性 + 统计口径 | 证据缺口清零（当前主线） |
| V2 | 发表：论文包 + 预印本/仓库 | 发布报告一 |
| S-上量（原 M4） | 工程上量（仅在 S-gate 通过且目标明确后） | 按定义好的指标度量 |

命名约定：M0–M3 为工程准备（完成）；V1/V2 为科学验证与发表（当前主线）；
S-上量 为远期工程（原"M4 上量"）。当前研究路径不依赖 S-gate。

## 2. 待验证问题拆分（谁做 / 验什么 / 怎么验）

| 问题 | 归属 | 要验证的是什么 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 重复标记（汪 t₁、汪 t₂） | 基底 | 能否表示"同一节点短窗内二次激活"，且不附带语义 | E13（基底内） | 已实现(exp13_repeat_marker) |
| 叠词外推（汪汪→慢慢→人人） | 语言模块发起规则 + 基底提供检索/标记 | 系统整体能否外推；功劳如何记账 | 系统级场景(exp17, language_stub) | 已实现 |
| 分叉点预测（ABC vs ABD） | 基底表示高阶结构 + 外部做选择 | 基底激活分布能否反映"前两项"上下文（三元组结构） | L2-基底实验(exp15) | 已实现(双前缀)；3 前缀以上属未来课题 |
| 嵌套/角色（狗咬猫 vs 猫咬狗） | 已判定：角色=输入标记（格/语序）→基底绑定；树结构无需下沉 | 带施事/受事标记输入下能否区分 | exp16 | 已实现并判定 |
| 多路证据（max vs sum） | 基底传播规则 | 两路信号同达是否应比单路强 | 规则对比实验(exp14) | 已拍板：基底 max 冻结，sum 为读取端候选 |
| 成块阈值 K | 基底 L4 形成规则 + 输入结构 | K 与经验规模的关系，何时该成块 | 参数扫描 | 未做 |
| 强反馈/情绪调制 | 情绪模块（外部） | 脉冲密度之外是否还需要独立的调制轴 | 系统级场景(exp19, emotion_stub) | 已实现并拍板：基底无需写侧调制轴 |
| 新旧程度 λ | 基底 | 检索变弱、记忆永存 | 已由 E10 验证 | 完成 |
| 情景分离干扰 | 基底 + 输入结构 | 共享枢纽下的句分离 | 已由 E11 验证 | 完成 |
| 方向不对称 | 基底 L1 | 倒走费劲 | 已由 E5 验证 | 完成 |

## 3. 已定结论（直接进入复用，不再重验）

- L0：连接本体 = N + last_seen，永不删除；W = N·e^(−λΔt) 只在检索时派生；
- 激活传播：种子=1.0，arrival = a(u) × strength × hop_decay，max 吸收；
- 归一化：每源 max 归一化（相对最强邻居）；
- L1：方向分向计数，软反向（floor 保底）；
- L4：重复带边界单元 ≥K 次自动成块，块内顺序记录，块可作原子；
- 闭包：激活输出 ⊆ 曾学过的节点；
- 底层不判对错；记忆存在 ≠ 想得起（阈值是读取端）；
- 无中生有是禁止项；想象 = 旧零件组合 + 外部渲染。

## 4. 可复用系统交付物（M3 清单）

1. `FORMULAS.md`：所有公式冻结（L0/L1/L4 + 读取端），标注参数默认值与可调范围；
2. 接口契约：
   - 输入：有序事件流（token + 时间戳 + 边界标记）→ 各层 learn；
   - 查询：种子节点 + 阈值 → 全量激活图；
   - 系统调用：外部模块读激活、注入情景、渲染输出；
3. 参数表：hop_decay、λ、K、floor、归一化方式、阈值语义；
4. 复杂度基线：典型节点/边规模下 learn/activate 成本；
5. 每层归因模板：实验结论按 BOUNDARY.md 记账。

## 5. 上量 gate（全部通过才允许上量）

1. M1/M2 的每个 open 问题都有结论，或明确划归外部模块；
2. 公式冻结并文档化，接口可被外部模块稳定调用；
3. 上量目标明确写出（例如：更稠密语义 → 检索/互译质量提升），并有度量方法；
4. 不默认"规模会自动涌现新能力"（宪章：规则外推属语言模块，不靠纯符号规模）；
5. 上量过程中持续检查：闭包、边界、功劳归因；行为变化即回到实验。

## 6. 执行规则

- 每步先答宪章四问（基底参与哪步 / 外部 stub 提供什么 / 结论归谁 / 做到哪步停）；
- 失败 → 冻结现场、记录现象、只做最小机制修改；
- 修改只动对应层；`synapse_net.py` 继续冻结；
- 每个里程碑完成时做一次评审，再进下一步。
