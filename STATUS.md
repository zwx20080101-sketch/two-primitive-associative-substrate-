# Synapse-Net 实验状态总结与下一步计划

日期：2026-09-06　范围：L0 冻结 + L1/L2/L4 层 + 外部 stub（E0–E22 已跑）

---

## 1. 当前实现快照

| 组件 | 状态 |
| --- | --- |
| `synapse_net.py` (L0 共现基底) | **冻结**：共现绑定 + 对称激活扩散，永不修改 |
| `order_layer.py` (L1 方向/顺序层) | 已建：分向计数 + 方向强度公式 + 有序自转移（重复标记，E13） |
| `chunk_layer.py` (L4 组块层) | 已建：重复单元 ≥K 次自动成块 + 顺序记录 + 块级序列 |
| `attention.py` | 读取端纯函数，不改底层 |
| `main.py` + `outputs/` | 实验入口与 JSON 报告 |
| 实验 E0–E22 | 全部 PASS（见第 3 节） |

文档：`DESIGN.md`（实验设计）、`LAYERS.md`（层次规划）、`README.md`。

---

## 2. 已确立的理论原则（经讨论锁定）

1. **记忆 = 连接，激活 = 当前心智内容**：连接本体只存 N(共现次数) 与 last_seen，
   永不衰减删除；有效强度 W 在检索时派生。
2. **底层不判对错**：不存"蛇=危险"，只存"蛇-咬-疼"共现；恐惧/笔画/画面是
   外部模块消费激活路径的产物。
3. **无中生有闭包**：激活输出 ⊆ 曾 learn 过的节点；想象 = 旧零件的新组合 +
   外部渲染；组合要成为记忆，必须再 learn 一次。
4. **方向来自经验统计**：顺向次数高才顺；没发生过的方向只有 floor 保底
   （弱可逆，倒背"可能但费力"），不预设 ABCD。
5. **记忆存在 ≠ 想得起**：弱连接永远在全量输出里，阈值决定可访问性；
   注意力是读取端旋钮，不是底层机制。
6. **预测 = 时序检索**：预警（空中→摔→疼 抢先点亮）不是预测模块，是
   记忆检索跑在物理信号前面。
7. **连接是图，不是线**：共享枢纽产生并行候选集；顺序层决定"这次说出哪条"；
   角色由方向/标记编码；嵌套需要组块层。
8. **一次"事件"在信号层是一阵脉冲**：密度本身可以解释"一次摔倒为什么记得住"，
   不必然需要情绪乘数（E7 已验证）。
9. **每次只加一层、每层一个实验**：先写可证伪预期与 PASS/FAIL 判据，失败才做
   最小机制修改；禁止"为了跑而跑"。

---

## 3. 已跑实验结论

| 实验 | 问题 | 结果 | 结论 |
| --- | --- | --- | --- |
| E0 冒烟 | 两条公理能否运行 | 空网络→learn([A,B])→2 nodes/1 edge；count 可累计 | 原语可用，连接永久 |
| E1 26字母链 | 纯共现能否成结构 | 26 nodes/25 edges，无 A-Z 边；activate(A) 全链按 0.9 逐跳衰减到 0.0718 | 相邻结构由共现长出；L0 对称、无方向 |
| E2 重复强度 | N 是否改变传播 | A-B×100 → 0.9；A-X×2 → 0.018，X 仍在全量输出 | 频率调制强度；弱记忆存在但阈值可挡 |
| E3 阈值开关 | 同一底层能否状态剧变 | 0.5/0.1/0.001 → 7/22/26 节点可见，网络零变化 | 焦点/梦境=读取端；底层不变 |
| E4 逐对强化走读 | 反复学后双向是否可走 | A..Z 与 Z..A 走读均完整；边 N=100 | L0 双向等强（对称是设计结果） |
| E5 L1 方向 | hello 正倒走是否不对称 | 正走 0.729 到达 o；倒走 e≈0.008、H≈7e-5 | 顺序不对称来自经验统计（L1），L0 保持对称 |
| E6a 分叉/上下文 | 分叉点由什么决定 | 无上下文 C=D 等强；100:10 → 10:1；X1/X2 → C/D 2:1；L1 大二元在分叉处仍对称 | 底层给强度分布不判对错；上下文=同窗共现；L2 候选 |
| E6b 翻译/同义 | 表面不同如何成"同一个" | A↔唉 双向互译不串线；P 经 S 取到 X(0.81)，无 S 则分离；加速→摔倒→疼 顺出 | 语义=拓扑位置；共享情景聚类；因果=共现链 |
| E7 密集瞬间 | 一次摔倒为什么记得住 | 摔倒前打快无疼路径；时间轴脉冲流(不同时注入)后：疼 degree=5、摔→疼 0.9、空中→疼 0.81(预警)、快→疼 0.36；轻碰只留 1 条边 | 密度+时间轴即可产生枢纽与预警，无需情绪乘数；区别在网络宽度，不在单边强度 |
| E8 共享枢纽 | 一张图能否存多条互相重叠的句子 | 狗咬猫/狗追老鼠/猫追老鼠×100：追-老鼠 共用 200；打狗 咬=追=0.9、猫=老鼠=0.81 并行；打咬 本从句更强；打老鼠 主语诚实歧义；走读跨枢纽出"狗咬猫追老鼠"且无新节点 | 连接是图不是线；共享枢纽给并行候选；组合≠直连（N(咬,老鼠)=0 但可达） |
| E9 部分输入补全 | 缺口回填是否只取回已存节点 | A-B-C-D-E×100 后：打 A 全链回忆；打 A+C+E 缺口 B、D 补出(0.9)；打 A+E 中间 C=0.81；activate(F) 报错；输出全 ⊆ 训练节点 | 补全=沿已存路径取回已存节点，无法无中生有；两端会合在 V0 max 规则下不叠加（记录） |
| E10 新旧程度 | 同样 N、更久没见，检索是否变弱但记忆永存 | N(A,B/C/D)=100，last_seen=100/1000/2000；λ=0 时三者 0.9；λ=0.005 时 D=0.9>C≈0.006>B≈7e-5；阈值 0.5 只见 D、1e-6 三者全在；λ 调回 0 立刻全回 0.9 | 时间衰减只在检索时刻；N 与连接永久（细胞新旧 = 检索强度，不是删除） |
| E11 多词干扰 | 共享词多了，单句还能否干净取回 | 1/2/5 句 → 打狗并行候选 1/2/5 全 0.9；打"抓"0.85 只见本句 {抓,狗,鸟}；无情景 5 候选全亮；X_i 情景下各句 0.85 独占可见；N 仍=50 | 干扰不是 bug：歧义集中在共享枢纽，独特处依然干净；从枢纽选句需要情景共现结构 |
| E12 组块与嵌套 | 整词/双写/块复用/嵌套句 | HELLO 成块 spell=[H,e,l,l,o]（平面链只能 H-e-l-o）；红苹果块两句共用 degree=2；嵌套句 [C1,的,狗,追,老鼠] 中 狗-追 块级相邻 0.9、从句首可走通 | 同一共现规则换尺度即成块；块=重复整体+顺序记录+可作原子；平面链无块身份 |
| E13 重复标记 | 基底能否表示"同一节点短窗内二次激活" | [汪,汪]×100 → n(汪→汪)=100；分开两次 [汪] → 0；[汪,汪,汪] → 2；打汪仍只有 {汪:1.0}（不附语义）；人 不受影响 | L1 有序自转移 = 纯时间性重复标记，不带语义；语义外推仍归语言模块（M2） |
| E14 多路证据 | 两路同达是否应比单路强（max vs sum） | max：双路=单路=0.81（无增益）；sum（单次发射+封顶）：双路=1.0、单路=0.81（有增益，比值 1.23）；三角形环终止且封顶 | 纯实验层对比：max 稳定但会合无加成；sum 需"单次发射冻结"策略防回环，且换全局规则要重基线 E9/E8/E6a-S3 |
| E15 高阶顺序 | L2 三元组（双前缀→下一个）能否反映上下文 | 滑动窗口锁定；[X1,B,C]/[X2,B,D]×100 → L1 看 B 等强、L2 [X1,B] 只有 C；[A,B]/[Q,B] 前缀区分；X/Y 在 3 前缀外仍等强（边界记录）；存储膨胀单调有界 | L2 独立外挂层补齐双前缀上下文；更长 N 元依赖不在 E15；L0/L1 零改动 |
| E16 角色标记归属判定 | 语序/格标记能否区分 狗咬猫 vs 猫咬狗 | 双向都学后：打狗→咬→猫、打猫→咬→狗（整句取回）；只打"咬"→狗≈猫 等强（诚实歧义）；L2 [狗,咬]→猫、[猫,咬]→狗；格标记 [狗,把]→猫、[把,猫]→咬 | 角色可由"输入中的语序/格标记"+基底绑定区分，无需把树结构下沉进基底；标记系统本身归语言模块（输入侧） |
| E17 叠词外推(系统级) | 基底+语言规则 stub 能否合成"人人" | 教 天/夜 叠词(标记 R)与特征 F 后：n(天→天)=100；R 绑 天/夜 不绑 的；打 F 天/夜/人都在；外推前 n(人→人)=0；stub 输出"人人"、拒绝"的的"；听回后 n(人→人)=1 | 基底不发明规则；外推=语言 stub 用基底证据判定+渲染；回写=输出变新经验；闭包不破；功劳记账：最终"人人"=系统整体 |
| E18 拼写输出(系统级) | 块记录→动作 stub 输出→听回闭环 | HELLO 成块 spell=[H,e,l,l,o]；打课堂→块 0.9；MotorStub 逐字母输出 H,e,l,l,o→"Hello"；听回后 L1 n(l→l)=100；读未知块报错 | 块记录/检索/听回=基底；逐字母渲染=动作 stub；E5 缺的双写由**自己的输出**补齐；系统整体闭环 |
| E19 情绪调制(系统级) | 强反馈是否需要独立调制轴 | 次数全等只差标记 剧/轻：基底两疼同为 0.9（不分轻重）；情绪 stub fear(蛇)=2.7>fear(棍)=0.9、躲避仅蛇；密度路径复核 300vs60 → 基底 0.9vs0.54 | 强反馈两条路径：输入密度 / 输入标记+外部读取端增益；**基底无需写侧调制轴**；情绪模块=外部读取端 |
| E20 消融/对照套件 | 效果是否来自机制本身（非随机/纯计数） | A 方向 ON 比值 10201、OFF=1；B 上下文 ON=1/OFF=0；C 翻译 ON=1/OFF=0/置换率 1.0；D 补全 ON=1/纯计数=0；E 双写 ON=1/OFF=0；F 角色 ON=1/OFF=0/随机≈0.33；G 叠词 ON=1/OFF=0；H 情绪 ON=1.8/OFF=0 | 每个主假设都有量化消融证据：机制 ON 有效、OFF 退化、随机近机会、机制与具体 token 无关 |
| E21 规模一致性 | 六关键现象在 1x/10x/100x 实例规模是否仍成立 | 方向/翻译/枢纽/补全/双写/角色：1x、10x、100x 通过率全部 = 1.0；L2 10k 单元 learn 14ms/query 10ms；L4 5k 词成块 19ms | 冻结公式在共享网络小→中规模无退化；L2/L4 成本极低（旁证） |
| E22 统计口径 | 随机输入族成功率 + Wilson 95% CI | 六现象正样本率 1.0（CI [0.94,1.0]）；零效应负样本率 ≤0.017；角色无信息负样本 0.57（CI [0.44,0.68] 覆盖 0.5） | 主证据具备统计口径：机制在随机输入族上稳定有效，负样本近零/机会水平 |

---

## 4. 已知边界（还没解决，按计划逐个打）

- **爆发 vs 分散**：L0 只计 N，分不清"1 秒内 300 次脉冲"和"300 天里 300 次"；
- **上下文进不了大二元**：L1 相邻顺序在 B 分叉处保持对称（E6a-S4）；
- **自由嵌套/角色绑定**：L4 目前依赖"的/that"标记与块级顺序，真正递归与角色指派未解决；
- **会合不叠加**：多路证据在 V0 max 规则下不求和（E9 记录）；
- **渲染/感受/情绪调制**：属于外部模块与未来层，不在 L0/L1 承诺内。

---

## 5. 下一步实验计划（按优先级）

| 编号 | 要解释的现象/问题 | 用哪层 | PASS 判据要点 | 失败后最小机制 |
| --- | --- | --- | --- | --- |
| E7 | 一次摔倒=密集脉冲流：密度本身能否造出强连接、疼枢纽与"空中预警" | L0+L1 | 单次密集经历后：疼节点度数明显大于单次轻事件；打"快"或"空中"能取出疼（路径穿透）；全程无乘数 | 记录；候选=时间结构层（爆发 vs 分散） |
| E8 | 连接是图不是线：共享枢纽能否支撑多条句子 | L0+L1 | 重叠句子学完后共享词成枢纽；打句首能整句取回；有上下文时偏置其中一条，无上下文时候选并行 | 若不能 → L2 上下文层 |
| E9 | 部分输入补全（缺口回填） | L0 | 学 ABCDE 后打 A(+E) 时 B/C/D 由已存节点补出；输出无任何新节点（闭包判据） | 记录扩散偏差 |
| E10 | 新旧程度：N 相同 Δt 不同 | L0 现有 λ 参数（不改文件） | λ>0 后旧经验检索变弱、但 N 与连接仍永久存在 | — |
| E11 | 干扰：共享词变多后单句是否仍可分 | L0+L1 | 每句在各自上下文下仍可分离，串扰可控 | L2 / 读取端抑制 |
| E12 | 组块与嵌套：整词、从句插入、双写 | 需新增 L4 | 块节点可整体唤起；嵌套句共享结构可复现 | 递归/组合机制记录 |

### 计划执行规则

1. 每个实验动手前，先向用户列出预期与 PASS/FAIL 判据；
2. 一次只做一个实验，跑完才决定下一个；
3. 若 FAIL：冻结现场、记录现象、只做"最小机制修改"；
4. 所有实验继续遵守：无中生有闭包、底层不判对错、不修改 `synapse_net.py`。

建议顺序：E7（闭合"强反馈"讨论）→ E8（闭合"图不是线"讨论）→ E9 → E10 → E11 → E12。

已完成：E7（`exp7_dense_episode`）PASS —— 一次摔倒按时间轴注入（平日 100 天 +
摔倒约 1.5 秒内 400+ 带时间戳脉冲），疼 成为 5 度枢纽；摔→疼 0.9、空中→疼 0.81（预警）、
快→疼 0.36；轻碰对照仅 1 条边。

已完成：E8（`exp8_hub_graph`）PASS —— 三句共享 狗/追 枢纽：打狗 两个从句并行亮起；
打咬 本从句更强；打老鼠 主语诚实歧义；走读器跨枢纽组合出"狗咬猫追老鼠"，全程无新节点。

已完成：E9（`exp9_partial_completion`）PASS —— 打 A+C+E 时缺口 B、D 补出；
打 A+E 时中间 C=0.81；从未学过的 F 无法被唤醒（闭包）。下一步按计划做 E10（新旧程度）。

已完成：E10（`exp10_recency_lambda`）PASS —— λ 首次激活：同样 N=100，
越旧越弱（D=0.9 > C≈0.006 > B≈7e-5），阈值降到 1e-6 三者全在；
同一网络把 λ 调回 0，B/C/D 立刻全部回 0.9——衰减只在检索时刻，N 与连接从未删除。

已完成：E11（`exp11_interference`）PASS —— 拥挤度 1/2/5 句 → 打狗候选 1/2/5；
独特动词处干净取回本句；无情景时歧义集中在"狗"；各配 X_i 后 0.85 阈值下五句互不串扰。

已完成：E12（`exp12_chunk_nesting`）PASS —— L4 组块层：HELLO 成块还原双写、
红苹果块两句复用、嵌套句 [C1,的,狗,追,老鼠] 主谓在块级相邻且可整句走通。
原 E7–E12 计划全部完成；全量 E0–E12 全部 PASS。

已完成：E13（`exp13_repeat_marker`）PASS —— L1 打开有序自转移：n(汪→汪) 可表示
同窗连续重复（×100→100），分散两次不计数，三次→2；不附带任何语义。
E5 判据同步更新（HELLO 现含 l→l 重复标记）；全量 E0–E13 回归 PASS。

已完成：E14（`exp14_multi_path_evidence`）PASS —— 纯实验层对比 max/sum：
max 会合无增益（双路=单路=0.81）；sum 单次发射+封顶 1.0 后有增益（双路 1.0、
比值 1.23），但需防回环策略，且全局换 sum 需重基线 E9/E8/E6a-S3。
**拍板结论（已确认）**：基底传播永久冻结 max（best-path），存量基线不改；
sum 仅保留为**读取端可选模式**（自带单次发射护栏 + 封顶 1.0），不全局替换底层公式。
遗留风险：sum 是带访问标记的 DAG 遍历，拓扑≠原始无向图，输出不能与 max 直接做
底层机制对比；封顶 1.0 使 ≥2 路饱和，>2 路梯度区分度消失，未来需归一化变体。

已完成：E15（`exp15_higher_order_context`）PASS —— L2 三元组上下文：
滑动相邻窗口锁定；[X1,B]→只有 C、[X2,B]→只有 D（L1 只看 B 时 C=D 等强）；
[A,B] 与 [Q,B] 由第二前缀区分；3 前缀外的 X/Y 分不开（边界记录，不在 E15）。
L0/L1 基线零改动。

已完成：E16（`exp16_role_markers`）PASS —— M1 收官：语序与格标记下基底可区分
狗咬猫/猫咬狗（整句取回 + L2 前缀消歧 + [把] 标记绑定）；只给中位词"咬"仍诚实歧义；
角色处理归"输入标记+基底绑定"，树结构无需下沉；标记系统属语言模块。

**M1（基底缺口实验）全部完成**：重复标记、多路证据（已拍板）、高阶顺序表示、
角色标记归属判定——四个 open 问题都有结论。下一步进入 M2（系统级场景，外部模块 stub）。

已完成：E17（`exp17_system_reduplication`）PASS —— **M2 第一个系统级场景**：
语言规则 stub（`language_stub.py`）读基底证据，外推"人→人人"、拒绝"的→的的"；
输出被听回后基底记录 n(人→人)=1；七步功劳记账写清归属；闭包全程不破。
工作假说"底层同源、上层异构"与建模范围（新皮层；海马/小脑等不在当前范围）
已并入 BOUNDARY.md。

已完成：E18（`exp18_spelling_output`）PASS —— **M2 第二个系统级场景**：
MotorStub（`motor_stub.py`）读块记录逐字母输出 "Hello"（双写还原）；
输出被听回后 L1 补齐 n(l→l)=100——E5 时代的洞由自己的输出闭合；
读未知块报错（闭包）；功劳记账完成。M2 剩余：情绪调制。

已完成：E19（`exp19_emotion_modulation`）PASS —— **M2-C（M2 收尾）**：
强反馈两条路径验证——输入密度（300vs60 → 基底 0.9vs0.54）与
输入标记+情绪 stub 读取端增益（fear(蛇)=2.7>fear(棍)=0.9，躲避仅蛇）；
拍板：基底无需写侧调制轴，情绪模块永远在外部。

**M2（系统级场景）全部完成**：叠词外推、拼写输出、情绪调制——三类场景
（形式泛化/目标组装/价值反馈）都在同一冻结基线上跑通。下一步：M3 四件套。

已完成：**V1.1（E20 消融/对照套件）PASS** —— 八行统一消融表：
每个现象 机制ON 有效、OFF 退化、随机基线近机会/纯计数不可达、
标签置换率 1.0（机制与具体 token 无关）。数据在 outputs/exp20_ablation.json。

已完成：**V1.2（E21 规模一致性）PASS** —— 六关键现象（方向/翻译/枢纽/补全/
双写/角色）在共享网络上 1x/10x/100x 实例通过率全部 = 1.0，无规模退化；
L2/L4 快基准：10k 单元 L2 learn 14ms、5k 词 L4 成块 19ms。

已完成：**V1.3（E22 统计口径）PASS** —— 六现象各抽样 60 个随机输入族：
正样本率 1.0（Wilson CI [0.94,1.0]）；零效应负样本率 ≤0.017；
角色无信息负样本 0.57（CI [0.44,0.68]，覆盖 0.5 机会水平）。

**V1（验证补强）全部完成：消融(E20) + 规模一致性(E21) + 统计口径(E22)。
证据缺口清零，进入 V2（论文写作与发布）。**

已完成：**M3 四件套** —— FORMULAS.md（公式冻结）、INTERFACE.md（接口契约）、
PARAMETERS.md（参数表）、BENCHMARK.md（复杂度基线）已交付。
基准发现：L0 近线性（6000 节点全图 65ms）；L1 activate 为 O(N·E) 二次增长
（1000 节点 1.27s）——上量 gate 前保留待办：L1 邻接索引化（纯结构优化，不动公式）。
**L1 邻接索引已完成**（纯结构优化）：activate 1000 节点 1.27s→14.3ms、
6000 节点 116ms 近线性；公式语义未变，全量回归 E0–E19 PASS。
下一步：转入 **V1 验证补强**（E20 消融/对照套件 → 规模一致性 → 统计口径 → V2 发表）。
工程上量已改名 **S-上量**（原 M4），其 gate 评审面向远期，不阻塞当前研究主线。

## 冻结记录（2026-09-06）

- 中文报告（REPORT-zh.md / .pdf）与代码、数据、校验工具：已定稿并提交（v0.9.0）。
- 网页玩具 v3：**已冻结**；12 个实验场景可复现、含批量时间输入、保存/恢复配置与副本、
  激活=逐跳波纹 + 因果 Token 序列 + 能量流；
- 已知未覆盖缺口：**分层影响 / 组合律**（L1+L2+L4 同时返回数据如何合成一次输出），
  仅记录于 [OPEN-QUESTIONS.md](./OPEN-QUESTIONS.md)，本轮不实现，留待后续实验。
- 后续开发计划见 [NEXT-PLAN.md](./NEXT-PLAN.md)：E23 组合律 → S1 模块分化 →
  S2 模块内动力学 → S3 模块间协调（外部建议的三层已评估并统一改名，均未实现）。

## E23 组合律（2026-09-11，已完成）

- 问题：L1 方向 + L2 三元组 + L4 组块**同时开启**时，各层现象能否共存且可分别归因；
- 方法：26 链 + 两个表外分叉（q>B>x、t>E>w）+ 9 个块单元，2³=8 种开关组合；
- 结果（7/7 PASS）：L1 方向比 1.68e7（L0-only=1.0）；仅 L2 时分叉选 C
  （仅 L1/L0 为平局）；仅 L4 时 9 块、A→Z 跳数 25→10；C111 三者共存；
  L1/L2 未改 L0；第二个分叉 (D,E)→F 一致；
- 诚实记录：设计初稿的分叉符号与字母链撞车造成"近路"（已改表外小写符号），
  数值式乘积/加权和规则因同义反复风险未采用，改为"维度先行 + 现象共存"判据。

## E24 模块分化（2026-09-11，已完成，含方法修订）

- 问题：L0 的共现统计能否被分解成少量可分离模块（S1，读侧分析，不改 L0）；
- 语料：植入 3 组（质量 1080/2000/3900，两两差距≥46%）、链、分叉、随机对照；
- **预注册 ICA 失败**（B09）：ARI≈0、模块数 1、稳定性 0.165——零对角破坏线性混合、
  模块质量简并不可辨识、去均值降维、成分正交使余弦失效；ICA 不适用于该数据形式；
- **最小方法修订：谱聚类读出**（对角线行最大插补 + 不做去均值 + 归一化拉普拉斯
  前 K 特征向量 + k-means）：植入语料 ARI=1.000、3/3 模块、稳定性 1.000；
  随机对照富集度 1.14（植入 555.4）；链 3 个连续段不交错；分叉分支分离=1.00；
- **LCA 旁证失败**（B10）：活跃比例 1.0、未正常收敛、与谱聚类一致性≈0；
- **方法学修正**（B11）：稳定性不能当"无结构"判据（随机图也稳定），改用模块富集度；
- 全程 L0 只读（节点/连接数跑前跑后一致）。
