# M1 + M2 阶段性评审

日期：2026-09-06　评审对象：M1（基底缺口实验）与 M2（系统级场景）前半段

## 1. M1 评审：基底缺口实验

| 问题 | 实验 | 证据 | 结论 | 评级 |
| --- | --- | --- | --- | --- |
| 重复标记（汪 t₁/汪 t₂） | E13 | [汪,汪]×100→n=100；分散两次→0；三次→2；不附语义；无串扰；E5 判据更新后全量回归 | L1 有序自转移可表示"同节点相邻再现"，纯时间性、无语义 | PASS |
| 多路证据（max vs sum） | E14 | max 双路=单路=0.81；sum（单次发射+封顶）双路=1.0；环上终止封顶；成本清单 E9/E8/E6a-S3 | 拍板：基底永久 max；sum 仅读取端候选（自带护栏，拓扑≠无向图） | PASS（已拍板） |
| 高阶顺序表示 | E15 | 滑动窗口锁定；[X1,B]→C、[X2,B]→D（L1 看 B 等强）；[A,B]/[Q,B] 区分；3 前缀外等强（边界）；存储有界 | L2 三元组补齐双前缀上下文；更长 N 元属未来课题 | PASS |
| 角色标记归属判定 | E16 | 语序：打狗→咬→猫、打猫→咬→狗；中位词诚实歧义；L2 [狗,咬]→猫；格标记 [把] 系 | 角色=输入标记/语序+基底绑定；树结构无需下沉；标记系统归语言模块 | PASS |

**M1 结论：四个 open 问题全部有结论，出口标准达成。**

## 2. M2 评审：系统级场景（前半段）

| 场景 | 实验 | 验证了什么 | 功劳记账 | 评级 |
| --- | --- | --- | --- | --- |
| 叠词外推（人人） | E17 | 基底不发明规则；语言 stub 读 R 绑定+特征 F 外推"人人"、拒绝"的的"；听回后 n(人→人)=1；闭包不破 | 重复标记/检索=基底；规则判定+渲染=stub；最终=系统整体 | PASS |
| 拼写输出（Hello） | E18 | 块记录→MotorStub 逐字母输出（双写还原）；听回后 L1 补齐 n(l→l)=100（E5 的洞自闭合）；未知块报错 | 块记录/听回=基底；渲染=动作 stub；整体=系统 | PASS |
| 情绪调制 | E19 | 密度相同只差标记时基底不分轻重；情绪 stub 读取端增益产生 fear 差异与躲避决策；密度路径(300 vs 60)基底自身可分 | 标记=输入侧；绑定=基底；fear/躲避=情绪 stub；结论=基底无需写侧调制轴 | PASS |

**M2 结论：主体达成（2/3 场景 PASS + 闭包/功劳记账模式验证）；情绪调制明确属外部模块，
三类场景全部 PASS，M2 完成。**

## 3. 对照 M3 前置清单（缺口审计）

| M3 交付物 | 现状 | 缺口 |
| --- | --- | --- |
| FORMULAS.md（公式冻结） | 公式散落在 DESIGN/BOUNDARY/LAYERS | 未成单一冻结文档 |
| 接口契约 | 各层 learn/activate API 存在，事件格式约定未成文 | 需正式化：事件流(t+有序+边界)→各层；查询→激活图 |
| 参数表 | hop_decay=0.9、λ=0/0.005、K=5、floor=1、max 归一化、阈值语义、sum 封顶 | 需集中成表并标注可调范围 |
| 复杂度基线 | 未测量 | 需小规模基准：节点/边增长 vs learn/activate 成本 |
| 归因模板 | 每个系统级实验手工记账 | 需固化成模板（步骤/谁干的/内容） |

## 4. 评审结论与建议

1. **M1：PASS**。基底缺口问题全部有结论，且全部有实验证据；
2. **M2：全部 PASS（3/3）**。叠词外推、拼写输出、情绪调制在同一冻结基线上跑通；
   基底不发明语义、外部 stub 消费证据、输出可听回成新记忆、闭包与功劳记账全程成立；
3. **M3 尚未就绪**：缺公式冻结文档、接口契约、参数表、复杂度基线四项；
4. **建议路径**：
   - 已按选项 A 完成 M2-C（exp19）：强反馈经"输入密度 / 输入标记+外部读取增益"
     两条路径生效，基底无需写侧调制轴；
   - **下一步进入 M3 四件套**：FORMULAS.md 公式冻结、接口契约、参数表、复杂度基线；
   - M3 准备期间的任何改动都不碰 L0 冻结基线。
