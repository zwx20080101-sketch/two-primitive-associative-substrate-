# 第 2 章 系统架构与数学定义（中文草稿）

本章只客观描述系统的实现方式与数学定义，不包含实验结果。
公式以 FORMULAS.md 为权威来源；接口约定见 INTERFACE.md。
参数全部为预设值，不是系统自涌现参数。

本章状态：**已定稿（2026-09-06），作为正式报告第二章主体**。

---

## 2.1 总体结构与层次划分

系统由四类组件构成，边界固定：

1. **核心联想基底（L0 共现层）**：本系统的"双原语"所在层，只实现
   两条规则——共现绑定（learn）与激活扩散（activate）。
   L0 层的规则定义永久冻结；v0.9.0 版本实现快照冻结，
   后续仅可用于复现，不修改动力学规则。
2. **上层表示层（L1/L2/L4）**：在同一基底思路之上外挂的统计/结构层：
   L1 记录相邻到达方向（含重复标记）；L2 记录双前缀三元组；
   L4 将重复出现的带边界单元组织为块。它们不属于 L0 的双原语，
   而是基底之上的表示层；L1/L2/L4 属于当前实验版本的上层表示层，
   未来可独立迭代，不属于冻结的双原语基底。
3. **读取端工具**：阈值过滤、走读器等只读工具；不改变任何网络状态，
   不属于联想层。
4. **外部模块（stub）**：语言规则、动作输出、情绪读取均为外部占位模块，
   消费基底证据并产出输出；其能力不归入基底原生动力学。

本报告所称"联想行为"均指第 1、2 类组件在给定输入下的行为；
凡涉及第 4 类组件的结果，正文会明确标注"外部 stub"并做功劳记账。

## 2.2 输入约定

系统接收离散符号事件流。符号标签为字符串，大小写敏感；块号 CH_n
为机器号，不携带语义。事件分为四类：

| 事件类型 | 表示 | 喂给 |
| --- | --- | --- |
| 同步集合 | 同一窗口内同时出现的符号集合（内部去重） | L0 |
| 有序流 | 按到达顺序排列的符号序列 | L1、L2 |
| 带边界单元 | 一次完整的词/短语（可含重复位置） | L4 |
| 块级序列 | 由普通符号与块号组成的序列 | L4 |

每个事件可带显式时间戳 t（float）；未提供时对应层内部时钟递增。
跨层对齐同一物理事件时，由外部驱动者统一传入相同 t。

## 2.3 L0 共现基底（核心联想层）

### 2.3.1 共现绑定

输入一个同步集合 E = {x₁,…,x_k}（去重后）。对每个无序符号对 (u,v)，
u≠v：

```text
N_uv      ← N_uv + 1
last_seen_uv ← t
```

节点首次出现时创建；节点的 first_seen/last_seen/appear_count 仅作观测。
连接本体只包含 N_uv 与 last_seen_uv，**永不删除、永不衰减**。
事件内两两成连（完全绑定），不产生自环。

### 2.3.2 检索时派生的有效强度

```text
W_uv(t) = N_uv · exp(−λ · max(0, t − last_seen_uv))
```

其中 λ≥0 为预设时间衰减参数（默认 0）。时间只影响检索强度，
不影响 N 与连接的存在性。

### 2.3.3 传播强度

从节点 u 流向邻居 v 的强度按"每源最大归一化"计算：

```text
s(u→v; t) = W_uv(t) / max_{w ∈ N(u)} W_uw(t)
```

其中 N(u) 表示 u 在 L0 层的所有共现邻居集合；分母只取源节点 u
一侧的连接权重，与对端 v 无关。因此 s(u→v) 与 s(v→u) 可以不同
（非对称读取），但连接本身是无向的。

### 2.3.4 激活扩散

给定种子集合 S：

```text
种子激活: a(s) = 1.0, s ∈ S
传导:      arrival(u→v) = a(u) · s(u→v) · hop_decay
吸收:      a(v) = max(a(v), arrival(u→v))
```

hop_decay∈(0,1] 为预设每跳衰减参数（默认 0.9）。节点仅在激活值
严格提升时再次发射；系统单调有界、必然终止。该规则等价于
"a(v) = 所有从种子到 v 的简单路径中逐跳乘积的最大值"
（best-path 语义）。当种子集合包含从未进入 L0 网络的符号节点，
激活流程直接终止报错；系统仅在已学习节点构成的闭包子图内传播。

## 2.4 L1 方向层

### 2.4.1 有序相邻计数

输入有序序列 [x₁,…,x_k]，对每个相邻对 (x_i, x_{i+1})：

```text
n(x_i → x_{i+1}) ← n(x_i → x_{i+1}) + 1
```

允许 x_i = x_{i+1}：此时记录"同一节点相邻再现"，称为**重复标记**，
仅表示时间性事实，不携带语义。

### 2.4.2 方向强度与软反向

```text
raw(u→v)      = n(u→v) + floor
strength(u→v) = raw(u→v) / max_{w 与 u 相邻} raw(u→w)
```

floor>0 为预设弱可逆保底（默认 1.0）：未发生过的方向仍有极小传导，
使反向检索"可能但显著更弱"。邻居集合包含双向可达节点。
floor 仅用于 L1 方向层传导，不作用于 L0 基底；两层参数相互独立。

### 2.4.3 激活

与 L0 相同的 max 波前规则（种子 1.0、hop_decay 0.9）。
L1 自身无时间衰减；新旧由 L0 的 W 承担。

## 2.5 L2 上下文层

输入有序序列，按**滑动相邻三元组**计数：

```text
对 i = 0..k−3:  n((x_i, x_{i+1}) → x_{i+2}) ← +1
```

只计相邻，非相邻不计数；前缀长度固定为 2。查询给定前两项 (a,b)：

```text
strength(c) = n((a,b)→c) / max_c n((a,b)→c)
```

未知前缀返回空；输出为候选强度分布，不做选句。三前缀以上的
长距离依赖不在本层表示范围内。

## 2.6 L4 组块层

### 2.6.1 成块

输入带边界单元 seq（有序、可含重复位置）。当同一单元出现次数
达到预设阈值 K（默认 5）时创建块：

```text
unit_count(seq) ← +1
当 unit_count(seq) == K:
    创建 CH_n, 顺序记录 record(CH_n) = list(seq)   # 保留重复位置
之后每次该单元出现:
    对 seq 的去重成员 m: member_bind[(CH_n, m)] ← +1
```

K 为预设成块阈值（默认 K=5）；仅**完整匹配**的带边界单元 seq
才累计计数，子序列不触发计数。

块号 CH_n 为机器号，无语义。

### 2.6.2 拼写记录（供外部动作模块）

```text
spell(CH_n) = 顺序成员表(含重复位置)    # 未知块报错
```

### 2.6.3 块级序列与激活

块可作为原子参与块级序列：相邻符号对按规范序计数（同 L0 绑定规则），
块级激活使用与 L0 相同的 per-source max 归一化与 max 波前。

## 2.7 读取端工具

| 工具 | 定义 |
| --- | --- |
| 阈值过滤 | 保留 activation≥threshold 的节点（threshold∈(0,1]）；可选 top_k 按 (−activation, label) 取前 k |
| 走读器 | 从种子出发，每步取激活最强的未访问邻居继续 |
| 求和读法（候选） | 单次发射、出队冻结、已发射节点不再接收贡献；累加封顶 1.0。求和读法仅为读取端候选检索策略，不属于联想基底原生传播规则；基底内部传播统一采用 max best-path 语义 |

所有读取端工具为零副作用：不改变任何连接与计数。

## 2.8 外部模块（stub）边界

| 外部 stub | 输入证据 | 职责 | 边界 |
| --- | --- | --- | --- |
| 语言规则 | 标记绑定、特征检索、重复标记 | 判定"可叠词"并渲染字符串 | 规则判定与渲染属外部 |
| 动作/书写 | 块记录 spell | 逐字母渲染输出 | 渲染属外部 |
| 情绪 | 激活图 + 强度标记 | fear 计算与躲避决策 | 读取端增益属外部 |

外部 stub 只读基底证据，通过公开 learn 接口把"输出被听回"作为新经验
喂回；不修改层内部结构。系统级实验必须输出功劳记账表。
将输出回灌为新经验属于**外部驱动逻辑**，并非基底内部闭环动力学。

## 2.9 冻结与版本纪律

- L0（synapse_net.py）永久冻结，不再修改；
- 公式以 FORMULAS.md 为唯一权威；参数表见 PARAMETERS.md；
- 接口以 INTERFACE.md 为唯一权威；
- 任何冻结公式改动必须先经实验与确认，再改文档，最后改代码。
