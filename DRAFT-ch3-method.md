# 第 3 章 实验方法（中文草稿）

本章说明测试套件设计、判据注册、运行环境、统计与复现方法。

## 3.1 测试套件总体设计目标（E0–E22）

套件按现象族组织，目标是给每个主结论提供"可复现 + 可核对"的证据：

| 组 | 实验 | 覆盖 |
| --- | --- | --- |
| L0 机制 | E0–E4 | 空网络启动、链结构、频率、阈值、双向等强 |
| 方向/顺序/角色 | E5、E15、E16 | 方向不对称、双前缀、角色标记 |
| 语义/结构拓扑 | E6a、E6b、E8、E11 | 诚实歧义、翻译绑定、枢纽并行、干扰 |
| 时间/事件/重复 | E7、E9、E10、E13、E14 | 脉冲密度、补全、新旧、重复标记、多路规则 |
| 组块/拼写 | E12、E18 | 双写位置、块复用、听回闭环 |
| 系统级 | E17、E19 | 外部语言 stub、外部情绪 stub |
| 证据综合 | E20、E21、E22 | 消融、规模一致性、随机抽样统计 |

## 3.2 预先注册判据

- 每个实验在运行前把判据写成可执行检查（实验文件内的 `Checker` 检查项），
  并同步记录于 DESIGN.md；
- 顺序固定为"先写预期判据 → 运行 → 记录 PASS/FAIL"；无事后调参；
- 任一判据 FAIL：冻结现场、记录现象，只做最小机制修改后重新验证；
- L0 文件（synapse_net.py）全程冻结，不允许因实验需要修改。

## 3.3 运行环境与固定参数

运行环境：Python 3.12.14（Codex 捆绑运行时），Windows，仅标准库，
无第三方依赖。

固定参数（全部为预设值）：

| 参数 | 默认/取值 | 用途 |
| --- | --- | --- |
| hop_decay | 0.9 | 每跳衰减 |
| λ | 0.0；E10 用 0.005 | 检索时间衰减 |
| K | 5 | L4 成块阈值 |
| floor | 1.0 | L1 软反向保底 |
| 读取阈值 | 各实验指定（0.5/0.1/0.001 等） | 读取端过滤 |

确定性说明：主体实验为确定性流程；含随机抽样的部分（E20 标签置换/
随机图、E22 随机输入族）使用固定种子的 `random.Random`，因此
在相同环境与代码版本下结果可精确复现。

## 3.4 三组证据方法

### E20 消融/对照

对 8 类现象分别测量"机制开启 / 机制关闭"与对照条件的指标：

- 机制关闭：去掉对应层或外部 stub（如 L0-only 无方向层、无共享情景、
  平面链无块、无外部 stub）；
- 纯计数对照：仅取直接邻居计数、无扩散；
- 标签置换/随机图对照：检查机制是否依赖具体符号（预期与具体 token 无关）
  或是否接近机会水平。

### E21 规模一致性

对方向、翻译、枢纽、补全、双写、角色六类现象，在共享同层、
标签互不冲突的网络上分别累加 1、10、100 个实例，
指标为"判据通过率"，要求 S=1 时全部等于 1.0、
S=10/100 时不低于 0.99。

### E22 随机抽样统计

每现象抽样 M=60 个随机输入族（随机标签 + 随机重复次数 30–80），
分别测量正样本（机制开启）与负样本（机制关闭/随机）成功率，
使用 Wilson 区间给出 95% 置信区间：

```text
p = k/n
区间 = Wilson(p, n, z=1.96)
```

判据：正样本率≥0.99 且 CI 下限≥0.9；零效应负样本率≤0.12；
二分机会类负样本 CI 覆盖 0.5。

## 3.5 复现流程与校验机制

```text
python main.py            # 全量 E0-E22, 输出 outputs/exp*.json
python benchmark.py       # 复杂度基准
python verify_claims.py   # 数据溯源核对(77 项)
python scan_norms.py      # 措辞规范扫描
```

- 每个实验运行结果写入 `outputs/<exp>.json`（含 passed 与逐项 checks）；
- `verify_claims.py` 逐项核对实验 passed、草稿数值与 JSON 一致性、
  措辞红线与外部 stub 边界标记；
- 复现包包含代码、文档、数据、校验脚本与冻结快照（v0.9.0）。

## 3.6 负结果与边界观测的采集方法

- 边界条目集中登记于 CLAIMS-REGISTRY.md（B01–B07），来源包括：
  对照实验（如平面链对双写）、越界测试（如 3 前缀外查询、
  未知符号激活）、规则对比（max 与 sum）与架构边界（外部 stub 归属）；
- 采集原则：与主结果同一套"先判据后运行"流程；
- 负结果不隐藏、不弱化，作为能力边界的正式观测写入报告。
