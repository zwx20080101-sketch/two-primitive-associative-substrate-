# LAYERS-SPEC：新增层的规范（L0 之外的枝）

日期：2026-09-16。状态：**规格文档（specification）**，非实验。
触发：E42 完成 L5 的创建，需要把"新增结构"的先例写成规范，
      供将来 L6+ 有模板可循。

---

## 0. 一句话

```text
L0 是根（不可变），L1–L4 是已建的枝（已验收），L5 是新增的枝（E42）。
本文件规定：新增层必须满足的五要素、显式声明原则、与 L0 的关系。
```

---

## 1. 与 L0-SPEC 的分工

```text
· L0-SPEC   管"L0 的架构约束 vs 实现约定"（冻结核心）
· LAYERS-SPEC 管"L0 之外新增层的规范"（可扩展的枝）

两者性质不同：L0 不可变、L4/L5 允许新增。
混在一份文档里会让读者分不清"哪些是不可碰的、哪些是可扩展的"。
```

---

## 2. 新增层的五要素

新层必须满足：

```text
① 新文件（不改任何已验收文件）
② 自有节点集（不写回 L0）
③ 自有边（不共用 L0 的边）
④ 同一条扩散规则（max 吸收 + hop_decay，与 L0 一致）
⑤ 显式 origins 字段（标明该符号的触发来源）
```

---

## 3. 显式声明原则

```text
新增符号必须显式声明为"新增结构"，同 L4 的 CH_n、L5 的 CMP_n。
不得伪装成"L0 自然涌现"。
```

---

## 4. 与 L0 的关系

```text
新增层不写回 L0、不违反 C30（闭包）。
C30 的审计对象是"每层的已学集合"——新层有自己的已学集合，
L0 的闭包不受影响。
```

---

## 5. 与 L0-SPEC §5 五步流程的关系

```text
改 L0   → 走 L0-SPEC §5 五步（判性质 / 新建平行文件 / 影响评估 / 更新版本表 / 新文件上跑）
新增层  → 走本文件：五要素 + 显式声明 + 首次哈希记录
两者不混淆。
```

---

## 6. 已建层登记

| 层 | 文件 | 触发条件 | 首次哈希 |
| --- | --- | --- | --- |
| L1 | `order_layer.py` | 有向相邻计数 | `419d54745a9eb795` |
| L2 | `context_layer.py` | 滑动三元组 | `bd029d7bbdc345be` |
| L4 | `chunk_layer.py` | 重复次数 ≥ K | `46f5857be0fc7c7e` |
| L5 | `compose_layer.py` | 共现绑定（coactivation） | `81e350497e9e9960` |

> 说明：L1/L2/L4 的哈希是**当前值**（各自实验后未再改动）；L5 的哈希是 E42 的首次记录。
> 哈希为 sha256 前 16 位，与实验 JSON 的记录口径一致。
