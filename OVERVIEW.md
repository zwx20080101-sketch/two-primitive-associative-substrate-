# Synapse-Net 总览（E0–E33）

日期：2026-09-14　版本：tag `v0.9.12-e33`　状态：**全绿**

> 一句话：一个只有两条公理（**共现绑定** + **激活扩散**）的联想记忆基底，
> 在受控实验里逐层长出顺序、上下文、组块、角色、模块化与多尺度读出；
> 每条结论都带实验编号、判据与 JSON 数据源，每条失败都登记为边界。

## 0. 规模（精确计数）

| 项 | 数量 | 说明 |
| --- | --- | --- |
| 实验 | **E0–E42（43 个）** | 全部 PASS（`python main.py` 汇总全绿） |
| C 条目（能做什么） | **40 条** | C01–C31 + C33–C41；**C32 未立**（E32 落在 B24，不是漏号） |
| B 条目（在哪不行） | **28 条** | B01–B34 中 **B22 / B25 / B27 / B28 / B29 / B31 未触发**（预注册的条件编号） |
| L0 实现版本 | **v1 + v2 + v3** | v1 `cf99a07a0e60fd4d`（冻结，E0–E36）；v2 `ee6df589eb2f6983`（平行分支，E37 起）；v3 `085be73a06f0694c`（从 v2 继承，E39 起） |
| 方法学类别 | **6 类** | 见 §4，含"不报错的坏结果"六个失效模式 |
| 归档的失败记录 | 4 份 | `exp32_nesting_depth_v1_FAIL.json`、`exp33_landscape_sampling_v1_FAIL.json`、`exp39_reach_compression_v1_FAIL.json`、`exp40_unconditional_sampling_v1_FAIL.json` |

---

## 1. 四段式全景

### 第一轮 E0–E22：单层能力目录（C01–C22 / B01–B07）

一次一层、每层一个现象；末尾追加消融（E20）、规模（E21）、统计（E22）。

```text
L0 基本盘    E1 链自组织 / E2·E3 频率与阈值 / E4·E5 对称与方向 / E6a·E6b 分叉与互译
             E7 密集脉冲 / E8 枢纽图 / E9 补全与闭包 / E10 时间衰减 / E11 干扰
上层表示     E12 成块与双写 / E13 重复标记 / E14 会合规则 / E15 双前缀 / E16 角色
系统级 stub  E17 叠词外推 / E18 拼写输出 / E19 情绪调制（外部模块参与，功劳记账）
方法与统计   E20 消融 / E21 规模 1×–100× / E22 随机输入族 + Wilson CI
```

### 第二轮 E23–E26：组合律 + S1/S2/S3（C23–C26 / B08–B19）

回答"多层同时打开时怎么合成"以及"从 L0 统计里能读出什么宏观结构"。

```text
E23 组合律    L1+L2+L4 同时开启 → 三层现象共存且可分别归因；L1/L2 不写回 L0
E24 S1 分化   预注册 ICA 失败(B09) → 最小方法修订 = 谱聚类读出（ARI=1.000）
E25 S2 动力学 模块平均激活的双稳 + 滞回（0.150 vs 对照 0.012）；联合二值模式 = 2³ = 8
E26 S3 协调   EI 整合量 + 剂量响应（10/10 种子严格单调；零耦合与消融精确归零）
```

### 边界量化 E27–E32：把第一轮的旧账升级成"带数字的律"（C27–C31 / B20–B24）

```text
E27 (B01) 上下文窗口：k_min = k*（k*=1..5 精确成立）；现有层天花板 = 2
E28 (B02/B03) 读取端：max 精确不叠加（max(a,a)=a）；sum 封顶且【顺序依赖】（拓扑判据）
E29 层级 = 多尺度读出：k=2 读超模块 / k=4 读子模块；消融只打掉层级不打坏统计
E30 (B05) 闭包审计：五个已学集合违例 = 0；失败语义表（KeyError / {} / None / ValueError）
E31 自动尺度（弱版本）：BIC 跨语料 3/3 命中；eigengap/modularity 嵌套语料偏向粗层(B23)
E32 可读性条件：r_j 不能完全预测可读性(B24)；可读性随嵌套深度下降，只有最粗层稳定可读
```

### 生成第一刀 E33（C33 / B26）

```text
命题：从 E25 的模块级能量景观【条件采样】时，能不能给出 max 传播之外的联合状态？
结果：4 个主导状态中 max 只给出 1 个；另外 3 个在精确 Boltzmann 分布下占 0.751 质量
      （扫描的每个 β 上均 ≥ 0.7503）；节点级有效支撑 86（β=0.5）→ 4（β≥1.5），
      与模块级 4 个自由度一致 ⇒ 节点级与模块级是【粗粒化关系】。
限定：已知种子下的条件采样（2²=4），不是无条件"新状态发现"（E25 为 2³=8）；
      这 4 个状态 E25 已枚举过，E33 的新内容是【可采样 + 质量分配】。
副产品：B26 —— 单点 Gibbs 在低温柔性下不混合（1000 样本只覆盖 1 个状态且不报错）。
```

### 工作记忆第一刀 E34（C34）

```text
命题：在零残留的联想基底上，外部 W 层能否跨次携带状态？
结果：activate(A) → W 存 top-3 {A:1.0, B:0.9, C:0.81}；下一次 activate([Z]+W.seeds()) 使
      A 从 0.071790（0.9^25，从 Z 走 25 跳）跳到 1.0（差异 0.928）；
      消融（W 存在但 seeds() 不调用）→ 回到 0.071790。
只读：W 不写 L0（26 节点/25 边签名逐位不变）、四个层文件 sha256 前后一致。
定性：W 是【实验侧读取端工具】（同 E14 的 sum 模式），不是 L5/S4、不是基底新能力。
```

### 工作记忆第二刀 E35（C35）

```text
命题：多步串联时，携带的是"每步重注入的集合"（构造性）还是"图传回的痕迹"（实证）？
结果：(a) 停止重注入后痕迹立即退化为纯图衰减 0.9^13 = 0.254187（与"从未注入"完全相同）；
      (b) 被携带成员的值全部 1.0 → 无优先级、无时间顺序；
      (c) 成员由 tie-break 决定：每步换种子 A/B/C/D/E 时 W 恒为 {A,B,C}，D/E 挤不进去。
⇒ W 栈是【载体】，不是【状态整合】。要区分痕迹来源，需要顺序标签或值域编码（E36 入口）。
```

### 工作记忆第三刀 E36（C36）——边界量化

```text
命题：顺序标记的【适用边界】（不是"标签可用"）。
结果：seeds(step=1) 只返回步1 痕迹 → A=1.0（与不注入差 0.928）——分类可测；
      但注入后 A/B/C/Z 在基底里【全部 1.0】，无相对权重差异——排序不可测；
      排除的效果可测：步2 独有节点 X 在有过滤时 0.81、无过滤时 1.0（差 0.19）。
定性：E36 不是"第三刀能力"，是【给 E35 的结论加边界】——分类 ≠ 排序；
      排序差异需要值域编码（改基底入口行为，越权嫌疑）。
附带：activate({"A":1}) 被静默接受（dict 当 iterable 只取 keys）→ 写入 B21 补充实例。
```

### L0 v2 分支 E37（C37 + B30）——第一次改"实现约定"

```text
背景：E33–E36 三连撞墙，撞的是 L0 的实现约定（种子入口抹平值），不是架构约束。
规格：L0-SPEC.md 把"架构（不可变）"与"实现约定（可改，但要评估）"分开；
      改实现的五步：判性质 → 新建平行文件 → 写影响评估 → 更新版本表 → 新文件上跑并记哈希。
改动：synapse_net_v2.py（子类覆盖，只重写 activate；存储层继承 v1）——v1 一字不动。
结果：带值种子保留值（B=0.9, C=0.81）、常量语义（{A:0.5,B:0.9} 下 A 仍 0.5）、
      名字列表向后兼容（与 v1 逐位相同）、整表 13 变化 / 13 不变。
边界：B30 —— 常量语义使种子"冻结"，不会被任何路径强化（语义选择，不是 bug）。
```

---

### 口径确认 E38（C33 限定 + B32）——粗粒化关系到底指什么

```text
背景：E33 的 C33 说"节点级与模块级是粗粒化关系"。E38 问：这句在【质量分配】上也成立吗？
做法：把节点级精确 Boltzmann 分布按模块符号聚合，再与模块级 3×3 块均值模型的概率逐状态比。
结果：支撑一致（落在 4 个纯二值状态内 0.999708），但【质量分配不同】——
      (+1,+1,+1) 节点级 0.311960 vs 模块级 0.252411，差 d = +0.059548；其余三态为负偏移
      （−0.0195 / −0.0202 / −0.0199，四态之和 = 0）。
      解耦对照（跨模块项全部置 0）→ 精确塌成 0.25 均匀 ⇒ 偏移由【跨模块耦合】产生。
登记：B32（分块均值 W3 不是节点级分布对模块符号的正确边缘化）+ C33 加限定
      （"粗粒化关系"= 支撑 / 自由度一致，不蕴含质量分配一致）。
两条诚实 caveat：
  · 种子轴退化——10 个聚类种子给出【同一划分】（仅标签排列不同），故 d 是确定量，不是多种子证据；
  · 折叠规则零影响是【本配置的巧合】（携带平票质量的模块恰好是被钳制的那个），不是普遍法则。
```

### 抑制控制第一刀 E39（C38 + B33）——能压到什么、压不动什么

```text
背景：B12 说非负权重造不出 WTA；L0-SPEC §2 ⑥ 把"到达值算式"判为【实现约定】（可改，但必须保留 max 吸收）。
做法：v3 从 v2 继承，activate 整体复制 + 1 行调制 `arrival = au·strength·hop_decay − g`；
      g 是【构造参数】（与 λ / hop_decay / β 同级），不动 activate 签名 ⇒ C37 的向后兼容契约不破。
结果：reach（阈值 0.5）随 g 单调不增 7→6→4→3→3→2→2→2→1→1，端点严格下降；
      种子值恒 1.0（不可被压制）；成对相对方向不反转（反转对 = 0）。
登记：**B33 先**（竞争性抑制在到达值算式层不可实现）**C38 后**（可及性压制能力）。
v1 的失败留档：E39 v1 用"序列逐位相等"判次序 → C1 上 FAIL（40 对精确并列，g=0 的 tie-break
      与 g>0 的值序相反；严格反转 = 0），归档 exp39_reach_compression_v1_FAIL.json；
      v2 把判据改成"成对方向一致性"（精确并列不计入）。
```

### 生成解 clamp E40（C39 + B34）——"把手松开"

```text
背景：E33 是【条件采样】（种子模块钳制，4 状态）；E40 解开钳制，8 状态全开。
做法：主对照 = (a) 单节点种子下 max 传播可达的模块状态（与 E33 同层级）；
      主采样器 = 并行回火（PT，读取端工具，n=5000，阶梯 β·[0.25,0.5,1,2,4]）。
结果：PT 在全部判定 β 上覆盖 8/8；单节点 max 只到 3/8 ⇒ 集合差给出【5 个生成状态】，
      在 β ∈ [0.5, 8] 上共携带 0.6253–0.6299 的质量（判据 > 0.5）。
      分层：4 个可由多种子组合到达、1 个（全关 (-1,-1,-1)）任何种子集都不可达。
登记：C39（采样覆盖检索逻辑封闭之外）+ B34（检索可达集是基态流形的【一个分支】——
      (1,1,1) 与 (-1,-1,-1) 是简并基态（E = −1.235577），但 max 永远到不了全关那一支）。
旁证：条件分布 = 无条件分布的限制 + 重归一化（逐位相同，总计精确 0.500000）
      ⇒ E38 的 B32 偏移不随条件化 / 无条件化改变。
两条留档：
  · v1 FAIL 的是【对照条款】——预检 n=1000 的期望被搬到 n=5000 的运行上；
    归档 exp40_unconditional_sampling_v1_FAIL.json，v2 把对照降为 P1a'（只报告）。
    一般化：预检数字进入预注册前必须核对参数（纪律⑨同族，新失效模式）。
  · 采样 − 精确 的生成质量差符号一致为正（+0.002 → +0.010，β=8 约 1.5 SE）——
    只报告，不下"PT 有偏"的结论（要判需新命题）。
```

---

### 工作记忆值域编码 E41（C40 + 三条状态条款）——"抹平不在 W 层"

```text
背景：E34–E36 的结论是"排序不可测"（C36：分类 ≠ 排序）。E41 问：把值域编码接回 W 层会怎样？
关键发现（成本极低的根本原因）：抹平【不在 W 层】——WorkingMemory 从 E34 起就存
      dict[str, float]，且早就有 snapshot() 带值出口；丢值的是【注入调用点】（选了 seeds() 的 [node] 形式）。
做法：只改调用点 1 行（W 层 0 行）：activate([seed] + W.seeds()) → activate({seed:1.0, **W.snapshot()})。
结果：节点集固定、A/C 值序对调 ⇒ 基底输出在 13 个节点上不同，其中 11 个是【非种子扩散节点】（D…N）；
      抹平注入对照 = 0（E36 结论复现）。
登记：C40（值序可测）+ 三条状态条款：
      · C36 的排序轴限定【被解除】（原文保留作时间戳；方向与 E38 对 C33 的"收窄"相反）；
      · "越权嫌疑"分句【不适用】（E37 已把带值入口合法化并登记 C37+B30）；
      · C35 加注解：其"无优先级"精确化为"W 自身不排序；排序信息来自被保存的 value"。
范围限定：这是【值序】，不是【时间序】—— C35 的"无时间顺序"仍成立；
      功劳归外部 W 层工具 + 已登记入口，不是基底新能力。
一条防误读：E37 与 E41 的差异节点数同为 13，但差异集不同（E37 对调 B/C → {B,C}∪D..N；
      E41 对调 A/C → {A,C}∪D..N）—— 数目是巧合，不是同源。
```

---

### 组合符号 E42（C41）——新增层 L5，造出【景观外】的新对象

```text
背景：E33（定义 A）= 在能量景观里【采】已有状态；E42（定义 B）= 两个模式共现绑定【造】新符号。
关键区分（先把话说死）：
  · 组块 CH_n（L4）：由【重复次数 ≥ K】触发；
  · 组合符号 CMP_n（E42/L5）：由【两个模式同时激活】（一次 coactivate(P,Q)）触发。
做法：新增层 L5（compose_layer.py，与 L4 同构：自有节点集 + 自有边 + 同一条扩散规则）。
      与 L4 的三点差异：① 触发 = coactivation；② 成员边【显式建】（L4 的 activate 不展开成员）；
      ③ 每个符号带 origins 字段。
结果：CMP_1 的可分性成立（activate(CMP_1) = {CMP_1:1.0, A/B/C/D:0.9}，4/4 成员）；
      与组块的双重区分：字段级（origins="coactivation" vs L4 无字段）
      + 行为级（交叉操作互不响应：L4 重复不造 L5 符号、L5 coactivate 不造 L4 组块）；
      带值：prod 内部值逐位 = v1×v2（{A:0, B:0.81, C:0.81, D:0}），keep = 全 1.0，4 个成员全不同；
      值序（定义 A：各自排名反转）：内部值变化 1 个共享成员（C 0.81→0.90）—— P5b【不是空判据】。
两条留档：
  · 【判据更正】v1 稿写"prod 下值序对调不改变 C（乘法交换律）"—— 该论证【错误】：
    交换律说的是乘法【顺序】，而操作是【输入值】变化。实测已推翻（见上）。
  · 【语义发现】同一成员集再 coactivate 会复用符号 id 并【覆写】内部值（last-write-wins，
    同 E34 的 W.hold）—— 由 P4 的失败暴露（P2b 探针复用了同一 L5 实例），现已写进实现与登记。
```

---

## 2. 架构速览（谁冻结、谁可迭代）

| 组件 | 角色 | 状态 |
| --- | --- | --- |
| `synapse_net.py`（L0） | 共现绑定（N 永久）+ 对称激活扩散（max best-path） | **冻结**（永不再改） |
| `order_layer.py`（L1） | 有向相邻计数 + 方向强度；有序自转移 = 重复标记 | 已建（E5/E13） |
| `context_layer.py`（L2） | 滑动三元组：双前缀 → 下一个 | 已建（E15） |
| `chunk_layer.py`（L4） | ≥K 次重复的带边界单元成块 + spell 顺序记录 | 已建（E12） |
| `compose_layer.py`（L5） | **两个模式共现绑定 → 组合符号**（coactivation 触发；成员边显式建；origins 字段） | **新增（E42）**；L0/L1/L2/L4 一字未动 |
| 读取端 | 阈值过滤、走读器；sum 是可开关的读取模式（不改基底） | 工具，不算能力 |
| 外部 stub | 语言规则（叠词）/ 动作（拼写）/ 情绪增益 | 外部，**不属基底** |
| S1/S2/S3 | 谱聚类读出 / 模块级动力学 / EI 整合量 | 读侧分析，不写回记忆 |
| 网页玩具 | P5a 只读分析面板（快照回放 + 字段级自检 + commit 溯源） | 已发布（P5b 取消） |

---

## 3. C 条目清单（40 条，**按主题聚类**，不按编号）

### 3.1 绑定与检索（L0 基本盘）——C01–C12

```text
C01  仅相邻共现事件可自组织成链结构（无预置知识）
C02  共现频率调制检索强度；弱连接保留在全量输出，阈值决定可访问性
C03  L0 无向连接 → 正/倒检索等强；方向不对称需要方向计数层
C04  顺序不对称可来自方向经验统计（正走深、倒走浅）
C05  无上下文时并存分支保持等强（诚实歧义——底层不判对错）
C06  上下文/情景作为共现结构可偏置分支
C07  两个表面可经共享情景发生绑定（互译）；无共享情景则分离
C08  密集时间脉冲可产生强连接、枢纽与提前检索（预警）
C09  连接是图结构：共享枢纽给出并行候选；组合可达，但"无直连 ≠ 已学"
C10  部分输入补全只取回已存在节点；未知节点无法激活（闭包）
C11  时间衰减只作用于检索强度；N 与连接永久存在
C12  干扰集中于共享枢纽；独特标记/情景可分离单句
```

### 3.2 上层表示：顺序 / 上下文 / 角色 / 组块 —— C13–C17

```text
C13  重复出现的带边界单元可成块并保留顺序记录（双写还原）
C14  同一节点相邻再现可由有序自转移表示，且不带语义
C16  双前缀三元组可消歧；3 前缀以上超出该表示（边界见 B01/E27）
C17  角色可由输入语序/格标记绑定区分；无需把树结构下沉到基底
```

### 3.3 读取端：选择与边界 —— C15 / C27 / C28 / C30

```text
C15  传播吸收取 max（best-path）；多路求和仅为读取端候选且拓扑不同
C28  max 与 sum 是读取端【两种选择，不是对错】：max 精确定律 max(a,a)=a；
     sum 单次发射 + 封顶，封顶损失随路径数单调增，且【顺序依赖】（拓扑判据）
C27  上下文窗口边界：k_min = k*（k* = 1..5 精确成立）；现有层天花板 = 2
C30  闭包是【设计不变量】：五个已学集合逐层审计，违例 = 0
```

### 3.4 系统级（外部 stub 参与的语言/价值场景）—— C18 / C19

```text
C18  叠词外推由外部语言规则完成；基底只提供标记与特征证据；输出听回成新记忆
C19  强反馈经"输入密度"或"输入标记 + 外部读取增益"生效；基底无需写侧调制轴
```

### 3.5 方法与统计（"怎么知道结论可靠"）—— C20–C22

```text
C20  消融：机制在则现象在、机制移除则退化；结论与具体 token 无关
C21  六关键现象在 1×/10×/100× 实例规模上无退化
C22  随机输入族下六现象成功率 1.0（Wilson 95% CI 下限 0.94）；负样本近零/机会水平
```

### 3.6 模块化读出与层级（S1 / S2 / S3 + 尺度）—— C23–C26 / C29 / C31

```text
C23  组合律：L1+L2+L4 同时开启时各层现象可共存且可分别归因；L1/L2 不写回 L0
C24  S1：L0 共现统计中的模块可被谱聚类读出恢复（ARI=1.000，稳定性 1.000）
C25  S2：弱耦合下每个模块各有两稳定态（盆地 0.46–0.54）；联合二值模式 = 2³ = 8
C26  S3：半稠密跨模块耦合产生可测整合（EI_int 逐种子严格单调；零耦合与消融精确归零）
C29  层级 = 多尺度读出：k=2 读超模块、k=4 读子模块；消融只打掉层级
C31  自动尺度（弱版本）：BIC 在 k=4/3/5 上 3/3 命中（压倒性极小值）
```

### 3.7 生成（第一刀）—— C33

```text
C33  从 E25 模块级能量景观条件采样：4 个主导状态中 max 只给 1 个，另 3 个占 0.751 质量；
     节点级支撑 86 → 4 与模块级自由度一致（粗粒化关系）。
     限定：条件采样（2²=4）；不声称"能想象新状态"、不声称"等于生成能力"。
```

---

### 3.8 工作记忆 / 生成口径 / 压制（E34–E39）—— C34–C38

```text
C34  外部 W 层保存上一次 activate 的快照，并在下一次作为额外种子注入：
     A 从 0.071790（0.9²⁵）跳到 1.0，差 0.928。零残留基底（同种子两次逐位差 0.0）
     ⇒ W 是【读取端工具】，不是基底长出了记忆。
C35  W 栈的多步串联是【载体】行为：只在每步重注入时维持；停止重注入即退化为纯图衰减
     0.9¹³ = 0.254187；痕迹之间无优先级、无时间顺序（被携带成员的值全为 1.0）。
C36  顺序标记的边界：**分类 ≠ 排序**。能按来源过滤（成员可测：X 有过滤 0.81 / 无过滤 1.0），
     不能给权重（所有被注入痕迹在基底里都是 1.0）。
C37  L0 v2 带值种子入口（**常量语义**）；名字列表形式向后兼容；带值种子改变传播路径的
     **绝对水平**（26 链上 13 个节点变化 / 13 个不变）；"排序差异"在 v2 下可测。
C38  L0 v3 **可及性压制**：构造参数 g 使超阈值节点数（reach，阈值 0.5）单调不增
     （7→6→4→3→3→2→2→2→1→1），端点严格下降。**不是竞争性抑制**（种子恒 1.0，见 B33）。
C39  解开钳制后（8 状态全开），PT 采样覆盖 8/8；其中 **5 个是任何单节点种子下 max 传播
     都到不了的**（集合差 8 ∖ 3），在 β ∈ [0.5, 8] 上共携带 0.6253–0.6299 的质量。
     分层：4 个多种子可达 + 1 个（全关）任何种子集不可达（见 B34）。
C40  **痕迹的排序可测（值序）**：W 保存的值经 v2 的已登记带值入口注入 ⇒ 同集合、值序对调
     时基底输出在 13 个节点上不同（含 11 个扩散节点）；抹平注入对照 = 0。
     ⇒ C36 的排序轴限定【被解除】。范围限定：是值序不是时间序；功劳归 W + 入口，非基底。
C41  **组合符号（共现绑定产生新符号）**：一次 coactivate(P,Q) 在 L5 产生 CMP_n；
     显式成员边 ⇒ activate(CMP_n) 可分（4/4 成员各 0.9）；与 L4 组块【字段级 + 行为级】双重区分；
     prod 内部值逐位 = v1×v2；值序（定义 A）变化 1 个共享成员 ⇒ 排序敏感。
     范围限定：景观外的**新对象**（≠ E33 的采样）；功劳归 L5；成员边是手工建的。
```

---

## 4. B 条目清单（28 条）+ 方法学六类

### 4.1 第一轮的旧边界（B01–B07）——**已全部清算**

```text
B01  3 前缀外的上下文不可达           → E27 升级为定量律（k_min = k*；天花板 = 2）
B02  max 下多路证据不叠加             → 指针，并入 C28（读取端设计选择，不是缺陷）
B03  sum 遍历拓扑 ≠ 底层无向图         → E28 升级为拓扑判据（顺序依赖的触发条件）
B04  平面链无法还原双写位置            → 由 E12 覆盖（块记录承载位置）
B05  未学过节点 activate 报错          → 指针，并入 C30（闭包是设计不变量）
B06  叠词语义外推属外部语言模块         → 由 E17 覆盖
B07  真实感知/运动、语法递归、人脑等同性不在结论内（宪章级，无需实验）
```

### 4.2 结构性硬边界（机制层面造不出来）

```text
B12  非负 L0 权重下无"赢者通吃"互斥切换（正耦合只能给出合作型锁定）
B16  整片等权稠密耦合会让"跨模块权重重排"退化为恒等变换（零分布失效）
B20  谱聚类读出需要外部给定尺度 k；层数分辨率有上限
B23  eigengap 在【嵌套】语料上系统性偏向粗层（近零特征值个数 = 最粗层块数）
B24  可读性不能由 r_j 完全预测；可读性随嵌套深度下降，只有最粗层稳定可读
B21  失败语义层间不统一：KeyError / {} / None / ValueError 并存（L2 未知前缀静默返回 {}）
```

### 4.3 读出方法与操作化（E24–E26 的三组）

```text
B09  ICA 不适用于共现矩阵（零对角破坏线性混合 + 质量简并 + 去均值降维 + 成分正交）
B10  LCA 第二分析（当前配置）不可用（活跃比例 1.0、未收敛、与谱聚类一致性≈0）
B11  稳定性不能当"无结构"判据（随机图上也稳定）→ 负对照改用模块富集度
B13  初值域未预注册会产生假失败（V0~U(0,1) → 盆地一边倒）
B14  收敛判据被短路（比较同一数组对象 → 未归类恒为 0）
B15  EI_int 主要由跨模块耦合【总量】决定，不分辨耦合图案（置换零分布有假阴性）
```

### 4.4 方法学六类（"不报错的坏结果"）

```text
① 索引约定错位            B17  块对角机制假阳性报出 EI_int = 1.82（本应精确为 0）
                              自检：块对角零
② 边际化遗漏              B18  TE 算出负值（首轮 −0.78）        自检：非负性
③ 置换不变量局部误用       B19  ARI = 1.000 却逐节点显示 6/15   自检：ARI ≠ 逐位一致
④ 指标对退化方向不敏感     E29  k=5 超分时 ARI 仍 0.928         自检：对偶指标
⑤ 统计量对随机性不敏感     E32v2 中位数 1.0000 但最小值 0.7202   自检：中位数 + 最小值
⑥ 抽样覆盖不足            B26  Gibbs 在 β=8 上 1000 样本只覆盖 1 个状态且不报错
                              自检：报告抽样覆盖（不同状态数 / 有效支撑）
```

**共同点**：**不报错，给出一个看起来合理的数**——比崩溃更危险。
其中 ①–③ 是"实现写错"、④ 是"指标选错"、⑤ 是"统计量选错"、⑥ 是"**采样算法没探索到目标分布**"
（**⑥ 是第一类不是代码错、也不是指标错的失效模式**）。

---

### 4.5 E38 / E39 的两条新边界（B32 / B33）

```text
B32  分块均值的模块级 W3 的 Boltzmann 概率【不是】节点级分布对模块符号的正确边缘化。
     C1 上 d = +0.059548（(+1,+1,+1)）；解耦对照（跨模块置 0）→ 精确 0.25 均匀
     ⇒ 偏移源于跨模块耦合。范围限定：限于 C1 / 块均值口径；种子轴退化（10 种子同一划分），
     d 是确定量而非多种子证据。
B33  竞争性抑制（WTA 式互斥）在【到达值算式层】不可实现（前提：max 吸收 + 种子固定为 1.0）。
     ① 构造性上界 1.0 × strength × hop_decay = 0.9 < 1.0（strength ≤ 1 由定义保证）；
     ② 实测减法 / 增益 / rival 三种形态下种子恒 1.000000；③ 只改 reach，不改成对相对方向。
     出口：要压制种子必须改吸收判据（`>` 改成别的）⇒ 违反 L0-SPEC §2 ⑥ 的硬线 ⇒ 越界。
B34  检索的可达集是【基态流形的一个分支】：(1,1,1) 与 (-1,-1,-1) 是简并基态
     （E 相同 = −1.235577，β=2 时各 0.126206），但 max 传播（**任何**种子集）只能到
     (1,1,1) 那一支 —— 机制是"任何非空种子集都会点亮其所在模块"（种子语义的推论）。
     采样覆盖两个分支且质量相同。与 C39 同时登记。
```

### 4.6 边界互引地图（同一片区域，交叉引用一次写清）

| 条目 | 相邻 | 关系（一句话） | 边界方向 |
| --- | --- | --- | --- |
| **C33** | **B32** | C33 说"节点级与模块级是粗粒化关系"；B32 定量指出该关系只到**支撑 / 自由度**，质量分配不同（d = +0.059548） | C33 的表述收窄为"支撑一致"，不再蕴含质量分配一致。**语料同源**：B32 的范围（C1 / 块均值）与 C33 一致（同为 E24/E25 的 C1），故 C33 的限制条款**不引入新的语料假设** |
| **B33** | **B12** | B12 说【权重层】非负权重无 WTA；B33 说【到达值层】即使外部负值进入算式也不产生互斥 | 两条并列，互不回写；要 WTA 须改吸收判据（架构级） |
| **C38** | **C28** | C28：max / sum 是【读取端】的两种选择；C38：g 作用于【到达值算式层】——同属"外部可调"，层不同 | C38 不引入读取端过滤（P5：键集合与 g 无关），故不覆盖 C28 的范围 |
| **C38** | **C34** | C34 的 W 是【读取端】工具（快照作额外种子）；C38 的 g 是【算式层】参数 | 两者分账；合用时须分别记录 |
| **B33** | **L0-SPEC §2 ⑥** | 规格把"到达值算式"判为实现约定（可改），硬线是"必须保留 max 吸收" | B33 正好卡在这条线上：改 g 可以，改吸收判据不行 |
| **B32** | **C25** | C25 是模块级动力学的**存在性**（8 个二值模式）；B32 说的是"模块级模型不能替代节点级分布" | B32 不动 C25 的存在性，只限其**定量用法** |
| **C39** | **B34** | 同时登记：C39 说"采样到得了 5 个检索到不了的状态"，B34 说"其中全关那一支**任何**种子集都到不了" | C39 依赖 P2/P3/P7a/P7b；B34 依赖 P6 —— 两者共用同一个 (a) 对照，不可拆开引用 |
| **B34** | **B12 / B33** | B12/B33 讲"抑制造不出来"，B34 讲"检索的可达集缺一整支" | 三条都在回答"基底做不到什么"，但机制各异（权重层 / 到达值层 / 种子语义），不互相推出 |
| **C40** | **C36** | 同一条"分类 ≠ 排序"的两半：E36 证分类可测、排序不可测；E41 证排序也可测 | C36 原文保留，加**状态更新条款解除排序轴**（方向与 E38 对 C33 的收窄相反） |
| **C40** | **C35** | C40 让"值序"可测；C35 说的"无时间顺序、无优先级"是另一轴 | C35 加**状态注解**：W 自身不排序，排序信息来自被保存的 value —— 值序 ≠ 时间序 |
| **C40** | **C37 / B30** | C40 的可测性完全依赖 v2 的带值入口与常量语义 | C37/B30 是 C40 的**前置**；C40 不新改入口，只把已有出口接上 |

---

## 5. 四块硬骨头的现状

| 硬骨头 | 已切到哪一刀 | 仍缺什么 |
| --- | --- | --- |
| **生成** | **E33（采样）+ E38（口径）+ E40（解 clamp）+ E42（组合）**：条件采样给 max 之外的 3 个状态（0.751）；**B32** 钉死模块级模型不能替代节点级分布；解 clamp 后采样覆盖 8/8、**5 个状态单节点检索到不了**（C39）、**B34** 钉死检索只覆盖基态流形的一支；**E42 造出景观外的新对象**（C41：L5 的组合符号，可分性 4/4，prod 内部值逐位 = v1×v2） | "合成"已做到**符号级**（共现绑定建新符号）；**开放**：L5 的成员边是手工建的，"**自动发现该建哪些边**"未做；深层状态仍受 B24 限制 |
| **工作记忆 + 规划** | **E34–E36（三刀）+ E41（值域编码）**：跨次携带成立；串联是**载体**行为；**分类可测（E36）+ 值序可测（E41）**两轴都通 —— 抹平不在 W 层，只改注入调用点 1 行 | **时间序**（需 W 存时间戳）仍不可测（C35 注解保留）；**分类+排序组合态**未测（E42 候选）；规划（前瞻、搜索空间）未做；跨划分稳定性（E38 的种子轴退化）待补 |
| **抑制控制** | **E39（第一刀）**：**C38 可及性压制**成立（reach 7→1，构造参数 g）；**B33 钉死"竞争性抑制在到达值算式层不可实现"**（构造性上界 0.9 < 1.0） | 真互斥（WTA）需改吸收判据 ⇒ 架构级变更，非实现约定；负权重来源若要用，须显式声明并重画功劳账 |
| **多模态输入** | 未动 | L0 为离散符号设计；图像连续/高维/有空间结构 → 需重新设计输入接口（L0 冻结 → 只能外部编码 + 新统计层） |

---

## 6. 未完成 / 已取消

```text
待办    E25 快照扩展：把 I 扫描的完整轨迹写进 JSON（现在 S2 面板画的是由转变点重建的示意）
待办    论文侧整理：Results 4.1–4.8 已有中文草稿；上层章节未并进正文（当前无排期）
待办    B32 跨划分稳定性检查：B32 已 scoped 到 C1 的【唯一划分】（E38 种子轴退化）；
        若将来要推广 B32（换语料 / 加噪声 / 边 dropout / 换块均值口径），须先补一次跨划分实验
        （入口记在 NEXT-PLAN §4.1）
已取消  P5b（网页"模块实验模式"）：P5a 的只读面板已够展示；取消决定记录在 DESIGN-P5 §7
已取消  竞争型互斥（负权重）：E25/B12 已给原理性结论，E26 明确排除
```

---

## 7. 下一步候选（两条）与已封死记录

```text
① 多模态接口设计     未动。L0 为离散符号设计；图像连续 / 高维 / 有空间结构 ⇒ 需重新设计输入接口
                     （L0 冻结 ⇒ 只能"外部编码 + 新统计层"）
② 工作记忆值域编码   给痕迹"排序 / 优先级"必须走值域编码（保留种子原始值）
                     风险：触及基底种子入口的行为 —— 先按纪律⑩判"架构 vs 实现"，再动

已完成（不再列为候选）：
  生成解 clamp —— E40 已落：C39（采样覆盖检索逻辑封闭外的 5 个状态，0.6253–0.6299 质量）
  + B34（检索只覆盖基态流形的一支）。**注意**："从两个已知状态【合成】从未出现过的组合"
  仍未做 —— 那是"生成"的下一个命题，与解 clamp 不在同一层（E40 是"采样到得了"）。
  工作记忆值域编码 —— E41 已落：C40（值序可测）+ C36 排序轴解除 + C35 注解。

由已完成项派生出的后续命题（**不自动成为候选，待拍板**）：
  · 生成：**"自动发现该建哪些成员边"**（E42 的成员边是手工建的；自动生成规则是另一命题）
  · 工作记忆：**时间序编码**（需 W 存时间戳；当前 W 只存值）、
    **分类+排序的组合态**（如"只注入 step=1 的痕迹且按值序"，E36 与 E41 的组合，E42 候选）
  · 组合：跨窗口/跨时间的"同时激活"（E42 的"同时"= 一次 coactivate 调用内）

已封死记录（**不列为候选**）：
  抑制控制 —— E39 已给出：可及性压制（C38）已实现；竞争性抑制在 max 吸收下不可实现（B33）；
  剩余路径需改架构（违反 L0-SPEC §1）或走读取端（同 E14 / C28）。
  该方向在本项目当前架构内封闭。
```

---

## 8. 怎么复现

```powershell
$env:PYTHONHASHSEED=0          # 顺序类结论可复现（纪律 ⑧ 配套，E28 起要求）
python main.py                 # 全量 E0–E33（逐项 PASS/FAIL）
python main.py exp33_landscape_sampling   # 只跑单个实验
python verify_claims.py        # 登记表核对（88 项）
python verify_playground.py    # 网页分析面板 ↔ JSON 逐字段一致（7 项）
python scan_norms.py           # 措辞规范扫描
```

**读法建议**：先读本文件 → 再读 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md)（C/B 逐条带证据）
→ 需要细节时看对应的 `DESIGN-E*.md` 与 `outputs/exp*.json`。
