# Synapse-Net 复杂度基线（BENCHMARK）

日期：2026-09-06　脚本：`benchmark.py`　原始数据：`outputs/benchmark.json`

## 方法

- 图结构：环 + 确定性随机弦（平均度约 4），保证全图连通；
- 规模：L0 测 200/1000/3000/6000 节点；L1 测 200/1000（原因见下）；
- 每次 learn 一个相邻对事件；activate 从 n0 全图扩散，取 3 次中位数；
- 峰值内存用 tracemalloc（不含边生成阶段）；
- 环境：Codex 捆绑 CPython，Windows，单线程。数值为**相对参照**，非硬件结论。

## L0（共现基底）

| nodes | edges | learn_avg(µs) | activate(ms) | peak(MB) |
| --- | --- | --- | --- | --- |
| 200 | 400 | 11.92 | 2.11 | 0.27 |
| 1000 | 2000 | 12.13 | 10.76 | 1.35 |
| 3000 | 6000 | 12.04 | 32.95 | 4.22 |
| 6000 | 12000 | 11.95 | 65.36 | 8.47 |

观察：learn 单次约 12µs，基本恒定；activate 与内存随规模近似线性
（6000 节点全图扩散约 66ms）。L0 在此范围内可上量。

## L1（方向层）

| nodes | edges | learn_avg(µs) | activate(ms) | peak(MB) |
| --- | --- | --- | --- | --- |
| 200 | 400 | 6.64 | 1.80 | 0.16 |
| 1000 | 2000 | 7.17 | 14.30 | 0.89 |
| 3000 | 6000 | 7.34 | 51.91 | 2.84 |
| 6000 | 12000 | 7.32 | 115.75 | 5.95 |

观察：**L1 已加邻接索引（2026-09-06，纯结构优化）**：activate 由原来的
O(N·E) 二次增长（1000 节点 1.27s）降到近线性（1000 节点 14.3ms，约 89 倍；
6000 节点 115.7ms）。语义与公式未变，全量回归 E0–E19 PASS。

## 发现与建议（S-上量前置）

1. **L0 与 L1 均可上量**：6000 节点/12000 边下 L0 全图扩散约 66ms、
   L1 约 116ms，均为近线性；learn 单次 7–12µs；
2. L2/L4 规模成本低（计数表/块表），上量前补同口径基准即可；
3. 上量时记录同样口径的 benchmark，作为回归线。

**结论：M3 四件套完成；L1 邻接索引待办已清。工程上量（S-上量）可进入其 gate 评审；
当前研究主线转入 V1 验证补强。**
