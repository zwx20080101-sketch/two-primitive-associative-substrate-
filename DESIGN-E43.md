# E43 设计：抑制控制 —— 负值注入对【轨迹】的压制效果

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-16。
依赖：E37（C37 + B30，v2 的带值入口与常量语义）、E39（C38 + B33，可及性压制与竞争性抑制的边界）、
E30（C30 闭包）、E41（值序，同一条已登记入口）。
**L0/L1/L2/L4 与 v1/v2/v3 一字不动；不引入 stub / 语义 / 奖励。**

---

## §0 概念基础

### 0.1 机制：负值是【路径屏障】，不是【数值压低】

```text
【写清机制，别写错】
  [错的说法] "负值降低了下游的值" —— max 吸收下负值不参与竞争（永远赢不过正值）。
  [对的说法] "负值节点进入 v2 的 frozen 集合，阻断了正值路径的传播。"

依据：synapse_net_v2.py 的 `if v in frozen: continue` —— 被注入的（负值）节点值恒定，
      从种子出发的正值路径无法穿过它（写不进去，也不会经过它继续传播）。
      故它的下游只能收到由它的值算出的到达值。
⇒ 这是【结构屏障】（集合运算），不是【数值压低】（算术减法）。
  这也正是它与 C38 的根本区别（见 §5 对照表）。
```

### 0.2 EPS 伪影：下游的"精确负值"只在特定拓扑下存在

```text
屏障下游分两情形（预检实测）：
  · 【紧邻叶子】：节点 v 的全部邻居都是 frozen（即 v 只挂在注入点上）
      ⇒ v 只收到一次到达值，永不再更新 ⇒ 精确值（实测 -0.8 x 0.9 = -0.720000）
  · 【其他位置】：v 有非 frozen 邻居
      ⇒ 负值沿无向边回传，而 max 取较大值 ⇒ 负值"越传越接近 0"，
        最终停在 EPS = 1e-12 的门槛上（实测 -4.93e-12）

【EPS 伪影不是物理量】实测随 hop_decay 剧烈变化：
  hop_decay = 0.9 → -4.93e-12 | 0.5 → -3.64e-13 | 0.2 → -4.19e-14 | 0.05 → -3.91e-15

⇒ 判据因此写【下游 <= 0】，不写精确值；只有"紧邻叶子"才额外报精确值 -0.72 作为证据。
```

### 0.3 割点条件的可判定形式

```text
定义：注入点 v 是目标 t 的【割点】<=> 把 v 从图中移除后，t 不再有从种子出发的正路径可达。
预检实测（阴性控制）：给目标加一条替代正路径（junction→tip1 直连）后，
  压制失败 —— tip1 保持基线 0.729000 逐位不变（替代正路径的值赢过负值）。
⇒ 割点条件是"轨迹压制"能成立的必要结构条件，写进 P4 作阴性控制。
```

### 0.4 注入位置 → 影响范围（三条映射表；P1 与 P4/P5 的机制基础）

```text
同一语料、同一注入值 -0.8，只改【注入位置】。基线：start_node 1.0 / trunk 0.9 / junction 0.81 /
arm1 0.729 / tip1 0.6561 / arm2 0.729 / tip2 0.6561
```

| 注入位置 | 位置性质 | 变化节点数 | 变化节点 | 下游取值 | 对应判据 |
| --- | --- | --- | --- | --- | --- |
| **arm1** | 分支1 的**割点**（下游只经它可达） | **2** | [arm1, tip1] | arm1 = −0.800000、tip1 = **−0.720000** | **P1 主判据**（路径排他） |
| **junction** | **分叉点**（两个分支的共同上游） | **5** | [junction, arm1, tip1, arm2, tip2] | 两分支下游均 ≤ 0（实测 −0.0） | **P4 阴性 1**（全局压制，非排他） |
| **tip1** | **非割点叶子**（终点，无下游） | **1** | [tip1] | tip1 = −0.800000，其余 6 个逐位不变 | ——（说明"压分支"需有下游） |

```text
⇒ P1（阳性）与 P4（阴性 1）的差别【只来自注入位置】：割点 → 一支；分叉点 → 两支。
   注入叶子 → 只压自身 ⇒ "压一个分支"必须【同时】满足：有下游、且下游只经它可达。
⇒ P5（阴性 2）再补一条：给 tip1 加替代正路径 ⇒ 连"压一个分支"也失败（割点的必要性）。
```

### 0.5 v2 only + 拍平自检（B21 的坑）

```text
【硬线】全程用 v2 的 net。v1 的 dict 入口会静默拍平（只取 keys、值全变 1.0，B21）。
预检实测：同一调用在 v1 上 → 注入节点 = 1.000000；在 v2 上 → -0.800000。
【自检】注入后立刻断言注入节点的实际值 == 注入值（容差 1e-12）；
  断言失败 ⇒ 报"v1 静默拍平"，P0 前置 FAIL，不继续跑主判据。
  （预检期间我自己踩过这个坑：corpus_planted() 返回的是 v1 的 net，那组数字作废。）
```
---

## §1 预检摘要（2026-09-16，只读，未动任何文件）

```text
① 负值入口：v2 保留（-0.8 → -0.800000）；v1 拍平（→ 1.000000，B21）
② 四定义实测：
   A 节点自身   平凡（= 输入差本身）
   B 下游扩散   紧邻叶子 → 精确 -0.72；其他位置 → 约 -5e-12（EPS 伪影）
   C 区域压制   C1 上模块0 均值 0.920 → 0.580（-0.340）；无关模块逐位不变；
                弱耦合模块 0.037125 → 0.001856（经 count=5 的弱边溢出）
   D 路径排他   分支1 被压（tip1 = -0.720000）；分支2 逐位不变
③ 割点条件：加替代正路径后压制失败（tip1 保持 0.729000）
④ 语料：自造"分叉 + 紧邻叶子"7 节点拓扑（见 §2）—— 四组控制全部符合预期
```

---

## §2 预注册参数（跑前定死，跑后不改）

### 2.1 语料（E43 自造，最小可复现）

```text
节点 7 个：start_node, trunk, junction, arm1, tip1, arm2, tip2
边 6 条（全部 x100）：
    start_node—trunk、trunk—junction、junction—arm1、arm1—tip1、junction—arm2、arm2—tip2

拓扑：
    start_node — trunk — junction — arm1 — tip1
                     └ arm2 — tip2

【节点命名自检（按 E42 的留痕，E43 预检实测两轮）】
  第 1 轮：起点节点原定名 `seed` —— 全仓库 `.py` 里 `seed` 作为【裸标识符】出现 **114 处**
    （RNG 种子 / 种子节点：spectral(seed=...)、fastica、gibbs_module、exp28 的 `SEED = "A"`、
     exp27 的 `seed = sX[-2]`）⇒ 同形风险高，改名。
  第 2 轮：改用 `src` —— 仍撞 **9 处**（exp23 的 `def hops(adj, src, dst)`、exp39 的 `def clone_into(src, g)`、
    `make_pdf.py` 的常量 `SRC`（值是 Path，不是字符串））⇒ 再撞，再改。
  第 3 轮（定稿）：全部七个节点名改用【零撞车】的名字（逐个实测）：
    start_node 0 ｜ trunk 0 ｜ junction 0 ｜ arm1 0 ｜ tip1 0 ｜ arm2 0 ｜ tip2 0
    （对照：seed 114 ｜ hub 14 ｜ src 9 ｜ s0 8 ｜ n0 5 ｜ mid 2 ｜ fork 3 —— 均弃用）
  【两层防护】① 节点名避开高频/内置词；② 代码里用常量引用（`START_NODE = "start_node"`），
    裸字符串只出现在常量定义处。
  （E42 的教训：`Z = 1.0` 与节点 `"Z"` 撞车 → NEXT-PLAN §4.1 已立命名约定，本实验按它执行。）

注入点（割点）= arm1 | 被压制分支 = {arm1, tip1} | 不受影响分支 = {arm2, tip2}

【为什么自造，不复用 E6a / C1】
  · E6a 的分叉语料是为"上下文偏置分支"设计的（多事件、共享情景、节点更多），
    没有"注入点紧邻叶子"的结构 ⇒ 会落到 EPS 伪影区，拿不到精确值；
  · C1 是模块语料（3 模块 / 15 节点），结构不同，用于"区域压制"辅助观察（P6）；
  · E43 需要的正是【割点 + 紧邻叶子】的最小形状 ⇒ 自造 7 节点。

【基线预期（start_node = 1.0 单点激活，无注入）】逐节点：
  start_node 1.000000 | trunk 0.900000 | junction 0.810000 | arm1 0.729000 | tip1 0.656100
  | arm2 0.729000 | tip2 0.656100

【阳性注入预期（{start_node:1.0, arm1:-0.8}）】：
  arm1 = -0.800000（自身）、tip1 = -0.720000（= -0.8 x 0.9，精确）、
  其余 5 个节点（start_node/trunk/junction/arm2/tip2）逐位不变
```

### 2.2 阈值（预设，跑后不改）

```text
压制阈值 = 【下游值 <= 0】（严格；不设 0.05 这类无依据的松弛阈值）
容差 = 1e-12（若某处浮点为 +1e-13，按容差处理）
不变性阈值 = 差异节点数 = 0（逐位，容差 1e-12）
注入值 = -0.8；种子值 = 1.0
运行环境：PYTHONHASHSEED = 0
```

### 2.3 两个语料的加载与切换（P6 用）

```text
· 主语料 = §2.1 的 7 节点自造拓扑（P1–P5 全部跑它）
· 辅语料 = E24/E25 的 C1（15 节点 / 3 模块）——【只给 P6 用】
  返回形态：`corpus_planted()` 返回四元组 `(net, nodes, labels, meta)`，其中 **net 是 v1 的
    SynapseNet 实例**（不是原始 connections 列表）。
  加载方式（重放三步）：
    ① `net1, nodes, labels, _ = corpus_planted()`
    ② `net2 = SynapseNetV2()`
    ③ `for (a, b), rec in net1.connections.items():  for _ in range(int(rec["count"])): net2.learn([a, b])`
    否则 dict 注入会被 v1 静默拍平（§0.5）。
  【重放后的逐位一致性校验（必须做，否则 P6 的数字作废）】
    比较 net1 与 net2 的**边集合与计数**：对每条边 (a,b)，`net1.connection_count(a,b) ==
      net2.connection_count(a,b)`，且边集合相同、节点数相同 ⇒ 违例数 = 0（容差 1e-12；
      计数是整数，实际应为精确相等）。
    若不一致 ⇒ **P6 的数字不可与 v1 的基线比**（会变成"v1 基线 vs v2 重放"的错配，
      数字看着对、实际不同源）⇒ P6 记 FAIL 并如实报告，不得直接引用该数字。
    报告：net1/net2 的节点数、边数、逐边计数比较结果。
  模块划分：spectral(impute_diagonal(matrix_from_net(net1, nodes)), 3, seed=20260911)
    【为什么用 net1 的矩阵而不是 net2 的】net1 是语料的原始实现；net2 已通过与 net1 的
      逐位校验（计数级）⇒ 两者结构等价。用 net1 的矩阵更直观——
      模块划分依据的是【语料结构】，不是【实现细节】。（不改结论，只是口径说明。）
  注入配置：种子 = 模块0 的第一个节点；负值注入 = 模块0 的第二个节点（-0.8）
【P6 不参与 PASS/FAIL】—— 它只在判据分层的"辅助观察"层，报告三模块均值对照即可。
```

---

## §3 判据（预注册）

### 3.0 判据分层

```text
· 主判据（可 FAIL）：P1 路径排他（定义 D）
· 辅判据（可 FAIL）：P2 下游压制（定义 B）
· 自检（可 FAIL，防 v1 拍平）：P3 注入值保留 + 种子未被压制（定义 A）
· 阴性控制（可 FAIL，证明 P1 的"排他"不是平凡的）：P4 割点条件 / P5 对称性
· 辅助观察（报告项，不判定）：P6 区域压制（C1 语料）
· 形式审计：P0 前置 / P7 只读
· 双向：P8
```

### P0 前置

```text
① 用 v2 的 net（不是 v1）—— 语料由本实验自建（7 节点 6 边，见 §2）
② 拍平自检：注入后立刻断言 arm1 的实际值 == -0.8（容差 1e-12）
   失败 ⇒ 报"v1 静默拍平"（B21），P0 FAIL，不继续跑主判据
③ 六文件哈希与 L0 签名跑前 = 跑后
```

### P1 主判据：路径排他（定义 D）

```text
两个条件【同时成立】：
  (a) 分支2 的激活分布在注入前后【逐位相同】：arm2 与 tip2 的差异 = 0（容差 1e-12）
(b) 分支1 的下游值【<= 0】：tip1 <= 0
预检预期：arm2 0.729000 → 0.729000、tip2 0.656100 → 0.656100（不变）；
          tip1 0.656100 → -0.720000（<= 0 ✓）
【"逐位不变"锚定到基线】基线 = 无注入时的单点激活 `activate({"start_node": 1.0})`：
  start_node 1.000000 | trunk 0.900000 | junction 0.810000 | arm1 0.729000 | tip1 0.656100
  | arm2 0.729000 | tip2 0.656100
  (a) 的判据 = 与上述基线逐位相等（容差 1e-12），不是"与某次别的运行比"。
报告：7 个节点的完整注入前/后对照表 + 变化节点列表（预期恰为 [arm1, tip1]）
```

### P2 辅判据：下游压制（定义 B）

```text
被压制分支的下游节点值 <= 0（严格阈值；预检 tip1 = -0.720000）。
【适用范围（写死）】本判据【只算 tip1】—— 它是拓扑里唯一的下游节点；
  arm1 自身【不算本判据】（那是 P3 自检的范围：arm1 = 注入值）。
  两者的分工：(P2) 下游被压到 <=0 ｜ (P3) 注入点自身保留注入值 + 种子未被压制。
【报告要求】同时报该值的【来源】：本语料下 tip1 是"紧邻叶子"（唯一邻居 arm1 是 frozen）
  ⇒ 报精确值 -0.720000 并注明"= 注入值 x hop_decay"；
  若将来换成含环的下游区域，该值会退化为 EPS 伪影（§0.2），届时只报"<= 0"。
```

### P3 自检：注入值保留 + 种子未被压制（定义 A）

```text
(a) 注入值保留：arm1 == -0.800000（容差 1e-12）—— 防 v1 拍平（B21）
(b) 种子未被压制：start_node == 1.000000（容差 1e-12）
    —— 这一条同时是【与 B33 的界面对齐】：B33 封死"压制种子"，本实验不碰它
```

### P4 阴性控制 1：割点条件（不是全局压制）

```text
对照：把注入点改成【分叉点 junction】。
【更正（2026-09-16，E44 预检发现）】原文写"junction 不是任一分支的割点"是**错的**：
  按标准定义（移除后连通分量增加），E43 拓扑的割点是 **4 个**：`[trunk, junction, arm1, arm2]`；
  junction 移除后图分成 3 块（`{start_node,trunk}` / `{arm1,tip1}` / `{arm2,tip2}`）。
  真正的区别是**断开范围**：`arm1` 的移除只断开**一个**分支（tip1），
  `junction` 的移除断开**两个**分支 ⇒ 一个节点可以同时是割点、且是"更上游的割点"。
预检预期（实测）：变化节点 = [junction, arm1, tip1, arm2, tip2]（5 个，两个分支都被压到约 0）
判据（确定性 ⇒ 可写死）：变化节点数 = 【5】（恰为 junction + 两个分支的 4 个节点），
  且【两个分支都变】；对照 P1 的 2 个。
  ⇒ 证明 P1 的"只有分支1 变"来自【割点结构】，不是"注入负值总会压到下游"的平凡结果
【注释】P4 的"= 5"是【确定性值】（v2 无随机性）。若未来在此处得到不同数字，
  先查实现（v2 是否被改动 / 是否误用了 v1 的 net），再判 FAIL ——
  不得直接用"实现漂移"解释判据失败（E39 v1 的教训：判据写死后，FAIL 先查实现再谈机制）。
```

### P5 阴性控制 2：破坏割点 → 压制失败

```text
对照：在 P1 的语料上加一条替代正路径 junction—tip1（tip1 不再只能经 arm1 到达）。
预检预期（实测）：注入 {start_node:1.0, arm1:-0.8} 后，变化节点 = [arm1]（仅注入点自身）；
  tip1 保持基线 0.729000（正的替代路径赢过负值）
判据：tip1 的注入后值 > 0（即"压制失败"必须复现）—— 这是割点条件【必要性】的实证
```

### P6 辅助观察（报告项，不判定）：区域压制（C1 语料）

```text
在 E24/E25 的 C1 语料上（用 v2 重建），种子取模块0 的一个节点、负值注入同模块另一个节点：
预检预期：模块0 均值 0.920 → 0.580（-0.340）；无关模块1 逐位不变；
          弱耦合模块2 0.037125 → 0.001856（经 count=5 的弱边溢出）
报告：三模块均值对照 + 差异节点列表（预期 7/15）
【前置校验（必做，见 §2.3）】重放后 net2 与 net1 的边集合与逐边计数必须一致（违例数 = 0）；
  不一致 ⇒ P6 的数字作废（不可与 v1 基线比），记 FAIL 并如实报告。
【不判定】—— 它是"跨模块溢出"的现象展示，不设阈值；但上面的前置校验【必须过】。
```

### P7 只读

```text
六文件逐一实测哈希（synapse_net.py cf99a07a0e60fd4d / v2 ee6df589eb2f6983 / v3 085be73a06f0694c /
  order_layer.py 419d54745a9eb795 / context_layer.py bd029d7bbdc345be / chunk_layer.py 46f5857be0fc7c7e）
  跑前 = 跑后
L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后
```

### P8 双向结局

```text
通过（P1 + P2 + P3 + P4 + P5 全过）→ 新 C 条目（轨迹压制能力）
  + 与 C38/B33 的状态说明（见 §5）
不通过 → 如实记录哪条挂、实测值多少、失败分层（入口 / 屏障 / 割点 / 排他性）
【编号纪律】本实验不预分配 FAIL 分支的 B 编号（同 E41/E42）；
  机制性 FAIL 才在跑完后新立 B 编号。
```

---

## §4 双向落点（跑前写死）

### 通过 → 新 C 条目（轨迹压制能力）

```text
C??：在【割点结构】上，用 v2 的已登记带值入口把负值注入【分叉点下游的第一个节点】，
    该节点成为【路径屏障】（frozen 集合阻断正值传播）⇒ 该分支的下游被压到 <= 0
    （本语料 tip1：0.656100 → -0.720000，精确 = 注入值 x hop_decay），
    而【另一分支逐位不变】（arm2/tip2 差异 = 0）。
对照组（阴性）：注入到共同上游 junction（**同为割点，但断开两支**）⇒ 两个分支都被压（5 个节点变化），
    证明"只有一支变"来自割点结构；给下游加替代正路径 ⇒ 压制失败（tip1 保持正值），
    证明割点条件的必要性。
    范围限定：① 机制是【结构屏障】（frozen 阻断），不是【数值压低】——与 C38 的
    "构造参数 g 在到达值算式做减法"（数学运算）机制不同（见 §5 对照表）；
    ② 下游的【精确负值】只在"紧邻叶子（全部邻居 frozen）"时成立，
      含环下游会退化为 EPS 伪影（§0.2）⇒ 判据写"<= 0"，不写精确值；
③ 不压制种子（start_node 恒 1.0）⇒ 与 B33 不冲突；④ 功劳归 v2 的已登记入口（C37/B30）
      的 frozen 行为 + 注入策略（选割点），不是基底新能力（L0 一字未动）；
    ⑤ 语料是 E43 自造的 7 节点最小拓扑。
```

### 不通过 → 如实记录

```text
不登记 C 条目。记录哪条判据挂、实测值多少，失败分层：
  ① 入口是否保留负值（P0/P3，防 v1 拍平）
  → ② 屏障是否形成（P2）
  → ③ 排他性是否来自割点（P4/P5）
机制性 FAIL 才在跑完后新立 B 编号；实现/判据问题则修正后重跑（不新立编号）。
```

---

## §5 影响评估

### 5.1 与 C38 的对照表（机制完全不同）

| 维度 | C38（可及性压制，E39） | E43（轨迹压制） |
| --- | --- | --- |
| 机制 | `arrival = au x s x hd - g`（到达值算式做减法） | **frozen 屏障阻断路径** |
| 数学 vs 结构 | **数学**（减法运算） | **结构**（集合屏障） |
| 参数形态 | 构造参数 g（全局，与 λ/hop_decay 同级） | **逐节点负值**（局部，dict 入口） |
| 可及性变化 | **整体** reach 单调不增（7→6→…→1） | **特定分支**的下游 ≤ 0 |
| 不受影响的范围 | 无（整体压低） | **另一分支逐位不变** |
| 种子是否受影响 | 否（恒 1.0） | 否（恒 1.0）→ 与 B33 不冲突 |
| 落点 | v3（`synapse_net_v3.py`） | **v2**（不改任何层文件） |

### 5.2 与 B33 / B30 / C37 的关系

```text
· B33（竞争性抑制在 max 吸收 + 种子固定下不可实现）：本实验【不碰种子】——
start_node 恒 1.0，被压的是非种子下游 ⇒ 不冲突、不回写 B33。
· B30（v2 的常量语义使种子冻结）：本实验的屏障机制【正是 B30 的直接后果】——
  因为种子（含负值种子）不被改写，才可能成为"挡住路径"的屏障。
  这是 B30 的一次正向运用，不是新边界。
· C37（v2 带值入口）：本实验用的是同一条已登记入口，不新增入口。
```

### 5.3 功劳记账

```text
机制来自两处，都不是基底新能力：
  · v2 的 frozen 集合（C37/B30 已登记入口的既有行为）
  · 注入策略（选割点）—— 实验侧的选择
⇒ 功劳归 v2 已登记入口 + 注入策略；L0/L1/L2/L4 与 v1/v3 一字未动。
```

---

## §6 产物与不做

**产物**

- `experiments/exp43_trajectory_suppression.py` + `outputs/exp43_trajectory_suppression.json`；
- 登记（双向，跑前写死）：通过 → 新 C 条目；不通过 → 如实记录（不预分配 B 编号）；
- **不新增层、不改任何已验收文件**（本实验只用已有的 v2 入口与自造语料）。

**必报清单**

```text
① P0：拍平自检（注入后 arm1 的实际值）+ 六文件哈希 + L0 签名
② P1：7 节点完整对照表（基线 / 注入后）+ 变化节点列表（预期 [arm1, tip1]）
③ P2：被压制下游的值 + 其"紧邻叶子"来源说明
④ P3：注入值保留 + 种子未被压制
⑤ P4：阴性控制 1（注入 junction）的变化节点列表（预期 5 个、两分支都变）
⑥ P5：阴性控制 2（破坏割点）的 tip1 值（预期保持 0.729000）
⑦ P6：C1 的三模块均值对照 + 差异节点列表（辅助观察）
⑧ 与 C38 的对照表（§5.1）
```

**不做**

- 不改 v1/v2/v3、不改 L0/L1/L2/L4、不新增层；
- **不写"下游精确负值"作判据**（除"紧邻叶子"外都是 EPS 伪影）；
- **不压制种子**（那会撞 B33）；
- 不做"多注入点同时压制"（属另一命题）；
- 不声称"基底学会了抑制"（功劳归 v2 入口 + 注入策略）；
- 不引用 `IDEAS-Modules.md` 作为设计依据（北极星纪律）；
- 预注册阈值跑后不改（压制阈值 <= 0、容差 1e-12、注入值 -0.8）。

---

## §7 已知陷阱（逐条对应到判据）

```text
① 重检索陷阱（E33 v1）  → P1(a)：另一分支【逐位不变】；且基底返回值本身变了（不是读取端过滤）
② 恒等式陷阱（E38）     → P1(b) + P2：下游也变（tip1 从正值变 <= 0），不是"只改输入"
③ B33 撞车              → P3(b)：明确断言"种子未被压制"（start_node 恒 1.0）
④ 单次漂亮数字（纪律9）  → 本实验确定性（无采样、无随机初始化）⇒ 不涉
⑤ B21（v1 静默拍平）    → P0② + P3(a)：注入后立刻断言；预检期间我自己已踩过一次
⑥ EPS 伪影（本实验新发现）→ §0.2 + P2 的报告要求（判据写 <= 0，不写精确值）
```
