# 论文中文事实草稿（PAPER-zh-DRAFT）—— Results 4.1–4.8

写作规则：每一句可回溯 CLAIMS-REGISTRY 登记表 + outputs JSON；
只写"观测到了什么"，不写宏大推论；负结果与边界如实写；
λ、hop_decay、阈值均为预设参数，不是系统自涌现参数；
读取端工具与外部 stub 的能力不归入双原语基底原生动力学。

小节归属说明：E6a（分叉与上下文）与共享情景/枢纽同属"结构-语义拓扑"
证据，故安排在 4.4；E9（补全/闭包）与 E14（多路会合规则）归入 4.2
（检索行为）；E11（共享枢纽拥挤）归入 4.4（结构拓扑）。
4.1–4.8 之外（Architecture/Method/讨论/引言/摘要）待结果定稿后再写。

---

## 4.1 结构自组织（中文草稿）

在"线性字母序列、逐对呈现"的受控输入族下（26 个符号节点，系统对字母语义
无预置先验知识；训练事件为相邻字母对 [A,B]、[B,C]、…、[Y,Z]，各出现 1 次），
双原语联想基底中的 L0 共现层从空网络出发，仅凭 L0 层的共现绑定机制，
自组织形成顺序关联网络（联想链）：最终网络包含 26 个节点与 25 条无向连接；
从未共现过的符号对（如 A–Z）不存在连接；以 A 为种子激活时，
26 个节点全部可达，激活强度沿链按每跳 0.9 的固定预设系数递减
（证据：E1，outputs/exp1_chain.json）。该观测限定于线性、无分叉的
相邻序列输入；带有上下文分叉的输入不在本组验证范围内。

在"对称双向训练"的受控输入族下（同一相邻符号对各强化 100 次），
以链首 A 与链尾 Z 分别作为种子激活，配合读取端走读工具，
正向与反向都能完整覆盖整条符号链；L0 的检索强度在正反两个方向上
对称相等，未观测到方向性偏置（证据：E4，outputs/exp4_walk_alphabet.json）。
方向不对称（"正走顺、倒走费力"）需要方向计数层 L1 的参与，
属于第 4.3 小节的范围；本小节仅报告 L0 结构层的观测。

### 4.1 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 26 节点 / 25 无向连接，A–Z 无连接 | C01 | exp1_chain.json | D | 线性无分叉序列 |
| 以 A 为种子全链可达，每跳 ×0.9 递减 | C01 | exp1_chain.json | D | 同上 |
| 双向训练下正/反激活对称等强 | C03（E4） | exp4_walk_alphabet.json | D | 仅 L0；方向层不在本小节 |
| 正向/反向走读完整 | C03（E4） | exp4_walk_alphabet.json | D | 走读为读取端工具，非基底 |

---

## 4.2 检索强度与阈值（中文草稿）

在"同一来源节点连接两条不同频率经验"的受控输入族下
（A–B 共现 100 次、A–X 共现 2 次，并附带 A–B–C–D 扩展链），
L0 的检索激活强度随共现次数增大：以 A 为种子激活时，
B 的激活为 0.9，而低频弱连接 X 的激活为 0.018；
高频连接还使信号可沿扩展链继续传播（C=0.81、D=0.729）。
弱连接 X 并未消失于底层输出：它始终存在于全量激活结果中，
只是在读取端阈值较高（0.5）时不进入可见集，阈值降至 0.01 时可见
（证据：E2，outputs/exp2_strength.json）。

在"同一底层激活分布"的受控条件下（单遍相邻对构成的 26 符号链，
以 A 为种子），读取端阈值分别取 0.5、0.1、0.001 时，
可见节点数分别为 7、22、26；三次过滤前后网络状态保持一致，
读取操作未改变任何连接或计数（证据：E3，outputs/exp3_threshold.json）。
阈值属于读取端工具，不属于本系统的联想基底。

在"共现次数相同、最近共现时刻不同"的受控输入族下
（三条连接 N 均为 100，last_seen 分别为 100/1000/2000），
检索强度受预设时间衰减参数 λ 调制：λ=0 时三条连接激活均为 0.9；
λ=0.005 时激活按新旧排序为 D=0.9、C≈0.0061、B≈6.7×10⁻⁵，
更久未见的连接检索激活更低；将 λ 恢复为 0 后三条连接激活立即回到 0.9，
而共现次数 N 与连接本体始终未变
（证据：E10，outputs/exp10_recency_lambda.json）。
λ 为预设参数，时间衰减只作用于检索时的派生强度，不删除记忆。

在"部分输入"的受控输入族下（五连链 A–B–C–D–E，各相邻对 100 次），
以 A、C、E 为种子激活时，缺口节点 B 与 D 均被补出（激活 0.9）；
仅给首尾 A 与 E 时，中间节点 C 被补出（激活 0.81）；
对训练中从未出现的符号 F 执行激活会直接报错，
输出集合始终未超出训练符号
（证据：E9，outputs/exp9_partial_completion.json）。
补全只取回已存在的节点。

在"两条独立路径会合于同一节点"的对照输入族下，
基底现行 max 吸收下双路目标与单路目标的激活相同（均为 0.81），
即会合不产生额外增强；在带"单次发射 + 封顶 1.0"护栏的求和读法中，
双路目标激活为 1.0、单路为 0.81，并在含环结构上保持终止与封顶
（证据：E14，outputs/exp14_multi_path_evidence.json）。
本系统基底传播采用 max 规则；求和读法仅作为读取端候选模式，
其遍历拓扑与底层无向图不同，不作为基底规则。

### 4.2 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 频率调制检索强度（100 vs 2 次 → 0.9 vs 0.018） | C02（E2） | exp2_strength.json | D | 同一来源竞争情景 |
| 弱连接保留在全量输出，仅阈值过滤 | C02（E2/E3） | exp2/exp3 | D | 阈值属读取端 |
| 阈值 0.5/0.1/0.001 → 7/22/26，网络零变化 | C02（E3） | exp3_threshold.json | D | 同一底层分布 |
| 相同 N、不同 last_seen → λ 调制检索 | C11（E10） | exp10_recency_lambda.json | D | λ 为预设参数 |
| λ 归 0 复原、N 不变 | C11（E10） | exp10_recency_lambda.json | D | 衰减只在检索阶段 |
| 部分输入补出缺口；未知符号激活报错 | C10（E9） | exp9_partial_completion.json | D | 只取回已存在节点 |
| max 会合无增益；sum 为读取端候选且拓扑不同 | C15（E14） | exp14_multi_path_evidence.json | D+B | 基底 max 冻结 |

---

## 4.3 方向、顺序与角色（中文草稿）

在"同一词汇按单一方向反复呈现"的受控输入族下（字母序列 H–e–l–l–o，
按到达顺序相邻计数 100 次，含同一节点相邻再现记录），
L1 方向层记录到 H→e、e→l、l→l、l→o 各 100 次，反向计数为 0。
以 H 为种子时正向检索到达词尾 o 的激活为 0.729；
以 o 为种子时反向检索到达 H 的激活约为 7.1×10⁻⁵，
即反向检索显著更弱但并未消失；对照条件下（仅 L0 对称链），
从 o 检索 H 的激活仍为 0.729
（证据：E5，outputs/exp5_direction_hello.json）。
该现象限定于方向经验不对称的序列输入；双向等量训练不在本组产生方向偏置。

在"双前缀消歧"的受控输入族下（事件 [X1,B,C]、[X2,B,D] 各 100 次），
L2 上下文层按相邻三元组计数：查询前缀 [X1,B] 时唯一候选为 C，
查询 [X2,B] 时唯一候选为 D；仅依据当前符号 B 的 L1 检索中，
C 与 D 的激活相等（均为 0.9），无法区分后续走向。
该能力限定于双前缀窗口：当区分信息位于第 3 个前缀位置以外时
（如 [X,A,B,C] 与 [Y,A,B,D] 中 X/Y 的位置），查询 [A,B]
仍得到 C 与 D 等强（证据：E15，outputs/exp15_higher_order_context.json）。

在"同一组符号按两种语序出现"的受控输入族下
（[狗,咬,猫] 与 [猫,咬,狗] 各 100 次），L1 记录到
狗→咬、咬→猫、猫→咬、咬→狗 四个方向各 100 次；
以狗为种子可检索出 咬→猫，以猫为种子可检索出 咬→狗；
仅以中位符号"咬"为种子时，狗与猫的激活相等（各 0.9）。
引入输入侧格标记后（事件 [狗,把,猫,咬] 与 [猫,把,狗,咬] 各 100 次，
"把"标记来自输入侧），查询 [狗,把] 唯一指向猫，
查询 [把,猫] 唯一指向咬（证据：E16，outputs/exp16_role_markers.json）。
该观测限定于语序或显式格标记提供角色信息的输入；标记系统本身
由输入侧提供，不在本系统基底内生成。

### 4.3 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 正走深、倒走浅；L0 对照仍对称 | C04（E5） | exp5_direction_hello.json | D+A | 方向经验不对称输入 |
| 双前缀唯一消歧；L1 只看当前符号等强 | C16（E15） | exp15_higher_order_context.json | D+B | 双前缀窗口；3 前缀外超出（B01） |
| 角色靠语序/格标记区分；中位词诚实歧义 | C17（E16） | exp16_role_markers.json | D+A | 标记由输入侧提供 |

---

## 4.4 语义、上下文与拓扑（中文草稿）

在"两套竞争延续各 100 次、无上下文标记"的受控输入族下
（事件 [A,B,C] 与 [A,B,D]），以 A、B 为种子时
C 与 D 的激活相等，系统未表现出偏向任一延续的选择
（证据：E6a，outputs/exp6_fork_context.json）。
在"加入情景标记或改变频率"的对照输入族下，行为出现可测差异：
频率 100:10 时 C:D 激活比约为 10:1；
分别带情景 X1/X2 的整窗共现输入下，X1 情景使 C 的激活
为 D 的约 2 倍（0.9 vs 0.45），X2 情景反向成立，
读取端阈值 0.7 时只放出对应延续。
该对照限定于情景作为共现结构进入输入的方案。

在"两串表面符号共享同一情景"的受控输入族下
（[S,P,Q,R] 与 [S,X,Y,Z] 各 100 次），以 P 为种子时，
经共享情景 S 可检索到另一表面 X（激活 0.81），反向同样成立；
移除共享情景后（两组事件互不共现），P 的检索结果中不出现 X
（证据：E6b，outputs/exp6b_translation_synonym.json）。

在"多条句子共享符号节点"的受控输入族下
（狗咬猫、狗追老鼠、猫追老鼠 各 100 次，相邻对呈现），
网络形成共享枢纽结构：追 与 狗 的度数分别为 3 与 2，
共用的 追–老鼠 连接计数为 200；以狗为种子时
咬与追的激活相等（0.9），猫与老鼠的激活相等（0.81），
即两个候选延续并行亮起而未选出其一；以老鼠为种子时，
狗与猫的激活相等（0.405），主语无法由结尾符号唯一确定。
在读取端走读下，可从狗出发跨枢纽组合出未直接学过的符号排列
"狗咬猫追老鼠"，但该排列中 咬–老鼠 从未共现（计数为 0），
且全程未出现训练集合之外的新符号
（证据：E8，outputs/exp8_hub_graph.json）。

在"共享枢纽拥挤度递增"的受控输入族下（同一符号"狗"分别挂载
1、2、5 个句子，各相邻对 50 次），以"狗"为种子时
并行候选数等于已学句子数（1/2/5），各候选激活均为 0.9；
以独特动词（如"抓"）为种子且读取阈值取 0.85 时，
仅本句节点可见（抓、狗、鸟）；为每句引入唯一情景标记 X_i 后，
从 X_i 激活且阈值取 0.85 时，仅本句内容可见；
拥挤过程未改变任何连接计数（N 仍为 50）
（证据：E11，outputs/exp11_interference.json）。

### 4.4 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 无上下文时竞争延续等强（诚实歧义） | C05（E6a） | exp6_fork_context.json | D | 无上下文输入族 |
| 频率/情景作为共现结构可偏置分支 | C06（E6a） | exp6_fork_context.json | D | 情景须进入输入 |
| 共享情景实现两表面互译；无 S 则分离 | C07（E6b） | exp6b_translation_synonym.json | D+A | 表面不同、共享情景 |
| 共享枢纽并行候选；组合可达但无直连 | C09（E8） | exp8_hub_graph.json | D | 无新符号（闭包） |
| 拥挤度 1/2/5；独特词/情景可分离单句 | C12（E11） | exp11_interference.json | D | 连接计数不变 |

---

## 4.5 时间、事件与重复（中文草稿）

在"一次事件按时间轴呈现为高密度脉冲流"的受控输入族下
（先有 100 天的正常跑步经验，随后在一次摔倒中注入约 400 个
带递增时间戳的脉冲：失衡→失衡加重→离地→撞击→疼痛反馈→站起），
摔倒前以"快"为种子时输出中不存在"摔/疼"路径；
该次事件后，"疼"节点获得 5 个连接（摔、地面、快、加速、站起）；
以"摔"为种子时"疼"的激活为 0.9，以"空中"为种子时
"疼"的激活为 0.81（第二跳），以"快"为种子时"疼"的激活为 0.36。
对照单次低密度事件（轻碰一次）仅产生 1 条连接。
各连接的 last_seen 记录保留时间信息（日常边为 100.0，
摔倒边约为 101.29），L1 记录了 快→不稳→失衡→空中→摔→疼
的顺向转移计数（证据：E7，outputs/exp7_dense_episode.json）。
该观测限定于脉冲时间轴由输入侧构造的方案；实验未引入任何
"情绪/重要性"乘数。

在"同一符号在窗口内相邻再现"的受控输入族下
（[汪,汪] 事件 100 次），L1 记录到有序自转移 n(汪→汪)=100；
若两次出现分属两个独立事件（t=1 与 t=100），该计数为 0；
单个事件 [汪,汪,汪] 产生计数 2。以汪为种子激活时输出仅含 {汪:1.0}，
没有自动生成任何表示"强调"的新节点
（证据：E13，outputs/exp13_repeat_marker.json）。
该计数仅标记"同一节点相邻再现"这一时间性事实，不携带语义。

### 4.5 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 脉冲密度产生强连接/枢纽/第二跳预警 | C08（E7） | exp7_dense_episode.json | D | 时间轴由输入侧构造 |
| 低密度对照仅 1 条边 | C08（E7） | exp7_dense_episode.json | D | 无情绪乘数 |
| 有序自转移表示重复、无新语义节点 | C14（E13） | exp13_repeat_marker.json | D | 纯时间性标记 |

---

## 4.6 组块、拼写与听回（中文草稿）

在"带边界符号序列重复出现"的受控输入族下
（单元 [H,e,l,l,o] 出现 100 次），L4 组块层在第 5 次出现时
创建块 CH_1，其顺序记录 spell(CH_1) 为 [H,e,l,l,o]，
即保留双写位置；以"课堂"为语境（与块共现 50 次）激活时，
块作为整体以 0.9 被唤起。
对照组"平面链"（仅逐字母相邻对 H–e、e–l、l–o）无法还原
双写位置，正向走读只能得到 4 个符号
（证据：E12，outputs/exp12_chunk_nesting.json）。
同一块还可作为原子参与两条不同句子序列（"我 喜欢 红苹果"与
"他 买了 红苹果"），其块级度数为 2。

在"块记录 + 外部动作模块"的系统级受控输入族下，动作/书写模块
（外部 stub）读取 L4 块记录并逐字母渲染出 H–e–l–l–o 对应的
字符串 "Hello"；该输出被"听回"为 100 次新经验后，
L1 记录到 H→e、e→l、l→l、l→o 各 100 次，
即双写重复标记由系统自身输出补齐；对不存在的块执行读取时
直接报错（证据：E18，outputs/exp18_spelling_output.json）。
此处功劳记账为：块记录与听回学习属于基底（L4/L1），
逐字母渲染属于外部动作 stub；最终字符串输出为系统整体结果，
不属于双原语基底的独立能力。

### 4.6 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 重复单元成块并保留双写位置 | C13（E12） | exp12_chunk_nesting.json | D+A | 带边界单元输入 |
| 平面链不能还原双写 | B04（E12 对照） | exp12_chunk_nesting.json | B | 双写需块/位置记录 |
| 块作原子参与多句 | C13（E12） | exp12_chunk_nesting.json | D | 块级序列输入 |
| 外部 stub 渲染 + 听回补齐 l→l | C13（E18 部分） | exp18_spelling_output.json | D | **外部 stub；功劳按 BOUNDARY 记账** |

---

## 4.7 系统级：外部 stub 参与的语言与价值场景（中文草稿）

在"叠词外推"的系统级受控输入族下，语言规则模块（外部 stub）
读取基底提供的三类证据：规则标记 R 与已教词的绑定（天/夜 各 100 次）、
特征类 F 下的候选集合（天/夜/人）、以及重复标记 n(词→词)。
外推前 n(人→人)=0；该外部 stub 依据"规则已学 + 有资格 + 未叠过"输出"人人"，
对无资格的功能词"的"拒绝输出；该输出被"听回"一次后，
基底记录到 n(人→人)=1
（证据：E17，outputs/exp17_system_reduplication.json）。
此处功劳记账为：重复标记、特征绑定与听回学习属于基底；
"两次出现表示强调"的规则判定与字符串渲染属于外部语言 stub；
最终"人人"为系统整体结果，不是基底单独能力。

在"价值反馈"的系统级受控输入族下（蛇–剧疼–剧 与 棍–轻疼–轻
各 100 次，剧/轻为输入侧强度标记），基底在共现次数相同、
仅标记不同的条件下，对两路疼痛节点的检索激活相等（均为 0.9），
即基底本身未区分轻重；外部情绪模块（stub）按预设增益表读取激活后，
fear(蛇)=0.9×3=2.7、fear(棍)=0.9×1=0.9，躲避决策仅对蛇触发。
对照"脉冲密度"路径（300 次 vs 60 次并含公共竞争邻居）下，
基底自身检索激活为 0.9 vs 0.54
（证据：E19，outputs/exp19_emotion_modulation.json）。
此处功劳记账为：标记注入属输入侧、绑定属基底、fear 计算与决策
属外部情绪 stub；基底未采用写侧调制，强反馈经由
"输入密度"或"输入标记 + 外部读取增益"两条路径生效。

### 4.7 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 外推属外部语言 stub；基底只给证据；听回成新记忆 | C18（E17） | exp17_system_reduplication.json | D | **外部 stub；闭包保持** |
| 密度相同时基底不分轻重 | C19（E19） | exp19_emotion_modulation.json | D | 标记来自输入侧 |
| 情绪差异来自外部读取端增益 | C19（E19） | exp19_emotion_modulation.json | D | **外部情绪 stub** |
| 密度路径基底自身可分 | C19（E19，E7 复核） | exp19_emotion_modulation.json | D | 无写侧调制 |

---

## 4.8 证据综合：消融、规模与统计（中文草稿）

在"逐机制消融"的对照方案下，对 8 类现象分别记录
机制开启/关闭与基线条件下的指标：方向不对称在 L1 开启时
正/倒激活比值为 10201，关闭（仅 L0）时为 1，随机图出现
高比值的比例为 0；翻译绑定开启时命中为 1、移除共享情景后为 0，
标签置换下命中率仍为 1.0；缺口补全开启时命中为 1，
纯计数（无扩散）为 0；双写还原开启时为 1、平面链为 0；
角色区分开启时正确-备选分数为 1、仅中位词时为 0、
随机结构下正确率约 0.33（机会水平）；叠词与情绪场景中
移除对应外部 stub 后行为分别退化为无产出与零差异
（证据：E20，outputs/exp20_ablation.json）。

在"规模一致性"方案下，方向、翻译、枢纽、补全、双写、角色
六类现象分别在共享网络上累加 1、10、100 个随机标签实例，
通过率均为 1.0，未观测到规模导致的退化
（证据：E21，outputs/exp21_scale.json）。

在"随机输入族抽样"方案下（每现象 60 个随机输入族，
随机标签与随机重复次数），六类现象正样本成功率均为 1.0，
Wilson 95% 置信区间下限为 0.94；零效应负样本成功率
为 0–0.017；二分机会对照（角色无前缀信息）成功率为 0.567，
置信区间覆盖 0.5
（证据：E22，outputs/exp22_stats.json）。

以上三组证据为系统内部观测的稳定性描述：消融说明机制在则现象在、
移除则退化；规模方案说明小到中规模无退化；抽样方案提供成功率
与置信区间。它们不构成对任何生物学系统的结论。

### 4.8 核对表

| 句子要点 | 登记条目 | 数据源 | 等级 | 边界 |
| --- | --- | --- | --- | --- |
| 八现象消融 ON/OFF/基线 | C20（E20） | exp20_ablation.json | A | 受控 toy 输入族 |
| 六现象 1/10/100 实例无退化 | C21（E21） | exp21_scale.json | S | 共享网络、随机标签 |
| 正样本率 1.0（CI 下限 0.94）；负样本近零/机会 | C22（E22） | exp22_stats.json | S | M=60，Wilson 95% CI |
