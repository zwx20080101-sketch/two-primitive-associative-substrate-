# L0 规格：架构约束与实现约定

日期：2026-09-15　状态：**规格文档（specification）**，非实验。
2026-09-15：§2 追加约定 ⑥（到达值算式归为实现约定）；附带条件 = 保留 max 吸收。拍板人：用户。
触发原因：E33–E36 连续三轮撞到同一堵墙——**撞的不是架构约束，是实现约定**。
本文件把"哪些不能改、哪些可改、改了怎么记"集中写清楚，避免下次再翻对话记录。

---

## 0. 一句话

```text
L0 的【架构】只有两条（存储 + 激活），永不改变；
L0 的【实现】有一批约定（如种子抹平值），可以改，但必须：新建平行文件 + 写影响评估 + 更新本表。
```

---

## 1. 核心架构（不可变）

L0 只做两件事：

```text
存储：共现绑定 —— 一次事件里出现过的无序对，N 永久累计（+ last_seen 时间戳），永不衰减/删除；
激活：沿连接的扩散 —— 从种子出发，best-path（max 吸收），返回全量激活分布（弱连接不丢）。
```

**任何对 L0 的扩展都不得改变这两条**。判断标准：如果某个改动让 L0 开始"判断对错""选择候选"
"生成新节点""写回记忆"，那就是**越界**，不属于 L0 的架构允许范围（无论它多有用）。

---

## 2. 实现约定 —— v1（`synapse_net.py`，冻结）

| # | 约定 | 位置 | 行为 | 性质 |
| --- | --- | --- | --- | --- |
| ① | **种子值抹平** | `:99` | `acts = {s: 1.0 for s in seeds}` —— 所有种子一律置 1.0 | 实现约定（可改） |
| ② | 种子输入格式 | `:91` | 接受 `str` 或 `list[str]` | 实现约定（可扩展） |
| ③ | 种子共享同一次 BFS | `:100-101` | 多个种子"同时激活"，共享一次遍历 | 实现约定（可改，但会改语义） |
| ④ | **种子是不动点** | `:109` | 推论自 ①：值恒 1.0，而 arrival ≤ 1.0×strength×hop_decay < 1.0 → `>` 判据永不抬高种子 | 构造性推论 |
| ⑤ | `dict` 被静默接受 | `:91` | `activate({"A":1})` → dict 被当成 iterable，只取 keys（不报错） | 实现约定（**B21 补充实例**） |
| ⑥ | **到达值算式** | `v1 :108 / v2 :58` | `arrival = au * strength * hop_decay` | 实现约定（可改，但须保留 max 吸收本身） |

**附带条件（硬线）**：任何对到达值算式的改动，**必须保留 max 吸收本身**（`v1 :109 / v2 :59` 的
`if arrival > acts.get(v, -math.inf) + EPS`）。把"取最大"改成"取最小 / 取和 / 取其他"，即越界，
不属于实现约定可改范围。

**依据**（供未来复核）：① 与"种子抹平值 / 输入格式 / 共享 BFS"同层级（描述"具体怎么算"）；
② 文档从未将其列入任何架构或实现表，属**规格空缺的补全**而非改动；
③ 改动不触及 §1 的"存储 + 扩散 + max 吸收"三条架构描述本身。

```text
v1 校验值：sha256(synapse_net.py) = cf99a07a0e60fd4d（前 16 位）
v1 适用范围：E0–E36 全部实验；tag v0.9.15-e36 即其精确快照
```

---

## 3. 实现约定 —— v2（`synapse_net_v2.py`，**已实现**，从 E37 起）

```text
唯一差异：种子入口支持【带值 mapping】，且种子值【常量】（不可变）。
v2 校验值：sha256(synapse_net_v2.py) = ee6df589eb2f6983（前 16 位，E37 首记录）
实现形态：【子类覆盖】——只重写 activate，learn / strength / state_snapshot 全部继承 v1，
          因此不存在"第二份 learn/strength"（B17 那类漂移风险的来源）。
```

| 约定 | v2 行为 |
| --- | --- |
| 种子值保留 | `activate({"A":1.0,"B":0.9,"C":0.81})` → 三者分别以各自的值作为起点 |
| **种子值不可变（常量）** | 传播**不抬高、也不降低**种子值；种子永远是它被传入的那个值 |
| 名字列表向后兼容 | `activate(["A","B"])` 仍全部 1.0 —— **与 v1 完全一致** |
| `dict` 处理 | 显式支持 mapping（不再是"静默当成 keys"，B21 那条在 v2 下不成立） |
| 传播公式 | 不变（`arrival = a(u)·s(u→v)·hop_decay`，max 吸收，best-path） |
| 存储 | 不变（`learn` 一字不改） |

**为什么选"常量"而不是"下界/浮动"**：

```text
常量：A=0.5，路径给 0.6 → A 仍是 0.5；路径给 0.4 → 仍是 0.5   ← 【已选】
下界：A=0.5，路径给 0.6 → A 变 0.6（只可抬高）              ← 产生"既非种子值也非传播值"的混合体
浮动：种子退化成"普通节点的初始值"，失去"外部注入"的特殊地位
选常量的三条理由：① 归因最干净；② 与 v1 结构对称（v1 也是"不可变"）；③ 下界语义无定义。
```

---

## 4. 版本关系与不混用纪律

```text
① v1 冻结：sha256 cf99a07a0e60fd4d 不变，E0–E36 全部有效；
② v2 是【平行分支】（新文件），从 E37 起；
③ 【不混用】：一个实验要么在 v1 上跑、要么在 v2 上跑，不许同一实验里两者并用；
④ 每个实验的 JSON 记录【所用版本 + 对应文件哈希】：
     synapse_net.py    = cf99a07a0e60fd4d   （v1）
     synapse_net_v2.py = ee6df589eb2f6983   （v2，E37 首记录）
   这样 E37+ 的只读检查口径与 E27–E36 一致。
```

**为什么不动 v1 而是新建平行文件（关键理由）**：

```text
E27–E36 的 JSON 里记录了 v1 的文件哈希，只读检查靠它自动复核。
【只要改动 v1 文件，这批记录就无法自动复核】——这与"语义是否向后兼容"无关：
即使语义一字不变，哈希也会变，证据链从"可自动复核"退化成"需要人工解释"。
平行文件只多一个文件，却把 36 个实验的证据链完整保留。
```

---

## 5. 修改原则（每次想改 L0 时按此执行）

```text
第 1 步：判性质 —— 这是【架构约束】还是【实现约定】？
        架构约束 → 不改（改了它就不是 L0 了）；
        实现约定 → 可改，进第 2 步。
第 2 步：新建平行文件（synapse_net_v2.py / v3 …），不动 v1。
第 3 步：写影响评估（DESIGN-<E>-IMPACT.md）：
        · 改动面（精确到行）
        · 哪些实验受影响、程度如何（判据是否还成立 / 只是记录表变 / 结论依赖）
        · 新增语义决定（必须预注册）
第 4 步：更新本文件的版本表（§2/§3/§4）。
第 5 步：实验在新的平行文件上跑，JSON 记录所用版本哈希。
```

**依据**：E33–E36 三连撞墙（撞的都是 §2 的 ①，不是 §1 的架构）；
E37 影响评估是第一次完整走完这五步的案例。
