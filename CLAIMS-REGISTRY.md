# 结论-证据登记表（CLAIMS-REGISTRY）

用途：论文写作的唯一结论来源。每条结论三件可追溯：
登记编号 → 实验 → 数据 JSON。表述已按"证据允许的程度"保守化。
证据等级：D=确定性演示；A=消融对照；S=抽样统计；B=结构边界/负结果。

| ID | 保守表述（范围限定在"所考察的受控输入族内"） | 实验 | 数据 | 等级 |
| --- | --- | --- | --- | --- |
| C01 | 仅相邻共现事件可自组织成链结构，无预置知识 | E1 | exp1_chain.json | D |
| C02 | 共现频率调制检索强度；弱连接保留在全量输出，阈值决定可访问性 | E2/E3 | exp2/exp3 | D+A |
| C03 | L0 无向连接导致正/倒检索等强；方向不对称需方向计数层 | E4/E5 | exp4/exp5 | D+A |
| C04 | 顺序不对称可来自方向经验统计（正走深、倒走浅） | E5/E20A/E22 | exp5/exp20/exp22 | D+A+S |
| C05 | 无上下文时并存分支保持等强（诚实歧义） | E6a | exp6_fork | D |
| C06 | 上下文/情景作为共现结构可偏置分支 | E6a/E20B | exp6_fork/exp20 | D+A |
| C07 | 两个表面可经共享情景发生绑定（互译）；无共享情景则分离 | E6b/E20C | exp6b/exp20 | D+A |
| C08 | 密集时间脉冲可产生强连接、枢纽与提前检索（预警） | E7 | exp7 | D |
| C09 | 连接为图结构：共享枢纽给出并行候选；组合可达但无直连≠已学 | E8 | exp8 | D |
| C10 | 部分输入补全只取回已存在节点；未知节点无法激活（闭包） | E9/E20D | exp9/exp20 | D+A |
| C11 | 时间衰减只作用于检索强度；N 与连接永久存在 | E10 | exp10 | D |
| C12 | 干扰集中于共享枢纽；独特标记/情景可分离单句 | E11 | exp11 | D |
| C13 | 重复出现的带边界单元可成块并保留顺序记录（双写还原） | E12/E18/E20E | exp12/exp18/exp20 | D+A |
| C14 | 同一节点相邻再现可由有序自转移表示，且不带语义 | E13 | exp13 | D |
| C15 | 传播吸收取 max（best-path）；多路求和仅为读取端候选且拓扑不同 | E14 | exp14 | D+B |
| C16 | 双前缀三元组可消歧；3 前缀以上超出该表示 | E15 | exp15 | D+B |
| C17 | 角色可由输入语序/格标记绑定区分；无需将树结构下沉到基底 | E16/E20F/E22 | exp16/exp20/exp22 | D+A+S |
| C18 | 叠词外推由外部语言规则完成；基底只提供标记与特征证据；输出听回成新记忆 | E17 | exp17 | D |
| C19 | 强反馈经"输入密度"或"输入标记+外部读取增益"生效；基底无需写侧调制 | E19/E20H | exp19/exp20 | D+A |
| C20 | 消融显示：机制在则现象在、机制移除则退化；与具体 token 无关 | E20 | exp20_ablation.json | A |
| C21 | 六关键现象在 1×/10×/100× 实例规模无退化 | E21 | exp21_scale.json | S |
| C22 | 随机输入族下六现象成功率 1.0（Wilson CI 下限 0.94）；负样本近零/机会水平 | E22 | exp22_stats.json | S |
| — | **—— 第二轮起点：E23–E26（组合律 + S1/S2/S3）；上为第一轮 E0–E22（单层能力目录）——** | — | — | — |
| C23 | 多层（L1 方向 / L2 三元组 / L4 组块）同时开启时，各层标志性现象可共存且可分别归因；L1/L2 不写回 L0，L4 新增仅限声明的 CH 节点与边 | E23 | exp23_composition.json | D+A |
| C24 | 在植入模块语料 + 规定预处理下，L0 共现统计中的模块可被谱聚类读出恢复（ARI=1.000，3/3 模块，稳定性 1.000）；链呈连续段；分叉分支分离=1.00；随机对照富集度 1.14 | E24 | exp24_module_differentiation.json | D+A |
| C25 | 在弱耦合（ρ=0.0019）与预注册动力学下，每个模块各有两个稳定态（盆地 0.46–0.54；两态 \|V\|≈0.80–0.96），联合二值模式数 = 2³ = 8；C1 滞回宽度 0.150≥10%（积分步数×5 稳健）；单模块对照无第二稳定态，其 0.012 = 1 个扫描步长的离散化伪影 | E25 | exp25_module_dynamics.json | D+A |
| C26 | 半稠密跨模块耦合（密度 0.51、w=65）在模块宏观层产生可测整合：EI_int 随耦合量**逐种子严格单调**（10/10，均值 0 → 0.0118 → 0.0560 → 0.1021 → 0.1474）；零耦合对照与跨模块边消融均**精确归零**（−1.3e−15）；EI_int 随经验量单调上升（0→0.1462） | E26 | exp26_module_coordination.json | D+A |
| — | **—— 边界量化：E27（B01 正式化）、E28（B02+B03 正式化）；不属于两轮能力目录，是边界实验 ——** | — | — | — |
| C27 | 在"中间 token 共享、只有最远 token 不同"的受控语料族下，现有层（L0/L1/L2）的最大可用消歧前缀长度 = 2；当判别上下文到决策点的距离 k* ≥ 3 **且两场景暴露均衡**时决定度恒为 0.50；最小可消歧前缀长度 k_min 精确等于 k*（k*∈{1,2,3,4,5} 验证）；对照行（无判别 token）所有窗口恒 0.50。阳性对照：k*=2 时 L2 决定度 = 1.00。**边界**：0.50 不是绝对量而是**均衡暴露下的值**（辅助记录：暴露比 1:2 → 0.333，1:5 → 0.167，该辅助段不参与判据） | E27 | exp27_context_window.json | D |
| C28 | max 与 sum 是读取端的**两种选择，不是对错**。(a) max 精确定律：`max(a,a)=a`，容差 1e-12，任意路径数下严格成立（T1/T2/T3 的 a(C) 全为 0.8100，极差 0.000e+00）；(b) sum 是另一种选择：单次发射 + 封顶 1.0，封顶损失随路径数单调增（原始和：单路 0.81→损失 0.00、双路 1.62→0.62、三路 2.43→1.43）；(c) max 顺序无关（重标签同构后逐点差 0），sum 顺序依赖（字典序为默认遍历口径） | E28 | exp28_readout_modes.json | D+A |
| C29 | 在 14 节点、4 子模块（3/3/4/4，质量 270/300/440/480 两两不同）、s2=40 的受控嵌套语料上，同一份 L0 统计支持**多尺度读出**：k=2 → 2 个超模块（ARI=1.000）、k=4 → 4 个子模块（ARI=1.000）、k=3 混合（0.738/0.640）；消融跨子模块边后 k=2 超模块塌到 0.286 而 k=4 子模块仍 1.000。**层级 = 读出尺度 k 的函数**，k 是预设参数（与 λ/hop_decay/K 同级），不是涌现的结构 | E29 | exp29_hierarchy_scales.json | D+A |
| C30 | 闭包是**设计不变量**：在受控语料上逐层审计**五个已学集合**（L0 节点 / L1 节点 / L2 前缀 token 集 / L4① 块节点 / L4② 块成员），**违例数 = 0** —— 所有输出集合 ⊆ 该层已学集合，基底不产生"闭包外"的节点。"不存在"的表现层间不统一（见 B21），但都不越界 | E30 | exp30_closure_audit.json | D |
| C31 | **自动尺度选择（弱版本）**：在三个植入真值互不相同（**k=4 / 3 / 5**）的受控语料上，**BIC 准则给出的 k̂ 与主真值精确相等（3/3）**，且是**压倒性**极小值（C1 的 k=4 = −4126，其余 −389 ~ −460；C2 的 k=3 = −2697；C3 的 k=5 = −5422）；对照：eigengap 仅在单层语料 C2 上命中（C1/C3 均落在粗层 k=2），modularity 同样仅在 C2 命中（C3 落粗层、C1 给出 3）——**嵌套语料上偏向粗层，见 B23**。**范围限定**：这是**已知真值语料上的命中检验**，不是自动层级发现；无真值语料上的稳定性不在范围内（B20 仍有效） | E31 | exp31_auto_scale.json | D |
| C33 | 从 E25 的模块级能量景观**条件采样**（种子模块钳制 +1）时，4 个主导联合状态中 **max 传播只给出 1 个**；另外 3 个在 β=2.0 时合计概率 **0.751**，且扫描的每个 β（0.5–8.0）上均 ≥ **0.7503**（判据 ≥0.5）。**结构关系**：节点级有效支撑（p>1e-3）从 **86（β=0.5）单调降至 4（β≥1.5）**，与模块级的 4 个自由度一致——**节点级与模块级是粗粒化关系**。**范围限定**：① 已知种子下的条件采样（2²=4 状态），非无条件"新状态发现"（E25 无条件枚举为 2³=8）；② 这 4 个状态在 E25（C25）中已被枚举为存在，E33 的新内容是【可采样 + 质量分配】；③ 不声称"能想象新状态"、"等于生成能力" | E33 | exp33_landscape_sampling.json | D |
| C34 | 在**零残留**的联想基底上（同种子两次 activate 逐位差 0.0、中间插入其他 activate 亦差 0.0、内部状态逐字节不变），**外部 W 层**可保存上一次 activate 的输出快照（top-3 = {A:1.0, B:0.9, C:0.81}）并在下一次 activate 时作为额外种子参与扩散：`activate([Z,A,B,C])` 使 A 从 **0.071790（0.9²⁵）跳到 1.0**，差异 **0.928**；消融（W 存在但 seeds() 不调用）→ A 回到 0.071790（与无 W 相同）。W 不写 L0（26 节点/25 边签名逐位不变）、层文件 sha256 前后一致。**定性**：W 是**实验侧读取端工具**（同 E14 的 sum 模式），不是 L5/S4、不是基底新能力，功劳归读取端 | E34 | exp34_working_memory.json | D |
| C35 | **W 栈的多步串联是"载体"行为，不是"状态整合"**：(a) 只在**每步重注入**时维持——停止重注入后痕迹立即退化为**纯图衰减 0.9¹³ = 0.254187**（与"从未注入过"完全相同；实测偏差 1.7e−10），W 不留持续影响；(b) 痕迹之间**无优先级、无时间顺序**（被携带成员的值全部 1.0）；(c) 成员由 **tie-break** 决定，新痕迹可能挤不进去（每步换种子 A/B/C/D/E 时 W 恒为 {A,B,C}，D/E 均未进入）。**边界登记**：要区分"哪个痕迹来自哪一步"，至少需要 ① 顺序标签（写入 W 时记时间戳）或 ② 值域编码（保留种子原始激活值）——两者都不在 E35 范围内（E36 的入口） | E35 | exp35_working_memory_chain.json | D |
| C36 | **顺序标记的适用边界：分类 ≠ 排序**。外部 W 层的顺序标记**只作用于读取端过滤**（哪些痕迹被包含在种子集里），不影响基底内部——基底种子入口把值抹平（E34 已证），因此不产生优先级排序。实测：`seeds(step=1)` 只返回步1 的 [A,B,C]，注入后 **A = 1.0**（与不注入对照差 **0.928**）；但注入后 A/B/C/Z 在基底里**全部是 1.0**（无相对权重差异）；**排除的效果**可测——步2 独有节点 **X** 在有过滤时 = **0.81**（0.9²）、无过滤时 = **1.0**（差 0.19）。**范围限定**：分类 ≠ 排序；外部工具 ≠ 基底能力；排序差异需要**值域编码**（改基底入口行为，越权嫌疑），不在本实验 | E36 | exp36_sequence_tags.json | D |

## 边界与负结果（必须写入论文）

| ID | 边界/负结果表述 | 实验 | 等级 |
| --- | --- | --- | --- |
| B01 | **（升级，E27 正式化）**现有层最大前缀 = 2；k* ≥ 3 的上下文**超出当前层集合能力**；k_min = k* 给出了消歧所需窗口的**下界**（加新层的可行性不在 E27 范围，未经实测）。原始观测：L1 大二元无法让 3 前缀外的上下文到达分叉点 | E6a-S4/E15-S3 + E27 | B |
| B02 | **（指针，已并入 C28）**max 的精确不叠加是**读取端设计选择，不是机制缺陷**；原始观测「多路证据在 max 下不叠加（双端会合无增强）」保留在此 | E9/E14 → E28 | B |
| B03 | **（升级）**sum 的遍历顺序影响封顶落点，触发条件是**拓扑判据**：**当拓扑中存在两个都可由种子一跳到达、且彼此相邻的节点**时，封顶落点依赖顺序（T5：升序 [1.0,0.9,1.0] vs 降序 [1.0,1.0,0.9]）；max 仍顺序无关（均 [1.0,0.9,0.9]）。反例对照 T2/T3：B/D/E 为中间节点、C 是唯一终点，封顶必落 C 上，顺序无关。封顶损失随路径数单调增：单路 0.00、双路 0.62、三路 1.43。故 sum 输出不可与 max 直接对比。**另注**：T5 的 max a(C)=0.9（非 0.81），因 A–C 直接相邻存在一跳路径；T1–T4 中 A、C 不相邻故一律 0.81——这是**拓扑差别**，不只是数值差别。exp14 的 JSON detail 抖动是这一性质的直接后果，不是测试缺陷 | E14 → E28 | B |
| B04 | 平面链无法还原双写位置；双写需要块/上层位置记录 | E5/E12/E18 | B |
| B05 | **（指针，已并入 C30）**闭包是设计不变量，不是机制缺陷；原始观测保留：未学过的节点 activate 报 KeyError（L0/L1/L4 硬拒绝、L2 返回空，接口语义见 B21） | E9/E13/E18 → E30 | B |
| B06 | 叠词语义外推属外部语言模块，不在基底能力内 | E17/BOUNDARY | B |
| B07 | 真实感知/运动、完整语法递归、人脑等同性不在本工作结论内 | — | B |
| B08 | 组合律采用"维度先行 + 现象共存"判据；数值式乘积/加权和规则因存在同义反复风险而未采用（记录于 DESIGN-E23） | E23 | B |
| B09 | 预注册的 ICA 读出在共现矩阵上失败（ARI≈0、模块数 1、稳定性 0.165）；原因：零对角破坏线性混合、模块质量简并不可辨识、去均值降维、成分正交使余弦失效——ICA 不适用于该数据形式 | E24 | B |
| B10 | LCA 第二分析（当前配置）不可用：活跃比例 1.0（不稀疏）、未正常收敛、与谱聚类归属一致性≈0 | E24 | B |
| B11 | 稳定性不能作为谱聚类的"无结构"判据（随机图上也稳定）；负对照改用模块富集度 | E24 | B |
| B12 | 非负 L0 权重下无"赢者通吃"互斥切换（C3 探索性：两模块正耦合 → 联合二值模式只有 2（同高同低，合作型锁定）而非 4；赢者通吃=False）；若需互斥切换须另立实验声明负权重来源 | E25 | B |
| B13 | 初值域未预注册会产生假失败：V0~U(0,1) 时高态盆地一边倒；固定为 V0~U(-1,1) 后盆地均衡——属操作化定义问题，非机制问题 | E25 | B |
| B14 | 收敛判据实现缺陷：`attractor_scan` 在积分循环后比较的是同一数组对象，判据恒为真——未归类比例恒为 0（P4 空过），未收敛轨迹被当作吸引子混入统计（伪影：细簇 136、盆地偏高 0.555/0.445）。改为不动点残差判据后细簇=8=2³、盆地 0.46–0.54。属实现缺陷，非机制问题；首轮该批数值已作废 | E25 | B |
| B15 | `EI_int` 主要由跨模块耦合**总量**决定，不区分耦合落在哪些对上：把跨模块权重在跨模块对之间随机重排（保总量）后，观测 0.0609 未超过零分布 p95=0.0637。因此**置换零分布不能当"协调存在"的主判据**（有假阴性风险），主判据改用剂量响应 | E26 | B |
| B16 | 整片**等权稠密**耦合会让"跨模块权重随机重排"退化为**恒等变换**，零分布失效——结构性结论。附带观察：预检构造下 w=91 的 EI_int 非单调（0.0447<0.0609），但**正式语料未复现**（w=91 在 10/10 种子上仍高于 w=65），故饱和区结论不推广 | E26 | B |
| B17 | 精确 EI 的状态索引约定必须与乘积分布表列序一致（倍增法 → node i 对应第 i 位，LSB-first）。写成 MSB-first 会让**块对角机制假阳性报出 EI_int=1.82**（本应精确为 0），且不报错。自查：块对角语料上 EI_int 必须为 0 | E26 | B |
| B18 | TE 的分子必须对非目标位边际化（P(v'_T\|v_T,v_S)），写成整个下一步状态的 P(v'\|v) 会算出**负 TE**（首轮 −0.78）。互信息不可能为负，可作自检 | E26 | B |
| B19 | 聚类/划分的**簇编号是任意的**：直接用 `assign[i]==truth[i]` 数"同划分节点"会得到假的分歧数（本数据 ARI=1.000 却算出 6/15）。必须做多数标签置换匹配后再计数（ARI 不受影响，但逐节点展示会错）。同类"看起来合理的错数"见 B17 | E24/P5 | B |
| B20 | 谱聚类读出**需要外部给定尺度 k**：当前实现没有"从数据自动判断层数"的机制（特征值间隙等自动选择准则不在本工作范围）。层数分辨率还有上限：更弱的第三层耦合（s3=10）在 k≥2 时对读数**完全无影响**（k=2/k=4 均为 1.000） | E29 | B |
| B21 | 失败语义在层间**不统一**：KeyError（L0/L1/L4 未学过）、空字典（L2 未知前缀）、ValueError（L2 参数错误）、None（L4 未达 K）。其中 **L2 未知前缀不报错、返回 `{}`** 是接口风险：调用方不检查空结果就无法区分"没学过"与"学过但无候选"（与"不报错、给出看起来合理的结果"同族）。**静默案例 = 1 处**——预检曾记为 2 处，正式跑更正：L4 的 None 与块 id 可区分，属有信息返回。本实验只记录，不修改报错行为。**补充实例（E36 预检）**：`activate({"A": 1})` **静默接受**——dict 被当成 iterable、只取 keys 作为种子集，不报错；与 L2 的 `{}`、L4 的 `None` 同族（不报错、行为不符直觉）。不新起 B 编号 | E30 / E36 | B |
| B23 | **eigengap 在嵌套语料上系统性偏向粗层**（B20 的机制补充）。**范围限定：只限"嵌套"语料**（粗层覆盖细层），不是"eigengap 总是选错"——单层语料 C2 三个准则都命中。实测：C1（4 子模块/2 超模块）eigengap 给 k=2 而主真值 k=4；C3（5 子模块/2 超模块）eigengap 给 k=2 而主真值 k=5；modularity 同样偏向粗层（C3 给 2、C1 给 3）。**机制（谱结构，可复现）**：近零特征值个数 = 最粗层块数——C1 `[0,0,0.517,0.593,1,…]`→2 个近零=2 个超模块；C3 `[0,0,0.485,0.647,0.690,1,…]`→2 个近零=2 个超模块；C2 `[0,0,0,1,…]`→3 个近零=3 个子模块；**最大间隙落在近零簇的右边界**，故 argmax 指向粗层。**与 B20 的关系**：B20 说"k 必须外部给定"；本条目补充——不是"没有准则"，而是"有准则但在嵌套结构上偏向粗层"，因此**单靠一个准则无法可靠选层**（BIC 在三个语料上命中，eigengap/modularity 不行） | E31 | B |
| B24 | **谱聚类读出的可读性不能由 r_j 完全预测**（r_j = min over groups of 组内质量/组外质量）。(a) **精确反例**：L=4 层2（b_j=6）的 **r_j = 0.796**，而唯一可读的 L=2 层1（b_j=4）**r_j = 0.750**——前者 r 更高却不可读；该点**中位数 ARI = 1.0000 但最小值 = 0.7202**，栽在**稳定性**上（纯中位数意义下 r\* 完全可分：r\* ∈ (0.34, 0.75]）。(b) **机制面**：可读性随**嵌套深度**急剧下降——L=2 的所有层可读；L=3/L=4 只有部分层可读；**唯一稳定可读的是最粗层**（boundary、r=∞、按定义即连通分量）。**与 B23 的关系**：B24 强化 B23 的解释——**如果深层本来就不可读，那准则偏向粗层不是"准则问题"，而是它只能看到能读的层**。**辅助记录**：最粗层的首个显著特征值随深度下降（L=2 0.517 → L=3 0.275 → L=4 0.114，1e-6 阈值当前仍有 5.1 个数量级余量）——趋势提示 L=5/L=6 可能需重定阈值 | E32 v2 | B |
| B26 | **（方法学第六类：抽样覆盖不足）**采样算法在目标分布上不混合时，返回的样本可能**全部相同且不报错**——样本"看起来"是分布的一个合法抽样，但**覆盖为零**。实例：E33 的单点 Gibbs 在 β=8、ρ=0.0019 的弱耦合模块景观上不混合（翻转概率 σ(−2βW_ii)=**4.5e−06**，2 万次尝试期望翻转 **0.09** 次；1000 个样本只覆盖 **1** 个状态，两种初始化结果相同）；β=0.5/2.0 正常（KL<0.01），β=4.0 覆盖 2 个。**机理**：局部场被自耦合主导（对角 0.69–1.0 vs 非对角 0.002）⇒ 条件分布极尖，**即使联合分布近均匀也翻不动**。**自检**：报告**抽样覆盖**（不同状态数 / 有效支撑），不能只报样本统计量 | E33 | B |

> 编号说明：**B25 是 E33 为"P1b 不通过"预注册的条件编号，条件未触发（落 C33），保持未使用**。B26 是下一个实际登记的空号——与 B22 同属"预注册条件编号未触发"，不是漏号。

> 编号说明：**B22 是 E30 为"闭包被软执行"预注册的条件编号，条件未触发（结局 A），保持未使用**。B23 是下一个实际登记的空号——不是漏号。

> **实现期"不报错错数"六类（B17 / B18 / B19 / E29 / E32-v2 / E33，合并总述）**
>
> | 类别 | 实例 | 表现 | 对应自检 |
> | --- | --- | --- | --- |
> | ① **索引约定错位** | B17 | 块对角机制假阳性报出 EI_int = 1.82（本应精确为 0） | **块对角零**：无耦合语料上整合量必须精确为 0 |
> | ② **边际化遗漏** | B18 | TE 算出负值（首轮 −0.78） | **非负性**：信息论量不可能为负 |
> | ③ **置换不变量的局部误用** | B19 | ARI = 1.000 却逐节点显示 6/15 | **ARI ≠ 逐位一致**：比较划分前先做标签置换匹配 |
> | ④ **指标对退化方向不敏感** | E29 | k=5 把子模块切成两块（超分），ARI vs 子模块仍达 0.928 | **对偶指标**：ARI 必须配簇大小分布（人造块检测），单指标不能定尺度 |
> | ⑤ **统计量对随机性不敏感** | E32 v2 | L=4 层2：中位数 ARI = 1.0000（看着完全可读）但最小值 = 0.7202（不稳） | **中位数 + 最小值都要报**：涉及随机初始化（k-means、采样）的量，单看中位数会把不稳定当稳定 |
> | ⑥ **抽样覆盖不足** | E33（B26） | 单点 Gibbs 在 β=8 上不混合：1000 个样本**只覆盖 1 个状态**，且不报错 | **报告抽样覆盖**（不同状态数 / 有效支撑），不能只报样本统计量 |
>
> 六条共同点：**不报错，给出一个看起来合理的数**——比崩溃更危险。
> 其中 ①–③ 是"实现写错"、④ 是"指标选错"、⑤ 是"**同一指标的不同统计量给出相反结论**"、
> ⑥ 是"**采样算法本身没探索到目标分布**"。**⑥ 是第一类"不是代码错、不是指标错"的失效模式**——
> 前五类靠"换指标 / 加自检"可解，⑥ 必须**先验证采样器真的覆盖了目标分布**。
> 合并成一条方法学结论：任何"看起来合理"的量，都必须先过与它匹配的**退化解自检**
> （无耦合、无非负性违反、无标签置换歧义、**对偶指标同向**、**中位数与最小值同向**、**抽样覆盖非零**），再谈机制。
>
> 编号约定：①②③ 各有独立 B 编号（它们是可单独复现、可单独修的实现陷阱）；
> **④ 不占 B 编号**——合并总述本身即方法学结论，第四类只记"实例：E29"。

## 写作纪律

- 正文任何结论句 = "登记表编号 + 范围限定 + artifact 引用"；
- 不允许出现登记表外的全称断言；不允许事后挑判据；
- 发布前逐条复核：结论表述 ≤ 数据支持强度。
- **B 条目只能来自正式跑数**。仅来自实现前预检的观察，必须标注
  "预检观察，未在正式语料复现"，不得当作结论（依据：E26 的 B16 降级）。
- **预注册不写方向倾向**：双向判据一律写"方向待定，双向都算完成"，
  跑完再记录落点（依据：E26 的 P6 方向预期与实测相反）。
- **实现自检两条硬线**：① 块对角/零耦合语料上"整合量"类指标必须精确为 0；
  ② 信息论量不得为负。两条任一不过，先查实现，再谈机制（依据：B17/B18）。
