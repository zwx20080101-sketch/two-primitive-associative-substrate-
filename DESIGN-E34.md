# E34 设计：工作记忆（最小版本）—— 外部 W 层跨次状态携带

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：E1 的 26 字母链语料、E27/E30 的只读口径（层文件 sha256 + L0 签名）。
**无 numpy 依赖；不改任何层文件。**

---

## 0. 前置（E34 预检，2026-09-15；三条已实测）

```text
① activate 天然接受多节点种子：synapse_net.py:84 `activate(self, start: str | list[str], hop_decay=0.9)`，
   seeds 统一置 1.0（:98），共享同一个 BFS（:99-100）；
   L1 order_layer.py:91、L4 chunk_layer.py:102 同构；L2 只有 query()，无 activate。
   ⇒ W 可以是【外部薄层】，不需要动 L0（改接口会触及 L0 冻结）。
② 零残留已实测：同种子两次 activate 逐位差 0.0；中间插入一次 activate(C) 后再 activate(A) 仍差 0.0；
   activate 前后网络内部状态（nodes/connections/_clock）逐字节相同。
   ⇒ 任何"跨次记忆"都只能来自 W 层，可归因干净。
③ 多种子入口会把值抹成 1.0（activate([A,C]) → A=1.0, C=1.0）。
   ⇒ W 的读出必须自己决定传哪几个节点；值信息在入口处丢失。
```

---

## 1. 命题与范围限定

```text
命题：在零残留的联想基底上，一个外部 W 层可以保存上一次 activate 的输出快照，
      并在下一次 activate 时作为额外种子参与扩散，实现【跨次状态携带】。
```

**范围限定（写进结论）**：

```text
W 是【实验侧读取端工具】（同 E14 的 sum 模式），不是 L5、不是 S4，不进 LAYERS、不改 BOUNDARY；
它不写 L0、不影响基底计数。任务输出的差异由【多种子入口的值抹除效应】贡献，
不是"基底长出了记忆"。功劳归读取端，不归基底。
```

---

## 2. W 的规格（写死，跑前不改）

```text
数据结构：{node: value} 字典 + 容量 k
容量   k = 3
写入   hold(acts)：取 acts 中 value 最大的 k 个节点存入
读出   seeds()   ：返回节点名列表（**不传 value** —— 因为多种子入口会把值抹成 1.0）
定位   实验侧读取端工具（同 E14 的 sum 模式）；不是 L5/S4
```

---

## 3. 语料与任务（两步依赖）

```text
语料：E1 的 26 字母链（A–Z，相邻对 ×100；25 条边）

步骤 1：S1 = activate("A")            → W.hold(S1)          ⇒ W = {A:1.0, B:0.9, C:0.81}
步骤 2：S2 = activate(["Z"] + W.seeds()) = activate([Z, A, B, C])

对照（无 W）：S0 = activate("Z")
   S0: Z=1.0, Y=0.9, X=0.81, … , A=0.9^25 = 0.071790（从 Z 走 25 跳）
   S2: Z=1.0, A=1.0, B=1.0, C=1.0（多种子把 A/B/C 置 1.0，从两端同时扩散）
```

---

## 4. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **接口不改**：四个层文件（`synapse_net` / `order_layer` / `context_layer` / `chunk_layer`）sha256 前后一致 | 一致 | 直接 FAIL |
| P1b | **L0 不改**：W 写读前后，L0 的节点数 / 边数 / **边集合签名**（排序逐项含计数）逐位相同 | 相同 | 直接 FAIL |
| P2 | （**数据记录，不作独立判据**）W 持存：3 次 activate 后 W 内容逐位相同——这是 Python 对象的自然属性，不是实验结论 | 仅记录 | — |
| P3 | **功能可见（核心）**：P3a 有 W 时 `A = 1.0`（作为额外种子）；P3b 无 W 时 `A = 0.071790`（从 Z 走 25 跳）；两者差异 ≥ 0.5 | 通过 | 记录负结果 → B27 |
| P4 | **消融**：移除 W → 步骤 2 只能调 `activate("Z")`，A 回到 0.071790（退化到无 W 状态） | 通过 | 记录 |
| P5 | **双向结局**：P3/P4 通过 → **C34**；不通过 → **B27**（文本见 §6） | 两分支都合格 | 不许跑完再挑解释 |
| 辅助 | 完整输出表：S0（无 W）/ S1（步骤1）/ S2（有 W）逐节点值；W 的 hold 前后内容；3 次 activate 后的 W 内容 | 仅记录 | — |

> 说明：**核心判据是 P3 与 P4**。P2 只作数据记录——把"Python 对象能存住东西"当作实验结论
> 是自明的事，不该占判据位。

> **P4 与 P3b 数值相同，但不是重复实验**：P3b 是"**W 对象根本不存在**"，
> P4 是"**W 对象存在、但 `seeds()` 不被调用**"。两者数值相同，恰好证明
> W 的作用来自 **`seeds()` 被调用**，而不是 W 对象创建时的副作用。

---

## 5. 预注册参数

```text
语料：A–Z 相邻对 × 100（25 条边，26 节点）
W：容量 k = 3；写入 = 取 value 最大的 3 个；读出 = 只返回节点名
种子：步骤 1 用 "A"；步骤 2 用 ["Z"] + W.seeds()；消融（P4）用 "Z"
期望值：无 W 时 A = 0.9^25 = 0.071789799（E1 已记录）；有 W 时 A = 1.0（种子）
差异阈值：>= 0.5                       只读：层文件 sha256 + L0 边集合签名
运行环境：PYTHONHASHSEED = 0
```

---

## 6. 双向结局（跑前写死，两分支文本）

```text
分支 C34（P3/P4 通过）：
C34：在零残留的联想基底上，外部 W 层可以保存上一次 activate 的输出快照（top-3），
     并在下一次 activate 时作为额外种子参与扩散（A 从 0.071790 变为 1.0）；
     W 不写 L0、不影响基底计数。
     定性：这是【实验侧读取端工具】，不是基底新能力——功劳归读取端。
     范围限定：两步依赖任务的输出差异由【多种子入口的值抹除效应】贡献，
     不是"基底长出了记忆"。

分支 B27（P3 或 P4 未通过）：
B27：W 层的功能【**数值幅度**】依赖多种子入口的"值抹除效应"（种子一律置 1.0）；
     若无该效应，W 的功能**仍然可见**（传入的节点至少是它被传入的那个值，而不是 0.9^25），
     只是幅度变小。
     这是【工具效果依赖具体实现细节】的边界——W 的可见性幅度不是基底的属性。
```

---

## 7. 产出物

- `experiments/exp34_working_memory.py` + `outputs/exp34_working_memory.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C34 / 负结果 B27）；
- **不改任何层文件**；**不新增层**；W 不进 LAYERS、不改 BOUNDARY。

---

## 8. 边界与不做

- **不把 W 说成"基底的工作记忆"**：它是读取端工具，基底对它一无所知（零残留已实测）；
- **不做 L5/S4 的层化**：若要层化，需另立实验并重画功劳账；
- **不做多步规划**：本实验只做"两步依赖"（步骤 1 写入 → 步骤 2 读出），
  更长的依赖链、前瞻、搜索空间都不在范围内；
- 不声称任何"意识/理解"类结论（B07）。
