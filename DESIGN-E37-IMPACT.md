# E37 影响评估：L0 带值种子分支（**决策文档，不改代码**）

状态：**决策文档，待用户拍板**。日期：2026-09-15。**不写代码、不跑实验、不改任何文件。**
依赖：E34–E36 的结论、E27/E30 的只读纪律（sha256）。

---

## 0. 结论摘要（含对前一轮提法的**三处更正**）

```text
① "抹平只需改一行" —— 实测需改约 6–10 行 + 1 个必须预注册的语义决定（见 §2）。
② "E33 依赖值抹平" —— 【不依赖】。E33 的判据用"clamp 固定 ±1 状态"与"二值化阈值"，
   与多种子入口无关（它只用单种子调用）。受影响的是 E34/E35/E36（见 §3）。
③ 真正会被"改实现"破坏的不是语义，而是【只读证据链】：E27–E36 的 JSON 里记着
   synapse_net.py = cf99a07a0e60fd4d，改文件即让这些记录无法自动复核（见 §4）。
```

**一句话**：这不是"架构能不能变"的问题（架构 = 存储 + 激活，不动），
是"**改一个实现约定，要不要连带处理已记录的证据链**"的问题。

---

## 1. 架构约束 vs 实现约定（逐条区分）

### 1.1 架构约束（不可变）

```text
存储：共现绑定（N 永久累计 + last_seen）
激活：沿连接的扩散（best-path，max 吸收）
```

**"种子值必须被抹平"不在这两条里。** 它是实现约定。

### 1.2 实现约定（可变，但需评估代价）

代码位置：`synapse_net.py` 的 `activate`（:84–:113）。**行号已复核**：

| # | 约定 | 行号 | 性质 |
| --- | --- | --- | --- |
| ① | 种子值一律置 1.0（**抹平**） | **:99**（`acts = {s: 1.0 for s in seeds}`；**前一轮引用的 :98 是 `now = self._clock`，偏了一行**） | 可改 |
| ② | 多种子共享同一次 BFS（"同时激活"语义） | :100–101 | 可改，但会改变语义 |
| ③ | 种子接受 `str` 或 `list[str]`；**`dict` 被静默当成 iterable 只取 keys** | :91 | 可改（B21 补充实例） |
| ④ | 种子是**不动点**：因值恒为 1.0，而任何 arrival ≤ 1.0 × strength × hop_decay < 1.0，:109 的 `>` 判据永远不会抬高种子 | :109（推论自 ①） | **v2 必须重新决定** |

④ 是 ① 的推论，也是 v2 的**核心新语义**：若种子带值（如 A=0.5），
传播路径（如 0.6）就会把种子**抬高**。v1 下不可能发生，v2 下必然发生。

---

## 2. L0-v2 的最小改动面（精确）

```text
文件：synapse_net.py（若走"原地扩展"）或 synapse_net_v2.py（若走"平行分支"）
函数：activate（:84–:113）

:91   seeds = [start] if isinstance(start, str) else list(start)
      → 需接受第三种形式：mapping[str, float]（名字形式仍默认 1.0）
:94-96 逐 seed 的 KeyError 检查 → 需遍历 keys
:99   acts = {s: 1.0 for s in seeds} → 需改为带值初始化
:100-101 queue = deque(seeds) / queued = set(seeds) → 需用 keys

改动量：约 6–10 行
新增语义决定（**已拍板：常量**，跑前写死）：
  种子值 < 1.0 时，允许被传播路径改变吗？三种可能：
  (a') 常量【已选】：种子值永不变。A=0.5，路径给 0.6 → A 仍是 0.5；路径给 0.4 → 仍是 0.5
  (a'') 下界：种子值只可被抬高（0.5 → 0.6），不可被降低
  (b)   浮动：种子退化成"普通节点的初始值"

  选【常量】的理由：
  ① 归因最干净（A 的最终值永远是种子值本身，不可能是"种子与传播的混合体"）；
  ② 与 v1 结构对称（v1 下种子恒 1.0 且不可变；v2 只是放开值域，不动"不可变"）；
  ③ 下界会引入无定义语义（0.5→0.6 的那个值既不是种子值也不是传播值）。
```

**向后兼容性（关键事实）**：第一轮共有 **11 处**多节点种子调用
（E6a 3 处、E9 2 处、E20 2 处、E21 1 处、E22 1 处、其余 2 处），
**全部传的是节点名列表**。若 v2 保留"名字列表 ⇒ 一律 1.0"，
则 **E0–E32 的语义完全不变**——v2 可以是**向后兼容的扩展**，而不是破坏性改动。

---

## 3. E33–E36 的受影响范围（逐条，含更正）

| 实验 | 是否依赖"值抹平" | 依据 |
| --- | --- | --- |
| **E33** | **不依赖**（前一轮列为依赖，**更正**） | 判据用的是"种子模块 clamp 为 ±1"与"二值化阈值 0.5"；`activate` 只被单种子调用（多节点入口未参与判据）。粗粒化结论来自能量景观结构（自耦合 0.69–1.0 vs 非对角 0.002），不是种子入口 |
| **E34** | **判据仍成立，但记录表变化（范围已实测，见 §3.1）** | W 里存的 A 本来就是**步1 的 top-1 = 1.0**，所以 v2 下 A 仍是 1.0、差异仍 0.928 → P3 通过；但 S2 表会变——**不是两个值，是 A 侧半条链 13 个节点** |
| **E35** | **核心结论依赖** | (b)"痕迹无优先级"与 (c)"成员由 tie-break 决定"**都由"注入值全 1.0"推出**；值不被抹平 → W_n 的内容与并列关系都变 → 结论失效 |
| **E36** | **核心结论依赖** | P4（"注入后全 1.0，无相对权重差异"）**就是抹平本身**；v2 下排序差异会变得可见 → C36 的"分类 ≠ 排序"要改写为"v1 分类≠排序；v2 两者皆可见" |

**汇总**：E0–E32 不受影响（11 处全是名字列表）；E33 不受影响；**E34 判据在、表变**；
**E35 / E36 的结论依赖 v1 的抹平行为**——这是必须写进 BOUNDARY 的版本差异。

### 3.1 E34 的表变范围（实测，2026-09-15；**更正前一轮的估计**）

```text
v1（种子全 1.0）：A=1.0, B=1.0, C=1.0, D=0.9,   E=0.81,   F=0.729,  G=0.6561, H=0.5905
v2（带值种子）：   A=1.0, B=0.9, C=0.81, D=0.729, E=0.6561, F=0.5905, G=0.5314, H=0.4783

全链 26 节点逐个比较：
  值发生变化 = 13 个：B, C, D, E, F, G, H, I, J, K, L, M, N   （A 侧半条链）
  值不变     = 13 个：A 与 Z 侧（O..Z）
```

**为什么不是"只有 B/C 变"**：种子值是**传播的起点值**，不是孤立量——
C=0.81 使 D=0.81×0.9=0.729、E=0.656… 一路传到 M；到 N/O 之后 Z 侧路径（Z=1.0）重新占优。
因此"**D 及之后不变（它们是传播路径，与种子入口无关）**"的说法**不成立**：
**传播路径的绝对水平正是由种子值决定的**。
（这也意味着 E37 的对照必须是**整表比较**，不能只比两个值。）

---

## 4. v1 / v2 并存的条款（含 sha256 的三种处理）

**为什么 sha256 是关键**：E27–E36 的 JSON 里记录了
`synapse_net.py = cf99a07a0e60fd4d`。**只要改动这个文件，这批记录就无法自动复核**——
注意：**这与"语义是否向后兼容"无关**：即使语义一字不变，哈希也会变。

三种处理：

```text
(a) 保留 v1 快照，新分支另起文件（synapse_net_v2.py）
    → tag v0.9.15-e36 就是 v1 的精确快照；历史实验的哈希记录【仍然可复现】（检出即得）。
    → v2 是新文件、新哈希，从 E37 起记录它自己的哈希。

(b) 原地扩展 v1 + 版本标注
    → 结果：当前文件哈希 ≠ E27–E36 记录的哈希 → 只读检查需要改成
      "记录 v1 基线哈希 + 当前 v2 哈希"两栏，并在 BOUNDARY 声明实现版本。
    → 代价：所有只读类脚本（含 verify_*）都要理解"两栏哈希"。

(c) 直接改、不记录 —— 【不推荐】：历史只读检查变成"当前文件与记录不符"，
    无法自动复核，证据链出现断点（这正是本项目一直避免的那类问题）。
```

**建议条款（若走 v2）**：

```text
① L0-v1 冻结不变（sha256 cf99a07a0e60fd4d），E0–E36 全部有效；
② L0-v2 是新分支文件（server 侧并列），实验编号从 E37 起；
③ 一个实验要么在 v1 上跑、要么在 v2 上跑，【不可混用】（JSON 里记录用的是哪个版本 + 对应哈希）；
④ BOUNDARY 增加一节"实现版本 v1/v2"，写明两者差异只在种子入口的值处理。
```

---

## 5. E37 的第一个实验设计（在 v2 上重跑 E34 的两步携带）

```text
语料：E1 的 26 字母链（与 E34 同）
变体对照（同一语料、同一 W 内容）：
  v1：activate(['Z'] + W.seeds())              → A/B/C/Z 全部 1.0（值被抹平）
  v2：activate({'Z':1.0, 'A':1.0, 'B':0.9, 'C':0.81}) → 注入值被保留

判据候选（跑前写死）：
  P_a 值被保留（**整表比较**）：v2 下 B=0.9、C=0.81、D=0.729、E=0.656…（A 侧半条链 13 个节点
      与 v1 不同，见 §3.1）；A 仍 1.0（W 里存的就是 1.0）
  P_b 排序差异出现：两个对照配置必须**节点集合完全相同、只有值的对应关系翻转**——
       配置甲 {A:1.0, B:0.9, C:0.81}（与 W 存的一致）
       配置乙 {A:0.81, B:0.9, C:1.0}（把 A 与 C 的值对调）
      若两者输出不同 → v2 下"排序/优先级"可测（v1 下不可测 —— C36 的边界被 v2 越过）
      若两者输出相同 → 说明值被某种机制抹平（v2 下不应发生）→ 先查实现
  P_c 向后兼容：名字列表形式仍全部 1.0（第一轮 11 处调用语义不变）
  P_d 只读（**两个哈希**，与 E27–E36 口径一致）：
       synapse_net.py    = cf99a07a0e60fd4d   （v1，不变）
       synapse_net_v2.py = <新哈希>            （v2，首次记录）
  双向：通过 → C37（带值种子可用）；不通过 → B30

已在跑前定死的语义：**常量**（种子值不可变；§2 的 (a')）。
```

---

## 6. 两个方案与建议

| | 方案 A：扩展 v1（原地 + 版本标注） | 方案 B：平行 v2 文件（`synapse_net_v2.py`） |
| --- | --- | --- |
| 语义 | 向后兼容可做（名字列表仍 1.0） | 同 |
| 一套还是两套实现 | 一套（推荐给"只要一条主线"的人） | 两套（v1 冻结副本 + v2） |
| **历史只读记录** | **失效**（哈希变）→ 需改两栏并升级 verify 脚本 | **完整保留**（v1 文件一字不动） |
| 改动量 | 6–10 行 + 语义决定 | 复制 + 6–10 行 + 语义决定 |
| 未来再改的风险 | 每改一次，历史哈希都要再处理一次（无底洞风险） | v1 永远可复现；v2 可迭代，坏了就丢 |

**我的建议：方案 B（平行 v2 文件）**，理由只有一条但很硬：

```text
本项目把【证据链可复现】放在【实现干净】之前——E27–E36 的只读检查是靠哈希自动复核的。
动 v1 会让这批记录从"可自动复核"退化成"需要人工解释"。
而 v2 作为平行文件，代价只是多一个文件，却把 36 个实验的证据链完整保留。

执行顺序（若采纳）：
  1) 先按 §5 把 E37 的判据写进 DESIGN-E37（含"种子能否被抬高"的语义决定）；
  2) 新建 synapse_net_v2.py（复制 + 改 6–10 行），不动 synapse_net.py；
  3) E37 全部在 v2 上跑，JSON 里记录 v2 哈希；v1 哈希照旧出现在历史 JSON 里。
```
