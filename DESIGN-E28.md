# E28 设计：读取端 max/sum 系统对比（B02 + B03 正式化）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-13。
依赖：L0 `synapse_net.activate`（max 口径）+ E14 的 sum 语义（单次发射 + 封顶，实验层实现）。
**纯计数，无 numpy 依赖；不改任何层文件。** 只读判据沿用 E27 的层文件 sha256 前后对比（纪律 ⑧）。

---

## 0. 前置决定（五条 + 一处补充）

### 决定一：B02 升级为 C28 的一部分（编号保留为指针行）

`max(a, a) = a` 是数学恒等式，**不是机制缺陷**，是读取端的设计选择。
处理方式：B02 那一行改成**指向 C28 的指针**（保留编号，不删行）——登记表的价值在可追溯，
直接删会留断口。C28 承载完整表述（§8）。

### 决定二：B03 的探针是 **T5 三角**，不是 T4 环

环里 C 的回馈被单次发射护栏挡住（B 先发射、C→B 被跳过），**测不出顺序依赖**；
三角三条边全通，封顶落点才会显形。该判断由预检支持（§4）。

### 决定三：sum 的遍历顺序固定为**字典序**

不固定就不可复现——exp14 的 JSON detail 抖动就是先例。标准口径 = 邻居按字典序；
"换序后的差异"单独作为 B03 的量化指标（见决定四）。

### 决定四：P4b 拆成"判据"和"记录"两半

```text
max 顺序无关  -> PASS 判据（容差 1e-12）：升序/降序输出必须相同
sum 顺序依赖  -> 记录项，不参与 PASS/FAIL：这是 sum 模式的固有性质，
                 实验要【证明它有】这个性质，不是【要求它没有】
```

把 `sum 升序 ≠ 降序` 写成 FAIL 判据，逻辑就反了。

### 决定五：T4 环的定位 = **sum 可用性自检**（防死循环），不是 B03 探针

环结构不导致 sum 死循环，是 sum 能被使用的**必要条件**，必须单独判（P4，复核 E14）。

### 补充：1e-12 覆盖什么、不覆盖什么

两条路径的乘积完全相同（都是 hop_decay^2），所以 P2 要求**严格等**（容差 1e-12）。
但要说清：**1e-12 只覆盖浮点误差，不覆盖真实机制差异**。若实测出现 > 1e-12 的差，
那不是浮点问题，是机制问题——**停下来查，不许调容差糊过去**。

### 决定六：到达节点数用**双阈值口径**

绝对阈值 `> 1e-12` 有风险：sum 累加时可能把两条路径的浮点残差（如 1e-15）加成一个
"已到达"节点，而 max 那边精确为 0。因此两个口径都记：

```text
口径 A（绝对）: 激活值 > 1e-12
口径 B（相对）: 激活值 > 最大激活值 × 1e-9
两者不一致时，以【口径 B】为准（说明绝对阈值把浮点噪声算进去了）
```

### 决定七：sum 语义照搬 E14，不得偷改

E14 的语义是：**每个节点只出队一次，出队时把自己的激活值冻结**（之后再来的贡献被丢弃）。
E28 必须逐字沿用——否则 E14 与 E28 的 sum 就不是同一个东西，B03 的量化也就无从对比。
实现时该语义写成注释钉在函数里。

### 决定八：`PYTHONHASHSEED=0` 纳入运行说明

exp14 的抖动根源就是字符串哈希随机化。E28 要测"顺序无关/顺序依赖"，
**必须先把哈希顺序固定**，否则升序/降序本身不可复现，测出的差异分不清是机制还是环境噪声。
措施：`PYTHONHASHSEED=0` 写进 README 的运行说明与 NEXT-PLAN §4.1（纪律 ⑧ 的配套）。

---

## 1. 要解释的现象（宪章四问）

1. **基底参与的步骤**：传播规则（max 吸收 vs 多路求和），以及读取端的遍历策略。
2. **需要外部模块吗**：不需要。
3. **结论归谁**：归"**读取端的设计选择**"。max 是 L0 的现有规则；sum 是本实验层实现，
   **不改 L0**，也不在 LAYERS 里登记为新层（沿用 E14 的处理）。
4. **做到哪一步算停**：算到"两种模式在同一拓扑上的差异 + 各自边界"就停，
   不做"哪个更好"的价值判断（那是上层任务的事）。

**要解释的现象**：同样的拓扑、同样的输入，两种读取模式给出什么不同结果？
差异从哪来？各自的适用边界在哪？

**可证伪预期**：若 max 在多路会合处给出 > 单路的值，则"max 精确不叠加"被推翻；
若 sum 在三角上顺序无关，则"sum 输出非交换"被推翻——两条都要能被数据打掉。

---

## 2. 语料（五条，每相邻对 100 次）

```text
T1 单路基准 : A-B, B-C
T2 双路会合 : A-B, B-C, A-D, D-C           （C 有两条独立来路）
T3 三路会合 : A-B, B-C, A-D, D-C, A-E, E-C （C 有三条独立来路）
T4 环       : A-B, B-C, C-B                 （sum 终止性自检，非 B03 探针）
T5 三角     : A-B, B-C, C-A                 （B03 探针：三条边全通）
无自环；种子统一为 A；hop_decay = 0.9（默认）
```

**P1 语料构造核查包含 T5**：三条边存在、且不存在的边逐条核对（无多余边）。

---

## 3. 两种读取模式的口径

```text
max：目标激活 = 到达路径的最大值（直接调用 L0 的 activate，单一实现来源）
sum：单次发射 + 出队冻结 + 封顶 cap=1.0（E14 语义，实验层实现）
     - 邻居遍历顺序固定为字典序（决定三）
     - 另跑一次 cap=inf 得到【原始和】，用于计算封顶损失
到达节点数 = 激活输出中值 > 1e-12 的节点个数（E28 首次显式定义）
```

`cap = 1.0` 是**预设参数**，不是涌现值。

---

## 4. 预检结果（已执行，2026-09-13，只读即算即弃）

| 语料 | max a(C) | sum a(C) | sum 原始和 | 封顶损失 | sum 出队数 | max/sum 到达节点数 |
| --- | --- | --- | --- | --- | --- | --- |
| T1 单路 | 0.8100 | 0.8100 | 0.8100 | **0.0000** | 3 | 3 / 3 |
| T2 双路 | **0.8100** | **1.0000** | 1.6200 | **0.6200** | 4 | 4 / 4 |
| T3 三路 | **0.8100** | **1.0000** | 2.4300 | **1.4300** | 5 | 5 / 5 |
| T4 环 | 0.8100 | 0.8100 | 0.8100 | 0.0000 | 3（终止） | 3 / 3 |
| T5 三角 | A1.0 B0.9 C0.9 | 见下 | C 原始和 1.71 | 0.7100 | 3 | 3 / 3 |

**T5 的顺序依赖（B03 的量化）**：

```text
sum 邻居升序：[A=1.0, B=0.9, C=1.0]      （封顶落在 C）
sum 邻居降序：[A=1.0, B=1.0, C=0.9]      （封顶落在 B）
max（与顺序无关）：[A=1.0, B=0.9, C=0.9]
```

**这也解释了 exp14 的 JSON detail 抖动**（{'A':1.0,'B':1.0,'C':0.9} vs B=0.9,C=1.0）：
不是测试缺陷，是 sum 顺序依赖的直接后果。

**一个与预期不同的预检发现**：**到达节点数在两种模式下相同**（3/4/5/3/3）——
差异不在"能到达哪些节点"，而在**值**。另外，E14 其实没有把"到达节点数"写进 JSON
（双路那次的出队数只在内部算过、未发布），所以本项是 **E28 首次定义并记录**，
不是"复核 E14 的旧数字"。

---

## 5. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1 | 语料构造核查：T1–T5 的边集合逐边核对（含 T5 无多余边） | 通过 | 语料 bug，改语料 |
| P2 | **max 精确不叠加（B02 主体）**：T1/T2/T3 的 a(C) 严格相等（= 0.81） | 容差 1e-12；> 1e-12 即停查机制 | 记录；**不许调容差糊过去** |
| P3 | **sum 封顶 + 原始和**：T2/T3 封顶后 = 1.0；原始和 1.62 / 2.43；损失 0.62 / 1.43（cap=1.0 为预设） | 通过 | 记录 |
| P4 | **T4 环 sum 终止且封顶**（sum 可用性自检，复核 E14）：终止 + 所有值 ≤ 1.0 + **单次 activate 调用的出队数** = 节点数（非累计） | 通过 | 若不死循环则记录为反例 |
| P5 | **max 顺序无关**（重标签同构检查）：重标签到**结构同构、但哈希顺序不同**的 token 名（A/B/C → P/Q/R；注意不是"字典序相反"），映射后激活值逐点相同 | 容差 1e-12 | 记录 |
| P6 | 只读：层文件 sha256 前后一致（纪律 ⑧）+ 测量前后计数不变 | 通过 | 直接 FAIL |
| P7 | 登记：写入 C28，B02 改指针行，B03 升级（文本见 §8） | 通过 | — |
| 辅助 | **sum 顺序依赖**（T5 升序 vs 降序输出不同）——记录项，**不参与 PASS/FAIL**；以及 max/sum **到达节点数对照（绝对阈值 + 相对阈值两套口径）** | 仅记录 | — |

---

## 6. 预注册参数

```text
每相邻对重复 = 100 次         种子 = A          hop_decay = 0.9
cap = 1.0（预设）             原始和口径 = cap=inf 的那次跑
sum 遍历顺序 = 邻居字典序（标准口径）；另跑降序用于顺序依赖记录
sum 语义 = 每节点只出队一次 + 出队冻结（照搬 E14，不得偷改）
容差：P2/P5 = 1e-12（仅覆盖浮点误差）
到达阈值口径 A（绝对）= 1e-12；口径 B（相对）= 最大激活值 × 1e-9（不一致时以 B 为准）
运行环境：PYTHONHASHSEED = 0（固定字符串哈希顺序，否则顺序类判据不可复现）
P4b：max 顺序无关 = 判据；sum 顺序依赖 = 记录项（不参与 PASS/FAIL）
```

---

## 7. 产出物

- `experiments/exp28_readout_modes.py` + `outputs/exp28_readout_modes.json`；
- 通过或失败都登记 CLAIMS-REGISTRY；
- 不改 L0/L1/L2/L4；不新增层文件；不引入语义/stub/奖励；
- sum 仍是**读取端可选模式**，不改变"基底传播永久 max"的 E14 拍板。

---

## 8. 拟登记的表述（用户给定）

```text
C28：max 与 sum 是读取端的两种选择，不是对错。
(a) max 精确定律：max(a, a) = a，容差 1e-12，任意路径数下严格成立；
(b) sum 是另一种选择：单次发射 + 封顶 1.0，封顶损失随路径数单调增（记录原始和）；
(c) max 顺序无关，sum 顺序依赖（字典序为默认遍历口径，换序差异是 B03 的量化指标）。

B03（升级）：sum 模式的遍历顺序影响封顶落点。触发条件是【拓扑判据】——
当拓扑中存在两个都可由种子一跳到达、且彼此相邻的节点时，sum 的封顶落点依赖遍历顺序
（T5 三角语料：升序 [1.0, 0.9, 1.0] vs 降序 [1.0, 1.0, 0.9]）；max 仍顺序无关
（均 [1.0, 0.9, 0.9]）。反例对照：T2/T3 里 B/D/E 是中间节点、C 是唯一终点，
封顶必然落在 C 上，谁先谁后不影响结果（顺序无关）。
封顶损失随路径数单调增：单路 0.00、双路 0.62、三路 1.43。
因此 sum 的输出不可与 max 直接对比。
另注：T5 的 max a(C) = 0.9（不是 0.81），因为 A 与 C 直接相邻、存在一跳路径；
T1-T4 中 A、C 均不相邻故一律 0.81——这是拓扑差别，不只是数值差别。
exp14 的 JSON detail 抖动是这一性质的直接后果，不是测试缺陷。

B02（指针）：已并入 C28 —— max 的精确不叠加是读取端设计选择，不是机制缺陷；
原始观测"多路证据在 max 下不叠加"保留在此。
```

---

## 9. 边界与不做

- **不做"哪个更好"的判断**：max / sum 是两种读取选择，适用场景由上层任务决定；
- **不改 L0**：sum 只在实验层实现，不进层文件、不进 LAYERS；
- **sum 的输出不可与 max 直接对比**（B03）：遍历拓扑不同，这是它的边界；
- 不声称任何"注意力/意识"类结论（B07）。
