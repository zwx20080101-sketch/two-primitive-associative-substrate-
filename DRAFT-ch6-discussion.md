# 第 6 章 讨论（中文草稿）

本章只做三件事：汇总第 4 章稳定涌现的联想行为；对经典联想记忆模型与
现代大模型的记忆机制做浅度对比（不做系统性文献综述、不做等价论断）；
列出后续工作方向。

## 6.1 稳定涌现的行为汇总

在受控输入族内，E0–E22 稳定复现以下行为族：

1. **结构自组织**：仅凭共现绑定可从空网络形成链式结构（4.1）；
2. **检索强度受频率、时间与阈值调制**：共现次数越高检索激活越强；
   旧经验检索变弱但连接永存；弱连接保留在全量输出，仅由读取端阈值
   决定可访问性（4.2）；
3. **方向不对称来自经验统计**：单向经验使正走顺、倒走显著更弱；
   对称经验不产生方向偏置（4.3）；
4. **双前缀消歧与角色区分**：最近两项前缀可唯一决定下一候选；
   语序或输入侧格标记可支撑角色区分（4.3）；
5. **共享情景与共享枢纽产生拓扑绑定**：两表面可经共享情景互译；
   共享枢纽给出并行候选；无上下文时保持诚实歧义（4.4）；
6. **时间结构、重复标记与补全**：脉冲密度产生强连接与第二跳预警；
   同一符号相邻再现可由有序自转移标记；部分输入可补出已存节点，
   未知符号不可激活（4.2、4.5、5.3）；
7. **组块与位置记录**：重复单元可成块并保留词内重复位置；
   平面链无法做到（4.6、5.1）；
8. **外部模块分工**：叠词规则外推、动作渲染、情绪读取分别由
   外部 stub 完成；基底只提供证据与检索（4.7、5.4）。

以上行为均限定于第 4 章给出的输入族；能力上限见第 5 章。

## 6.2 与既有工作的浅度对比

本系统在机制上属于联想记忆与激活扩散一类方法，与以下工作方向存在
概念上的相邻关系：

- **扩散激活类模型**：以节点间连接强度与激活沿边传播为检索方式，
  与本系统 L0/L1 的传播机制同属一类思想；
- **吸引子/关联记忆模型**：以模式间的相似关联完成回忆，
  与本系统"连接即记忆、激活即检索"的划分有对应关系，
  但本系统不采用模式重建式的动力学；
- **n-gram/序列统计模型**：以相邻符号的条件频率做序列预测，
  与本系统 L1/L2 的相邻与双前缀计数在形式上有重叠；
- **现代大模型的内存机制**：权重与上下文窗口承担大规模隐式记忆，
  与本系统显式、可审计的连接表在表示形式与可解释性上不同。

对比声明：

- 上述对比为方向性的浅度描述，本报告不进行系统性文献综述；
- 本系统不作出与上述任何模型功能等同的论断；
- 本系统当前规模与输入形式远小于现代大模型，不具备直接对比条件。

## 6.3 本工作回答的问题与证据等级

本工作回答的是一个问题：极简的"共现绑定 + 激活扩散"机制，
配以上层表示与外部读取层，能否在受控输入族内复现一组
联想/时序/语言式/价值式行为。

证据等级说明：

- 主体实验为确定性演示（机制在给定输入下必然产生给定观测）；
- E20 提供机制开关对照；E21 提供小到中规模一致性；
- E22 提供随机输入族抽样成功率与 95% 置信区间；
- 本工作不包含任何人群统计实验，也不对生物系统作等价论断。

## 6.4 后续工作

后续工作分两类：

**面向扩展的实验方向（由第 5 章边界直接引出）：**

- 在上层表示层中引入更长上下文（针对 B01）；
- 评估传播规则变体与读取端策略的边界（针对 B02/B03）；
- 为外部驱动闭环建立更完整的输入/输出接口（针对 B06）；
- 引入真实时序输入（如视频帧事件流）检验现有绑定规则是否迁移
  （对应开放计划中的视频时序输入扩展）。

**面向展示与探索的方向：**

- 网页可视化玩具：从空白网络开始，按规则逐步输入符号并实时
  展示节点、连接与激活过程，作为理论的可交互演示；
- 无约束开放探索：在玩具之外尝试更自由的输入结构，记录
  哪些现象仍成立、哪些触发新的边界；
- 视频时序输入扩展：将连续帧抽象为有序符号流后接入现有层，
  检验时间维规则在多模态输入上的适用性。

以上方向仅作路线描述，尚未执行，不构成本文的实验结果。
