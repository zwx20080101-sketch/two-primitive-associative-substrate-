# E35 设计：工作记忆的串联 —— 携带的到底是"重注入"还是"图传播"？

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：E34 的 `WorkingMemory`（同一实现，不重写）、E1 的 26 字母链语料、E27/E30 的只读口径。
**无 numpy 依赖；不改任何层文件；不新增层。**

---

## 0. 前置（E35 预检，2026-09-15；四组数据）

```text
变体1（骨架原样：每步重注入上一步 top-3，N=5）
  步1 ['M']              → top-3 [M,L,N]   seed1(M)=1.000000
  步2 ['Z','M','L','N']  → top-3 [L,M,N]   seed1(M)=1.000000
  步3..5 同上            → top-3 [L,M,N]   seed1(M)=1.000000
变体4（步数扫描 N=2/3/5/8）：最终 W 恒为 [X,Y,Z]，seed1(Z) 恒 1.000000
变体3（每步换种子 A,B,C,D,E）：W_1..W_5 恒为 [A,B,C]；步4/5 的 D、E 挤不进去
变体2（只注入一次）：步2 seed1(M)=1.0；步3 起不再注入 → seed1(M)=0.254187 = 0.9^13
```

**结论（本设计的出发点）**：骨架原样是**恒等式**——注入的种子一律置 1.0，而 1.0 是最大值，
所以它们必然继续占 top-3，痕迹"永存"是 **top-k 规则 + 值抹平**决定的，不是携带能力。

---

## 1. 命题（改版后）

```text
E35 = 多步串联时，"携带"的内容是【每步重注入的集合】（构造性）
      还是【通过图传回的痕迹】（实证）？
```

**范围限定**：W 仍是**实验侧读取端工具**（E34 的定性不变）；本实验不涉及"规划"
（多步依赖链 ≠ 前瞻/搜索）。

---

## 2. W 栈规格（写死，跑前不改）

```text
W 复用 E34 的同一实现（import，不重写）：{node: value} + 容量 k
k = 3
top-k 选择规则（预注册，必须写死）：按激活值【降序】排序；值相同则按【节点名升序】。
    理由：预检发现并列——activate(M) 的 top-5 = M(1.0), L(0.9), N(0.9), K(0.81), O(0.81)，
    L/N 并列 0.9、K/O 并列 0.81；若不写死 tie-break，W 的内容不可复现
    （与 E28"sum 遍历顺序固定字典序"是同一类问题）。
读出：seeds() 只返回节点名（不传 value —— 多种子入口会把值抹成 1.0，E34 已证）
```

---

## 3. 三条路径（同一语料、同一 W 实现）

```text
语料：E1 的 26 字母链（A–Z 相邻对 × 100，26 节点 / 25 边）

P_a 重注入路径（构造性对照）：步1 activate('M') → W_1.hold；
     步n（n>=2）activate(['Z'] + W_{n-1}.seeds()) → W_n.hold；N = 5
P_b 单次注入（实证）：步1 同上；步2 activate(['Z'] + W_1.seeds()) → W_2.hold；
     步3 起只用 activate(['Z'])（不再注入）；报步 N 时 seed1('M') 的值
P_c 平权性：① 在 P_a 里报 W_n 各成员的值分布（是否全部 1.0）；
     ② 变体3（每步换种子 A,B,C,D,E）报 D/E 是否挤入 W_n
```

---

## 4. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **接口不改**：四个层文件 sha256 前后一致 | 一致 | 直接 FAIL |
| P1b | **L0 不改**：节点数/边数/边集合签名（排序逐项含计数）逐位相同 | 相同 | 直接 FAIL |
| P_a | **重注入是构造性的（对照）**：N=5 链里 seed1('M') 每步 = 1.0，且 W_n 集合从步 2 起恒定 | 通过 | 记录（**不作正结果**——这是规则决定的） |
| P_b | **单次注入后痕迹 = 纯图衰减**：步 3/5 时 seed1('M') 与 **0.9¹³ = 0.254186583** 之差 | ≤ 1e-9 | 记录反例 |
| P_c | **平权性**：① P_a 里被携带成员的值**全部为 1.0**（无优先级/顺序）；② 变体3 里 D、E **均未挤入** W_n | 两子项都通过 | 记录反例 |
| P5 | **双向结局**：P_b + P_c 通过 → **C35**；不通过 → **B28**（文本见 §6） | 两分支都合格 | 不许跑完再挑解释 |
| 辅助 | 变体1/3/4 的完整逐步数据（种子列表、W 内容、seed1 值） | 仅记录 | — |

**0.9¹³ 的推导（写死，便于独立复核，不依赖预检）**：

```text
26 字母链：M 是第 13 个字母，Z 是第 26 个。
从 Z 沿链反向走到 M：Z→Y→X→…→N→M，共 26 − 13 = 13 跳。
衰减 = 0.9^13 = 0.254186583（与 E1 记录的逐跳公式 a(v)=a(u)·s·hop_decay 一致）。
```

---

## 5. 预注册参数

```text
语料：A–Z 相邻对 × 100（26 节点 / 25 边）
W：复用 E34 的 WorkingMemory（k=3）；tie-break = 值降序、同值节点名升序
链长 N = 5                                  只读：层文件 sha256 + L0 边集合签名
P_b 的参照值 = 0.9^13 = 0.254186583        容差 1e-9
运行环境：PYTHONHASHSEED = 0
```

---

## 6. 双向结局（跑前写死，两分支文本）

```text
分支 C35（P_b + P_c 通过）：
C35：W 栈可以在多步链路中携带 N 个痕迹，但
     (a) 只在【每步重注入】时维持——停止重注入后痕迹立即退化为纯图衰减（0.9^13 = 0.254187），
         W 不留下任何持续影响；
     (b) 痕迹之间【无优先级、无时间顺序】（被携带成员的值全部 1.0）；
     (c) 成员由 tie-break 决定，新痕迹可能挤不进去（变体3：D/E 均未进 W）。
     ⇒ 这是【载体】行为，不是【状态整合】。
     范围限定：26 字母链语料；W 仍是实验侧读取端工具（同 E34）。

分支 B28（P_b 或 P_c 未通过）：
B28：多步串联的行为与上述三条不符（具体偏差见 JSON）；W 栈的"携带"在该语料上
     不由"重注入 + 值抹平 + tie-break"三者决定，需重新定位机制。
```

**区分痕迹来源需要什么（C35 的边界登记，写进结论）**：

```text
要区分"哪个痕迹来自哪一步"，至少需要以下机制之一：
  ① 顺序标签：每次注入的种子带一个时间戳（写入 W 时记录）；
  ② 值域编码：不用值抹平的种子入口，保留种子原始激活值。
两种机制都不在 E35 范围内——这是 E36 的入口。
```

---

## 7. 产出物

- `experiments/exp35_working_memory_chain.py` + `outputs/exp35_working_memory_chain.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C35 / 负结果 B28）；
- **不改任何层文件**；**不新增层**；W 不进 LAYERS、不改 BOUNDARY。

---

## 8. 边界与不做

- **不做规划**：多步依赖链 ≠ 前瞻 / 搜索空间 / 目标函数；
- **不把 W 说成"基底的工作记忆"**（E34 的定性不变：读取端工具，基底对它一无所知）；
- **不实现顺序标签或值域编码**（那是 E36 的候选，不在本实验）；
- 不声称任何"意识/理解"类结论（B07）。
