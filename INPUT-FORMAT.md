# Synapse-Net 外部输入格式说明书（INPUT-FORMAT）

用途：把"一连串经验"用文本写出来（AI 或人自己写都能一键载入），
含时间戳与三种关系：同一时间、先后顺序、整体单元。

## 1. 基本约定

```text
# 井号开头是注释（整行或行尾）
; 或 换行   = 事件之间隔断
@<数字>     = 该事件的时间戳（可用小数，如 @1.2；缺省 = 上一个事件 +0.1）
[A B C]     = 同一时间出现（同步窗口）→ L0 两两绑定
A>B>C       = 先 A、后 B、再 C（有序序列）→ L0 相邻对 + L1 方向
{A B C}     = 一个带边界的整体单元（如一个词）→ L4 成块
A           = 单个符号出现（创建节点）
```

## 2. 示例

```text
# 一个最小的学习序列
@1.0 [A B]        # 时间 1.0：A、B 同时出现
@1.1 A>B>C>D      # 时间 1.1：按 A-然后-B-然后-C-然后-D 的顺序出现
@1.4 {H e l l o}  # 时间 1.4：整体说出 HELLO（含双写 l）
@2.0 A            # 单符号 A 再次出现
@2.0 X            # 与 A 同一时刻出现 X
```

## 3. 分层 -> 喂给什么

| 写法 | 结果 |
| --- | --- |
| `[A B C]` | L0：A-B、A-C、B-C 各计一次共现（无先后） |
| `A>B>C` | L0：A-B、B-C 计一次；L1：A→B、B→C 计数 +1；L2：记录 (A,B)→C |
| `{H e l l o}` | L4：该单元出现 +1；达到 K 次成块，spell 保留双写 |
| `@t ...` | 该事件所有涉及节点/连接的时间戳记录为 t |

## 4. 一键载入

在网页"批量输入"框粘贴上述文本 → 点"载入"→ 自动解析、按规则喂给各层、
在图上复现；随后可正常激活/调阈值/看链路。

Python 侧同样支持：`playground_rules.py` 提供 `load(text)`，
可把同一格式粘进脚本，跑完打印统计与链路。
（v3.3 实现。）
