# E48 设计：组合的嵌套（`combine(C, R) → D`）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-16。
依赖：E42（C41 + L5 + `compose_layer.py`）、E47（**判定规则：新内容必须在输入层判**）、
E30（C30 闭包）、B35（L5 的 last-write-wins）。
**不改任何已验收文件；L0 一字不动；不引入 stub / 语义 / 奖励。**

---

## §0 概念基础

### 0.1 L5 接口现状：**对输入类型无检查**（预检读码）

```text
def coactivate(self, P: dict, Q: dict, rule: str = "prod") -> str
    members = sorted(set(P) | set(Q))                       <- 只取【键】，对键的类型无任何检查
    vals    = {m: P.get(m,0.0) * Q.get(m,0.0) for m in members}
    ...
    for m in members:  self.high_pairs[self._key(cid, m)] = 1   <- 成员边【无条件建立】

=> "模式"的表示 = 任意 dict[str, float]；成员 = 任意字符串
=> C（L5 的符号，id 形如 "CMP_1"）天然可作为成员 => 无需改 L5、无需 L6、不触及 L0
```

### 0.2 与 E47 的关系：**E48 是 E47 判据的第一个反例**

```text
E47（分类+值序组合态）：
  组合输入 = E41 输入在 E36 集合上的【投影】  => 两个单独输入的拼接 => 简单叠加（无新量）

E48（组合嵌套）：
  组合输入 = {CMP_1} 并 keys(R)
  其中 CMP_1 是【E42 的输出符号本身】—— 它不是任何单独配置的输入里的量
  => 【输入层出现了新量】=> E48 不满足 E47 的"简单叠加"判据 => 值得单独立实验

=> 这条对比是"为什么 E48 值得跑"的直接回答：
  E47 的判据（"输入层是否出现新量"）在 E47 上给出"否"（封箱），在 E48 上给出"是"（立实验）。
```

### 0.3 嵌套的结构性后果：`0.9^d`（**数学必然，不是涌现**）

```text
预检实测：一层符号 CMP_1 的 L0 成员激活 = 0.9；二层符号 CMP_2 经一次级联后 = 0.81 = 0.9 的平方
=> 嵌套深度 d 的 L0 可及强度 = hop_decay^d（本例 hop_decay = 0.9）

【必须在设计里明说】这是 hop_decay 的自然结果（任何 d 跳可达都是 hop_decay^d），
  不是"系统学会了衰减"。登记时写"【嵌套深度与可及强度的定量关系】"，
  不写"系统学会了衰减 / 学会了递归"。
  （与 E43 的区别：E43 是【屏障阻断】，E48 是【串联衰减】—— 前者改变可达性，后者只降低强度。）
```

### 0.4 命名自检（E42/E43 教训；预检实测）

```text
候选变量名撞车实测（全仓库 .py 的裸标识符计数）：
  nested 0 / depth2 0 / symbol_a 0 / symbol_b 0 / outer 0 / inner 0 / compose2 0   <- 可用
  nest   2   <- 【弃用】
=> E48 若需新变量名，从上面那组零撞车候选里取；符号 id 沿用 L5 的 CMP_n（自动生成）
```

---

## §1 预检摘要（2026-09-16，只读，未动任何文件）

```text
语料：E1 的 26 字母链（v2 实例）；P = top3(activate{"A":1.0})、Q = top3(activate{"C":1.0})、
      R = top3(activate{"Z":1.0})；L5 = ComposeLayer()

(1) C = coactivate(P, Q) = CMP_1，成员 = [A, B, C, D]，origins = "coactivation"（E42 已证）
(2) D = coactivate({"CMP_1": 1.0}, R) = 【CMP_2】，成员 = 【['CMP_1', 'X', 'Y', 'Z']】
      => 符号 CMP_1 成为 D 的成员（进的是符号本身，不是它的成员 A/B/C/D）
(3) activate(D) 的级联（两口径同结果）：
      CMP_2=1.0 | CMP_1=0.9 | X=0.9 | Y=0.9 | Z=0.9 | A=0.81 | B=0.81 | C=0.81 | D=0.81
      => 两跳：D -> CMP_1 -> C 的成员
(4) 层级：D 与 C 都在【同一个 L5 实例】=> 层级不膨胀，不需要 L6
      二层嵌套也通过：C2 = CMP_3、D2 = combine(C, C2) = CMP_4，成员 = [CMP_1, CMP_3]
(5) L0 未被触碰：节点 26 -> 26、边 25 -> 25 逐位不变；全程未调用 net.learn / net.activate
(6) 【prod 的退化（预检新发现）】两个输入不相交时 prod 恒为 0：
      C 的内部值 = {A:0.0, B:0.81, C:0.81, D:0.0}（P、Q 有共享成员 B、C）
      D 的内部值 = {CMP_1:0.0, X:0.0, Y:0.0, Z:0.0}（P={CMP_1} 与 Q=R 无共享成员）
      但【成员边照样建立】=> 级联不受影响（级联用的是边，不是内部值）
```

---

## §2 预注册参数（跑前定死，跑后不改）

```text
语料：E1 的 26 字母链（与 E42 同），v2 实例
三个模式（与预检一致）：
  P = {A:1.0, B:0.9, C:0.81}      Q = {C:1.0, B:0.9, D:0.9}      R = {Z:1.0, Y:0.9, X:0.81}
组合：C = coactivate(P, Q)；D = coactivate({C: 1.0}, R)

【rule 口径】
  主口径 = keep（全 1.0）—— 因为 prod 在【不相交输入】下退化（内部值全 0，无信息量，见 §1(6)）
  辅助口径 = prod —— 记录其退化结果（D 的内部值恒 0）
  【关键】P1/P2/P3 三条判据与 rule【无关】（P1 判成员集、P2/P3 判级联），
    实测两口径下级联结果逐位相同 => rule 的选择不影响任何判据。

【刻意设计：避免触发 B35】本实验四个符号的成员集互不相同：
  CMP_1 = {A,B,C,D} | CMP_2 = {CMP_1,X,Y,Z} | CMP_3 = {X,Y,Z,M,N,L} | CMP_4 = {CMP_1,CMP_3}
  => 不触发 last-write-wins（即：本实验故意选择不重叠的成员集）

【阈值（预设，跑后不改）】
  P3 衰减律：|实际值 − 0.9^d| ≤ 1e-12
  P2 级联：CMP_1 与 C 的全部 4 个成员都必须出现在 activate(D) 里（阈值：各 > 0）
  容差：1e-12
运行环境：PYTHONHASHSEED = 0
```

---

## §3 判据（预注册）

### 3.0 判据分层（按 E47 规则：**先判输入层**）

```text
· 输入层判据（先判，决定实验是否成立）：P1
· 输出层判据（实质）：P2 级联 / P3 衰减律
· 结构判据：P4 层级不膨胀 / P5 B35 不触发（刻意设计）
· 形式审计：P0 前置 / P6 只读
· 双向：P7
【顺序】P1 不成立 => 同 E47 处理（可行但无新内容，不立条目、不写后续判据）。
```

### P1 输入层：**出现新量**（先判；本实验的成立条件）

```text
判据：传入 combine 的输入里含【至少一个 L5 符号】—— 即存在节点 n 使得
  n 属于这次 combine 的输入键集，且 n 属于 L5.high_nodes 但【不属于 L0 节点集】。
预检预期：D 的输入 = {CMP_1} 并 keys(R)，其中 CMP_1 属于 L5 且不属于 L0 => 成立。
报告：输入的键集 + 其中哪些是 L5 符号（并列出它们不属于 L0 的证据）。
【为什么这是 E48 的成立条件】E47 证明"投影拼接"无新量；E48 的非投影性正体现在这里。
```

### P2 输出层：级联可分（两跳）

```text
【"出现"的精确定义】= key 出现在 activate(D) 的输出字典里，【且对应值 > 0】
  （不是"只看 key 不看 value"）

判据：activate(D) 的输出里，【CMP_1】与【C 的全部 4 个成员 A/B/C/D】都满足上述定义。
预检预期：CMP_1=0.9、A/B/C/D=0.81（两跳：D -> CMP_1 -> C 的成员）
报告①：activate(D) 的完整分布 + 逐节点对照（含一跳层 X/Y/Z=0.9）。
报告②【供 P3 对照，不判定】：CMP_1 与 A/B/C/D 的【实际值】——
  若 P2 过、P3 挂 => 诊断 = "级联发生但强度不对"；
  若 P2 挂      => 诊断 = "级联未发生"。
【与 P3 的分工】P2 判【结构事实】（路径存在、信号传到）；P3 判【定量事实】（值 = 0.9^d）。
  分开后：P2 过而 P3 挂时诊断清楚 = "结构对、强度错"，两条不会同挂而同因。
【注意】这条检验的是"符号作为成员后，激活能穿过它到它自己的成员"—— 即嵌套的**可分性**。
```

### P3 定量关系：`0.9^d`（**标为数学必然**）

```text
对 d = 1（C 的 L0 成员）与 d = 2（D 的 L0 成员）分别取该成员的实际激活值，判据：
  |实际值 − hop_decay^d| ≤ 1e-12（hop_decay = 0.9）
预检预期：d=1 → 0.9；d=2 → 0.81。
【登记措辞】写成"嵌套深度与可及强度的定量关系"，**不写**"系统学会了衰减/递归" ——
  它是 hop_decay 的自然结果（任何 d 跳可达都是 hop_decay^d），不是涌现。
```

### P4 层级不膨胀

```text
D（以及二层嵌套的 D2）都必须是【同一个 L5 实例】的节点 => 不需要新层（L6）。
判据：D ∈ L5.combination_ids()，且 L5 的实例数 = 1（全程只用了一个 ComposeLayer）。
报告：L5 的全部符号 id + 每个符号的成员集。
```

### P5 B35 不触发（**刻意设计**）

```text
判据：本实验用到的全部符号，其成员集两两不同（=> 无 last-write-wins 覆写）。
【设计声明】这是刻意选择：若两个符号成员集相同，B35 会让后者覆写前者，
  使"嵌套"与"覆写"两件事混在一起，归因不清。故本实验【故意选不重叠的成员集】。
报告：各符号的成员集 + 两两比较结果（预期：全不相同）。
```

### P6 只读

```text
八文件逐一实测哈希：synapse_net.py cf99a07a0e60fd4d / v2 ee6df589eb2f6983 / v3 085be73a06f0694c /
  order_layer.py 419d54745a9eb795 / context_layer.py bd029d7bbdc345be / chunk_layer.py 46f5857be0fc7c7e /
  compose_layer.py 81e350497e9e9960 / experiments/exp34_working_memory.py e821f0df7ca165c3
  跑前 = 跑后
L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后。
```

### P7 双向结局

```text
通过（P1 + P2 + P3 + P4 + P5 全过）→ 新 C 条目（见 §4）
不通过 → 如实记录；若 P1 挂（输入仍是投影拼接）→ 同 E47：可行但无新内容，不立条目
【编号纪律】本实验不预分配 FAIL 分支的 B 编号（同 E41/E42/E43）；机制性 FAIL 才在跑完后新立。
```

---

## §4 双向落点（跑前写死）

### 通过 → 新 C 条目

```text
C??：E42 的组合符号可作为模式【再次传入 combine】（**符号身份进入组合，而非它的成员**）；
     嵌套不膨胀层级（D 仍在 L5 同一实例，不需要 L6）；L0 可及强度随嵌套深度按 **0.9^d** 衰减
     （d=1 → 0.9、d=2 → 0.81，实测逐位吻合）。
     范围限定：
       ① 这是 L5 的【层内递归】，不是基底新能力（L0 一字未动，节点/边签名逐位不变）；
       ② 衰减律 0.9^d 是 hop_decay 的**数学必然**，不是涌现；
       ③ 不声称"系统学会了递归"、"等于泛化"、"产生新机制"；
       ④ 语料限于 E1 的 26 字母链；成员集刻意不重叠（避免 B35 覆写）。
```

### 不通过 → 如实记录

```text
不登记。记录哪条挂、实测值多少：
  · P1 挂 → 输入仍是投影拼接 => 同 E47（不立条目）
  · P2/P3 挂 → 级联或衰减律不成立 => 先查实现（L5 的成员边是否建立、hop_decay 是否为 0.9），
    再谈机制（E39 v1 的教训）
```

---

## §5 影响评估

```text
(a) L0/L1/L2/L4 与 v1/v2/v3：一字不动（P6 用哈希证明）；本实验只用一个 ComposeLayer 实例。
(b) E42 / C41：本实验【不改】compose_layer.py，只使用它已有的接口（coactivate 接受任意 dict）。
    这不是"用上了漏洞"，而是 L5 接口的既定语义（"模式"= 任意 dict[str,float]）。
(c) E47 的判定规则：本实验是该规则的第一个"输入层有新量"的正例 ——
    它验证了规则的可判定性（在 E47 上给"否"、在 E48 上给"是"）。
(d) B35：本实验【刻意避开】它的触发条件（成员集不重叠），并在 P5 记录这一点。
(e) C30 闭包：D 的成员 ⊆ L5.high_nodes（CMP_1 是 L5 自己的节点）=> 不越界。
(f) 与 E43 的区别：E43 的负值是【屏障】（改变可达性）；嵌套是【串联衰减】（只降强度）。
```

---

## §6 产物与不做

**产物**

- `experiments/exp48_nested_composition.py` + `outputs/exp48_nested_composition.json`；
- 登记（双向，跑前写死）：通过 → 新 C 条目；不通过 → 如实记录（不预分配 B 编号）；
- **不新增层、不改任何已验收文件**（只用已有的 compose_layer.py 与 v2）。

**必报清单**

```text
① P1：输入的键集 + 其中哪些是 L5 符号（含"不属于 L0"的证据）
② P2：activate(D) 的完整激活分布 + 逐节点对照
③ P3：d=1 与 d=2 的实测值 + 与 0.9^d 的差
④ P4：L5 的全部符号 id + 成员集 + 实例数
⑤ P5：各符号成员集的两两比较结果
⑥ 辅助：rule = prod / keep 两口径的级联对照（预期逐位相同）
⑦ P6：八文件哈希（前后）+ L0 签名（前后）
```

**不做**

- 不改 `compose_layer.py`、不改 L0/L1/L2/L4、不改 v1/v2/v3、不新增层；
- **不做"成员集重叠"的嵌套**（那会触发 B35，归因不清）；
- 不声称"系统学会了递归 / 泛化 / 新机制"（衰减律是数学必然）；
- 不做三层以上嵌套的穷举（本实验只验 d=1 与 d=2 + 一个二层结构样例）；
- 预注册阈值跑后不改（P3 的 1e-12、P2 的"各 > 0"）。

---

## §7 已知陷阱（逐条对应到判据）

```text
① 重检索陷阱（E33 v1）  -> P1：必须证明输入层出现【新量】（符号本身），不是成员的回读
② 恒等式陷阱（E38）      -> P2：级联必须真的穿过 CMP_1 到它的成员（两跳），不是一步到位
③ B35 撞车              -> P5：成员集刻意不重叠（已声明为设计选择）
④ 闭包 C30              -> P4 + §5(e)：D 的成员 ⊆ L5.high_nodes
⑤ 单次漂亮数字（纪律9）  -> 全确定性（无随机量）=> 不涉
⑥ 命名自检              -> §0.4（撞车实测表；nest 弃用）
⑦ 只读                  -> P6（八文件 + L0 签名）
```
