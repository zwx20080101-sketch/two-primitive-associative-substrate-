# P5 设计：S1–S3 接入网页玩具（接口先定死）

状态：**设计稿，待拍板；未动手**。日期：2026-09-11。
依赖：E24（S1 模块划分）、E25（S2 模块内动力学）、E26（S3 模块间协调）
+ 现有 `playground.html`（单文件静态页，L1/L2/L4 三个经验层开关）。

---

## 0. 三个接口问题（本文档的核心）

### 决定一：S1/S2/S3 在网页里是"读出面板"，不是"经验层开关"

这是本设计最重要的一条，因为**它决定了网页会不会说谎**。

| 类型 | 现有 | 勾选后的效果 | 会不会改变实验结果 |
| --- | --- | --- | --- |
| **经验层** | L1 方向 / L2 三元组 / L4 组块 | 从事件里**学**方向/三元组/组块 | **会**（它们写自己的层） |
| **读出面板** | S1 / S2 / S3（新增） | 只对已有统计做**分析**，不学、不写 | **不会**（必须逐字节一致） |

所以 S1–S3 放在**独立的"分析面板"区**，与 L1/L2/L4 视觉上分开，面板标题写明
"只读分析：开关只影响显示，不改变实验"。

**验收硬线（V2）**：勾选/取消 S1–S3 前后，网络状态与激活结果**逐字节一致**
（沿用 E25 的 P7 只读检查思路）。

### 决定二：三个面板各显示什么

每个面板只显示它那个实验的**实测量**，不叠加解释：

| 面板 | 显示 | 数据来源 |
| --- | --- | --- |
| **S1 模块分化** | 节点按模块着色 + 模块×模块块均值热图 + 划分稳定性 ARI（+ 有真值语料时与真值的 ARI） | `outputs/exp24_module_differentiation.json` |
| **S2 模块内动力学** | 每模块盆地（低/高占比）+ 滞回示意（由记录的两个转变点与宽度重建）+ 联合二值模式数 | `outputs/exp25_module_dynamics.json` |
| **S3 模块间协调** | EI_int 剂量曲线（在记录点之间切换）+ "零耦合/消融 = 精确 0"基准线 + 经验曲线 | `outputs/exp26_module_coordination.json` |

**每个面板底部必须标注数据来源到 commit**，例如：

```text
数据来源：E26，commit b5fdce7，tag v0.9.4-e26，outputs/exp26_module_coordination.json
```

只有"某个版本的某个数"不够——必须能追溯到具体快照。这条与 V1（逐字段一致）配合，
才构成真正的可复现。

**已知局限（如实标注）**：E25 快照里只存了转变点与滞回宽度，**没有存完整的 I 扫描轨迹**，
所以 S2 面板画的是"由转变点重建的滞回示意"，不是原始轨迹，面板上必须写明。
若要真实曲线，需扩展 E25 快照格式（另立一轮，不在 P5a 内）。

### 决定三：数据来源 = JSON 回放 + 一致性自检；**不在浏览器里重算 EI**

理由（这条是从 B17 直接学来的）：

1. 精确 EI 需要 2ⁿ×2ⁿ 的中间表（12 节点就是 134 MB，15 节点 8.6 GB），浏览器里
   重算会把"可视化"变成**第二份实现**；两份实现一旦不一致，网页就在说谎——
   B17 正是"两份索引约定不一致，结果假阳性"的教科书案例。
2. 回放冻结 JSON 能保证**网页上每个数字都能追溯到快照**，符合项目底线
   （结论 ≤ 数据支持强度）。

交互只允许"在记录范围内移动"：拖动 w 在 0/10/25/40/65 五个**记录点**之间切换，
允许线性插值显示但标注"记录点"，**不显示外推**。

**一致性自检（页面加载即跑，V4）**：零耦合语料 EI_int 必须为 0（|·|<1e−9）、
剂量响应 `monotone_per_seed` 必须全 true、`min_EI_int_w65` 必须 ≥0.01、
ρ/A​​RI 等关键字段必须存在。任一不过 → 面板顶部红条警告并列出失败项。

**自检失败必须具体到字段**（不能只弹"自检失败"）。输出格式固定为三列：

```text
字段 | 期望 | 实际
exp26: C2_prime_EI_int | |值| < 1e-9 | 1.8e+00
exp26: dose_response.monotone_per_seed | 全 true | [true, false, ...]
```

否则参观者分不清是**数据坏了**还是**网页坏了**。

**可选例外（需先过守卫）**：若用户自建语料且节点数 ≤12，可启用"页内 EI 试算"，
但**必须先通过块对角零自检**才允许显示结果（B17 的网页版）。

### 决定四：w 的交互边界（只许在记录点上切换）

```text
允许：在 w = 0 / 10 / 25 / 40 / 65 五个记录点之间切换（点选或拖动吸附到最近记录点）
禁止：手动输入 w 的值、把曲线外推到记录点之外、把插值得到的中间值当作数据点引用
越界处理：超出 [0, 65] 直接禁用并显示"无记录数据"（不是画一条虚线糊过去）
```

理由：网页是**展示与复现**工具，不是新实验（§4）。插值只允许出现在两个记录点
**之间**的视觉连线上，且必须标注"记录点"。

---

## 1. 规模守卫

EI / 模块分析只在 **12 节点以内**可用（2¹² 精确可算）。页面规则：

- 节点数 >12 → S1–S3 面板禁用，显示原因文案：
  "精确 EI 需要 2ⁿ×2ⁿ 内存，15 节点需 8.6 GB；本面板限 12 节点"；
- 节点数 ≤12 但**没有模块结构**（未载入模块语料）→ 面板显示
  "需要模块语料"，并给一键载入。

---

## 2. 工作量分级（便于决定做到哪一档）

| 档 | 内容 | 代价 | 产出 |
| --- | --- | --- | --- |
| **P5a 最小版（推荐先做）** | 只加"分析面板"区：载入三份 JSON，静态展示划分/盆地+滞回/剂量曲线三张图 + 只读自检 + 数据来源标注 | 小（不触碰现有交互逻辑） | 网页能证明 E24–E26 的结果，可复现、可追溯 |
| **P5b 完整版** | 加"模块实验模式"：C1/C1'/C2'/C3' 语料一键载入；节点着色跟随 S1 划分；S2/S3 面板随语料切换；含规模守卫 | 中（要动渲染与语料装载） | 参观者能自己切语料看三层分析怎么变 |
| **不做** | 在线 EI 重算（除非过守卫）、S1–S3 写回记忆、把 S3 说成"意识/理解" | — | — |

建议：**先做 P5a**，把"网页数字 = 快照数字"这条链跑通；P5b 单独一轮。

---

## 3. P5 的 PASS 判据（预注册）

| # | 判据 | 阈值 |
| --- | --- | --- |
| V1 | 数字一致：页面显示的每个数值与 `outputs/*.json` 逐字段一致 | 误差 0 |
| V2 | 只读：开关 S1–S3 前后，网络状态 + 激活结果逐字节一致 | 一致 |
| V3 | 规模守卫：节点 >12 时 EI 面板禁用并给出原因文案 | 生效 |
| V4 | 自检：零耦合语料页面上 EI_int 显示为 0；`monotone_per_seed` 全 true 才算通过 | 通过 |
| V5 | 无越权措辞：页面不出现"意识/理解/自由能"等越界词 | 0 处 |

**P5a 的范围包含 V1–V5 全部五条**，其中 V2 不许留到 P5b——
P5a 不做这条验收，参观者第一眼就会把"分析面板"和"经验层开关"混为一谈，后面再改就晚了。

**V2 的验收方法（预注册，逐字节 0 差异，不是"接近 0"）**：

```text
1. 载入一个已有实验语料（用 E5：H e l l o ×100，chain 模式）
2. 记录状态签名 S0 = {节点数, 连接数, 每条边的 N 计数矩阵, L1 方向计数, L2 三元组, L4 块}
   再激活一次（种子 H），记录输出向量 A0（节点 → 激活值）
3. 逐个勾选/取消 S1、S2、S3（含全开/全关），每次重算 S、A
4. 断言：对所有组合，S == S0 且 A == A0（数值逐位相等, 差值必须为 0）
5. 页面提供"只读自检（V2）"按钮自动跑这套流程并显示差异条数（期望 0）
```

| # | 判据 | 阈值 |
| --- | --- | --- |
| V1 | 数字一致：页面显示的每个数值与 `outputs/*.json` 逐字段一致（脚本比对） | 误差 0 |
| V6 | 数据来源可追溯：每个面板标注 commit hash + tag | 3/3 面板 |

---

## 4. 边界与不做

- S1–S3 是**读出**，永不写回 L0/L1；页面上任何"分析"都不改变记忆；
- 网页是**展示与复现**工具，不是新实验；不产生新结论；
- 不把网页上的插值点当作实验数据点；
- 不把三个面板拼成"这就是大脑"的叙事（B07）。

---

## 5. 与已有文档的关系

- 现有 `playground.html` 已能复现 E1/E2/E4/E5/E6a/E15/E16/E7/E12/E18/E17/E19；
  本设计只**追加**分析面板，不改这些既有复现的判据；
- 纪律沿用 [NEXT-PLAN.md](./NEXT-PLAN.md) §5 与 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md)
  "写作纪律"（尤其：块对角零自检、信息论量不得为负）。

---

## 6. P5a 实测（2026-09-11 已实现）

实现：`playground.html` 新增"分析面板（只读 · S1/S2/S3）"+ 内嵌快照
`<script id="analysisSnapshot">`；新增校验脚本 `verify_playground.py`。

| 判据 | 结果 |
| --- | --- |
| V1 数字一致 | **28/28 字段**与 `outputs/exp24/25/26.json` 逐字段相等（脚本比对，误差 0） |
| V2 只读 | 真浏览器跑 8 种 S1/S2/S3 组合：**差异条数 = 0**（网络状态与激活向量逐位相同） |
| V3 规模守卫 | 生效（4 节点时提示"≤12"；>12 节点文案已内置） |
| V4 自检 | 页面加载即跑 **11 项字段级检查全 OK**，失败时列出"字段/期望/实际"三列 |
| V5 无越权措辞 | 0 处（脚本扫描"意识/理解/自由能/灵魂/主观体验"） |
| V6 来源可追溯 | 三张卡片底部均标注 commit + tag + 文件（3/3） |
| 运行时 | 无 pageerror、无 console error |

**实现期抓到两个显示错误（登记 B19）**：

1. S1 卡片原本直接算 `assign[i] == truth[i]` 的节点数，得到 **6/15** ——
   但谱聚类输出的**簇编号是任意的**（本数据里 0↔1 互换了），ARI=1.000 并不代表
   逐位相等。修法：先做**多数标签置换匹配**，再数一致节点（现显示 15/15）。
   这是"看起来合理的错数"的又一例，与 B17 同类。
2. 模板里 `\|V\|` 的转义反斜杠直接漏到界面上（显示成 `\|V\|≈0.87`）。

---

## 7. P5b 范围提案（**待拍板；未实现**）

不建议一次做完，分两小步：

### P5b-1：着色跟随 + 语料载入（展示价值）

| 内容 | 约束 |
| --- | --- |
| 新增"模块实验模式"入口：C1/C1'/C2'/C3' 语料一键载入 | **不得**与现有"手动构造事件"入口混用（两条装载路径共存） |
| 节点着色跟随 S1 划分 | **不改**现有 L1/L2/L4 的经验层着色逻辑；渲染管线加一层"着色来源"切换 |
| 规模守卫接到真实节点数 | 由"是否载入模块语料"改为动态读 `state.nodes.size` |

判据：沿用 P5a 的 V1–V6，**追加 V7**——着色切换前后 L0/L1 计数不变（仍是只读）。

### P5b-2：页内试算（科研价值，**可选，甚至可以不做**）

- 仅当 P5b-1 全过后再考虑，且**节点 ≤12**；
- **必须先过"块对角语料 EI_int 必须为 0"自检才允许显示结果**（B17 的网页版）；
- 代价：等于在浏览器里放**第二份 EI 实现**——B17 的教训正是"两份实现不一致，
  网页就在说谎"。除非有强需求，不值得冒这个险。

### 三个候选补丁（与 P5b 解耦，见 [NEXT-PLAN.md](./NEXT-PLAN.md) §4.1）

E25 快照扩展（I 扫描完整轨迹 → S2 面板可画真曲线）、`PYTHONHASHSEED=0` 运行确定性、
方法学小节（B17/B18/B19 合并成论文一节）。
