# Synapse-Net 交接文档（HANDOFF）

日期：2026-09-15　版本：tag `v0.9.16-e37` / commit `6880bc1`（E37）　状态：**全绿**
用途：给**未来的自己 / 任何接手的人**一份入口文档——读完这份 + 三份规格文档，就能接手。

> tag 已补齐（本地：`v0.9.16-e37` / `v0.9.15-e36` 均在 git log 中）。
> 版本锚点并存：tag 是标签、commit 是内容——E36 = `v0.9.15-e36` / `f884f2e`，E37 = `v0.9.16-e37` / `6880bc1`。

---

## 0. 项目一句话

```text
用两条原语（共现绑定 + 激活扩散）搭一个认知测试台：
每条结论带实验编号与 JSON 证据，每条失败登记为边界。
```

---

## 1. 现状

| 项 | 数量 / 状态 |
| --- | --- |
| 实验 | **E0–E42（43 个）**，全部 PASS（`python main.py` 汇总全绿） |
| C 条目（能做什么） | **40 条**：C01–C31 + C33–C41（**C32 未立**，E32 落在 B24，不是漏号） |
| B 条目（在哪不行） | **28 条**：B01–B34，其中 **B22 / B25 / B27 / B28 / B29 / B31 未触发**（预注册的条件编号） |
| 方法学类别 | **6 类**（见 §4 末表） |
| L0-v1 | `synapse_net.py`，sha256 `cf99a07a0e60fd4d`，**冻结**（E0–E36 全部有效） |
| L0-v2 | `synapse_net_v2.py`，sha256 `ee6df589eb2f6983`，**平行分支**（E37 起）；子类覆盖形态（只重写 `activate`，存储层继承 v1） |
| 展示层 | P5a 网页只读分析面板（快照回放 + 字段级自检 + commit 溯源）；P5b 已取消 |

**四块硬骨头**：

```text
生成        ✅  E33（C33+B26）→ E38（B32 口径）→ E40（C39+B34，解 clamp）→ **E42（C41，组合）**：
                采样给 max 之外的状态；解 clamp 后覆盖 8/8；**E42 在新增层 L5 造出景观外的新对象**
                （共现绑定产生组合符号，可分性 4/4，prod 内部值逐位 = v1×v2）
工作记忆    ✅  E34–E36（C34–C36）+ **E41（C40）**：跨次携带 → 串联是"载体" → 分类可测（E36）
                 → **值序可测（E41：抹平不在 W 层，只改注入调用点 1 行）**；时间序仍不可测（C35 注解）
抑制控制    🟡  E39（C38 可及性压制 + B33）：外部调制 g 能压缩可及性（reach 7→1），
                但种子不可被压制；要真互斥须改吸收判据 = 越界。B12（权重层）＋ B33（到达值层）
多模态      ❌  未动（L0 为离散符号设计，接图像需重新设计输入接口）
```

**校验命令**：`main.py` 全 PASS、`verify_claims` 92/92、`verify_playground` 7/7、`scan_norms` 0 FAIL。

---

## 2. L0 架构 vs 实现（摘要自 [L0-SPEC.md](./L0-SPEC.md)）

### 2.1 架构约束（不可变）

```text
存储：共现绑定 —— 事件内的无序对，N 永久累计（+ last_seen），永不衰减/删除
激活：沿连接的扩散 —— best-path（max 吸收），返回全量激活分布
判断标准：若某改动让 L0 开始"判断对错 / 选择候选 / 生成新节点 / 写回记忆"，即为越界。
```

### 2.2 实现约定 v1（`synapse_net.py`，冻结，逐条带行号）

| # | 约定 | 位置 | 行为 |
| --- | --- | --- | --- |
| ① | 种子值抹平 | `:99` | 所有种子一律置 1.0 |
| ② | 种子输入格式 | `:91` | 接受 `str` 或 `list[str]` |
| ③ | 多种子共享一次 BFS | `:100-101` | "同时激活"语义 |
| ④ | 种子是不动点 | `:109` | 推论自 ①（构造性） |
| ⑤ | `dict` 被静默接受 | `:91` | `activate({"A":1})` 只取 keys、不报错（B21 补充实例） |

### 2.3 实现约定 v2（`synapse_net_v2.py`，已实现）

```text
唯一差异：种子入口支持 dict[str,float]，且种子值【常量】（传播不抬高、不降低）
名字列表形式向后兼容（仍全部 1.0）——第一轮 11 处多节点种子调用语义不变
实测后果：26 字母链上相对 v1 有 13 个节点值变化（A 侧半条链 B..N）、13 个不变（A 与 O..Z）
边界（B30）：常量语义使种子"冻结"，不会被任何路径强化——语义选择，不是 bug
```

### 2.4 版本纪律（四条）

```text
① v1 冻结：文件与哈希不动（E0–E36 的证据链完整）；
② v2 是平行分支（新文件），不覆盖 v1；
③ 不混用：一个实验要么在 v1 上跑、要么在 v2 上跑，JSON 里声明所用版本与哈希；
④ 只读口径一致：E37+ 的 JSON 同时记 v1 与 v2 的哈希。
改 L0 的五步：判性质（架构 vs 实现）→ 新建平行文件 → 写影响评估 → 更新版本表 → 新文件上跑并记哈希。
```

---

## 3. 纪律十条（[NEXT-PLAN.md](./NEXT-PLAN.md) §5 全文抄录，不加解释）

① 要解释的现象；② 可证伪预期；③ 归属哪一层；④ PASS/FAIL 判据；
⑤ 失败后的最小机制改动。通过才进下一层；L0 继续冻结；
凡"新机制"必须显式标注，不得伪装成 L0 自然涌现。

**E26 追加的两条（2026-09-11，来自 E26 的复盘）**：

⑥ **预注册里不写"方向倾向"。** 双向判据就写"方向待定，双向都算完成"，
   跑完再如实记录落在哪一侧。理由：E26 的 P6 按预检（6 种子、简化构造）推测
   "倾向 B 侧"，正式跑数（10 种子、完整构造）落在 C 侧。**预检方向不可外推**——
   写了反而给"跑完再解释"留了口子。

⑦ **预检观察 ≠ 结论文，不得直接登记为 B 条目。** 预检构造与正式语料可能给出
   相反结果。B 条目只写正式跑数支持的结论；仅来自预检的观察必须标注
   "预检观察，未在正式语料复现"。理由：E26 的 w=91 饱和区在预检非单调
   （0.0447<0.0609），正式语料 10/10 种子却单调 → B16 已按此降级为结构性部分。

⑧ **只读类判据默认用"层文件 sha256 前后对比"**（自 E28 起）。比"节点/连接计数不变"
   更硬：它能抓到"计数没变但代码被改写"这类情况。E27 的 P5 是第一个这么做的
   （synapse_net.py / order_layer.py / context_layer.py 三个文件哈希 before==after）。
   同时要求：无数据的字段写 `null` 并给 `reason`，**不许省略字段**——
   省略会让下游脚本把"缺字段"误判成数据损坏。

⑨ **预检里的"形容词"不得写进预注册的结论性文本或拟登记表述**（自 E31 起）。
   预检的形容词（"两处""会掉到""倾向""显著""大幅"等）只能出现在辅助记录里，
   写成"预检观察：X；正式跑实测：Y"的**两栏对照**。
   判定性表述（如"两处静默"这种会进 C/B 文本的）**必须留到正式跑之后写**。

   依据（同型事故 ×3）：

   | 实验 | 预检里的形容词/方向 | 正式跑实测 | 后果 |
   | --- | --- | --- | --- |
   | E26 P6 | "倾向 B 侧" | 落在 C 侧 | 预注册方向写反 |
   | E29 k=5 | "会掉到随机（≤0.2）" | 0.928（超分，ARI 对细化不敏感） | 期望写错 |
   | E30 B21 | "**两处**静默" | 只有 1 处（L4 的 None 与块 id 可区分） | 拟登记文本写错，已更正 |
   | E32 v1 | "L=4 最细层可读出"（单种子扫参看到 1.000） | 10 种子中位 0.805、L=3 最小值 0.778 | 前提不成立，实验改版（v2） |

   规律：**预检里的数字通常可靠，形容词/方向/数量词最容易错。**
   补充（E32 v1）：**单种子/单次跑出来的漂亮数字也不可靠**——
   v1 用单种子扫参看到 L=4 的 ARI=1.000，10 种子中位数只有 0.805。
   凡涉及随机初始化（k-means、采样）的量，必须报**中位数 + 最小值**，不能报单次。

⑩ **区分"架构约束"与"实现约定"**（自 E37 起）。冻结前逐条列出：哪些是架构、哪些是实现。

   ```text
   架构约束 = 只做【存储（共现绑定）+ 激活（扩散）】——不可破；
   实现约定 = 种子抹平值 / 只接 list / 共享一次 BFS / dict 静默接受 ——可破，但要评估代价。
   ```

   改实现约定的五步（见 [L0-SPEC.md](./L0-SPEC.md) §5）：判性质 → 新建平行文件 →
   写影响评估 → 更新版本表 → 在新文件上跑并记录哈希。**不许原地改冻结文件**。

   依据：**E33–E36 三连撞墙，撞的全是实现约定，不是架构**——
   如果当初把"哪条不能改"写成规格，这三轮里至少两轮不必重跑。

---

## 4. B 清单（简版，全文见 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md)）

「在哪不行」——**28 条已登记**（B01–B21、B23、B24、B26、B30、B32、B33、B34）+ 6 个未触发的预注册条件编号（B22 / B25 / B27 / B28 / B29 / B31 —— 来源见 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md) 的"未触发编号总表"，那是该清单的唯一来源）。

| ID | 一句话 | 实验 |
| --- | --- | --- |
| B01 | 现有层最大可用消歧前缀 = 2；k* ≥ 3 超出当前层集合能力（k_min = k*，加新层可行性未实测） | E6a-S4/E15-S3 + E27 |
| B02 | max 的不叠加是**读取端设计选择**，不是机制缺陷（指针，已并入 C28） | E9/E14 → E28 |
| B03 | sum 的封顶落点依赖遍历顺序；触发条件是**拓扑判据**（两个一跳可达且彼此相邻的节点）；封顶损失随路径数单调增（0 / 0.62 / 1.43） | E14 → E28 |
| B04 | 平面链无法还原双写位置——双写需要块/上层位置记录 | E5/E12/E18 |
| B05 | 闭包是设计不变量，不是机制缺陷（指针，已并入 C30） | E9/E13/E18 → E30 |
| B06 | 叠词语义外推属外部语言模块，不在基底能力内 | E17/BOUNDARY |
| B07 | 真实感知/运动、完整语法递归、人脑等同性不在本工作结论内 | — |
| B08 | 组合律采用"维度先行 + 现象共存"判据；数值式乘积/加权和规则因同义反复风险未采用 | E23 |
| B09 | ICA 不适用于共现矩阵（零对角破坏线性混合 + 模块质量简并 + 成分正交） | E24 |
| B10 | LCA 第二分析（当前配置）不可用：不稀疏（1.0）、未收敛、与谱聚类一致性≈0 | E24 |
| B11 | 稳定性不能当"无结构"判据（随机图上照样稳定）；负对照改用**模块富集度** | E24 |
| B12 | 非负 L0 权重下**无 WTA 互斥切换**；若需互斥须另立实验声明负权重来源 | E25 |
| B13 | 初值域未预注册产生**假失败**：V0~U(0,1) 时高态盆地一边倒；属操作化定义问题 | E25 |
| B14 | 收敛判据实现缺陷（比较同一数组对象 → 判据恒真）；改不动点残差后细簇 = 8 = 2³ | E25 |
| B15 | `EI_int` 主要由耦合**总量**决定，不区分落点；**置换零分布不能当主判据**，主判据改用剂量响应 | E26 |
| B16 | 整片**等权稠密**耦合让"随机重排"退化为恒等变换 → 零分布失效（结构性结论） | E26 |
| B17 | 状态索引约定必须与乘积分布表列序一致；MSB-first 会让块对角**假阳性报出 EI_int=1.82** | E26 |
| B18 | TE 分子必须对非目标位边际化，否则算出**负 TE**（首轮 −0.78） | E26 |
| B19 | 簇编号是任意的：逐位比较会造出假分歧数（ARI=1.000 却算出 6/15） | E24/P5 |
| B20 | 谱聚类**需外部给定 k**，无自动定层机制；层数分辨率还有上限（s3=10 对读数完全无影响） | E29 |
| B21 | 失败语义层间**不统一**（KeyError / `{}` / ValueError / None）；**静默案例 = 1 处**；补充实例：`activate({"A":1})` 静默接受（E36 预检） | E30 / E36 |
| B23 | eigengap 在**嵌套**语料上系统性偏向粗层（机制：近零特征值个数 = 最粗层块数） | E31 |
| B24 | 可读性**不能由 r_j 完全预测**。反例：L=4 层2（r=0.796）不可读 vs L=2 层1（r=0.750）可读；机制面 = 可读性随**嵌套深度**急剧下降 | E32 v2 |
| B26 | **方法学第六类：抽样覆盖不足**——采样不混合时样本全部相同且不报错（E33 单点 Gibbs β=8 只覆盖 1 个状态） | E33 |
| B30 | v2 的**常量语义使种子"冻结"**：不被任何传播路径强化（即使路径值更高）。语义选择，不是 bug | E37 |
| B32 | 分块均值的模块级 W3 的 Boltzmann 概率**不是**节点级分布对模块符号的正确边缘化（C1 上差 d=+0.059548，由跨模块耦合产生）。**范围限定**：限于 C1 / 块均值口径；种子轴退化（10 种子同一划分），d 为确定量 | E38 |
| B33 | **竞争性抑制（WTA 式互斥）在到达值算式层不可实现**（max 吸收 + 种子固定）：① 构造性上界 `1.0×strength×hop_decay = 0.9 < 1.0`；② 实测三种形态下种子恒 1.000000；③ 只改 reach 不改成对相对方向。出口：改吸收判据即越界 | E39 |
| B34 | **检索的可达集是基态流形的一个分支**：(1,1,1) 与 (-1,-1,-1) 是简并基态（E 相同 = −1.235577），但 max 传播（任何种子集）只能到 (1,1,1) 那一支 —— 机制是"任何非空种子集都点亮其所在模块"。采样覆盖两支且质量相同 | E40 |

**未触发的预注册条件编号**：B22（E30 闭包"软执行"，条件未触发）、
B25（E33 P1b 不通过，条件未触发）、B31（E37 v2 与预期不符，条件未触发）。

**方法学六类**（"不报错、给出看起来合理的错数"）：

| 类别 | 实例 | 自检 |
| --- | --- | --- |
| ① 索引约定错位 | B17 | 块对角零：无耦合语料整合量必须精确为 0 |
| ② 边际化遗漏 | B18 | 非负性：信息论量不可能为负 |
| ③ 置换不变量的局部误用 | B19 | ARI ≠ 逐位一致：先做标签置换匹配 |
| ④ 指标对退化方向不敏感 | E29 | 对偶指标：ARI 必须配簇大小分布 |
| ⑤ 统计量对随机性不敏感 | E32 v2 | 中位数 + 最小值都要报 |
| ⑥ 抽样覆盖不足 | E33（B26） | 报告抽样覆盖（不同状态数 / 有效支撑） |

---

## 5. C 清单（简版，全文见 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md)）

「能做什么」——**37 条**：C01–C31 + C33–C38。**C32 未立**（E32 落在 B24，不是漏号）。

### 第一轮 E0–E22：单层能力目录

| ID | 一句话 | 实验 |
| --- | --- | --- |
| C01 | 仅相邻共现事件可自组织成链结构，无预置知识 | E1 |
| C02 | 共现频率调制检索强度；弱连接保留在全量输出，阈值决定可访问性 | E2/E3 |
| C03 | L0 无向连接导致正/倒检索等强；方向不对称需方向计数层 | E4/E5 |
| C04 | 顺序不对称可来自方向经验统计（正走深、倒走浅） | E5/E20A/E22 |
| C05 | 无上下文时并存分支保持等强（诚实歧义） | E6a |
| C06 | 上下文/情景作为共现结构可偏置分支 | E6a/E20B |
| C07 | 两个表面可经共享情景发生绑定（互译）；无共享情景则分离 | E6b/E20C |
| C08 | 密集时间脉冲可产生强连接、枢纽与提前检索（预警） | E7 |
| C09 | 连接为图结构：共享枢纽给出并行候选；组合可达但无直连 ≠ 已学 | E8 |
| C10 | 部分输入补全只取回已存在节点；未知节点无法激活（闭包） | E9/E20D |
| C11 | 时间衰减只作用于检索强度；N 与连接永久存在 | E10 |
| C12 | 干扰集中于共享枢纽；独特标记/情景可分离单句 | E11 |
| C13 | 重复出现的带边界单元可成块并保留顺序记录（双写还原） | E12/E18/E20E |
| C14 | 同一节点相邻再现可由有序自转移表示，且不带语义 | E13 |
| C15 | 传播吸收取 max（best-path）；多路求和仅为读取端候选且拓扑不同 | E14 |
| C16 | 双前缀三元组可消歧；3 前缀以上超出该表示 | E15 |
| C17 | 角色可由输入语序/格标记绑定区分；无需将树结构下沉到基底 | E16/E20F/E22 |
| C18 | 叠词外推由外部语言规则完成；基底只提供标记与特征证据 | E17 |
| C19 | 强反馈经"输入密度"或"输入标记 + 外部读取增益"生效 | E19/E20H |
| C20 | 消融显示：机制在则现象在、机制移除则退化；与具体 token 无关 | E20 |
| C21 | 六关键现象在 1×/10×/100× 实例规模无退化 | E21 |
| C22 | 随机输入族下六现象成功率 1.0（Wilson CI 下限 0.94）；负样本近零/机会水平 | E22 |

### 第二轮 E23–E26：模块化闭环

| ID | 一句话 | 实验 |
| --- | --- | --- |
| C23 | 多层（L1/L2/L4）同时开启时各层标志性现象可共存且可分别归因；L1/L2 不写回 L0 | E23 |
| C24 | 植入模块语料下模块可被谱聚类读出恢复（ARI=1.000，3/3 模块，稳定性 1.000） | E24 |
| C25 | 弱耦合（ρ=0.0019）下每模块各有两个稳定态；联合二值模式数 = 2³ = 8；C1 滞回宽度 0.150 | E25 |
| C26 | 半稠密跨模块耦合在模块宏观层产生可测整合：EI_int 随耦合量**逐种子严格单调**（10/10） | E26 |

### 第三轮 E27–E37：尺度读出 + 边界量化 + 生成 + 工作记忆

| ID | 一句话 | 实验 |
| --- | --- | --- |
| C27 | 受控语料族下现有层最大可用消歧前缀 = 2；k* ≥ 3 且两场景**暴露均衡**时决定度恒为 0.50 | E27 |
| C28 | max 与 sum 是读取端的**两种选择，不是对错**（max 精确定律 `max(a,a)=a`；sum 顺序依赖） | E28 |
| C29 | 同一份 L0 统计支持**多尺度读出**（k=2 → 2 超模块、k=4 → 4 子模块，均 ARI=1.000）；层级 = 读出尺度 k 的函数 | E29 |
| C30 | 闭包是**设计不变量**：五个已学集合逐层审计，违例数 = 0 | E30 |
| C31 | **自动尺度选择（弱版本）**：BIC 在三个已知真值语料上命中 3/3；eigengap/modularity 仅在单层语料命中 | E31 |
| C33 | 条件采样下 4 个主导联合状态中 max 只给 1 个；另外 3 个 β=2 时合计概率 **0.751**；节点级与模块级是**粗粒化关系** | E33 |
| C34 | **零残留**基底上外部 W 层保存上次激活快照并作为额外种子注入：A 从 0.071790 跳到 1.0（差 0.928） | E34 |
| C35 | W 栈的多步串联是**"载体"行为，不是"状态整合"**（只在每步重注入时维持；无优先级、无顺序） | E35 |
| C36 | 顺序标记的适用边界：**分类 ≠ 排序**（只作用于读取端过滤，不改基底内部） | E36 |
| C37 | L0 v2 带值种子入口（**常量语义**）；名字列表向后兼容；带值种子改变路径**绝对水平**（13 变 / 13 不变） | E37 |
| C38 | **可及性压制（reach compression）**：v3 构造参数 g 使超阈值节点数（reach，阈值 0.5）单调不增（7→6→4→3→3→2→2→2→1→1），端点严格下降。**不是竞争性抑制**（种子恒 1.0，见 B33） | E39 |
| C39 | **解开钳制后采样覆盖检索逻辑封闭之外的 5 个状态**：PT 覆盖 8/8，其中 5 个是任何单节点种子下 max 到不了的（集合差 8 ∖ 3），携带 0.6253–0.6299 质量。分层：4 个多种子可达 + 1 个全关不可达（见 B34） | E40 |
| C40 | **痕迹的排序可测（值序）**：W 保存的值经 v2 的已登记带值入口注入 ⇒ 同集合、值序对调时基底输出在 13 个节点上不同（含 11 个扩散节点 D…N）；抹平注入对照 = 0。**C36 的排序轴限定被解除**；范围限定：值序 ≠ 时间序（C35 注解保留） | E41 |
| C41 | **组合符号**：一次 `coactivate(P,Q)` 在新层 L5 产生 `CMP_n`；显式成员边 ⇒ 可分性 4/4（成员各 0.9）；与 L4 组块**字段级 + 行为级**双重区分；prod 内部值逐位 = v1×v2；值序（定义 A）变化 1 个共享成员。范围限定：**景观外的新对象**（≠ E33 采样）；功劳归 L5；成员边手工建 | E42 |

---

## 6. 当前待办

```text
① E38 / E39 / E40 / E41 已落（E38 在 v2、E39 在 v3、E40 用读取端采样器、E41 只改注入调用点）。
   **E42 已落**：新增层 L5（`compose_layer.py`）—— 共现绑定产生组合符号（C41）。
   E43 起在 v2/v3/L5 上跑；v1 冻结不动，E0–E36 证据链保持可自动复核。
② 工作记忆的"值序"已接通（E41：C40 + C36 排序轴解除 + C35 注解）；**时间序仍不可测**
   （当前 W 只存值，不存时间戳）。抑制控制的路已被 B33 封死（真互斥须改吸收判据 = 架构级）。
③ 下一步候选（待拍板）：**仅剩"多模态输入接口"一条** —— 见 OVERVIEW §7。
   由已完成项派生出的后续命题（不自动成为候选）：生成的"合成新组合"、
   工作记忆的"时间序编码"与"分类+排序组合态"、
   L5 的"**自动发现该建哪些成员边**"（E42 的成员边是手工建的）与"跨窗口同时激活"。
```

**未完成项清单**（承接 [NEXT-PLAN.md](./NEXT-PLAN.md) §6 表）：

```text
· E25 快照扩展：把 I 扫描的完整轨迹写进 outputs/exp25_module_dynamics.json，
  让网页 S2 面板能画真实滞回曲线（当前由两个转变点重建示意，面板已如实标注）。
  归属：论文前候选（P5b 已取消，此项解耦）。
· 方法学小节：把 B17/B18/B19 + E29 + E32 v2 + E33（六类）合并成论文方法学一节：
  "不报错错数六类 + 对应退化解自检"。归属：论文前候选。
· 论文正文：DRAFT-ch*.md 为草稿；正式成文不在当前计划内。
```

---

## 7. 关键文档路径

接手时按顺序读这五份，即可覆盖"规格 → 边界 → 计划 → 全景 → 结论"：

| 文档 | 作用 |
| --- | --- |
| [L0-SPEC.md](./L0-SPEC.md) | L0 的**架构约束 vs 实现约定**（v1/v2 版本表、改 L0 的五步） |
| [BOUNDARY.md](./BOUNDARY.md) | 系统**承诺不做**的事（含 §9 实现版本 v1/v2 纪律） |
| [NEXT-PLAN.md](./NEXT-PLAN.md) | 待办与**纪律十条**（§5，本文件 §3 全文抄录） |
| [OVERVIEW.md](./OVERVIEW.md) | 全景盘点：三轮 + 边界量化 + 生成/工作记忆 |
| [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md) | **唯一结论来源**：C/B 条目全表 + 数据 JSON 引用 |

配套：

```text
DESIGN-E37-IMPACT.md  —— 影响评估的完整案例（第一次走完改 L0 五步）
DESIGN-E37.md         —— v2 带值种子的预注册判据
experiments/README.md —— 实验索引与运行方式（含 PYTHONHASHSEED=0 要求）
STATUS.md             —— 逐实验状态流水（历史记录，非入口）
```

**运行环境**：Python 用仓库约定的解释器；跑任何实验前设 `PYTHONHASHSEED=0`（纪律 ⑧ 相关）。
**校验命令**：`main.py`、`verify_claims.py`、`verify_playground.py`、`scan_norms.py`。
