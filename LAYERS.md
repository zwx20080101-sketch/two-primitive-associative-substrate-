# Synapse-Net 层次规划 (Layers)

## 总原则

1. **L0 冻结**: `synapse_net.py` 是 V0 参考实现, 冻结于 2026-09-06, 永不再修改。
   它只有两条公理: 共现绑定(永久 N) + 激活扩散(对称)。
2. **每次只加一层**: 一层 = 一个最小机制 + 一个实验。先写预期, 再跑, 失败就冻结现场,
   记录现象, 再决定最小改动。
3. **上层不写回下层**: 上层可以观测、过滤、组合 L0/L1 的输出, 绝不改动下层的记忆。
4. **顺序从不预设**: 任何方向/结构都必须从经验统计里长出来, 不是代码里写死
   "ABCD 就应该这样"。

## 层次清单

| 层 | 名称 | 内容 | 状态 |
| --- | --- | --- | --- |
| L0 | 共现基底 | 共现绑定 + 对称激活扩散 (`synapse_net.py`) | **冻结** (V0) |
| L1 | 方向/顺序层 | 相邻到达分向计数 + 方向强度公式 + 有序自转移(重复标记) (`order_layer.py`) | 已建, E5/E13 验证 |
| L2 | 上下文/三元组层 | 双前缀 -> 下一个 的滑动相邻三元组计数 (`context_layer.py`) | 已建, E15 验证 |
| L3 | 时间新鲜度 | λ·Δt 检索衰减 (旧经验弱但永存) | 候选, 实验驱动 |
| L4 | 组块/整词 | HELLO 作为整块 + 双写位置还原 (`chunk_layer.py`) | 已建, E12 验证 |
| L5 | 读取阈值 | 注意力/梦境开关 | 现为纯函数工具, 是否成层待定 |

候选新层（外部建议 + 自查缺口，**均未实现**；命名用 S 前缀以避开 L 编号）：

| 层 | 名称 | 内容 | 状态 |
| --- | --- | --- | --- |
| S1 | 模块分化层 | 从 L0 统计中分出可分离模块；**谱聚类读出已验证**，ICA/LCA 均失败（B09/B10） | 读侧已验证（E24） |
| S2 | 模块内动力学层 | 平均场 / Hopfield / Boltzmann，模块级双稳态 | 候选，见 NEXT-PLAN.md（E25） |
| S3 | 模块间协调层 | 变分自由能 / 有效信息（EI），模块间预测与协调 | 候选，见 NEXT-PLAN.md（E26） |
| — | 组合律 | L1+L2+L4 同时返回数据如何合成一次输出 | **已实现（exp23_composition，7/7 PASS）** |

上表之后的候选(情绪调制、多模态等)一律等前面某一层出现明确失败现象再讨论。

## 当前结论 (截至 E5)

已建立的结论:

1. L0 两条公理可运行: 只靠共现能长出相邻结构(E1)、累计频率(E2)、沿结构扩散(E1/E4);
   没有预置字母、没有完整图污染——前提是事件按"相邻对"或带上下文的结构喂入。
2. "记没记住" 与 "想不想得起" 是两件事: 弱连接永远在 L0 全量输出里,
   只有读取阈值决定它是否可见(E2/E3); 阈值本身不改底层(E3)。
3. L0 是纯对称的: 正走、倒走一样容易(E4)。顺序/流畅度不对称不存在于 L0,
   这是设计结果, 不是缺陷。
4. L1 方向层用"经验统计"造出不对称: 顺向次数高 -> 每跳只损耗 hop_decay;
   反向次数为 0 -> 只有 floor 保底, 信号迅速耗尽(E5)。人类"能 Hello 也难 olleh"
   这种费力感, 由 L1 的统计强度解释, 不是代码写死方向。

仍未解决(也是后面每个实验必须点名回答的问题):

- 分叉点的"下一步"如何确定: 只给 A,B 时 C、D 都亮(诚实歧义); 给上下文后能否偏向其一?
- 意义聚类/翻译: 两个完全不同表面的序列(H..o 与 你..好)如何通过共享情景绑成"同一个东西";
- 新旧程度: 同样 N, 更久没发生的经验检索是否变弱但永存(λ 至今未激活, 未测);
- 部分输入补全: 给 A 和 C(隔一个缺口), B 是否被补出;
- 整词/组块: HELLO 的双写 l、以词为单位回忆(未测);
- 多条词互相挤占(干扰): 共享字母的词多了之后, 单条经验还能否干净取回。

## 实验设计红线 (E6 起)

每个实验在跑之前必须写清: ① 要解释的现象; ② 可证伪的预期; ③ 测的是哪一层;
④ PASS/FAIL 判据; ⑤ 失败后要加的最小机制是什么。禁止"为了跑而跑"。

### 无中生有闭包 (硬判据, 全部实验适用)

底层不负责想象, 只负责连接与传播:

- 激活的输出节点集合必须 ⊆ 曾经 learn 过的节点集合;
- 对从未学过的标签 activate() 必须失败(实现里直接 KeyError), 底层不可能凭空造出零件;
- "补全/预测" 本质是沿已有路径取回已有节点(如 A,B 唤起记忆里的 C), 不是发明 C;
- "想象/新颖性" 只允许是旧零件的组合(神话生物 = 见过的头+身体+翅膀),
  组合由外部模块渲染; 若组合要成为新记忆, 必须作为一次新经验再 learn 一次;
- 恐惧、笔画、画面等都是外部模块消费激活的结果, 不是底层储存的属性。

### 关键: "同步注入"会改变问题本身, 不能混在一个实验里

旧设计(ABC x100 + ABD x100 后问 C 还是 D)把两件事搅在一起:

- 若 ABC 与 ABD **同时同窗**注入: 共现规则会把它们绑成同一坨经验,
  预期行为是"合并/同义", 而不是"二选一"——问它选 C 还是 D 就是问错问题;
- 若 ABC 与 ABD **不同时刻/不同情景**注入: 才存在真正的"哪个结尾跟上来"的竞争。

因此拆成两个独立实验:

#### E6a 分叉/上下文 (要解释: 下一步预测是否依赖上下文)

```text
情景 X1: [X1, A, B, C] x100     情景 X2: [X2, A, B, D] x100   (两情景互不共现)
对照组: [A, B, C] x100 + [A, B, D] x100, 无 X
```

- 无上下文对照组: 预期 A,B 处 C 与 D 等强 -> "系统诚实歧义", 不硬选;
- 有上下文: 预期 X1 下 C 占优、X2 下 D 占优(先测 L0 全事件绑定
  是否靠"与情景的距离"就足够; 再测 L1 相邻顺序是否在 B 处仍对称);
- 若 L0+L1 在 X1/A,B 下仍无法偏向 C -> 记录失败, 最小改动候选 = L2 上下文层。

#### E6b 同义/翻译 (要解释: 语义=拓扑位置, 表面不同也能是一件事)

```text
情景 S(问候):  [S, P, Q, R] x100     (PQR = "Hello" 的表面)
情景 S(问候):  [S, X, Y, Z] x100     (XYZ = "你好" 的表面)
两个序列永不直接同窗出现, 只共享 S
```

- 预期: S 成为枢纽, 激活 P 能经 S 取到 X/Y/Z(互译); 去掉 S 后两簇分离;
- 表面不融合: 因为两串 token 从未同窗共现, 只有 S 把它们连起来——
  这正对应"人看到两个人同时说 Hello 和 你好, 连起来的是情景, 不是把两句话搅成一个词"。

### 后续实验与它们要解决的问题 (从属于 LAYERS 表格)

| 实验 | 解决什么问题 | 失败后的最小机制 |
| --- | --- | --- |
| E6a | 分叉点预测能否上下文条件化 | 已实现(exp6_fork_context, 四子场景) |
| E6b | 翻译/同义是否靠共享情景聚类 | 已实现(exp6b_translation_synonym, 三场景) |
| E7 | 密集瞬间: 一次摔倒=时间轴上的脉冲流, 密度能否造出强连接/枢纽/预警 | 已实现(exp7_dense_episode) |
| E8 | 连接是图不是线: 共享枢纽能否支撑多条句子 | 已实现(exp8_hub_graph) |
| E9 | 部分输入补全(缺口回填) | 已实现(exp9_partial_completion) |
| E10 | 新旧程度(同样 N, 更久没见) | 已实现(exp10_recency_lambda, 激活 L0 现有 λ 参数) |
| E11 | 多词干扰下能否干净取回 | 已实现(exp11_interference) |
| E12 | 整词/双写 l/以词为单位回忆与嵌套 | 已实现(exp12_chunk_nesting) |

完整状态与计划见 [STATUS.md](./STATUS.md)。

## L1 方向/顺序层 (已建)

数据: `directed_count[(u, v)]` = 事件中 u 紧挨着先于 v 出现的次数。
与 L0 的 `N_ab`(无向共现次数)分开存, 两者不互相污染。

公式:

```text
raw(u->v)      = directed_count[(u, v)] + floor      # floor 默认 1: 弱可逆保底
strength(u->v) = raw(u->v) / max_{w 与 u 相邻} raw(u->w)
arrival        = a(u) x strength(u->v) x hop_decay
```

性质:

- 顺向(真实发生过的方向): 次数高 -> strength 接近 1 -> 每跳只损耗 hop_decay;
- 反向(没发生过的方向): 只有 floor 保底 -> strength 极小 -> 信号迅速耗尽, 想倒背要低阈值/多次提起;
- 若两个方向都发生过(n(u->v) 与 n(v->u) 各自计数), 各自按真实次数传播, 不假定相等。

实验: `exp5_direction_hello` (HELLO 正走 vs 倒走)。
