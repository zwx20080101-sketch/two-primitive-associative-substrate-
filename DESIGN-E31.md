# E31 设计：自动尺度选择（**弱版本**）—— 准则能否命中植入的 k

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-13。
依赖：E24 的谱聚类读出、E29 的多尺度读出语料与结论、E30 的只读纪律、
NEXT-PLAN §5 纪律 ⑨（预检形容词不得进结论文本）。
**有 numpy 依赖；不改任何层文件。**

---

## 0. 三件事的拍板（本文档的核心）

### 决定一：只做**弱版本**（在有真值的植入语料上测命中）

```text
弱版本（本实验）：语料是我们植入的，"正确的 k" 已知 → 看自动准则能否命中。
                  它能证明的是"在已知正确答案的语料上，工具能命中"。
强版本（不在本次范围）：在没有明确正确答案的语料上，准则给出的 k 是否稳定、可复现、
                  有独立依据 —— 那需要先定义"稳定"和"依据"，留 E32+。
```

**范围声明写在最前面**：E31 **不是**"自动发现层级"，是"**在已知真值上检验准则的命中率**"。
任何把弱版本结果说成"系统能自己找出层数"的表述都越界（B20 已把自动层级发现划到范围外）。

### 决定二：**严格命中**（k̂ == 主真值），"命中粗层"单列记录、不算命中

```text
命中     = k̂ 精确等于该语料的【主真值】（最细的植入层）
命中粗层 = k̂ 等于该语料的【粗层】（若有），单列记录，不算命中、也不算失败
未命中   = 其余
```

理由：E29 语料有**两个合法尺度**（k=2 超模块、k=4 子模块）。若把"命中粗层"也算命中，
判据就滑了（用户定：严格）。但同时必须把粗层单列——否则会把"命中了另一个合法层"
误报成"完全没命中"。

### 决定三：**必须有对照语料**（≥2 个植入结构不同）

只在 E29 语料上测"准则给出 k=4"没有说服力（可能是巧合）。因此至少三个语料、
主真值各不相同：**k=4 / k=3 / k=5**（见 §2）。
**判据不是"某个语料命中"，而是"某个准则在全部语料上都命中"**——跨语料稳定才算数。

---

## 1. 要解释的现象（宪章四问）

1. **基底参与的步骤**：无。本实验只对**已有的 L0 共现统计**做读出与准则计算。
2. **需要外部模块吗**：不需要。
3. **结论归谁**：归"**读出端的准则**"。准则不是基底能力，也不写回任何层。
4. **做到哪一步算停**：算到"三个准则 × 三个语料的命中矩阵"就停，
   不实现自动层级发现、不做无真值语料上的稳定性论证。

**要解释的现象**：特征值间隙 / 模块度 / BIC 这三个常见准则，
在**已知真值**的植入语料上能否命中主真值？能否跨语料稳定命中？

**可证伪预期**：若某准则在全部三个语料上都命中主真值 → 记 C（弱版本成立）；
若没有任何准则跨语料命中 → 记 B（当前准则不足以自动定尺度）。**两个结局都合格。**

---

## 2. 语料（三个，主真值各不相同）

```text
C1（沿用 E29 语料）：4 子模块 大小 3/3/4/4、强度 90/100/110/120；超模块 (m0,m1)(m2,m3) s2=40
                     主真值 k=4 ；粗层 k=2        （14 节点）
C2（单层语料）    ：3 子模块 大小 4/4/4、强度 90/100/110；无上层耦合
                     主真值 k=3 ；粗层 k=1（退化，不记）  （12 节点）
C3（五子模块语料）：5 子模块 大小 3×5、强度 90/100/110/120/130；超模块 (m0,m1,m2)(m3,m4) s2=40
                     主真值 k=5 ；粗层 k=2        （15 节点）
每个语料的子模块质量（大小×强度）两两不同（沿用 E24 的可辨识性要求）
```

**构造核查（P1b）**：边集合逐边核对 + 质量两两不同 + 用 k=主真值 跑谱聚类，
ARI vs 子模块真值应 ≈1.0（**这是"真值确实可读出"的先决条件**，不是本实验的主判据）。

---

## 3. 三个准则的精确定义（公式写死，跑前不许改）

记 `A` = `impute_diagonal(共现矩阵)`，`D` = diag(行和)，归一化拉普拉斯
`L = I − D^{-1/2} A D^{-1/2}`，特征值 `0 = λ1 ≤ λ2 ≤ … ≤ λn`；扫描范围 `K_max = 8`。

```text
准则 1 特征值间隙（eigengap）：
    k̂ = argmax_{k = 1..K_max} ( λ_{k+1} − λ_k )

准则 2 模块度（modularity）：
    对每个 k 用谱聚类（E24 算法）得到划分 P_k，计算
    Q(P_k) = (1/2m) Σ_ij ( A_ij − d_i d_j / 2m ) δ(c_i, c_j)
    k̂ = argmax_{k = 1..K_max} Q(P_k)

准则 3 BIC（谱嵌入 + k-means 的球形高斯模型，启发式）：
    对每个 k，在谱嵌入（前 k 个特征向量）上跑 k-means，取簇内平方和 SSE_k：
    BIC(k) = n·k·ln(SSE_k / (n·k)) + k·(d_k+1)·ln(n)，  d_k = k（嵌入维度）
    k̂ = argmin_{k = 1..K_max} BIC(k)

并列处理（预注册）：若两个 k 的准则值之差 < 1e-12，取【较小】的 k。
k-means 多种子：每个 k 跑 10 个种子取最优（SSE 最小），与 E24/E29 口径一致。
```

**嵌入维度 d_k = k（跑前写死，已核对 E24 源码）**：

```text
E24 的 spectral 用 V[:, :k] —— 即对【归一化拉普拉斯】L = I − D^(-1/2) A D^(-1/2)
取前 k 个特征向量，喂给 k-means 的就是 k 个坐标 ⇒ d_k = k。

注意（实测核对，纠正一个常见误解）：该矩阵的【第一个特征向量不是常数向量】——
λ1 = 0 对应的向量是 D^(1/2)·1 ∝ sqrt(度)，【携带度信息】（实测：V[:,0] 与 sqrt(d)
逐元素成正比）。常数向量是【非归一化】拉普拉斯 L = D − A 的第一特征向量（实测亦确认）。
所以这里不存在"要跳过的无信息常数坐标"，d_k 不需要减 1。
```

**过分割保护（预注册）**：

```text
k > floor(n/2) 时，BIC(k) 标 degenerate: true，【不参与 argmin】。
理由：该规模下每簇只剩 1–2 个点，SSE → 0 会把 BIC 拉低，从而误选大 k。
n = 该语料节点数（C1=14 / C2=12 / C3=15 → floor(n/2) = 7 / 6 / 7）
```

---

## 4. 记录项与预检纪律

```text
主表（3×3）：准则 × 语料 → k̂ 与 命中/命中粗层/未命中
辅助记录：① JSON 的 eigenvalues 字段 —— 每个语料一份，前 10 个特征值；
          ② JSON 的 criteria_curves 字段 —— 每个准则的完整扫描曲线
          （eigengap 的每个间隙值 / modularity 的 Q(k) / BIC 的 BIC(k)，含 degenerate 标记）
```

**为什么 eigenvalues 必须进 JSON（不是可选项）**：它是判断 eigengap 是否可靠的**直接依据**——
若 λ 在某处断崖式跳变，eigengap 就命中那里；若 λ 平滑过渡，eigengap 的选择本就不稳定。
没有它，eigengap 一旦未命中，就无法区分"准则不好"与"特征值本身没有明显间隙"。
该项不参与 PASS/FAIL，但必须落盘。

**纪律 ⑨ 的应用（写死在设计里）**：本设计**不写**任何"某准则应该会命中/会掉到"的形容词。
预检只用于确认三个准则的实现能跑通（在**与本实验无关的玩具语料**上验证），
**不在目标语料上预跑**——避免把"预检观察"污染成"预期"。
正式跑之后，若需要记录任何观察，按两栏写："预检观察：—；正式跑实测：X"。

---

## 5. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **接口回归**：E24 植入语料重现 ARI=1.000 / 3 模块 / ρ=0.0019 | 通过 | 接口被改坏，先修接口 |
| P1b | **语料构造核查**：三个语料的边集逐边核对 + 质量两两不同 + **k=主真值 时 ARI vs 子模块 ≥0.99** + 三个语料的主真值分别为 **4 / 3 / 5，互不相同（设计先验，非实测）** | 通过 | 语料或可读性有问题，先改语料 |
| P2 | **跨语料命中（主判据）**：至少一个准则在**全部三个语料**上命中主真值（严格相等，见 §0 决定二） | 通过 | 见 P5 双向结局 |
| P3 | （**原 P3「对照有效性」已并入 P1b**——主真值互不相同是**设计先验**，不是实测结果；编号保留不重排，避免引用错位） | — | — |
| P4 | 只读：层文件 sha256 前后一致（纪律 ⑧）+ L0 统计签名前后一致（E30 口径：边集合排序逐项比较） | 通过 | 直接 FAIL |
| P5 | **双向结局**：P2 通过 → 记 C31；无准则跨语料命中 → 记 B22（当前准则不足以自动定尺度） | 两分支都合格 | 不许跑完再挑解释 |
| P6 | 登记：按 P5 落定，文本见 §8 | 通过 | — |
| 辅助 | 3×3 命中矩阵 + 特征值序列 + 三个准则的完整扫描曲线 | 仅记录 | — |

---

## 6. 预注册参数

```text
扫描范围 K_max = 8         k-means 种子 = 10（取 SSE 最小）
并列容差 = 1e-12（取较小 k）  谱聚类 = E24 算法（impute_diagonal → 归一化拉普拉斯 → k-means）
严格命中 = k̂ == 主真值     命中粗层 = 单列记录（不算命中）
三准则 = eigengap / modularity / BIC（公式见 §3，跑前不许改）
只读 = 层文件 sha256 + L0 签名（边集合排序逐项比较）
运行环境：PYTHONHASHSEED = 0
```

---

## 7. 产出物

- `experiments/exp31_auto_scale.py` + `outputs/exp31_auto_scale.json`；
- 通过或失败都登记 CLAIMS-REGISTRY；
- 不改任何层文件；不新增层；不引入语义/stub/奖励；
- **不得**把弱版本结果表述为"系统能自动发现层级"（B20 的边界仍然有效）。

---

## 8. 拟登记的表述（两个分支，跑前写死；纪律 ⑨：不含任何预检形容词）

```text
分支 C（若 P2 通过 —— 某准则在三个语料上都命中主真值）：
C31：在三个植入真值互不相同（k=4 / 3 / 5）的受控语料上，<准则名> 给出的 k̂
     与主真值精确相等（3/3）。**范围限定**：这是在已知真值语料上的"命中检验"，
     不是自动层级发现；无真值语料上的稳定性不在本工作范围（见 B20）。
     （`<准则名>` 是**运行期占位符**：落定 C 时由程序替换为实际跨语料命中的准则，
       同时写入 JSON 的 `registration.C31_resolved`——跑前不预设是哪一个准则。）

分支 B（若 P2 未通过 —— 无准则跨语料命中）：
B22：**在当前这三个语料结构上**（C1 = 4 子模块/2 超模块、C2 = 3 子模块单层、
     C3 = 5 子模块/2 超模块），eigengap / modularity / BIC 三个准则均未能跨语料
     命中主真值（命中矩阵见 outputs/exp31_auto_scale.json）。
     范围限定：本结论只覆盖这三个语料结构 —— **不写成"当前准则不足以自动定尺度"**
     （那跨度太大）；E29 的"k 必须外部给定"（B20）在本实验范围内仍然成立。
```

---

## 9. 边界与不做

- **不做强版本**（无真值语料上的稳定性/依据论证）——留 E32+；
- **不实现自动层级发现**，也不修改任何层；
- **不把"命中"说成"准则理解结构"**：命中只是统计量在已知真值上的表现；
- 不声称任何"概念层级/语义层级"结论（B07）。
