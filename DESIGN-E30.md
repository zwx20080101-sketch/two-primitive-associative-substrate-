# E30 设计：闭包审计（B05 正式化）——"已学集合"与失败语义

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-13。
依赖：L0 `synapse_net` / L1 `order_layer` / L2 `context_layer` / L4 `chunk_layer` 的现有接口。
**纯只读审计，无 numpy 依赖；不改任何层文件**（沿用纪律 ⑧ 的层文件 sha256 检查）。

**为什么重定义 B05**：原来的 B05（"未学过的节点 activate 报 KeyError"）是**一句话实现事实**，
照原样跑一遍等于跑恒等式——B02（`max(a,a)=a`）的教训。E30 把它改成
**闭包审计**：逐层枚举"什么算已学集合"，逐层核对"输出 ⊆ 该集合"，并产出**失败语义表**。

---

## 0. 双向预注册（写在最前面，跑前定死）

```text
结局 A（违例数 = 0）：闭包是【设计不变量】→ 登记为 C（设计选择）。
                      不写成"发现"、不写成经验律。
结局 B（违例数 > 0）：闭包在某层被【软执行】→ 登记为 B22（边界条目），
                      并列出违例明细（层 / 调用 / 输出节点 / 该层已学集合）。

两个结局都在跑前定死；不许跑完再挑解释。

与闭包结局【独立】的另一条（总是登记，编号在前）：
B21（总是登记）= 失败语义在层间不统一。它是确定会发生的事实（预检已见），
                 不依赖 P2 结果；编号排在前，避免"结局 A 成立时编号空着"的缺口。
```

---

## 1. 要解释的现象（宪章四问）

1. **基底参与的步骤**：各层对"不存在"的判定与响应（检索边界，不涉及新机制）。
2. **需要外部模块吗**：不需要。
3. **结论归谁**：归"**各层的接口语义**"。审计是读取端工具，不新增能力。
4. **做到哪一步算停**：算到"逐层闭包核对 + 失败语义表"就停，
   **不修改任何层的报错行为**（那是实现改动，不在本实验范围）。

**要解释的现象**：闭包约束（输出 ⊆ 已学节点）在**每一层**是怎么执行的？
"不存在"的统一性如何？失败的语义是否一致？

**可证伪预期**：若某层的输出出现了该层已学集合之外的节点，则闭包被违反（结局 B）。

---

## 2. "已学集合"的逐层定义（写死，P2 按此逐层核对）

```text
L0 已学集合  = net.nodes                      （网络中存在的节点）
L1 已学集合  = order_layer.nodes              （方向层中存在的节点）
L2 已学集合  = context_layer.counts 的键集合  （已登记的 2-前缀集合）
L4 已学集合  = ① 块节点集合 = chunk_ids()     （spell 的【定义域】）
               ② 块成员集合 = 所有 spell 值域  （块内 token）
               —— 这两个集合必须【分开核对】，不能混：它们不相等（见 §4 辅助 2）
```

---

## 3. 审计口径与失败语义表（字段预注册）

```text
闭包判据：对每个 (层, 调用)，输出集合 ⊆ 该层已学集合 → 违例数 += |输出 \ 已学集合|

失败语义表【字段预注册，5 列】：
  调用 | 输入类型 | 期望语义 | 实测语义 | 是否报错

输入类型枚举（预注册，不跑完再决定记哪些）：
  ① 已学        ② 未学过        ③ 未知前缀（L2）      ④ 参数错误（L2 长度 ≠ 2）

审计语料（固定，不随参数变化）：
  L0/L1/L2：A-B-C × 100        L4：单元 [H,e,l,l,o] × 100
```

---

## 4. 预检结果（已执行，2026-09-13，只读即算即弃）

| 层 | 调用 | 结果 | 语义 |
| --- | --- | --- | --- |
| L0 | `activate(未学过)` | **KeyError** `node not in network` | 硬拒绝 |
| L1 | `activate(未学过)` | **KeyError** `node not in order layer` | 硬拒绝 |
| **L2** | `query([未学, 未学])` | **返回 `{}`** | **静默空返回** |
| L2 | `query([A])`（长度 ≠ 2） | **ValueError** | 参数错误 |
| L4 | `spell(不存在的块)` | **KeyError** `unknown chunk` | 硬拒绝 |
| L4 | `learn_unit([单元素])` | **None，不建块** | 静默忽略 |
| **L4** | `activate("H")`（H 是块成员） | **KeyError** | 硬拒绝（块成员 ≠ 可激活节点） |

**两个非平凡发现（本实验的价值点）**：

1. **失败语义不统一**：KeyError（L0/L1/L4）/ `{}`（L2 未知前缀）/ ValueError（L2 参数）/
   None（L4 未达 K）并存。其中 **L2 对未知前缀返回 `{}` 而不报错**是本实验认定的接口风险：
   调用方若不检查空结果，就无法区分"这个词没学过"与"学过但无候选"。
   这与本项目一贯的主题同源：**不报错，给出一个看起来合理的结果**。
2. **"闭包"必须写明对哪个集合**：L4 的 `spell` 能返回 `H`，但 `activate("H")` 报 KeyError——
   块成员集合 ≠ 可激活节点集合。

> **正式跑对预检的更正（如实记录）**：预检里我写的是"**两处**静默（L2 → `{}`；L4 → None）"，
> 正式跑显示**只成立一处**——L4 的 `None` 是**有信息**的返回（`None` 与块 id 可区分，
> 调用方能判断"没成块"），因此不计入静默。静默案例 = **1 处**（L2 未知前缀）。
> 这条更正已写进 B21 的登记文本。

---

## 5. PASS / FAIL（预注册）

| # | 判据 | 阈值 / 期望 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1 | **接口回归**（沿用 E26/E29 先例）：E24 植入语料跑出 ARI=1.000 / 3 模块 / ρ=0.0019 | 通过 | 接口被改坏，先修接口，不谈闭包 |
| P2 | **闭包审计（逐层）**：按 §2 的五个集合（L0 / L1 / L2 / L4① / L4②）分别核对"输出 ⊆ 该集合" | 见 §0 双向预注册 | 违例 > 0 → 登记 B，并给明细 |
| P3 | **失败语义表完整**：§3 的 5 列 × 输入类型枚举，逐格填满 | 表完整、无空格 | 缺格即 FAIL（不许跑完再决定记哪些列） |
| P4 | （**原 P4 已按评审拆成辅助 1 / 辅助 2**；编号保留不重排，避免引用错位） | — | — |
| P5 | 只读（纪律 ⑧）：层文件 sha256 前后一致 | 通过 | 直接 FAIL |
| P6 | 登记：按 §0 双向结局落定为 **C**（违例数 = 0）或 **B**（违例数 > 0） | 通过 | — |
| 辅助 1 | `auxiliary: true` — L2 未知前缀的返回（**期望：`{}`**，预检已给） | 仅记录 | 即便不符，也只记"预检观察未被正式复现"，**不阻断 P1–P6 的落定** |
| 辅助 2 | `auxiliary: true` — L4 块成员的 activate 响应（**期望：KeyError**，预检已给） | 仅记录 | 同上：不阻断 P1–P6 |

**P2 的核对顺序（预注册）**：**先 L0 / L1 → 再 L2 → 最后 L4 的两个集合分开核对**。
从最简单的层开始逐层往上；某层出现异常时，前面的层已确认干净，可直接定位到该层。

**辅助 1/2 的性质（写清楚）**：它们的期望来自**预检观察**，不是从机制推出来的。
因此它们是**记录项、不是判据**；若正式跑不符，记为"预检观察未被正式复现"，实验照常落定。

---

## 6. 预注册参数

```text
审计语料：A-B-C × 100（L0/L1/L2）；单元 [H,e,l,l,o] × 100（L4）
输入类型枚举：已学 / 未学过 / 未知前缀(L2) / 参数错误(L2 长度 != 2)
失败语义表列：调用 | 输入类型 | 期望语义 | 实测语义 | 是否报错
闭包判据：输出集合 ⊆ 该层已学集合（§2 的五个集合逐个核对）
只读 = 层文件 sha256 前后一致（纪律 ⑧）
运行环境：PYTHONHASHSEED = 0
```

---

## 7. 产出物

- `experiments/exp30_closure_audit.py` + `outputs/exp30_closure_audit.json`；
- 通过或失败都登记 CLAIMS-REGISTRY；
- **不改任何层文件**：本实验只审计报错行为，不修改它（改报错语义是另一个实验的事）；
- 不声称"闭包 = 安全意识/理解"之类的结论（B07）。

---

## 8. 拟登记的表述（**三条各自独立、各自有编号、各自跑前写死**）

跑完不用临时决定登记哪几条——三条的触发条件与文本都已定死：

```text
B21（总是登记，与闭包结局无关）：失败语义在层间不统一——
KeyError（L0/L1/L4 的未学过）、空字典（L2 未知前缀）、ValueError（L2 参数错误）、
None（L4 未达 K）。其中【L2 的未知前缀不报错、返回 {}】是本实验认定的接口风险：
调用方若不检查空结果，就无法区分"这个词没学过"与"学过但无候选"，
与"不报错、给出看起来合理的结果"同族。本实验只记录，不修改报错行为。
（更正：预检曾写"两处静默"，正式跑显示 L4 的 None 是【有信息】的——None 与块 id 可区分，
故不计入静默；静默案例 = 1 处。）

C30（结局 A，仅当 P2 违例数 = 0 时登记）：闭包是【设计不变量】——
在受控语料上逐层审计 L0/L1/L2/L4（五个已学集合），所有输出集合 ⊆ 该层已学集合，
违例数 = 0。"不存在"在层间的表现不统一（KeyError / 空字典 / None / ValueError），
但不产生闭包外节点。

B22（结局 B，仅当 P2 违例数 > 0 时登记）：闭包在某层被【软执行】——
明细：<层 / 调用 / 输出节点 / 该层已学集合>。
```

**编号与独立性说明**：B21 说"失败怎么表现"，C30/B22 说"约束有没有被违反"——
两者独立。把**确定会发生的**（B21）排在前号，结局 A 成立时不会有编号缺口：

```text
结局 A：B21 + C30（两条都落地）
结局 B：B21 + B22（两条都落地）
```

---

## 9. 边界与不做

- **不修改任何层的报错行为**：统一失败语义是一个独立的实现改动（若要做，另立实验）；
- **不做"闭包 = 记忆安全"这类外推**；
- **不把审计工具的能力算作基底能力**：审计是读取端工具；
- 不声称任何"理解/意识"类结论（B07）。
