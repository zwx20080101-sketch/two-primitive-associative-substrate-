# 第 5 章 边界与负结果（中文草稿）

本章集中整理本系统在受控输入族内表现出的能力上限与失效场景。
每条边界对应 CLAIMS-REGISTRY.md 的 B01–B07 登记条目；
观测方式与采集原则见第 3.6 节。

本章状态：**已定稿（2026-09-06），作为正式报告第五章主体**。

## 5.1 结构表示边界

### B01 三前缀之外的长距离上下文不可达

当区分"下一个符号"的信息位于当前符号前第 3 位或更早时
（如事件 [X,A,B,C] 与 [Y,A,B,D] 中 X/Y 的位置），
本系统的 L2 双前缀表示与 L1 相邻方向表示均无法利用该信息：
查询 [A,B] 时 C 与 D 保持等强，未表现出消歧。
本系统仅对"最近两项前缀 → 下一项"的三元组结构进行了验证
（证据：E15，outputs/exp15_higher_order_context.json；
同型限制在 E6a 的 L1 对照中出现，
outputs/exp6_fork_context.json）。
更长的 N 元依赖不在当前表示范围内。
本结论仅针对当前 L1–L2 双层架构；不排除未来通过额外上层表示层
实现长距离依赖。

### B04 平面链无法还原词内重复位置

仅依靠 L0/L1 平面相邻共现链（如 H–e、e–l、l–o），缺少位置记忆，
无法还原词内重复符号的位置：其正向走读只得到 4 个符号；
重复位置信息只能保存在 L4 块的 spell 记录中
（证据：E5 对照与 E12，outputs/exp5_direction_hello.json、
outputs/exp12_chunk_nesting.json）。

## 5.2 传播规则边界

### B02 max 吸收下多路证据不叠加

当两条独立路径会合于同一节点时，在基底 max 吸收规则下，
该节点的激活与单路径到达时相同：双路与单路目标激活均为 0.81
（证据：E14，outputs/exp14_multi_path_evidence.json；
两端种子会合于中间节点的情形见 E9，outputs/exp9_partial_completion.json，
中间节点激活亦为 0.81）。
即"多路佐证"不会在基底检索强度上产生额外增强。
该结论仅针对基底原生 max best-path 传播规则；
求和读法（读取端）存在叠加，但不属于基底原生机制。

### B03 求和读法的遍历拓扑与底层不同

作为读取端候选的求和读法依赖"单次发射、出队冻结、
已发射节点不再接收贡献、封顶 1.0"的护栏才能终止；
其遍历拓扑带访问标记，与基底原始无向图不同，
因此其输出不能与基底 max 传播直接做机制层面的数值对比
（证据：E14，outputs/exp14_multi_path_evidence.json）。

## 5.3 闭包与错误语义

### B05 无法激活从未学习过的符号

当种子集合包含从未进入网络的符号时，激活流程直接报错；
系统仅在已学习节点构成的闭包子图内传播。该约束在
补全、重复标记与拼写读取场景中均被观测到
（证据：E9、E13、E18，outputs/exp9_partial_completion.json、
outputs/exp13_repeat_marker.json、outputs/exp18_spelling_output.json）。
含义：系统不能无中生有地产生新符号节点。

## 5.4 架构归属边界

### B06 规则外推属外部语言模块

叠词外推（如对从未叠过的"人"输出"人人"）由外部语言规则 stub
依据基底提供的重复标记与特征证据完成判定与渲染；
基底本身不发起该规则。输出被"听回"为新经验属于外部驱动逻辑，
不是基底内部闭环动力学
（证据：E17，outputs/exp17_system_reduplication.json）。

### B07 不在本工作结论内的范围

以下内容不属于本工作的结论范围：

- 真实感知与运动接入（输入为给定符号事件流）；
- 完整语法递归与系统性泛化证明（仅验证受限的结构复用）；
- 本工作不作出该机制与人脑、生物神经元系统功能等同的论断；
- Transformer 级规模行为或对比。

上述限制来自系统输入形式与实验范围设定
（见 BOUNDARY.md 与 CLAIMS-REGISTRY.md B07）。

## 5.5 边界观测小结

所有边界观测与正向实验结果，采用同一套预先注册判据、
后执行实验的流程采集。边界条目不是用来掩盖系统缺陷，
而是对该联想机制能力上限、失效场景的形式化记录，
形成一份清晰的改动点清单，为后续上层表示层扩展、
传播规则变体、外部驱动闭环等拓展方向提供锚点。
