# E39 设计：可及性压制（reach compression）—— v3 到达值算式层的外部调制参数

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：L0-SPEC §2 约定 ⑥（到达值算式 = 实现约定，须保留 max 吸收）、E38（只读口径与 v2 字段处理）、
B12（非负权重无 WTA）、E28/C28（max vs sum 的读取端选择）。**v1 与 v2 一字不动；v3 是平行分支。**

---

## 0. 概念基础

### 0.1 本实验引入的"抑制"定义（**本实验引入，非已登记定义**）

```text
本实验引入并使用的定义：
  「若某个机制只改变【有多少节点超过阈值】，而不改变节点之间的【相对强弱次序】，
    则不称其为抑制。」
  抑制的必要条件 = 改变竞争双方的相对地位。
声明：这条定义是本实验为澄清 E39 的命题而引入的，【不是】登记表里的已定义术语。
      它只用于本实验的命名与判据，不追溯适用于 E33–E38。
```

### 0.2 命名裁定：叫"可及性压制"，不叫"抑制控制"

```text
本能力的精确名字 = 可及性压制（reach compression）。
不叫抑制控制（inhibitory control）——后者在脑科学语义里包含"主动压制特定干扰"，
而本能力只改 reach（超阈值节点数）、不改次序（相对强弱不变），不是竞争性抑制。
命名混淆会污染后续所有引用，故在标题、C 条目、B 条目里统一用"可及性压制"。
```

### 0.3 与 B12 的分层关系

```text
B12 说【权重层】：非负 L0 权重造不出赢者通吃，负权重必须从外部来。
E39 测【到达值层】：即使外部负值进入了到达值算式，也不产生互斥（预检实测）。
两层不重复：B12 是"权重从哪来"，E39 是"到达值算式能不能造出压制"。
E39 的结论不回写 B12，而是与它并列（见 §4 的 B33）。
```

---

## 1. 预检结果摘要（2026-09-15，只读；不改任何文件）

### 1.1 三条硬结果

```text
① 到达值层做不到抑制。26 链 seed=A：
     base        A=1.0000 B=0.9000 C=0.8100 D=0.7290 | reach=7
     sub  g=0.05 A=1.0000 B=0.8500 C=0.7150 D=0.5935 | reach=4
     sub  g=0.20 A=1.0000 B=0.7000 C=0.4300 D=0.1870 | reach=2
     gain 0.95   A=1.0000 B=0.8550 C=0.7310 D=0.6250 | reach=5
   三种形态（减法 / 增益 / rival 依赖他节点激活）都只改 reach，不改次序。

② 种子不可被压制（构造性 + 实测）。单跳到达上界 = 1.0 × strength × 0.9 = 0.9 < 1.0，
   任何 ≤0 的抑制项只会更小 → 无法覆盖值为 1.0 的种子。
   实测（C1 两种子，模块 0 与模块 2）：sub g ∈ {0.05, 0.5, 5.0}、gain(1−0.5)/(1−0.9)、
   rival g ∈ {0, 0.1, 0.5, 2, 10} × passes ∈ {1, 3} → 两种子值【全部保持 1.000000】。

③ 次序不变量成立。26 链 seed=A，g 扫 10 点：按值降序（并列按名升序）的节点序列
   在全部 g 上逐位相同 = True。
```

### 1.2 v2 单体方法的约束（**为什么 v3 只能整体复制 activate**）

```text
事实：v2.activate 是【单体方法，没有可注入的钩子】——不存在 _arrival(...) 之类的扩展点。
      文件结构：class SynapseNet(_SynapseNetV1)（synapse_net_v2.py:23），
      只定义 activate（:26，方法体 39 行，文件到 :64 结束）。

后果：子类【无法只替换到达值算式】而不复制整个方法。
  形态 A（本设计采用）：v3 继承 v2，整体复制 activate（39 行）+ 增/改 2–4 行。
  形态 B：继承 v1，需重实现 dict 种子 + 常量语义，代码量更大。
  形态 C（排除）：在 v2 里插一个扩展点 —— 会让 v2 的 sha256 改变，
          破坏 E37/E38 已记录的只读证据（L0-SPEC §5 第 2 步：不许原地改冻结文件）。
```

### 1.3 预检暴露的判据陷阱（**必须在 §3 里避开**）

```text
reach(g) 是【单调不增】，不是【严格递减】—— 存在平台：
  thr=0.5 序列 = [7,6,4,3,3,2,2,2,1,1]（g=0.10/0.15 同为 3；g=0.20/0.30/0.40 同为 2）
把主判据写成"严格递减"会直接 FAIL，且 FAIL 的原因是判据错、不是机制错。

本判据修订留痕：原判据（v1 稿）= "reach(g) 严格递减"；修订后 = "相邻不增 + 端点严格"。
  修订依据 = 预检实测的平台结构（非事后解释）。修订时机 = DESIGN 阶段，跑前。
```

---

## 2. 预注册参数（跑前定死，跑后不改）

```text
语料甲：E1 的 26 字母链（A–Z 相邻对 ×100，26 节点 / 25 边）—— reach 曲线 + 次序不变量
语料乙：E24/E25 的 C1（corpus_planted()，15 节点 / 3 模块）—— 两种子"不可压制"

【g 的定位】v3 的【构造参数】：SynapseNetV3(inhibition=0.0)，与 λ / hop_decay / β 同级。
             不作为 activate 参数 —— 保持 activate 签名兼容（C37 向后兼容契约不破）。

扫描网格：g ∈ {0, 0.02, 0.05, 0.1, 0.15, 0.2, 0.3, 0.4, 0.5, 0.7}（10 点）

reach 阈值：主口径 = 0.5【预设值，需拍板】
  候选依据（预检实测，26 链 seed=A）：
    thr=0.3 → 8 个不同取值，reach(0)=12
    thr=0.4 → 7，reach(0)=9
    thr=0.5 → 6，reach(0)=7        ← 选定
    thr=0.6 → 5，reach(0)=5
    thr=0.7 → 4，reach(0)=4
    thr=0.9 → 2，reach(0)=2
  选 0.5 的两条理由：① 与 E33 的 BIN_THR = 0.5 同口径；② reach(0)=7 留出 6 档下降空间。
  辅助报告：JSON 同时报 thr = 0.3 / 0.5 / 0.7 三条曲线（判定只用 0.5）。

容差：1e-12          运行环境：PYTHONHASHSEED = 0
```

---

## 3. 判据（预注册）

### P0 文件与接口

```text
v1 文件 synapse_net.py     不动：sha256 = cf99a07a0e60fd4d（跑前跑后一致）
v2 文件 synapse_net_v2.py  不动：sha256 = ee6df589eb2f6983（跑前跑后一致）
v3 文件 synapse_net_v3.py  新建：sha256 首记录
继承链：issubclass(SynapseNetV3, SynapseNetV2) 为真（形态 A：从 v2 分支）
activate 签名保持 str | list[str] | dict[str, float]（不改签名）
inhibition 是构造参数，不是 activate 参数
```

### P1 主判据：reach(g) 单调不增 + 端点严格下降

```text
语料甲（26 链），seed = A，reach 阈值 = 0.5，扫描网格见 §2：
  判据甲：reach(g_i) ≥ reach(g_{i+1}) —— 相邻 9 对全部成立
  判据乙：reach(0) > reach(0.7) —— 端点严格下降
端点 = 预注册网格 {0, 0.02, 0.05, 0.1, 0.15, 0.2, 0.3, 0.4, 0.5, 0.7} 的【首末点】。
若将来网格变更，本判据需重定，不得沿用本次结果。
【判据是"不增"，不是"严格递减"】：预检实测存在平台（0.10/0.15 同为 3；0.20/0.30/0.40 同为 2）。
  写成"严格递减"会直接 FAIL，且原因是判据错、不是机制错（见 §1.3）。
报告：完整 reach 序列（10 个点），以及 thr = 0.3 / 0.5 / 0.7 三条曲线。
```

### P2 边界判据：种子不可压制

```text
扫描的每个 g 上，被注入的种子值恒为 1.0（容差 1e-12）：
  · 语料甲：seed = A
  · 语料乙：两种子（模块 0 与模块 2 各一个节点）
【构造性依据】单跳到达上界 = 1.0 × strength × hop_decay = 0.9 < 1.0；
  任何 ≤0 的抑制项只会让到达值更小 → 到达值永远无法覆盖值为 1.0 的种子。
【前提：strength ≤ 1.0 —— 构造性成立，不依赖语料】v1 的 strength(u,v) 定义为
  W_uv / max{ W_uw : w 是 u 的邻居 }（synapse_net.py:129-142），
  分母是【同一节点 u】邻居里的最大权重 → 该比值恒 ≤ 1.0，对任何语料自动成立。
  实测复核：26 链 25 条边 min=max=1.000000；C1 34 条边 min=0.050000 / max=1.000000 —— 均 ≤ 1.0。
  故上界 1.0 × 1.0 × hop_decay = 0.9 < 1.0（hop_decay < 1）在【两个语料上都成立】，
  无需为 C1 单独表述。若将来某语料允许 strength > 1，本条的构造性依据需重述。
【若此条 FAIL（种子被压低）】→ 说明吸收判据被改动过，属越界（L0-SPEC §2 ⑥），
  按 P7 的 FAIL 分支记录，并附诊断：先查 v3 是否保留了 `arrival > acts.get(v, -inf) + EPS` 判据。
```

### P3 次序不变量（**v2 修订为"成对方向一致性"**）

```text
【v1 的 FAIL 记录 —— 保留为负面记录，不丢】
v1 原判据 = "按值降序（并列按名升序）的节点序列在每个 g 上逐位相同"。
  实测：26 链通过；【C1 失败】—— 不同次序数 = 2。
  精确诊断：g=0 处 g0_1 与 g0_2 【精确相等】（0.04050000000000001 == 0.04050000000000001），
    按名字 tie-break 排成 g0_1 在前；而 g>0 时 g0_2 = 0.010461538 > g0_1 = 0.0025，值序相反。
  g=0 处精确并列的对数 = 40；值严格不等的对里出现次序反转的对数 = 【0】；
  g>0 的 9 个点给出的次序【完全相同】。
  即：没有任何严格强弱反转，唯一变化是"精确并列被 tie-break 解出与 g>0 相反的方向"。
  v1 的 FAIL JSON 归档：outputs/exp39_reach_compression_v1_FAIL.json

【v2 判据 —— 成对方向一致性（可执行口径）】
  精确并列定义：|v_i − v_j| ≤ 1e-12        严格不等定义：|v_i − v_j| > 1e-12
  对每一对节点 (i, j)：
    · 若在所有 g 上该对都是精确并列 → 该对【不计入】
    · 若某 g 上该对精确并列、其他 g 上严格不等 → 该 g【不计入该对】
    · 该对在每个"严格不等"的 g 上给出的方向，必须【全部一致】
  判据：所有对在所有 g 上的方向一致（无反转）。语料甲（seed=A）与语料乙（两种子）各测一次。

【必报三项 —— **每一项都必须按语料分别报**（这是唯一可能读错的地方）】
                    26 字母链（325 对）        C1（105 对）
  ① 全程精确并列的对数      0                        38
  ② 至少一个 g 上并列的对数  0                        40
  ③ 并列出现的 g 点集合      空集                     全部 10 个网格点

  26 链的 0 是预期的：seed=A 的激活值 = 1.0, 0.9, 0.81, …, 0.9²⁵，全互异 → 不可能并列。
  C1 的 38/40 来自模块内对称节点（如 g1_1..g1_4 同值 0.9、g2_1..g2_5 同值 0.9）
  与两个同值种子（g1_0 = g2_0 = 1.0）。示例：(g1_0,g2_0)=1.0、(g1_1,g1_2)=0.9、(g0_1,g0_3)=0.0405。
  注：v1 的"40 对"是 g=0 这一点的并列【对数】，与 ③（g 点集合）不是同一个量。

【判据修订留痕】原判据（v1）= "序列逐位相等"；修订后（v2）= "成对方向一致性"。
  修订依据 = v1 的正式跑数（40 对精确并列 + 0 反转）。修订时机 = v1 FAIL 之后、v2 跑之前。
  修订理由 = 原判据把"相对强弱次序"与"并列的任意解法"混在一句里；机制主张只涉及前者。
```

### P3a' 辅助（不参与 PASS/FAIL）：v1 口径的复现记录

```text
同时计算 v1 的原口径（序列逐位相等），作为负面记录随 JSON 一起落盘 ——
它应当在 26 链上通过、在 C1 上失败（正是 v1 FAIL 的原因）。不计入 PASS/FAIL。
```

### P4 向后兼容（g = 0 时等价于 v2）

```text
inhibition = 0.0（默认）时，v3 的 activate 输出 = v2 的 activate 输出（逐位，容差 1e-12）。
两种输入形态都要测：名字列表（C37 契约）与 dict 带值（B30 契约）。
```

### P5 作用域分账（压制发生在到达值层，不在读出层）

```text
判据（可判定）：对任意 g，v3.activate 返回的【键集合】= v2.activate 的键集合。
  （本实现不剔除节点：负到达值也会写入 acts，故键集合 = 可达集合，与 g 无关。）
这条把 v3 与 E34 的 W 层分开记账：W 是读取端工具（快照作额外种子），
v3 的 g 是到达值算式层的调制参数。
```

### P6 只读口径（v2 字段不许省，同 E38）

```text
四个层文件 synapse_net.py / order_layer.py / context_layer.py / chunk_layer.py：
  sha256 跑前 = 跑后；L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后
v1 = cf99a07a0e60fd4d（前后一致）
v2 = ee6df589eb2f6983（前后一致；JSON 仍保留该字段 +
     reason: "本实验不调用 v2 的修改版；字段为只读口径一致性保留（纪律⑧）"）
v3 = 首记录
```

### P7 双向结局（文本见 §4）

```text
通过（P1 + P2 + P3 + P4 + P5 全过）→ C38 + B33
不通过 → 如实记录哪条挂、实测值多少、诊断顺序：
         先查 v3 是否与 v2 等价（P4）→ 再查到达值算式是否保留了 max 吸收 → 再谈机制
```

---

## 4. 双向落点（跑前写死）

> **登记顺序**：B33 写在 C38 前面。E39 的价值 B33 > C38 ——
> C38 是"一个可及性压制参数"（小能力），B33 是"竞争性抑制在 max 吸收下不可实现"
> （把四块硬骨头之一钉死的第一条记录）。先记做不到什么，再记能做什么。

### 通过 → B33（做不到什么）

```text
B33：在 max 吸收 + 种子固定（值 1.0）下，【竞争性抑制（WTA 式互斥）在到达值算式层不可实现】。
     ① 构造性上界：单跳到达 ≤ 1.0 × strength × hop_decay = 0.9 < 1.0，
        任何 ≤0 的抑制项只会让它更小 → 到达值永远无法覆盖值为 1.0 的种子；
     ② 实测：减法 / 增益 / 依赖他节点激活的 rival 项
        （g ∈ {0, 0.1, 0.5, 2, 10}，passes ∈ {1, 3}）下种子值恒为 1.000000，
        被改变的只有非种子节点的可及性；
     ③ 次序：减法与增益只改 reach，不改【成对相对方向】（v2 口径；无严格反转）。
        注：v1 的"序列逐位相等"口径在存在精确并列时不成立（C1 上 40 对精确并列，
        g=0 的 tie-break 与 g>0 的值序相反）—— 该负面记录见 §3 P3。
     与 B12 的分层：B12 说【权重层】（非负权重无 WTA），本条目说【到达值层】——
     即使外部负值进入算式，也不产生互斥。
     出口：要压制种子，必须改吸收判据（`>` 改成别的），而 L0-SPEC §2 ⑥ 明写必须保留
     max 吸收 → 属越界，不在实现约定可改范围。
     范围限定：限于当前 v3 形态（max 吸收 + 固定种子）；若将来改架构（§1 越界），须另立实验。
```

### 通过 → C38（可及性压制能力，与 B33 同时登记）

```text
C38：L0 v3 的到达值算式支持外部调制参数 g（构造参数，与 λ / hop_decay / β 同级）。
     在 26 字母链上，超阈值节点数（reach，阈值 0.5）随 g 单调不增
     （7→6→4→3→3→2→2→2→1→1，g ∈ {0, 0.02, 0.05, 0.1, 0.15, 0.2, 0.3, 0.4, 0.5, 0.7}），
     端点严格下降（7 → 1）。
     范围限定：这是【可及性压制（reach compression）】，不是竞争性抑制 ——
     种子值恒为 1.0（不可被压制），节点相对次序不随 g 改变。
     v3 为空分支（synapse_net_v3.py，从 v2 继承）；v1/v2 冻结不变，E0–E38 全部有效。
```

### 不通过 → 如实记录

```text
不登记 C38/B33。记录"哪条判据挂、实测值多少"，附诊断三步
（先查 v3 与 v2 的等价性 → 再查 max 吸收是否保留 → 再谈机制）。
若挂的是 P2（种子被压制），一律按越界处理：先查实现，再谈机制（纪律⑧/B14 的教训）。
```

---

## 5. 影响评估

```text
(a) 直接依赖 activate 输出的实验：34 个文件 / 147 处调用
    （形态：str 74 / list 20 / dict 3 / 变量传入 50）
    → v3 是【新文件】，这些调用全部指向 v1（或 E37 起指向 v2），【不受影响】。

(b) 吸收判据（`arrival > acts.get(v, -inf) + EPS`）：
    本实验【不涉】—— 走的是出口 (c)，不改吸收判据，C28/E28 不受影响。
    【修正预检的表述】预检 §4 曾写"若 v3 改吸收判据，C28 的范围要重述"——
    那是出口 (b) 的后果；本实验走 (c)，该影响【不适用】。

(c) 种子入口契约：v3 继承 v2 → 保持 C37（名字列表 → 全 1.0 向后兼容）+ B30（常量语义）。
    由 P4 在 g = 0 时验证；这两条是 E34–E38 跨实验对照的基础，不能破。
```

---

## 6. 产物与不做

**产物**

- `synapse_net_v3.py`（平行分支，从 v2 继承；不动 v1/v2）；
- `experiments/exp39_reach_compression.py` + `outputs/exp39_reach_compression.json`；
- 登记（双向，跑前写死）：通过 → **C38 + B33**；不通过 → 如实记录（不占编号）。

**不做**

- 不改吸收判据（`>` 判据一字不动）—— L0-SPEC §2 ⑥ 的硬线；
- 不做读取端过滤（那是 E14/C28 的同型，且会与 E34 的 W 层混账）；
- 不声称"竞争性抑制/抑制控制"（本实验的能力叫可及性压制，见 §0.2）；
- 不动 v1/v2 一个字节；不新增层；不引入 stub / 语义 / 奖励；
- 预注册阈值跑后不改（reach 阈值 0.5、扫描网格 10 点皆预设）；
- 不声称任何"意识 / 理解"类结论（B07）。
