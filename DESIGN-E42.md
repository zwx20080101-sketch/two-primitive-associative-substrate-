# E42 设计：组合符号 —— 两模式共现绑定产生新符号（E33 定义 B 的展开）

状态：**定稿**（预检已完成，骨架中的 `[预检待填]` 已全部替换为实测值或拍板结论）。日期：2026-09-16。
依赖：E33（定义 A 已完成）、E12/E18/E23（L4 组块机制，作对照）、E37（v2 带值入口）、
E41（值序可测）、C30（闭包硬线）、B32/B34（模块级 vs 节点级口径）。
**L0/L1/L2/L4 与 v1/v2/v3 一字不动；新增层 L5（`compose_layer.py`）。**

---

## §0 概念基础

### 0.1 组合符号 vs 组块（NEXT-PLAN §8.7 判定规则，逐字落地）

```text
组块（L4 的 CH_n）：新符号的创建由【重复次数超过阈值 K】触发。
组合符号（E42）  ：新符号的创建由【两个模式同时激活】触发。
两者可以共存，但【功劳各自记账】。
—— 本实验的所有判据必须以"触发条件"区分两类符号，不得事后归类。
```

### 0.2 与 E33 定义 A 的谱系：**景观外**

```text
E25/E33 的景观 = 15 个 L0 节点按 3 个模块聚合出的 2³ = 8 个模块符号状态。
组合符号 C = 两个模式（节点子集）的绑定对象 —— 它【不在那 8 个状态里】，也不是 15 节点的某个配置。
量级对照：8 个景观状态 vs 2^15 = 32768 个节点子集 ⇒ 泛型情况下 C 不是景观状态。
⇒ E42 引入的是【景观外的新对象】⇒ 必须显式声明为新增机制，不能伪装成"景观里采出来的"。
  与 E33 的分工：E33 采【已有】状态；E42 造【新】对象。
```

### 0.3 闭包声明：L5 的 C 是【显式声明的新增结构】

```text
L5 的 C 节点同 L4 的 CH_n —— 建在【该层自己的节点集】里，不写进 L0。
C30 的审计对象是"每层的已学集合"：L5 有自己的已学集合（含 C）⇒ L0 的闭包不受影响。
【审计口径】本实验的 P6 只审 L0/L1/L2/L4 四层，【不审 L5】——
  L5 是新增层，本实验是它的首次声明（同 E12 首次声明 CH_n 的处理）。
```

### 0.4 prod 规则的语义（**非共享成员清零不是 bug**）

```text
prod 规则下非共享成员清零（预检实例：A、D → 0）—— 这是"共现绑定"的语义：
  组合符号 = 两个模式【共同激活】的部分；非共现部分不进组合。
若期望"组合 = 并集"，那是另一条规则（本实验不做，见 §6 不做清单）。
```

### 0.5 P、Q 【刻意】选高度重叠（压力测试，不是失误）

```text
预检选 P = top3(activate('A')) = {A:1.0, B:0.9, C:0.81}
      Q = top3(activate('C')) = {C:1.0, B:0.9, D:0.9}      共享 B、C
理由：重叠是"共现绑定"压力最大的情形 —— max 会退化（vs Q 仅差 1 个节点）、
      sum 会爆炸（B=1.8、C=1.81 > 基底上界 1.0）、prod 会清零非共享。
      这是对候选规则的【压力测试】；若选不重叠的 P、Q，四种规则都表现良好、失去区分力。
```

### 0.6 来源字段与一条**局限说明**

```text
L5 的 C 节点带 origins[cid] = "coactivation" 字段（组块侧对应 "repeat"）。
【不改 chunk_layer.py】—— L4 的 CH_n 没有来源字段，本实验不动 L4。
【局限（如实记录）】组块侧的"来源"只能靠【触发路径】推断：实测 unit_counts 的写入点
  只有 chunk_layer.py:49 一处（`learn_unit` 内），`learn_sequence`/`activate` 都不碰它，
  故"重复触发"与"共现触发"在代码级不相交。该推断是【结构性推理】，不是字段比对 ——
  若将来要字段级比对，需要给 L4 加字段（会破坏 E12/E18/E23 的只读证据，须另立实验）。
```

---

## §1 预检摘要（2026-09-16，只读，未动任何文件）

```text
① L4 组块机制（chunk_layer.py）
   L45-61 learn_unit：唯一起用路径；触发条件 = unit_counts[seq] >= k（:50）
          存储：chunks[cid] = list(seq)（有序成员，允许重复，:53）、high_nodes.add(cid)（:55）、
                member_bind[(cid, m)] 计数（:57-60）
   L72-77 learn_sequence：块级序列 → high_pairs（无向相邻）
   L102-122 activate：与 L0 同一条扩散规则（max 吸收 + hop_decay 0.9），跑在 high_pairs 上
   L63-67 spell：按有序记录返回成员（双写还原）
   实测：k=3 时第 3 次 learn_unit 才产生 CH_1；chunks['CH_1'] = [h,e,l,l,o]
   【触发路径代码级不相交】unit_counts 的赋值行只有 :49 一处

② L4 的 activate【不展开成员】（本条最关键）
   实测 activate('CH_1') = {CH_1:1.0, s:0.9}；成员 h/e/l/o 【一个都不在输出里】
   （docstring 明写"展开用 spell"）⇒ 直接复用 L4 模式【不满足 E42 的 P3】

③ L4 的 activate 对 dict 静默接受（B21 家族新实例）
   实测 activate({h:0.5, e:0.5}) → {h:1.0, e:1.0, l:0.9, o:0.81}（dict 被当 iterable，只取 keys、值抹成 1.0）

④ 两模式与四种带值规则（P、Q 刻意重叠）
   P = {A:1.0, B:0.9, C:0.81}   Q = {C:1.0, B:0.9, D:0.9}（共享 B、C）
   规则        组合值                                  是否超 1.0
   v1+v2      {A:1.0, B:1.8,  C:1.81, D:0.9}          B、C 超 ⇒ 破坏基底上界
   max        {A:1.0, B:0.9,  C:1.0,  D:0.9}          无
   v1*v2      {A:0.0, B:0.81, C:0.81, D:0.0}          无（非共享清零）
   不保留      {A:1.0, B:1.0,  C:1.0,  D:1.0}          无
   基底输出互比（差异节点数）：sum~max 2｜sum~prod 14｜sum~不保留 13｜max~prod 14｜max~不保留 12｜prod~不保留 14

⑤ 恒等式陷阱实测（组合输出 vs 单独 P / Q）
   sum:  vs P 13 / vs Q  3
   max:  vs P 12 / vs Q 【1】  ← 几乎就是 Q（只多一个 A=1.0）⇒ 近乎触发恒等式陷阱
   prod: vs P 12 / vs Q 14
   不保留: vs P 13 / vs Q 13
```

---

## §2 预注册参数（跑前定死，跑后不改）

```text
语料：E1 的 26 字母链（A–Z 相邻对 ×100，26 节点 / 25 边）—— 与 E41 同
两个模式：P = top3(activate('A')) = {A:1.0, B:0.9, C:0.81}（值来自 L0 的实际激活输出）
          Q = top3(activate('C')) = {C:1.0, B:0.9, D:0.9}（与 P 共享 B、C —— 刻意压力测试）

【"同时激活"的定义】同一次调用里的两个模式：在 L5 上用一次 coactivate(P, Q) 声明；
  不跨调用、不跨时间窗（跨窗口属另一命题，见 §6 不做清单）。

【落地层】新层 L5：compose_layer.py，与 L4 同构（自有节点集 + 自有边 + 同一条扩散规则）。
  与 L4 的差异：① 触发条件 = coactivation（而非 repeat）；② 成员边【显式建】（见下）；
  ③ 带 origins 字段。**L0/L1/L2/L4 与 v1/v2/v3 一字不动。**

【成员边（拍点④的落地）】C 与 P∪Q 的【所有】成员之间建显式成员边，边权 = 1.0。
  activate(C) 的预期：成员收到 1.0 × 1.0 × 0.9 = 0.9，再向各自邻居扩散。

【带值规则】
  主口径 = prod（C 的内部值 = v1 × v2，v ∈ {0} ∪ (0,1]）
  辅助口径 = 不保留（C 的内部值全 1.0，丢值序）
  sum 与 max 【排除】：sum 破坏基底上界（实测 B=1.8、C=1.81）；max 近乎恒等于 Q（vs Q 仅 1 个节点不同）

【阈值（预设，跑后不改）】
  P2a 字段级：L5 的 origins 字段 = "coactivation"
  P2b 行为级：交叉操作后对方层的符号集合逐项不变（两条都要成立）
  P3 可再激活：activate(C) 的输出【包含 P∪Q 的全部 4 个成员】（A、B、C、D）
  P4 带值被保留：C 的内部值逐位 = v1×v2（prod）/ = 1.0（不保留）；
                 两口径内部值的差异成员数 ≥ 1（实测 4）
                 —— 辅助项：两口径的基底输出差异节点数 ≥ 10（预检 14）
  P5 区分性：三分布两两差异节点数 ≥ 1（预检：prod vs P 12 / vs Q 14）
  P5b 值序敏感（定义 A：各自排名反转）：C 内部值变化的共享成员数 ≥ 1（预检：1，C 0.81→0.90）
  容差：1e-12
运行环境：PYTHONHASHSEED = 0
```

---

## §3 判据（预注册）

### 3.0 判据分层

```text
· 实质判据（可 FAIL）：P1 组合产生 / P2（a 字段级 + b 行为级）/ P3 可再激活 /
  P4 带值被保留（内部值逐位）/ P5 区分性 / P5b 值序敏感度
· 形式审计（预期恒成立，仍必须报）：P0 前置 / P6 闭包硬线 / P7 只读
· 双向              ：P8
```

### P0 前置

```text
① 落地层已声明（L5 = compose_layer.py，与 L4 同构）
② origins 字段已定义（L5 侧 = "coactivation"；组块侧无字段，靠触发路径推断，见 §0.6 局限）
③ 闭包处理已声明（§0.3：C 是 L5 的显式新增结构）
④ L0/L1/L2/L4 与 v1/v2/v3 的哈希跑前 = 跑后
```

### P1 组合产生（核心）

```text
两个模式 P、Q 同时激活（一次 coactivate(P, Q)）→ L5 产生组合符号 C。
判据：C 出现，且在 L5 的 origins 里记为 "coactivation"。
报告：C 的 id、成员边（C↔A/B/C/D）、C 的存储形式（在 L5 自己的节点集里）。
【与 L4 的区分】C 不是由"重复 K 次"触发的 —— 用 origins 字段比对，不用命名约定。
```

### P2 组合 vs 组块严格区分（核心）

```text
【P2a 字段级】L5 的 origins 字段 = "coactivation"（组块侧无字段，见 §0.6 局限）。

【P2b 行为级 —— 把"结构性推断"升级为"可观测"】
  L4 的 CH_n 虽无字段，但它"只响应重复、不响应共现"是【行为事实】，可观测：
  · 正向：把 C 的成员节点交给 L4 的 learn_unit 重复 K 次 → L4 产生的是它自己的 CH_n，
    而【L5 的 C 集合不变】（重复不会在 L5 造出组合符号）;
  · 反向：把 CH_n 的成员交给 L5 的 coactivate → L5 产生的是它自己的 C，
    而【L4 的 chunks 集合不变】（共现不会在 L4 造出组块）。
  判据：两条"互不响应"的行为都必须成立（对方的符号集合前后逐项不变）。

对照实验：把同样两个模式的内容【重复 K 次】交给 L4 的 learn_unit → 产生 CH_n。
报告：两组对照表（触发条件 / 产生对象 / 存储层 / 来源依据）+ P2b 的两条行为记录。
【禁止】用命名约定（CH_/C 前缀）作为区分依据。
```

### P3 组合符号可再激活（可分性）

```text
activate(C)（在 L5 上）的输出【包含 P∪Q 的全部成员】= {A, B, C, D}。
依据：§2 的成员边（边权 1.0）⇒ 成员收到 1.0 × 1.0 × 0.9 = 0.9。
判据：4 个成员全部出现在输出里（阈值：包含率 = 4/4）。
报告：activate(C) 的完整激活分布。
【为什么需要成员边】预检实测 L4 的 activate 不展开成员（CH_1 输出里没有 h/e/l/o）
  ⇒ 不给 C 建成员边，P3 直接不成立。
```

### P4 带值的角色（用户明确变量 ②）

```text
【v2 修订：判据改成对 C 的【内部值】逐位检查 —— 直接回答"值是否被组合符号保留"】
  主口径 prod：C 的内部值【逐位等于】v1×v2：
      C = {A:0.0, B:0.81, C:0.81, D:0.0}（共享成员 B、C 保留乘积；非共享 A、D 清零）
  辅助口径 不保留：C 的内部值【逐位等于】1.0：
      C = {A:1.0, B:1.0, C:1.0, D:1.0}
  判据：两口径的【内部值逐位对照】，差异成员数 ≥ 1（实测：4 个成员全部不同）。
  辅助项（保留但不作主判据）：两口径的【基底输出】差异节点数 ≥ 10（预设；预检 14）。

【为什么改成内部值】v1 稿用"两规则输出差异 ≥ 10"——那只能证明"两种不同规则会给出不同输出"
  （任何两种不同规则都会），不能证明"带值被组合符号保留"。对 C 的内部值逐位判定才贴近问题本质。

报告：C 的内部值逐位对照表（两口径）+ 原"输出差异 ≥ 10"作辅助项。
【回答用户约束 ②】"带值是否让组合结果有优先级"：prod 答"是"（C 保留 v1×v2）、
  不保留答"否"（全 1.0）。两面都跑，才能判定该变量。
```

### P5 区分性（防"拼接即生成"）

```text
【口径】本判据在【L0 级】比较（与预检数字同源）：把"组合符号的内部值"、"P 的值"、"Q 的值"
  分别作为种子（各自加 Z=1.0）注入 L0，比较三个激活分布。
  （P3 的 activate(CMP) 是 L5 级 —— 两条判据层级不同，报告里必须分别标注。）
C 的激活分布 ≠ P 单独激活 ≠ Q 单独激活。判据：三分布两两差异节点数 ≥ 1。
预检实测（主口径 prod）：vs P 12 个节点不同、vs Q 14 个节点不同。
报告：逐节点差异表。
```

### P5b 值序敏感度（**实质判据**；"值序对调"的定义必须写死）

```text
【判据】prod 口径下，把 P、Q 的【值序对调】⇒ C 的内部值至少 1 个共享成员发生变化（阈值 ≥ 1）。

【"值序对调"的定义 —— 必须写死，否则判据随定义变】
  基准：P = {A:1.00, B:0.90, C:0.81}，Q = {C:1.00, B:0.90, D:0.90}
        prod 下 C 的内部值 = {A:0, B:0.81, C:0.81, D:0}（共享 B、C）

  四种定义的【精确操作】与实测（全部落进 JSON 的 P5b.definitions_measured）：

  定义 A（采用）：【每个模式各自】把其成员的【降序排名反转】（值按排名对调）
      P → P' = {A:0.81, B:0.90, C:1.00}      Q → Q' = {C:0.90, B:0.90, D:1.00}
      结果：C 的内部值 {A:0, B:0.81, C:0.81, D:0} → {A:0, B:0.81, C:0.90, D:0}
            变化成员 = ['C']（0.81 → 0.90）  ⇒ 判据成立

  定义 B：【共享成员 B/C 在两个模式内同时互换】（同一个有序对，P 与 Q 各换一次）
      P → {A:1.00, B:0.81, C:0.90}            Q → {C:0.90, B:1.00, D:0.90}
      结果：C 的内部值【不变】= {A:0, B:0.81, C:0.81, D:0}
      （原因：prod 对"同时在两个因子内对同一对做同一置换"是恒等的）

  定义 C：【共享成员 C 的值在 P、Q 之间互换】（同一节点、跨模式换值）
      P → {A:1.00, B:0.90, C:1.00}            Q → {C:0.81, B:0.90, D:0.90}
      结果：C 的内部值【不变】= {A:0, B:0.81, C:0.81, D:0}

  定义 D：【P 的非共享 A 与共享 C 对调；Q 的非共享 D 与共享 C 对调】
      P → {A:0.81, B:0.90, C:1.00}            Q → {C:0.90, B:0.90, D:1.00}
      结果：C 的内部值 → {A:0, B:0.81, C:0.90, D:0}（本实例与定义 A 【重合】）

  ⇒ 采用 B/C 时该量为空判据（恒不变）；采用 A/D 时有效。本实验采【定义 A】——
    因为"值序对调"的语义就是【排名反转】，与 E41 的"值序"读法一致。

【一处更正留痕】v1 稿曾写"prod 下值序对调不改变 C 的内部值（乘法交换律）"——【该论证错误】：
  交换律说的是乘法【顺序】与结果无关，而这里是【输入值】变化。实测已推翻该预期（见上表定义 A）。
  该错误与 E39 的"判据把两件事混在一句里"同型：真正的问题是"值序对调"未定义，不是量本身为空。

报告：逐成员对照表（对调前 / 对调后）+ 基底输出差异节点数。

【四种定义的完整操作与实测结果同时落在 exp42 JSON 的 `P5b.definitions_measured` 里】
—— 后人复现时无需回读本设计，直接从 artifact 取。
```

### P6 闭包硬线（C30）

```text
审计口径（§0.3）：只审 L0/L1/L2/L4 四层 —— 每层的输出集合 ⊆ 该层的已学集合，违例数 = 0。
【不审 L5】：L5 是新增层，本实验是它的首次声明；C 属于 L5 自己的已学集合。
报告：四层的违例数（预期全 0）+ L5 的节点集合（含 C，作为新增结构的记录）。
```

### P7 只读

```text
L0/L1/L2/L4 与 v1/v2/v3 的哈希跑前 = 跑后：
  synapse_net.py cf99a07a0e60fd4d / v2 ee6df589eb2f6983 / v3 085be73a06f0694c
  order_layer.py 419d54745a9eb795 / context_layer.py bd029d7bbdc345be / chunk_layer.py 46f5857be0fc7c7e
L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后。
新文件 compose_layer.py：记录【首次哈希】。
```

### P8 双向结局

```text
通过（P1 + P2 + P3 + P4 + P5 全过）→ 新 C 条目（组合符号能力）+ 与 C36/C40 的状态说明
  （若与 E41 的值序有关联 —— 由 P4/P5b 的实测结果决定是否要写）
不通过 → 如实记录哪条挂、实测值多少、失败分层（触发 / 产生 / 可再激活 / 带值）。
【编号纪律】本实验【不预分配 FAIL 分支的 B 编号】（同 E41）；
  若 FAIL 是【机制性】的，跑完后新立 B 编号；若是【实现/判据问题】，修正后重跑、不新立。
```

---

## §4 双向落点（跑前写死）

### 通过 → 新 C 条目（组合符号能力；编号跑完按登记表定）

```text
C??：两个模式经【共现绑定】（一次 coactivate(P, Q)）在 L5 产生新的组合符号 C；
     C 与 P∪Q 的成员之间有显式成员边 ⇒ activate(C) 可分性成立（4/4 成员被激活）。
     L5 的 origins 字段（"coactivation"）把 C 与 L4 的组块 CH_n（"repeat" 触发）在【数据上】区分开。
     【带值】主口径 prod：C 的内部值 = v1×v2（非共享成员清零 —— 共现语义）；
     辅助口径 不保留：全 1.0。两口径的输出差异 ≥ 10 个节点（判据）。
     范围限定：① C 是【景观外的新对象】（§0.2）—— 与 E33 的"采已有状态"不同层；
     ② 功劳归【L5 这一新增层的创建机制】，不是基底新能力（L0 未动）；
     ③ C 的可再激活依赖【显式成员边】—— 若不给成员边，C 会退化成 L4 那样的"不展开"符号；
     ④ 语料限于 E1 的 26 字母链；P、Q 刻意重叠（压力测试）；
     ⑤ **C 与 P∪Q 的成员边是【E42 手工建】的，不是 L5 自动产生的** ——
        "组合符号自动建立成员边"属另一命题（L5 的成员边生成规则），不在 E42 范围内。
```

### 不通过 → 如实记录

```text
不登记 C 条目。记录哪条判据挂、实测值多少，并给失败分层：
  ① 触发是否发生（P1）→ ② 是否与组块可分（P2）→ ③ 可分性（P3）→ ④ 带值敏感（P4）
机制性 FAIL 才在跑完后新立 B 编号；实现/判据问题则修正后重跑（不新立编号）。
```

---

## §5 影响评估

```text
(a) L0/L1/L2/L4 与 v1/v2/v3：一字不动（P7 用哈希证明）；新增的只有 compose_layer.py。
(b) E12/E18/E23 的组块结论：不受影响 —— 本实验不改 chunk_layer.py；
    组块侧的"来源"仍是推断（§0.6），该局限已显式声明。
(c) C30 闭包：审计口径收窄为四层（§0.3）；L5 的 C 属于 L5 自己的已学集合 ⇒ 不越界。
(d) E33/C33：E42 与 E33 是【景观外 vs 景观内】的分工（§0.2）；不改 C33 的任何文本。
(e) E41/C40：E42 的带值来自同一条已登记入口（C37/B30），不新增入口；
    若 P4/P5b 显示"组合符号的值序"与 C40 的"痕迹值序"有可比的结论，再决定是否要加状态说明。
(f) B32/B34：本实验在节点级（不是模块级），不触及两条边界的适用范围。
```

---

## §6 产物与不做

**产物**

- `compose_layer.py`（新层 L5，与 L4 同构；含 origins 字段与显式成员边）；
- `experiments/exp42_composition_symbol.py` + `outputs/exp42_composition_symbol.json`；
- 登记（双向，跑前写死）：通过 → 新 C 条目；不通过 → 如实记录（不预分配 B 编号）。

**必报清单**

```text
① L4 与 L5 的对照表（触发条件 / 产生对象 / 存储层 / 来源依据）
② C 的 id、成员边、origins 字段的实际值
③ activate(C) 的完整激活分布（P3）
④ prod vs 不保留 的逐节点差异表（P4）
⑤ 三分布（C / P / Q）的两两逐节点差异表（P5）
⑥ P5b：值序对调前后的 C 内部值 + 基底输出差异数（报告项，不下结论）
⑦ P6：四层闭包审计的违例数 + L5 的节点集合
⑧ 哈希：L0/L1/L2/L4 与 v1/v2/v3（前后）+ compose_layer.py（首次）
```

**不做**

- 不改 L0/L1/L2/L4、不改 v1/v2/v3、不改 `chunk_layer.py`（不给 CH_n 加字段）；
- **不做"组合 = 并集"的规则**（本实验的 prod 是"只留共现成员"，并集是另一条规则）；
- **不做跨窗口/跨时间的同时激活**（本实验的"同时"= 一次 coactivate 调用内）；
- **不做"分类+排序组合态"**（E36 的 step 过滤 + E41 的值序；那是 E42 之外的独立命题）；
- 不声称"系统能想象 / 能创造"（B07）；
- **不引用 IDEAS-Modules.md 作为设计依据**（北极星纪律：远期假说不参与当前实验）；
- 预注册阈值跑后不改（P3 的 4/4、P4 的 ≥10、P5 的 ≥1、容差 1e-12）。

---

## §7 已知陷阱（逐条对应到判据）

```text
① 与 L4 组块混淆  → P2（origins 字段比对）+ §0.6（组块侧靠触发路径推断的局限声明）
② 闭包违反        → §0.3 声明 + P6（四层审计；L5 是新增层，本实验首次声明）
③ 恒等式陷阱      → P5（三分布两两不同）+ 预检已挡住 max 规则（vs Q 仅 1 个节点不同）
④ 单次漂亮数字    → 本实验用确定性 activate（无采样、无随机初始化）⇒ 不涉；若将来引入采样须报中位数+最小值
⑤ 重检索陷阱      → P3（可分性）+ P5（区分性）：C 必须能激活 P∪Q 成员，且其分布不同于单独 P/Q
```
