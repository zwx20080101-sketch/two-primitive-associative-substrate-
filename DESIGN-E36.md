# E36 设计：顺序标记的**适用边界**（分类 ≠ 排序）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-15。
依赖：E34 的 `WorkingMemory`（扩展，不重写语义）、E35 的串联结论、E1 的 26 字母链语料、
E27/E30 的只读口径。**无 numpy 依赖；不改任何层文件；不新增层。**

**前置依赖（显式声明）**：E36 的全部数据建立在 **E34（W 层外部工具）** 与
**E35（W 栈行为：重注入构造性、停止注入后退化为图衰减）** 之上。
**若 E34/E35 的任一结论被推翻，E36 暂停**——本实验不重新验证它们。

---

## 0. 前置（E36 预检，2026-09-15；三问的实测答案）

### 问题一：带标记的种子能不能进基底？

```text
activate(("A", 1))     → KeyError: node not in network: 1
activate(["A", 1])     → KeyError: node not in network: 1
activate([("A", 1)])   → KeyError: node not in network: ('A', 1)
activate({"A": 1})     → 【被接受】——dict 被当成可迭代的 key 列表（list({"A":1}) == ["A"]）
```

**结论**：**标签无法进入基底**——`activate` 只吃节点名。所以"顺序标记"的作用范围
**只在 W 层的过滤函数 `seeds(step=k)` 里**，不在基底内部。
（附带记录：传 dict 会被**静默**当成 key 列表，不报错——属"静默接受"类接口细节。）

### 问题二：按 step 过滤的注入能测到什么？

```text
W 内容（node:(value,step)）= {A:(1.0,1), B:(0.9,1), C:(0.81,1), Z:(1.0,2), Y:(0.9,2), X:(0.81,2)}
seeds(step=1) = [A,B,C]      seeds(step=2) = [Z,Y,X]      seeds() = [A,Z,B,Y,C,X]

步3 注入步1痕迹 activate(['Z','A','B','C']) → A = 1.000000
对照            activate(['Z'])            → A = 0.071789799（= 0.9^25）
差异 = 0.928210
```

**结论**：分类差异**可测**（差 0.928，与 E34 的数值一致）。

### 问题三：顺序标签能否改变基底的值分布？

```text
两个来源一起注入 activate(['Z'] + seeds()) → 注入集合 = {A,B,C,X,Y,Z}（6 个）
它们在基底里的值：全部 = 1.0
```

**结论**：**不能**。基底只看到"成员"，看不到"来自哪一步"。
这是**设计前提**，不是判据——E34 已证多种子入口会把值抹成 1.0。

---

## 1. 命题（**边界量化**，不是"标签可用"）

```text
E36 = 顺序标记的【适用边界】被量化：它只作用于读取端过滤（哪些痕迹被包含在种子集里），
      不影响基底内部；因此能产生【分类差异】，不能产生【排序差异】。
```

**为什么改这个措辞**：E34 的 P3（`activate([Z]+seeds())` → A=1.0，差 0.928）与
E36 的 P3（`activate([Z]+seeds(step=1))` → A=1.0，差 0.928）**数字完全一样、机制也一样**——
都是"注入导致值跳到 1.0"。所以 E36 的 P3 不是新内容，是 E34 P3 加了过滤后的同型实验。
E36 真正的新内容只有两条：

```text
P5：过滤 vs 无过滤对【非注入节点】的影响（X = 0.81 vs 1.0）——测的是"排除的效果"，不是"注入的效果"
P4：值抹平导致无排序差异，且带证伪条件（若出现权重差异 → 前提错）
```

⇒ **E36 不是"工作记忆第三刀"，是"给 E35 的结论加边界"**。

**为什么必须分清这两件事**：若把二者混在一个判据里，跑完会发现"标签区分了成员、
却没区分权重"，届时无法判定 PASS/FAIL。

---

## 2. W 的扩展规格（写死，跑前不改）

```text
数据结构：{node: (value, step)}          （在 E34 的 {node: value} 上加 step 字段）
写入    hold(acts, step)：取 value 最大的 k 个，连同 step 一起存
读出    seeds(step=k)  → 只返回 step == k 的节点（按 value 降序、同值按节点名升序）
         seeds()       → 返回全部（退化为 E34/E35 行为）
k = 3（不变）      tie-break = 值降序、同值节点名升序（不变，E35 已写死）
```

---

## 3. 任务（三步链）

```text
语料：E1 的 26 字母链（A–Z 相邻对 × 100，26 节点 / 25 边）

步1：activate('A') → W.hold(acts, step=1)      ⇒ W 得 {A:(1.0,1), B:(0.9,1), C:(0.81,1)}
步2：activate('Z') → W.hold(acts, step=2)      ⇒ 追加 {Z:(1.0,2), Y:(0.9,2), X:(0.81,2)}
                                                 （步2 不注入步1 的痕迹）
步3：activate(['Z'] + W.seeds(step=1))          ⇒ 只注入步1 的痕迹
对照：activate(['Z'])                            ⇒ 不注入
消融对照：activate(['Z'] + W.seeds())            ⇒ 去掉 step 过滤 = 注入两个来源（退化为 E35）
```

**P5 的观测量（写死）**：用**步2 独有节点 X** 的值区分"有过滤"与"无过滤"：

```text
只注入步1（有过滤）：X 不是种子，距 Z 两跳 → 0.9^2 = 0.81
注入全集（无过滤）：X 自己就是种子 → 1.0
```

---

## 4. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **接口不改**：四个层文件 sha256 前后一致 | 一致 | 直接 FAIL |
| P1b | **L0 不改**：节点数/边数/边集合签名（排序逐项含计数）逐位相同 | 相同 | 直接 FAIL |
| P2 | **顺序标记正确**：W 中每个节点带正确的 step 字段；`seeds(step=1)` / `seeds(step=2)` 与写入内容逐项一致 | 逐项一致 | 直接 FAIL（工具写错） |
| P3 | **分类差异可测（核心）**：`seeds(step=1)` 只返回步1 的 3 个节点；注入后 **A = 1.0**；与"不注入"对照（A = 0.071790）之差 ≥ 0.5 | 通过 | 记录负结果 → B29 |
| P4 | **排序差异不可测（设计前提，负面记录，不是 FAIL）**：注入后所有被注入节点在基底里**都是 1.0**，无相对权重差异 | 记录 | 见下方"证伪时的诊断顺序" |
| P5 | **消融（去掉 step 过滤）**：`activate(['Z'] + seeds())` 时 **X = 1.0**；有过滤时 X = 0.81；两者差 ≥ 0.1 | 通过 | 见下方"证伪时的诊断顺序" |
| P6 | **双向结局**：P3 + P5 通过 → **C36**；不通过 → **B29**（文本见 §6） | 两分支都合格 | 不许跑完再挑解释 |
| 辅助 | 预检的"**dict 被静默接受**"（`activate({"A":1})` → 只激活 A，不报错）——**同时写进 B21 的补充实例**（与 L2 的 `{}`、L4 的 `None` 同族：不报错、行为不符直觉），不新起 B 编号 | 仅记录 | — |

**证伪时的诊断顺序（P4/P5 共用，写死）**：E36 的结论完全建立在
"**基底种子入口抹平值**"这个前提上；前提一旦失效，E36 的所有数字都不能信。
所以若出现偏差（P4：被注入节点非 1.0；P5：X 无过滤时非 1.0、或有过滤时非 0.81），
**先查实现，再谈机制**（B14 的教训）：

```text
① 检查 synapse_net.py:98（`acts = {s: 1.0 for s in seeds}`）是否被改
   —— sha256 前后一致已核对，但要再看这一行本身；
② 检查 W 的 seeds() 是否意外传了值而不是节点名；
③ 检查 activate 内部是否在某种情形下保留了值（如 EPS 分支、去重分支）。
上述三条全否 → 说明 E34 预检的"值抹平"结论失效，E36 暂停，改设计。
```

---

## 5. 预注册参数

```text
语料：A–Z 相邻对 × 100（26 节点 / 25 边）
W：{node: (value, step)}，k = 3，tie-break = 值降序、同值节点名升序
   （**top-k 选择规则复述**：按激活值降序；值相同按节点名升序；k=3。
     与 E35 §6 一致——此处重复是为了让 E36 单份设计即可独立复现）
链：步1 存 A 痕迹 / 步2 存 Z 痕迹（不注入）/ 步3 只注入步1
观测量：A（P3）与 X（P5）      阈值：P3 ≥ 0.5；P5 ≥ 0.1
参照值：0.9^25 = 0.071789799（A，从 Z 走 25 跳）；0.9^2 = 0.81（X，从 Z 走 2 跳）
只读：层文件 sha256 + L0 边集合签名          运行环境：PYTHONHASHSEED = 0
```

---

## 6. 双向结局（跑前写死，两分支文本）

```text
分支 C36（P3 + P5 通过）：
C36：外部 W 层的顺序标记【只作用于读取端过滤】（哪些痕迹被包含在种子集里），
     不影响基底内部——基底种子入口把值抹平（E34 已证），
     因此顺序标记不产生优先级排序。
     范围限定：分类 ≠ 排序；外部工具 ≠ 基底能力；排序差异需要值域编码，
     涉及改基底入口行为，属越权嫌疑，不在本实验。

分支 B29（P3 或 P5 未通过）：
B29：按 step 分类的注入未能产生可测差异（具体偏差见 JSON）；
     顺序标记在该语料上不足以支撑"按来源分类"，需要重新定位机制。
```

---

## 7. 产出物

- `experiments/exp36_sequence_tags.py` + `outputs/exp36_sequence_tags.json`；
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C36 / 负结果 B29）；
- **不改任何层文件**；**不新增层**；W 不进 LAYERS、不改 BOUNDARY。

---

## 8. 边界与不做

- **不做值域编码**（保留种子原始值）：那会改基底入口行为（多种子一律置 1.0 是 L0 的现有语义），
  属越权嫌疑——要做得先重新界定归属与功劳账，不在 E36 内；
- **不做规划**（前瞻 / 搜索空间 / 目标函数）；
- **不把标签说成"基底的记忆顺序"**：标签只活在 W 层内部，基底对它一无所知（问题一已证）；
- 不声称任何"意识/理解"类结论（B07）。
