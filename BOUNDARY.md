# Synapse-Net 边界宪章（实验默认约束）

日期：2026-09-06　状态：经用户确认，作为后续一切实验的默认边界。

## 0. 一句话定义

Synapse-Net 基底 = **记忆与联想基底**：

> 只负责"连接"与"传播"——记录什么和什么一起出现过、谁先谁后、
> 多少次、什么时候；点亮一个节点，把激活沿连接扩散；返回全量激活分布。

它不负责"生成"。一切"生成"（说话、写字、恐惧、画面、语法选择）都是
外部模块消费激活之后的产物。

## 1. 归属判断硬规则

```text
内部（基底）   = 建立连接 / 沿连接传播 / 按阈值读取
外部（模块）   = 消费激活、产出新模态（声音/笔画/情绪/决定/画面…）
观测工具       = 实验侧的读取器（threshold、走读器），不改基底，不算能力
功劳记账       = 功能归"系统整体"时，必须写清每一步是谁干的
```

如果某个功能必须靠外部模块先加工信号、注入标记、或最后渲染输出，
那它的功劳是系统整体的，**不是基底单方的**。

## 2. 能力归属清单

### 属于基底

- 共现绑定：N（次数）、last_seen（时间）、方向计数（L1）；
- 激活扩散：全量返回，弱连接不丢；
- 时间衰减只在检索时刻（W=N·e^(−λΔt)），N 永久；
- 同一规则换尺度成块：重复的整体自动获得块节点（L4 的形成规则）；
- 无中生有闭包：激活输出 ⊆ 曾学过的节点。
- 基底传播永久 max（best-path，最强单路径胜出）；
  多路求和（sum）是**读取端可选模式**，不是底层传播规则（E14 拍板）；
  sum 自带单次发射护栏 + 封顶 1.0，且其扩散拓扑≠原始无向图。

### 读取端工具（观测，不算基底能力）

- `attention_filter`（阈值过滤）；
- 走读器（取最强未访问邻居继续走）。

### 属于外部模块（不在基底承诺内）

- 感知编码：听/看/身体感觉 → 节点事件；
- 语言/韵律模块：分词、选词、把激活变成有语法的话、发起"叠词"这类规则；
- 动作输出：把节点变成笔画/声音（拼写、发音）；
- 情绪/奖励模块：把激活变成害怕、高兴、疼；
- 渲染：画面、意象、感受。

## 3. 叠词的正确记账（汪汪/慢慢/人人）

拆成三层，禁止混为一个黑盒：

1. **基底（我们）**：表示"汪 这个节点在相邻时刻又响了一次"。
   已由 E13 实现：L1 有序自转移 n(汪→汪) 是一个**纯时间性重复标记**，
   同窗连续重复计数、分散出现不计数、不附带任何语义；
2. **语言/韵律模块（外面）**：负责"两次出现 = 强调/普遍"的规则抽象，
   从 汪汪、慢慢、夜夜 的经验中外推到 人人；
3. **合成**：外部模块调用规则，基底提供"人 节点 + 重复标记"，
   两者相辅相成。"人人"的功劳归**系统整体**，不归基底单方。

推论：基底缺的是"重复标记"，不是"叠词语义"——语义从来不该由基底发明。

## 4. 实验终止线（每个实验动手前先答四问）

1. 这个现象，基底参与的到底是哪一步？（绑定/检索/标记？）
2. 哪些环节必须由外部模块 stub 提供？（分词/选词/渲染/规则注入？）
3. 实验通过时，结论归谁？（基底 / 外部 / 系统整体——必须写清）
4. 做到哪一步算停？（基底到"给出正确的激活分布/标记"就停，
   不追到"说出人话"）

## 5. 历史实验功劳重标（诚实记账）

| 实验 | 基底真实贡献 | 外部/工具贡献 | 结论归属 |
| --- | --- | --- | --- |
| E0–E3 | 绑定/计数/强度/全量输出 | 阈值读取工具 | 基底 |
| E4 | 双向等强、对称结构 | 走读器（读取端） | 基底事实 + 工具演示 |
| E5 | 倒背费劲（L1 方向统计） | 无 | **基底自己长出的能力** |
| E6a/E6b | 绑定、并行候选、经情景互译 | 情景节点由实验注入（输入结构） | 基底绑定 + 输入结构 |
| E7 | 密集脉冲绑定、枢纽、第二跳预警 | 时间轴脉冲由实验侧构造（传感器模型） | 基底检索 |
| E8 | 图结构、并行候选、组合可达 | 走读器 | 基底（结构）/ 工具（走读） |
| E9 | 缺口补全、activate(F) 闭包 | 无 | 基底 |
| E10 | λ 检索衰减、N 永久 | λ 由实验打开（基底已有参数） | 基底 |
| E11 | 歧义集中在枢纽、情景绑定分离 | 情景节点由实验注入 | 基底 + 输入结构 |
| E12 | 重复整体自动成块（L4 形成规则） | spell 位置表与回放（语言侧模块） | **混合**：成块规则贴基底；双写还原靠上层记录 |

## 6. 遗留与后续

- 基底已补：同一节点短窗内二次激活的表示（重复标记）——E13 完成；
- 叠词语义外推（汪汪→人人）属于语言模块，只做系统级场景，不写进基底；
- 真正嵌套与角色指派、会合求和、L2 上下文：仍是开放问题，按四问再决定归属。

## 7. 承诺

本宪章是后续一切实验的默认约束：

- 新实验必须先声明四问，再写判据；
- 若某结论与宪章冲突，先改宪章（经用户确认），再改实验；
- 基底文件（`synapse_net.py`）继续冻结。

## 8. 工作假说与建模范围（2026-09-06 归档）

【工作假说：底层同源，上层异构】

- 大脑皮层内部神经元共享统一的突触底层机制（对应 Synapse-Net L0 基底：
  共现、时序绑定、时间衰减联想网络），不存在分领域的专用底层单元；
- 视觉、听觉、语言功能的差异来自同一基底之上的不同上层回路
  （局部统计层 L1/L2/L4 + 特化 stub 回路），不是底层机制不同；
- 视网膜等外周感受器属于信号采集硬件，不属于皮层记忆基底；
- 语言不是神经元原生能力，是在通用联想基底之上演化出的特化上层回路。

【建模范围边界】

- 当前 Synapse-Net 建模对象 = 大脑**新皮层通用联想机制**；
- 海马、小脑、皮层下核团等其他脑区的特殊记忆机制**不在当前建模范围**，
  未来可作为可选插件模块外挂（L0 冻结基线不变）；
- 本路线验证的问题是：仅用通用联想基底 + 上层 stub，
  能否复现一大部分序列记忆、联想、语言泛化行为。

---

## 9. 实现版本 v1 / v2（2026-09-15）

**为什么要分版本**：E33–E36 连续三轮撞到的限制，**不是 L0 的架构约束，是实现约定**
（种子入口把值抹平）。架构（存储 + 激活）不可变；实现约定可变，但必须留痕。
完整规格见 [L0-SPEC.md](./L0-SPEC.md)。

```text
v1：synapse_net.py，sha256 cf99a07a0e60fd4d，【冻结】
    种子入口把值一律置 1.0（:99）→ 种子是不动点（构造性）
    适用范围：E0–E36 全部有效；tag v0.9.15-e36 即其精确快照

v2：synapse_net_v2.py（平行分支，从 E37 起），sha256 ee6df589eb2f6983
    唯一差异：种子入口支持带值 mapping，且**种子值常量**（传播不抬高也不降低）
    名字列表形式向后兼容（仍全部 1.0）→ 第一轮 11 处多节点种子调用语义不变
    实现形态：子类覆盖（只重写 activate，存储层继承 v1）→ 无第二份 learn/strength
```

**四条纪律**：

1. **v1 冻结**：文件、哈希、E0–E36 的证据链都不动；
2. **v2 是并行分支**：新文件，不覆盖 v1；
3. **不混用**：一个实验要么在 v1 上跑、要么在 v2 上跑，JSON 里声明所用版本与哈希；
4. **只读口径一致**：E37+ 的 JSON 同时记 `synapse_net.py`（v1，不变）与
   `synapse_net_v2.py`（v2，新哈希）——与 E27–E36 的只读检查同口径。

**改 L0 的正确姿势**（五步，见 L0-SPEC.md §5）：先判"架构 vs 实现" →
新建平行文件 → 写影响评估（改动面 / 受影响实验 / 新增语义决定）→ 更新版本表 →
在新文件上跑实验并记录哈希。**不许原地改 v1**：那会让 E27–E36 的只读记录无法自动复核
（与语义是否向后兼容无关——哈希一定变）。
