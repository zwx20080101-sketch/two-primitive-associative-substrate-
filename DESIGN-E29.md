# E29 设计：层级 = 多尺度读出（不是"涌现"）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-13。
依赖：E24 的谱聚类读出（`spectral` / `ari` / `impute_diagonal`）、E25 的块均值矩阵、
E26 的消融对照、E27/E28 的三条纪律（层文件 sha256 只读、阈值性质写清、无数据写 `null`+`reason`）。
**有 numpy 依赖**（谱聚类）；**不改任何层文件**。

---

## 0. 前置决定（五条）

### 决定一：命题措辞 = "层级 = 读出尺度 k 的函数"

**不写"超级模块涌现"。** 预检已证明：同一份 L0 统计，k=2 读出 2 个超模块（ARI=1.000）、
k=4 读出 4 个子模块（ARI=1.000）、k=3 是混合。把 k=2 的读出说成"涌现"，
等于**把预设参数的产物说成机制**——正是 B17/B19 那一类"看起来合理的错数"。

```text
C29 的表述钉死：层级 = 读出尺度 k 的函数；k 是预设参数（与 λ、hop_decay、K 同级），
不是从数据里自己长出来的结构。
```

### 决定二：第三层弱耦合（s3）只做记录，不进判据

预检：加入 s3=10 的跨超模块耦合后，k=2 与 k=4 的读数**完全不变**（连小数都一致）。
这个结果本身是信息：**弱耦合的第三层在 k≥2 时不可见，层数分辨率有上限**。
按辅助记录进 JSON；"层数上限"要另立实验（k 扫描 + 特征值间隙），不属于 E29。

### 决定三：阈值性质必须写明（避免读设计的人误读）

```text
P2 的 0.99 = 【余量阈值】，不是预检复现值（预检是精确的 1.000）。
            正式跑若 < 0.99 → 先查语料是否退化（多种子扰动），再谈机制。
P3 的 0.4  = 【粗阈值】，不是精确值（预检 0.286）。
            正式跑落在 0.35–0.40 → 记"通过但不理想"；
            落在 0.40–0.50 → 先复查消融是否彻底，再判定。
```

### 决定四：只读检查分两层（sha256 保层文件，保不住数据）

```text
第一层（纪律 ⑧）：三个层文件的 sha256 前后一致 —— 证明代码没被改写；
第二层（本次新增）：L0 统计跨 k 逐位相同 —— 证明【k 只影响读出，不影响统计】：
   - 每次 activate 前后：节点数 / 边数一致；
   - 所有 k 跑完后：节点数 / 边数 / 计数矩阵（逐元素）在所有 k 之间完全相同。
```

### 决定五：k 扫描扩成完整曲线（k=1..5），期望**预先写死**

```text
k=1：退化为单块 → ARI = 0.000（definitional: true —— 见下方注）
k=2：超模块读出 → ARI vs 超模块 = 1.000
k=4：子模块读出 → ARI vs 子模块 = 1.000（曲线峰值）
k=5：**【预检证伪了原预期】** 原以为会掉到随机水平（≤0.2），实测 ARI vs 子模块 = 0.928 ——
     因为 k=5 是把某个子模块【切成两块】（超分），而 ARI 对"细化"惩罚很轻。
     真正的信号是：ARI vs 超模块掉到 0.385，且出现【人造块】。
```

> **k=1 的 ARI=0 是 ARI 的定义性质**（单簇对任何多簇真值恒为 0），
> **不是这个语料或算法的经验结果**。JSON 里该点标 `definitional: true`。
> 不改这一条，k 曲线的最小端就是个**假数据点**——看起来像"实测退化"，其实是恒等式。

**由此得到一条判据读法（重要）**：判断"k 不对"**不能只看 ARI vs 子模块**——
ARI 对超分不敏感（0.928）。必须同时看 ① ARI vs 超模块、② 簇大小分布（是否出现人造块）。
这一条作为 E29 的方法学副产品登记（§8）。

---

## 1. 要解释的现象（宪章四问）

1. **基底参与的步骤**：只有绑定（L0 共现统计）；读出的层级结构由**读取端的尺度参数 k** 决定。
2. **需要外部模块吗**：不需要。
3. **结论归谁**：归"**读取端的多尺度读出**"。层级不是基底新增的能力，也不是新层。
4. **做到哪一步算停**：算到"k 曲线 + 消融 + L0 统计不变"就停，
   不实现层级抑制、不实现自动层级发现（k 由外部给定）。

**要解释的现象**：植入两层嵌套结构（4 子模块 / 2 超模块）后，
同一份 L0 统计能否**在不同读出尺度下呈现不同层级**？超模块是不是算法的产物？

**可证伪预期**：若消融跨子模块边后 k=2 仍能读出超模块（ARI 不塌），
则超模块是聚类算法的伪影，不是统计结构；若 k=4 读不出子模块，
则"多尺度可读出"不成立。两条都要能被数据打掉。

---

## 2. 语料

```text
4 个子模块：大小 3 / 3 / 4 / 4（共 14 节点），强度 90 / 100 / 110 / 120
           （质量 270 / 300 / 440 / 480，两两不同 → 非简并，沿用 E24 的可辨识性要求）
阶段 1（建子模块）：子模块内所有节点对 × 各自强度
阶段 2（建超模块）：跨子模块对 A-B、C-D × s2 = 40     → 超模块 = (A,B) 与 (C,D)
阶段 3（第三层，仅辅助）：跨超模块对 {(A,B)}×{(C,D)} × s3 = 10
消融对照：把阶段 2（以及阶段 3）的跨子模块边全部置零，重跑同一读出
```

---

## 3. 读出与指标口径

```text
读出 = E24 的谱聚类（impute_diagonal → 归一化拉普拉斯前 k 个特征向量 → k-means）
指标 = ARI（置换不变）—— 因为簇编号是任意的（B19：绝不用逐位比较）
  ARI vs 子模块真值（4 组）
  ARI vs 超模块真值（2 组）
尺度扫描 k = 1 / 2 / 3 / 4 / 5
多种子：谱聚类 k-means 跑 10 个种子，报中位数与范围（0.99 余量就是给它的）
L0 统计签名 = (节点数, 边数, **边集合排序后逐项比较**（含每条边的计数）)
              —— 用于决定四的第二层只读检查。选这个口径而不是"计数矩阵逐元素"，
                 因为它不依赖节点的排列顺序，更稳；**口径写死，实现时不许换**。
```

---

## 4. 预检结果（已执行，2026-09-13，只读即算即弃）

| 读出尺度 k | ARI vs 子模块（4 组） | ARI vs 超模块（2 组） |
| --- | --- | --- |
| 1 | 0.000 | 0.000 |
| 2 | 0.432 | **1.000** |
| 3 | 0.738 | 0.640 |
| 4 | **1.000** | 0.432 |
| 5 | 0.928（超分，见下） | 0.385 |

（10 个 k-means 种子下**每个 k 的读数都完全一致**，min = max = 中位数，
所以 0.99 的余量阈值实际给的是"语料种子扰动"的余量，不是 k-means 随机性的余量。）

```text
消融跨子模块边后：k=2 的 ARI vs 超模块 1.000 → 0.286（塌到接近随机）；k=4 的子模块读出仍 1.000
加入第三层 s3=10 后：k=2 与 k=4 读数完全不变（1.000 / 1.000）
k=5 的"超分"：把一个子模块切成两块 → ARI vs 子模块仍 0.928（ARI 对细化不敏感），
              但 ARI vs 超模块掉到 0.385 —— 这才是"k 不对"的信号
```

**读法**：层级不是"长出来"的，而是**同一份统计在不同 k 下呈现的结构**；
超模块的存在由植入的跨子模块统计支撑（消融即塌），不是聚类算法的伪影。

---

## 5. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1a | **接口回归**（沿用 E26 的先例）：用 E24 的植入语料跑本实验里的**同一套**谱聚类/ARI/impute_diagonal 实现，必须重现 **ARI=1.000、3 模块、ρ=0.0019** | 通过 | 接口被改坏，先修接口，不谈层级 |
| P1b | 语料构造核查：4 子模块 / 2 超模块的边集合逐边核对；4 个子模块质量两两不同（非简并） | 通过 | 语料 bug，改语料 |
| P2 | **多尺度读出**：k=4 → ARI vs 子模块 ≥ 0.99；k=2 → ARI vs 超模块 ≥ 0.99 | 0.99 是**余量阈值**（预检 1.000 精确）；< 0.99 先查语料退化 | 先查语料，再谈机制 |
| P3 | **消融必要性**：切掉跨子模块边后，k=2 的 ARI vs 超模块 ≤ 0.4 | 0.4 是**粗阈值**（预检 0.286）；0.35–0.40 记"通过但不理想"；0.40–0.50 先复查消融彻底度 | 复查消融，再判定 |
| P4 | **L0 统计不变（两条）**：① 每次 activate 前后节点数/边数一致；② 所有 k 跑完后，**边集合排序后逐项比较**（含每条边计数）在所有 k 之间完全相同（口径写死：不依赖节点排列顺序） | 通过 | 直接 FAIL（说明 k 影响了统计） |
| P5 | 只读（纪律 ⑧）：层文件 sha256 前后一致 | 通过 | 直接 FAIL |
| P6 | 登记：写入 C29 + B 条目（文本见 §8） | 通过 | — |
| 辅助 1 | **k 曲线**（期望预先写死，10 种子已实测）：k=1 → 0.000（**definitional: true**，ARI 定义性质，非经验结果）；k=2 → 超模块 1.000；k=4 → 子模块 1.000（峰值）；**k=5 → 超分：vs 子模块 0.928、vs 超模块 0.385 且出现人造块**（原预期"≤0.2"已被预检证伪，按实测写死）。不参与 PASS/FAIL，若正式跑不符须作为异常记录 | 仅记录 | — |
| 辅助 2 | **第三层 s3**：加 s3 后 k=2/k=4 读数是否变化（预检：不变） | 仅记录 | — |

> 说明：辅助 1 虽然不进 PASS/FAIL，但**期望值是预注册的**——k=5 掉到随机水平是
> "k 是预设参数"这条结论的直接证据；若它没掉，必须如实记录为异常，不许事后解释。

---

## 6. 预注册参数

```text
子模块大小 = 3/3/4/4（14 节点）   子模块强度 = 90/100/110/120
s2（跨子模块）= 40                s3（跨超模块，辅助）= 10
读出 = E24 谱聚类；k 扫描 = 1/2/3/4/5；k-means 种子 = 10（报中位数与范围）
指标 = ARI（置换不变，绝不用逐位比较 —— B19）
P2 阈值 = 0.99（余量阈值）        P3 阈值 = 0.4（粗阈值）
消融 = 跨子模块边（含 s3）全部置零后重跑同一读出
只读 = 层文件 sha256 + L0 统计跨 k 逐位相同（两条；L0 口径 = 边集合排序后逐项比较）
k=1 的 ARI 点标 definitional: true（ARI 定义性质，非实测）
P1a 接口回归 = E24 植入语料上重现 ARI=1.000 / 3 模块 / ρ=0.0019
```

**参数锁定（正式语料 = 预检语料，逐参数一致）**：

```text
节点数 14；子模块大小 3/3/4/4；强度 90/100/110/120；s2=40；s3=10（辅助）；
谱聚类 k-means 种子 = 1000..1009（10 个）；hop_decay = 0.9（L0 默认）
```

辅助 1 的"期望写死"依赖这条前提：**任何参数改动都必须重跑预检**，
否则 k=5 的 0.928 就不该被期待复现，写死的期望随即失效。

---

## 7. 产出物

- `experiments/exp29_hierarchy_scales.py` + `outputs/exp29_hierarchy_scales.json`；
- 通过或失败都登记 CLAIMS-REGISTRY；
- 不改任何层文件；不新增层；不引入语义/stub/奖励；
- 不声称"基底自己长出了层级"——层级是**读出尺度**的函数。

---

## 8. 拟登记的表述（待用户确认）

```text
C29：在 **14 节点、4 子模块（大小 3/3/4/4，质量 270/300/440/480 两两不同）、
跨子模块强度 s2=40** 的受控嵌套语料上，同一份 L0 共现统计支持多尺度读出：
谱聚类 k=2 读出 2 个超模块（ARI=1.000），k=4 读出 4 个子模块（ARI=1.000），k=3 为混合；
消融跨子模块边后 k=2 的超模块读出塌到 0.286（接近随机）。
因此层级 = 读出尺度 k 的函数；k 是预设参数（与 λ、hop_decay、K 同级），不是涌现的结构。
（参数必须随结论一起引用——不要被读成"任意嵌套语料都成立"。）

B（候选，编号待定）：**谱聚类读出需要外部给定尺度 k**。当前实现没有"从数据自动判断层数"
的机制；特征值间隙等自动选择准则**不在本工作范围**。层数分辨率有上限：
更弱的第三层耦合（s3=10）在 k≥2 时对读数无影响（辅助记录）。
```

**方法学副产品（并入总述，不单独开 B）**：E29 发现"**指标对退化方向不敏感**"——
k=5 把某个子模块切成两块时，ARI vs 子模块仍达 0.928。按 §10 的处理，它属于
方法学总述的**第四类**（见 [CLAIMS-REGISTRY.md](./CLAIMS-REGISTRY.md) 的合并总述）：
**任何单一指标都可能对某个方向的退化不敏感，必须配至少一个对偶指标**；
ARI 的对偶 = 簇大小分布（人造块检测）。

**编号决定（拍板：不占 B 编号）**：第四类**只写"实例：E29"，不占 B 编号**。
理由：合并总述本身即一条方法学结论；B17/B18/B19 之所以各有编号，
是因为它们各自是**具体的实现陷阱**（可单独复现、可单独修），
而第四类是同一层级的方法学教训，放进总述更干净。

---

## 9. 边界与不做

- **不做层级抑制**：E25 的 B12 已给出原理性结论（非负权重造不出互斥），E29 不问抑制、只问读出；
- **不做自动层级发现**：k 由外部给定，这是本实验的边界（已写进 B 条目）；
- **不把 k=2 的读出说成"涌现"**：那会把预设参数的产物说成机制；
- **不声称任何"概念层级/语义层级"结论**（B07）。
