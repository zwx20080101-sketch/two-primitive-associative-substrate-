# 网页玩具 2.0 设计稿（上层建筑版）

目标：在现有 L0 演示基础上，把"上层建筑"搬进来，并且能**按规则持续学习**：
底层（L0 双原语）一律不动，上层表示层与外部 stub 作为独立层级加进来；
每个现象对应我们做过的实验（E 编号），页面可复现、可继续训练。

## 1. 概念与边界（沿用 BOUNDARY）

- **L0 = 基底**（冻结）：共现绑定 + max 激活扩散 + 阈值读取；
- **上层表示层**：L1 方向、L2 双前缀、L4 组块——属于"上层建筑"，
  不是基底原生能力；
- **外部 stub**：语言规则（叠词外推）、动作（拼写/块 spell）、情绪（读取端增益）；
- 页面每类可视化都标注"基底 / 上层表示层 / 外部 stub"，功劳记账；
- 所有参数（hop_decay、λ、K、floor、threshold）均为预设值，
  在"公式态"行以**数值**体现（不贴静态公式文字）。

## 2. 事件输入升级（对应"继续训练"）

把输入从"同步窗口/相邻链"升级为**带语义的事件流**，每类事件喂给对应层：

| 事件类型 | 例子 | 喂给 | 记录 |
| --- | --- | --- | --- |
| 同步窗口 | 同一时刻出现的符号 | L0 | 两两共现 N |
| 有序序列 | 到达顺序 | L1 | n(a→b)，含重复标记 n(a→a) |
| 带边界单元 | 一个词（可含重复位置） | L4 | 成块 K 次 + spell 记录 |
| 序列（词/块组成） | 一句 | L2 + L4 块级 | 三元组 n((a,b)→c) |
| 情景/标记节点 | 主语/受事/情景 | 输入侧 | 供 E6a/E6b/E11/E16 用 |

交互即训练：连续输入事件，各层统计随每次 learn 更新；
支持"继续训练"（一直加经验），网络与上层结构持续成长。

## 3. 可视化分层（图层开关）

每层一个可开关图层：

- **L0 层**：连接与 N（现有）；节点激活值 a；
- **L1 方向层**：边上显示方向箭头，粗细 = 方向强度；
  同时给出 s(u→v) 与 s(v→u)（正强反弱，反向只剩 floor）；
- **L2 三元组层**：选择"前缀"（环状/列表），显示该前缀下候选分布条
  （体现 B01 双前缀上限：3 前缀外不消歧）；
- **L4 组块层**：块节点 CH_n（圆角块），双击/点开后展示 spell 序列
  （含双写位置）；块级连接加粗；
- **外部 stub 行为通道**：语言外推输出、动作拼写输出、情绪 fear 读数。

"公式态"行按层扩展为数值：N、s、n(a→b)、n((a,b)→c)、块 record、fear 增益。

## 4. 演示场景库（一个场景一个按钮，对应 E 编号）

| 场景 | 现象 | 用到的层 |
| --- | --- | --- |
| E1/E4 | 链自组织、双向等强 | L0 |
| E5 | Hello 正走顺、倒走费力 | L1（方向） |
| E15 | 双前缀消歧 [X1,B]→C | L2 |
| E6a | 上下文偏置（L1 等强 vs L2 唯一） | L1/L2 对比 |
| E16 | 角色（语序/格标记） | L1+L2+标记 |
| E7 | 密集事件近似（多次事件/高计数） | L0（标注：不模拟真实时间轴） |
| E12/E18 | 组块双写 + 拼写输出 | L4 + 动作 stub |
| E17 | 叠词外推 人人 | 语言 stub + L0 证据 + 听回 |
| E19 | 情绪 fear 差异 | 情绪 stub 读取端 |
| E20–E22 | 消融/规模/统计 | 属方法学，不直接演示（页面标注） |

## 5. 交互原则

- 控件少而明确：图层开关、场景下拉、种子多选（已有）、继续训练；
- 点/选节点看该层细节；块双击看 spell；
- 每个场景载入后给出"预期现象 + 对应实验编号"的一行说明；
- 所有层状态在"公式态"以数值实时刷新。

## 6. 诚实边界（页面明示）

- 上层按"上层建筑"呈现，不冒充基底；stub 标注外部；
- λ、hop_decay、K、floor、threshold 为预设参数，非涌现；
- 时间轴、真实多模态、无监督自组织不在本页范围；
- "规则等价复现"而非宣称生物学等价。

## 7. 实施阶段

| 阶段 | 内容 | 出口 |
| --- | --- | --- |
| v2.1 | L1 方向箭头 + E5/E4 场景 | 方向强/弱可见 |
| v2.2 | L2 三元组 + E6a/E15 | 候选分布条 |
| v2.3 | L4 组块 + E12/E18（含动作 stub） | 块节点 + spell |
| v2.4 | 外部 stub：语言 E17、情绪 E19 | 外推输出、fear 读数 |
| v2.5 | 图层开关 + 状态行统一 + 场景库整理 | 全量可切换 |

每阶段完成后做代码一致性检查；
涉及公式改动时引用 FORMULAS.md 并保持基底 L0 冻结。
