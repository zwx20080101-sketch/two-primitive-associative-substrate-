# E41 设计：工作记忆的值域编码（痕迹的排序可测）

状态：**设计稿，待用户过目；未写代码**。日期：2026-09-16。
依赖：E34/E35/E36（W 层与 C34–C36）、E37（C37 + B30，带值入口已登记）、E1 的 26 字母链。
**W 层一字不动；不改任何层文件；不引入 stub / 语义 / 奖励。**

---

## 0. 概念基础

### 0.1 抹平不在 W 层，在【注入调用点】（本实验成本极低的根本原因）

```text
W 层（experiments/exp34_working_memory.py 的 WorkingMemory；exp35 直接 import 复用；exp36 的
TaggedWorkingMemory 同构）从 E34 起就是 dict 承载值：

  self.items: dict[str, float]                 ← 值【一直在】items 里
  hold(acts)   = 按 (-value, node) 取 top-k
  seeds()      = 返回 [node]                   ← 值在这里被丢掉（调用点选了 list 形式）
  snapshot()   = 返回 dict[node, value]         ← 带值出口【早就有】

⇒ E34–E36 的"排序不可测"【不是 W 层缺陷】，是注入调用点选了 [node] 形式（抹平）。
⇒ E41 只改调用点（1 行），不改 W 层（0 行）：

     现在：        net.activate([seed] + W.seeds())
     E41（形态 A）：net.activate({seed: 1.0, **W.snapshot()})
```

### 0.2 形态 A 是唯一合理形态（形态 B 经实测排除）

```text
形态 A（dict 直接注入）  ：W 层 0 行 + 调用点 1 行
形态 B（list + 另存 value 序列）：组装出的 dict 与 A【完全相同】（预检实测 cfgA == cfgB 为 True）
⇒ B 只是"拆开再组装"，多绕一圈且无任何差异 ⇒ 不进候选列表。
exp36 的 tag 版同理：{n: v for n, (v, st) in W.tags().items() if st == step}（1 行转换）。
```

### 0.3 分工：本实验测的是【值序】，不是【时间序】

```text
值序（本实验）  ：痕迹之间谁强谁弱 —— 由 W 存的 value 承载，注入基底即可测（C36 的排序轴）
时间序（不在本实验）：痕迹来自哪一步 —— 需要 W 存时间戳并按时间重排，当前 W 只存值
⇒ 值序可测【不推出】时间序可测；C35 的"无时间顺序"边界【仍然成立】。
```

---

## 1. 预检摘要（2026-09-16，只读，未动任何文件）

```text
W 层现状：items 已保存值（{A:1.0, B:0.9, C:0.81}）；seeds() 丢值；snapshot()/tags() 带值出口已存在。

顺序可测性实测（26 链，节点集 {Z,A,B,C} 固定、A/C 值对调）：
  值域编码（形态 A）  差异节点 = 13
    · 种子节点 2 个：A (1.0 → 0.81)、C (0.81 → 1.0)      ← 平凡，等于输入差本身
    · 扩散节点 11 个：D…N（逐节点值见 §3 P3 的要求，JSON 里给全表）
  抹平值对照（list 形式）  差异节点 = 0                    ← E36 的结论复现

恒等式检查：注入种子 4 / 26 节点（非种子 22 个）⇒ 输出 ≠ 输入，非恒等式。
  仅种子节点上"输出 == 输入"（B30 常量语义的必然结果，属已知性质）；非种子节点上有真实差异。

【E37 与 E41 的 13 是巧合，不同源】E37 的差异集是 {B,C} ∪ D..N（对调 B/C）；
  E41 的差异集是 {A,C} ∪ D..N（对调 A/C）。数目同为 13，集合不同 —— 两实验的对照配置不同。
```

---

## 2. 预注册参数（跑前定死，跑后不改）

```text
语料：E1 的 26 字母链（A–Z 相邻对 ×100，26 节点 / 25 边）
W 层：复用 experiments/exp34_working_memory.py 的 WorkingMemory（k=3，tie-break = 值降序、同值按名升序）
      【W 层代码一字不改】；若需要值域变体，写在 E41 自己的实验文件里
注入形态：形态 A —— net.activate({seed: 1.0, **W.snapshot()})
注入种子：seed = "Z"，W 内容来自 activate("A") 的 top-3 = {A:1.0, B:0.9, C:0.81}
对照配置（节点集相同，仅值序对调）：
    config1 = {Z:1.0, A:1.00, B:0.9, C:0.81}
    config2 = {Z:1.0, A:0.81, B:0.9, C:1.00}

【阈值（预设，跑后不改）】
  P2 顺序可测：差异节点数 ≥ 10（预检实测 13；留空间给实现细节差异，不写"= 13"）
  P5 恒等式  ：至少 X = 5 个非种子节点上输出 ≠ 输入（预检 11；单看"≥1"太弱）
  逐节点差异：容差 1e-12
  【P2 的报告口径】必须显式写出实测值本身：若实测 = 13（与预检一致）→ 报"13 ≥ 10 ✓"；
    若实测 < 10 → FAIL，并先诊断实现细节（v2 是【确定性】的、无随机性，
    实现正确时实测应精确等于 13；偏离即提示实现有出入，而不是"噪声"）。
运行环境：PYTHONHASHSEED = 0
```

---

## 3. 判据（预注册）

### 3.0 判据分层

```text
· 实质判据（可 FAIL）：P1 值域保留 / P2 顺序可测 / P3 差异分层 / P5 恒等式
· 形式审计（预期恒成立，仍必须报）：P0 前置 / P6 只读
· 报告项            ：P4 与 E36 的对照（含逐节点列表）
· 双向              ：P7
```

### P0 前置

```text
W 层可跑通（hold / seeds / snapshot 三个方法行为与 E34 一致）；v2 的带值入口可用（C37 已登记）。
报告：W.items / W.seeds() / W.snapshot() 三者的实际内容。
```

### P1 值域保留

```text
经 W 层注入 v2 后，被注入节点的取值 = W 保存的原始值（逐位，容差 1e-12）。
依据：B30（v2 的常量语义）—— 种子值不被抬高、不被降低。
判据：A=1.0、B=0.9、C=0.81 三者逐位等于 W.items 里的值。
```

### P2 顺序可测（主判据）

```text
同节点集、值序对调（config1 vs config2）⇒ 基底输出差异节点数 ≥ 10（预设阈值；预检 13）。
对照（必须同报）：抹平值形态（activate([Z] + W.seeds())）下差异节点数 = 0（E36 的结论复现）。
```

### P3 差异分层（**必须逐节点列，不只报数字**）

```text
把差异节点分成两层，两层都要报：
  · 种子节点（平凡层）：差异 = 输入差本身（A、C）
  · 扩散节点（真传播层）：【逐节点列出节点名与两侧取值】—— 这是"真传播"的核心证据，
    只报"变了 11 个"不足以证明，必须给出是【哪些】非种子节点变了（预检：D…N 共 11 个）。
判据：扩散层节点数 ≥ 1 且逐节点列表非空（列表本身是报告项，不作为阈值判据）。
```

### P4 与 E36 的对照

```text
分类轴（E36）：哪些痕迹被包含 —— 可测（C36 已登记）
排序轴（E41）：谁强谁弱 —— 可测（本实验）
⇒ C36 的范围限定需要【状态更新】（解除排序轴的限定），见 §4。
报告：显式给出两轴的状态对照表（分类：E36 可测；排序：E36 不可测 → E41 可测）。
```

### P5 恒等式检查

```text
排除"全节点都是种子 ⇒ 输出 = 输入"的恒等式陷阱：
  ① 注入种子数 < 节点总数（预检 4 < 26）
  ② 至少 X = 5 个非种子节点上输出 ≠ 输入（预设阈值；预检 11）
另报：种子节点上"输出 == 输入"为 True —— 这是 B30 常量语义的【已知性质】，不是陷阱。
```

### P6 只读

```text
六文件逐一实测哈希：synapse_net.py cf99a07a0e60fd4d / v2 ee6df589eb2f6983 / v3 085be73a06f0694c
                   order_layer.py 419d54745a9eb795 / context_layer.py bd029d7bbdc345be /
                   chunk_layer.py 46f5857be0fc7c7e —— 跑前 = 跑后，且等于上述预期值。
另：experiments/exp34_working_memory.py 的 WorkingMemory 一字不动（跑前 = 跑后哈希）。
L0 签名（节点数 / 边数 / 边集合逐项）跑前 = 跑后。
```

### P7 双向结局

```text
通过（P1 + P2 + P3 + P5 全过）→ 新 C 条目（C40）+ C36 状态更新条款 + 越权嫌疑状态说明（见 §4）
不通过 → 如实记录哪条挂、实测值多少、失败分层（W 层 / 注入 / 基底）
【注】本实验【不预分配 FAIL 分支的 B 编号】—— 避免再造一个未触发编号进"未触发编号总表"。
【但"不预分配"≠"不记录机制性边界"】不通过时按 FAIL 的原因分两种处理：
  · 【机制性 FAIL】（基底/入口在原理上做不到）→ 跑完后【新立 B 编号】记录该机制性边界
    （此时编号是跑完才定的，不是预分配的）；
  · 【实现 / 判据问题】→ 修正后重跑（同 E39 v1 → v2 先例），【不】新立 B 编号。
```

---

## 4. 双向落点（跑前写死）

### 通过 → C40（痕迹的排序可测）

```text
C40：W 层保存的痕迹值经【已登记的带值种子入口】（v2，C37 + B30）注入基底时，
     痕迹的【排序可测】—— 同节点集、值序对调（A/C 互换）→ 基底输出在 13 个节点上不同，
     其中 11 个是【非种子（扩散）节点】（D…N，逐节点值见 JSON）。
     ⇒ C36 的"分类 ≠ 排序"里【排序轴】的限定被解除（分类轴不变）。
     范围限定：① 这叫【值序可测】，不是【时间序可测】—— C35 的"无时间顺序、无优先级"
     边界【仍然成立】；② 功劳归外部 W 层工具（从 E34 起就把值存在 items 里）
     + v2 的已登记入口，【不是基底新能力】；③ 本实验不改 W 层（0 行），只改注入调用点（1 行）；
     v1/v2/v3 一字不动。语料限于 E1 的 26 字母链。
```

### 通过 → C36 的状态更新条款（**方向是"解除"，不是"收窄"**）

```text
【C36 的状态更新（E41，2026-09-16）】
C36 原文的排序轴限定（"排序差异需要值域编码，不在本实验"）在 E41 下被【解除】，
前提是走 E37 已登记的带值入口（C37 + B30）：
  同集合、值序对调 → 基底输出在 13 个节点上不同（含 11 个扩散节点）。
C36 的原文保留（记录 E36 当时的真实判断）；本条款只更新状态，不改 C36 的判据与范围
（【分类可测】那一轴不变）。
【与 E38 对 C33 的做法方向相反】：E38 是【收窄】（加限定），E41 是【解除】（去限定）——
  两者都不改原条目的判据，只更新其适用状态。
```

### 通过 → "越权嫌疑"的状态说明（**加状态行，不删原句**）

```text
【越权嫌疑的状态说明（E41，2026-09-16）】
C36 写"越权嫌疑"时（E36，2026-09-14），带值 mapping 入口【尚不存在】。
E37（2026-09-15）已由用户拍板落地并登记为 C37 + B30。
E41 走的是【已登记的合法入口】，不是新改基底。
故"越权嫌疑"这一分句在 E41 之后【不适用】—— 但保留原文，作为时间戳证据。
```

### 通过 → C35 的状态注解（**加注解，不改 C35 文本**）

```text
【C35 的状态注解（E41，2026-09-16）】
C35 原文的"无优先级"记录的是 E35 当时的观察（被携带成员的值全部 1.0，注入口抹平）。
E41 之后，该表述的精确含义是：**W 自身不排序；排序信息来自被保存的 value，
注入时由入口保留**。C35 原文保留；本注解只澄清语境，不改 C35 的判据与范围。
【与 C36 处理方式一致】原文保留（时间戳证据）+ 注解澄清（语境更新）；
  两者都不改原条目的判据。
```

### 不通过 → 如实记录

```text
不登记 C40。记录哪条判据挂、实测值多少，并给出失败分层：
  ① W 层是否保存了值（P0/P1）→ ② 注入是否传了值（P1）→ ③ 基底是否保留值（B30 已确认）
不预分配 B 编号（见 §3 P7 的注）。
```

---

## 5. 影响评估

```text
(a) C34（跨次携带）
    不受影响 —— 它不涉及排序，只涉及"是否被携带"。E41 不改 W 层、不改注入语义的携带部分。

(b) C35（载体行为：无优先级、无时间顺序）
    【不受影响，但加状态注解（不改 C35 文本）】E41 的值域序是【值序】，不是【时间序】。
    C35 说的"被携带成员的值全部 1.0"是【抹平注入】下的观察；E41 改的是注入形态，
    不改 W 的"多层覆盖"行为，也不引入时间戳。
    ⇒ C35 的"无优先级、无时间顺序"边界在 E41 之后【仍然成立】。
    注解文本见 §4「C35 的状态注解」——与 C36 同样采取"原文保留 + 注解澄清"的方式，
    不修改 C35 的正文（§5 原先写的"表述需要精确为…"等于改文本，已按此改为注解）。

(c) C36（分类 ≠ 排序）
    直接受影响 —— 加【状态更新条款】（解除排序轴），见 §4。原判据与分类轴不变。

(d) B30（常量语义使种子冻结）
    是 P1 的直接依据，不受影响。

(e) 读取端工具定位
    W 层是读取端工具（E34–E36 已定性）；本实验不改基底、不改 W 层代码、不改层文件。
```

---

## 6. 产物与不做

**产物**

- `experiments/exp41_seed_value_encoding.py` + `outputs/exp41_seed_value_encoding.json`；
- 登记（双向，跑前写死）：通过 → **C40 + C36 状态更新条款 + 越权嫌疑状态说明**；
  不通过 → 如实记录（不预分配 B 编号）；
- **W 层一字不动**（新 W 变体若需要，写在 E41 自己的实验文件里，不改 exp34）。

**必报清单**

```text
① W.items / W.seeds() / W.snapshot() 三者的实际内容（形状对照）
② config1 / config2 的注入字典
③ 差异节点数（形态 A）与抹平对照的差异节点数（0）
④ 【扩散节点逐节点列表】（节点名 + 两侧取值）—— 不只报数字
⑤ 种子节点差异（平凡层）与扩散节点差异（真传播层）的分层
⑥ 恒等式检查：注入种子数 / 节点总数；非种子节点中输出 ≠ 输入的个数（阈值 X=5）
⑦ E37 与 E41 的"13 不同源"说明（差异集不同：{B,C}∪D..N vs {A,C}∪D..N）
⑧ 六文件哈希（前后）+ exp34 W 层哈希（前后）+ L0 签名（前后）
```

**不做**

- 不改 W 层代码（0 行）、不改层文件、不新增层；
- **不测"分类 + 排序的组合态"**（如"只注入 step=1 的痕迹、且按值序"）——
  那是 E36 的分类与 E41 的排序【组合】后的新命题，留作后续（E42 候选），不占 E41；
- 不声称"时间序可测"（值序 ≠ 时间序；C35 保留）；
- 不声称"基底长出了排序能力"（功劳归 W 层 + 已登记入口）；
- 预注册阈值跑后不改（P2 的 ≥10、P5 的 X=5、容差 1e-12）。
