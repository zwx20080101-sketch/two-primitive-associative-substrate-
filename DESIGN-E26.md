# E26 设计：S3 模块间协调（有效信息 EI / 因果涌现）

状态：**设计稿，待用户拍板；未写代码、未跑实验**。日期：2026-09-11。
依赖：E24（模块划分读出）+ E25（模块宏观状态 V_i 与 β=2.0 动力学）。

沿用仓库的固定纪律：先判据、后实验；失败就冻结现场、如实登记为 B 条目；
不改 L0/L1；不引入语义/情绪/奖励；不把分析指标说成基底新能力。

---

## 0. 三个前置问题的拍板（本文档的核心）

### 决定一：E26 的"协调"是合作型还是竞争型？

**拍板：主口径 = 合作型耦合；另设第二口径 = 共享下游仲裁；
竞争型互斥（赢者通吃）明确排除在 E26 之外。**

三档分开，不许混：

| 档 | 协调形态 | 权重需求 | 非负 L0 权重下能否造出 | E26 处理 |
| --- | --- | --- | --- | --- |
| A | **合作型耦合**：模块间正连接 → 同高同低 | 非负 | 能 | **主口径**（P3/P5） |
| B | **共享下游仲裁**：两个源模块投射同一目标，按相对强度分先后 | 非负 | 能 | **第二口径**（C3' 语料） |
| C | **竞争型互斥**：A 高则 B 低（赢者通吃） | **必须负权重** | **不能**（E25 的 C3 已实测，B12） | **不做**；另立实验时须显式声明负权重来源（外部调制） |

理由：

1. E25 的 C3 已经在非负权重下实测"两模块正耦合 → 联合模式只有 2（同高同低），
   赢者通吃 = False"。在 L0 共现计数上再做竞争型，得到的结论是**已知的**，
   跑一遍只是重复 B12。
2. 竞争型互斥需要负权重；负权重的唯一合法来源是**外部调制**（stub）。
   一旦引入，结论归属就不是"基底协调"而是"外部控制"，属于另一层的问题。
   同一实验里既做基底协调、又偷偷引入外部负权重，正是 BOUNDARY §1 要禁止的越权。
3. 档 B（共享下游仲裁）是**非平凡**的：它能产生"谁驱动谁"的方向性路由，
   而这是非负权重能做到的、最容易与"划分算法的伪影"区分开的现象。

### 决定二：模块划分是否沿用 E24/E25？

**拍板：沿用同一套接口与算法，划分在 E26 自己的语料上重跑并冻结；
用 E24 语料做回归校验，证明接口没被改动。**

```text
接口（与 E24/E25 完全一致）
  L0 统计（共现计数矩阵）
    → impute_diagonal（行最大值填对角，E24 修订）
    → 归一化拉普拉斯前 K 个特征向量 + k-means（E24 定稿算法）
    → 模块划分 assign（E26 语料上重算，算完冻结，后续分析不再动）
    → 模块宏观状态 V_i（E25 定义：模块内节点二值化后的均值）
```

- **不沿用 E24 的划分结果**（语料不同，划分必须重算），但**沿用算法、参数、K 的选择规则**。
- **回归校验（P1）**：E26 代码跑 E24 的植入语料，必须重现 ARI=1.000、3 模块、
  ρ=0.0019。重现不了 = 接口被改坏，先修接口，不谈协调。
- 三个语料都用同一套谱聚类读出（E24 已证明零耦合语料上谱聚类照样能分出植入的 3 模块）。
  预注册例外：若某语料上多种子一致性 ARI < 0.8（划分不稳定），该语料改用**植入真值标签**
  作为划分，并在 JSON 里标注"划分未达稳定门限，已改用真值"，避免"依赖阻塞"卡死对照。
  （C3' 无真值标签时改用多种子多数投票划分，并记录一致性。）

### 决定三：语料是否需要非成对事件？

**拍板：需要，而且是硬前提。至少 C1' 与 C3' 必须含窗口/序列事件。**

- E25 的 P6 已实测：**成对事件下 L1 对称化方向计数 = L0 共现计数，方向信息为零**。
  只要语料还是成对事件，任何"方向性协调"都不可能被观测到——不是机制不行，
  **是语料没给信息**（与 E24 的分叉语料 bug 同一类坑）。
- 所以 E26 的事件形式改为**窗口事件**：一次事件按顺序注入一串 token
  （如 `[m1_a, m1_b, m2_a, m2_b]`），这样：① 窗口内的跨模块对给 L0 提供**跨模块共现信号**；
  ② 序列顺序给 L1 提供**非零方向计数**。
- **耦合必须由语料植入，不能由算法制造**：C1' 的跨模块信号写在语料里，
  划分算法只负责读出模块，不负责建立耦合。若某语料的耦合密度/块均值不达标，
  先改语料再谈机制（P2 构造核查）。

---

## 1. 要解释的现象 / 可证伪预期（宪章四问）

1. **基底参与的步骤**：绑定（跨模块共现 + 时序计数）与传播（宏观状态由 L0/L1 统计决定）。
   EI 是**读取端分析指标**，不在基底内。
2. **需要外部模块吗**：不需要。E26 不引入任何 stub、语义、情绪、奖励。
3. **结论归谁**：归"**基底统计 + 读取端分析**"。若宏观协调成立，它是基底统计的
   **宏观后果**，不是新机制、不是新层能力。
4. **做到哪一步算停**：算到"EI_int 是否随跨模块耦合量单调上升、能否被消融精确归零、
   宏观密度是否不劣于微观"就停。不追"协调 = 理解 / 意图 / 意识"。

**要解释的现象**：L0 里被植入的模块间耦合，是否在**模块宏观层面**表现为可测量的
信息传递（协调），而不是只在节点层面存在的一堆边。

**可证伪预期**：若把模块间耦合换成同规模的零耦合语料，**EI_int 应精确等于 0**；
若把耦合边切断（消融），同样应精确回到 0。回不到 0，就说明"协调"里混了划分
或读出的伪影。且 EI_int 应随跨模块耦合量**单调上升**（剂量响应），
而不是"有耦合就跳到某个值"。

---

## 2. 语料

**规模上限（由 §3.4 的可行性实测决定）**：E26 的语料一律 **12 个节点**
（微观状态 2¹² = 4096，精确 EI 单次约 0.2 s、峰值内存约 134 MB）。
E24 的 15 节点语料**不能**用于精确 EI（2¹⁵ 需要 8.6 GB），只用于回归校验。

| 语料 | 形式 | 内容 | 用途 |
| --- | --- | --- | --- |
| **C1' 耦合** | 窗口事件 | 3 模块，大小 3/4/5（共 12 节点），强度 90/110/130（质量 270/440/650，两两差距 ≥38%，非简并）+ **半稠密跨模块耦合**：约 50% 的跨模块对有权重，权重 w=65（剂量扫描另取 0/10/25/40/65） | 主测试 |
| **C2' 零耦合** | 窗口事件 | 同规模、同模块内强度，**事件只在模块内**，无任何跨模块共现 | 负对照 |
| **C3' 仲裁** | 序列事件 | 源 S1/S2 + 目标 T（4/4/4 = 12 节点），强度 90 / 60 / 130；S1、S2 都投射到 T，但强度不等 | 第二口径（共享下游仲裁） |
| **R 回归** | 成对事件 | E24 的 `corpus_planted()` 原样（15 节点，只做划分与 ρ 校验，不算 EI） | 接口回归校验（P1） |

**为什么是"半稠密"而不是"稠密"或"稀疏"**——见 §2.1 的预检数据：
稀疏耦合会被块均值稀释（ρ 只有 0.167），稠密耦合虽然 ρ=0.5 但把动力学推进饱和区
（EI_int 不再单调、置换对照退化）。半稠密是唯一同时满足"耦合真实存在 + 剂量响应单调"
的区间。

**前置门（P2，已按预检修订）**：不再用 ρ 当门槛（ρ 是**块均值**，会被大量零共现的
跨模块对稀释，不适合当"有没有耦合"的判据）。改为：

```text
C1'：跨模块非零对密度 ≥ 50%（构造目标）   且   跨模块块均值 ≥ 25（构造目标）
C2'：跨模块非零对 = 0（逐对检查，不靠阈值）
ρ 仍然计算并写进 JSON，但只作为"动力学落在独立分支还是同步分支"的诊断（E25 含义）
```

说明：这条门是**语料构造核查**，不是显著性检验。不能用"跨模块块均值 > 置换零分布 95 分位"
当门——置换是块均值的**不变变换**（块均值 = 跨模块总权重 / 跨模块对数），比出来恒等。
显著性由 P3 的剂量响应承担，不依赖零模型。

不达标 = 语料 bug，先修语料再谈机制（E24 的教训：先查语料，再怪算法）。

### 2.1 实现前预检（已执行，2026-09-11，不改判据）

**预检一：ρ 的真实算法与稀释效应。** ρ 用的是**块均值**（E25 的 `block_matrix`），
不是单边最大值。跨模块耦合只覆盖一部分对时，块均值会被"权重为 0 的跨模块对"拉低：

```text
跨模块耦合形态        跨模块块均值    ρ = 块外/块内    谱聚类 ARI
无耦合                    0.0          0.000          1.000
稀疏（12/47 对）×65       21.7          0.167          1.000
半稠密（约 50%）×65       34.7          0.267          1.000
整片稠密（47/47）×65      65.0          0.500          1.000
整片稠密 ×91              91.0          0.700          1.000
```

两条结论：① 想靠"把跨模块权重提到 65"来达到 ρ≥0.5，**必须同时是整片稠密**，
只提高权重而不提高密度没用；② **稠密耦合不会破坏模块划分**——ARI 在所有形态下都是 1.000，
所以"要 ρ 就得牺牲划分"这个担心不成立。

**预检二：剂量响应（半稠密，改变跨模块权重 w，6 个语料种子）。**

```text
w            0       10      25      40      65      91
EI_int 均值  0.0000  0.0096  0.0287  0.0391  0.0489  0.0559
（6 个种子严格单调 w=0→65：全部成立；w=0 时 EI_int = 0，最大绝对值 2.1e-15）
```

**预检三：置换零分布（同跨模块总量、随机重排位置，半稠密 w=65）。**

```text
观测 EI_int = 0.0609   零分布 mean = 0.0553   p95 = 0.0637   max = 0.0667
→ 观测值没有超过 95 分位：随机重排与植入图案无显著差异
```

这条推翻了我最初写的 P3（拿置换零分布当主判据）。它说明：**EI_int 主要由
"跨模块耦合总量"决定，不分辨耦合的具体图案**（登记 **B15**）。因此主判据改为
**剂量响应**——比"某一个数大于零分布"更难被伪影解释。

**预检四：饱和区（同一批种子，w=91）。** w=91 时 EI_int 在种子间忽高忽低
（0.0244 / 0.0467 / 0.0504 / 0.0507 / 0.0677 / 0.0953），不再单调，且跨模块权重
整片相同时"随机重排"退化为恒等变换（零分布不再是零分布）。**登记 B16**，
剂量扫描因此只取 w ≤ 65，w=91 仅作饱和区的记录点。


---

## 3. 指标：EI 的精确定义（可枚举，不需蒙特卡洛）

机制（与 E25 同源）：一步并行更新，节点 i 的目标取值由
`tanh(β·Σ_j W_ij x_j)` 决定的 Bernoulli 概率给出（Glauber，β=2.0）。

```text
EI(S) = I(X_t ; X_{t+1})  ，X_t ~ 均匀（最大熵干预，Tononi 口径）
```

- **微观**：`x ∈ {−1,+1}^12`（节点二值）→ 状态数 2¹² = 4096，**精确枚举**。
- **宏观**：`V ∈ {−1,+1}^3`（模块均值按 0.5 二值化，同 E25）→ 状态数 2^3 = 8，**精确枚举**。
- 条件熵有闭式（每节点独立 Bernoulli，`H(X'|x) = Σ_i h(p_i(x))`），
  边际熵对状态精确求和，所以 **EI 是精确值，不是估计值**。
  因此方差**不来自采样**，只来自语料（哪些边被学到）——置信区间因此定义在语料侧。

### 3.0 主指标：整合量 EI_int（不是裸的 EI_macro）

裸的 `EI_macro = I(V;V')` **不能**用来测"协调"：它主要被每个模块的**自预测**
（模块自己预测自己）贡献，跨模块耦合为零时它照样很高（预检里零耦合语料
EI_macro = 0.9172）。所以主指标定义为**整合量**：

```text
EI_int = EI_macro − Σ_m EI_macro,m        （宏观整体 − 各模块单独之和）
EI_macro,m = I(V_m ; V'_m)                （第 m 个模块自己的二态互信息）
```

**精确性质（可当机制自洽性检查）**：当模块之间没有边时，机制按模块因子化，
而互信息对乘积分布可加，所以

```text
无跨模块边  ⇒  EI_int = 0   （精确为 0，不是"接近 0"）
```

预检实测：零耦合语料 `|EI_int| ≤ 2.1e-15`（浮点零），半稠密 w=65 时 `EI_int ≈ 0.049`
（6 个种子范围 0.0435–0.0519）。**中间隔了 13 个数量级，不需要零分布也知道是两回事。**
这条性质同时把 P4（零耦合对照）和 P5（消融）变成**精确预测**，而不是含糊的"回落 80%"。

### 3.4 实现前可行性验证（已执行，2026-09-11）

精确 EI 的代价由"混合分布的展开"决定：把每个状态 x 对应的乘积分布摊开成
2^n 维向量，内存是 **2^n × 2^n**。实测（随机 12 节点对称非负 W，β=2.0）：

```text
n=8   状态 256     EI_micro=3.2231    0.00 s
n=10  状态 1024    EI_micro=3.4669    0.01 s
n=12  状态 4096    EI_micro=3.7376    0.15 s     内存 ≈134 MB
推算 n=15 需 8.6 GB → 不可行（因此 E26 语料锁定 12 节点）
```

结论：12 节点下 500 次保度置换零分布约 2 分钟，完全可跑；
**不需要蒙特卡洛估计**，因而不需要 bootstrap——"EI 估计方差大"的问题
在 12 节点规模下不存在。若将来要上更大规模，那时才需要改用采样估计，
并且必须重新预注册估计口径（不属本轮）。

### 3.1 归一化口径（跑前定死，跑后不改）

`EI_micro` 天然比 `EI_macro` 多出自由度的优势，直接比大小会得出错误结论。
预注册**双口径同时报告**，PASS/FAIL 只认第一口径：

```text
口径一（主口径，判据用）: EI_density = EI / log2(|S|)     微观 12 bit / 宏观 3 bit
口径二（旁证，仅记录）  : EI_raw（未归一化比特）
```

理由：口径的选择会改变结论方向，所以必须**跑前定死**、并且**两个都报**，
不许跑完挑一个好看的。哪条口径支持结论、哪条不支持，都写进 JSON。

### 3.2 零分布（**仅记录，不作判据**）与语料侧区间

- **零分布（跨模块权重重排）**：保持块内结构与跨模块总权重不变，把跨模块权重在
  跨模块对之间随机重排，500 次，报 EI_int 的零分布（mean/p95/max）。
  **不作为判据**——预检三已证明它与植入图案无显著差异（见 §2.1），
  拿它当门槛会把"耦合真实存在"误判成失败。
  （B11 的教训仍然成立：不能用"稳定性"当"有结构"的判据。）
- **语料侧区间（判据用）**：10 个语料种子（跨模块对抽样 + 强度抖动）→
  剂量响应必须在每个种子上都严格单调。

### 3.3 辅助记录（不参与判据）

- **EI–经验量曲线**：事件次数 0 / 1 / 5 / 20 / 100 时各自的 EI（检验协调是
  "随经验长出来的"，而不是语料先验塞进去的）。"多少参数、多少经验才长出结构"
  正是本项目关心的问题，这里只记录曲线。
- **方向性（结构口径，公式预注册）**：把 L1 的有向计数聚合成模块级有向块矩阵 D
  （`D_ab` = 模块 a 的节点紧邻先于模块 b 的节点的计数之和），定义非对称度

  ```text
  asym = ‖D − Dᵀ‖_F / ‖D‖_F          （‖·‖_F 为 Frobenius 范数）
  ```

  预期：C1'（窗口事件）asym 显著 > 0；成对事件下 D 对称，asym ≈ 0（E25 的 P6）。
  这是"非成对事件"这个决定的直接检验。
- **方向性（功能口径，探索性）**：C3' 上再报一次**方向的**信息量——转移熵

  ```text
  TE(S→T) = I( V_T' ; V_S | V_T )        （给定目标自身当前态后, 源的额外预测力）
  口径（预注册，与主实验对账）：同一条 Glauber 一步机制、同一个均匀干预分布、
  同一套精确枚举 —— 不用采样估计、不用其他 TE 估计器。
  报 TE(S1→T)、TE(S2→T)、TE(T→S1)、TE(T→S2)
  ```

  结构口径（asym，来自 L1 有向计数）是必要条件、功能口径（TE）是充分证据，
  两者都报，都不参与 PASS/FAIL。

  **预期先写在预注册里**：主实验的机制 W 由 **L0 共现计数**构成，是**对称**的，
  所以 TE 前向与后向**可能接近相等**——若出现这种结果，说明"方向性来自 L1 层、
  不来自 L0 机制"，如实记录，不算失败（该口径本来就是探索性的）。

---

## 4. PASS / FAIL（预注册）

| # | 判据 | 阈值 | 不过时怎么处理 |
| --- | --- | --- | --- |
| P1 | 接口回归：E26 代码在 E24 语料上重现谱聚类 ARI=1.000、3 模块、ρ=0.0019；各语料划分覆盖 100% 节点 | 通过 | 接口被改坏，先修接口 |
| P2 | 语料构造核查（修订）：C1' 跨模块非零对密度 ≥50% 且跨模块块均值 ≥25；C2' 跨模块对 = 0（逐对检查） | 通过 | 语料 bug，改语料，不算机制结论 |
| P3 | **协调存在（主判据，剂量响应）**：对 w=0/10/25/40/65，**10 个语料种子每一个**都必须满足 `EI_int(0) < EI_int(10) < EI_int(25) < EI_int(40) < EI_int(65)`；且 `EI_int(w=65) ≥ 0.01 bit`。**不取均值凑**，**任一种子不单调即 FAIL** | 通过 | 记录负结果；若单调性失败但 EI_int>0，说明落在饱和区，改语料密度 |
| P4 | 负对照（**精确预测**）：C2' 的 EI_int = 0，容差 \|EI_int\| < 1e-9 | 通过 | 不为 0 = 实现 bug 或理论错，先查实现 |
| P5 | 必要性消融（**精确预测**）：C1' 把跨模块边置零后 EI_int = 0（同容差 1e-9） | 通过 | 不为 0 = 划分/读出引入了耦合，记录 |
| P6 | **因果涌现检验（双向判据）**：EI_macro_density 与 EI_micro_density 比较；\|差值\| < 1e-6 记 B"无显著差异" | 双向都算完成 | 高于 → 记 C；不高 → 记 B"该层不必要"。不硬凑正结论 |
| P7 | 无越权：不改 L0/L1；不引入负权重与 stub；EI 只读、不新增结构节点；跑前跑后 L0/L1 计数一致 | 通过 | 直接 FAIL |
| — | **C3'（第二口径）：探索性观察，不参与 PASS/FAIL**（按评审建议） | 记录 | 数据照报：EI_int、定向 TE 差、asym |

> P6 是本实验的**研究问题**，不是"必须为正"的门槛。得出"宏观不如微观（该层不必要）"
> 也是一个合格的科学答案，按惯例登记为 B 条目。预检数值提示：半稠密 w=65 下
> `EI_micro_density≈0.373`、`EI_macro_density≈0.310`，**倾向于落在 B 侧**——
> 这个方向先写在预注册里，避免跑完再解释。
>
> P3 的置换零分布结果（§2.1 预检三）在本设计中**不作为判据**，只作为"图案无关"的
> 负结果记录：`EI_int` 由跨模块耦合**总量**决定，不区分耦合落在哪些对上。

---

## 5. 预注册参数

```text
β = 2.0（与 E25 一致，Glauber 更新）          干预分布 = 均匀（最大熵）
节点二值化 = 符号（x_i = ±1）；模块二值化阈值 = 0.5（同 E25）
微观机制权重 = impute_diagonal(M) 后整体除以全局最大值（对角插补与 E24 读出同处理）
K = 3 模块（C1'/C2'）；C3' = 2 源 + 1 目标；每个语料 12 节点
微观规模上限 = 12 节点（2¹² 状态；精确 EI；见 §3.4 实测）
跨模块耦合密度 = 50%（半稠密）；剂量扫描 w = 0 / 10 / 25 / 40 / 65（w=91 仅记录，饱和区）
保度置换零分布 = 500 次                     语料种子 = 10
主判据阈值 = 剂量响应严格单调 + C1' EI_int ≥ 0.01 bit
精确零容差 = 1e-9（P4/P5）                   P6 容差 = 1e-6
主口径 = EI_density = EI / log2(|S|)        旁证口径 = EI_raw
EI–经验量曲线取样点 = 0 / 1 / 5 / 20 / 100 次事件
```

---

## 6. 明确不做（out of scope，防止越权）

- **不做竞争型互斥切换**（需负权重；E25/B12 已给出原理性结论）。
- **不引入任何外部 stub / 负权重 / 语义 / 情绪 / 奖励**。
- **不把 EI 说成"自由能""意识""理解"**；它是信息论分析指标，
  与"系统知道自己在做什么"无关。
- **不改 L0/L1 的任何公式**；EI 只在读取端算。

---

## 7. 产出物与登记

- `experiments/exp26_module_coordination.py` + `outputs/exp26_module_coordination.json`；
- **剂量响应必须整段落进 JSON**（不是只记一个 PASS）：

  ```json
  "dose_response": {
    "w_values": [0, 10, 25, 40, 65],
    "EI_int_mean": [0.0000, 0.0096, 0.0287, 0.0391, 0.0489],
    "EI_int_per_seed": [[...], [...], "..."],
    "monotone_per_seed": [true, true, "..."],
    "min_EI_int_w65": 0.0435
  }
  ```

  另外照旧写出每个语料的：划分（assign + 覆盖率 + 多种子一致性 ARI）、
  EI_micro / EI_macro / 各模块分量 / EI_int、ρ、asym、TE（C3'）、
  `EI–经验量曲线`、以及 L0/L1 只读检查的前后计数。
- 通过或失败都登记 CLAIMS-REGISTRY（正结果 C26 / 负结果 B15 起）；
- **已预料的负结果 1（B15）**：`EI_int` 由跨模块耦合**总量**决定、不区分耦合图案，
  因此置换零分布当主判据有**假阴性**风险（§2.1 预检三：观测 0.0609 vs p95 0.0637）。
- **负结果 2（B16，实测后修正）**：整片**等权稠密**耦合会让"跨模块权重重排"退化为
  **恒等变换**（零分布失效）——这一条是结构性的，成立。至于"w≥91 进入饱和区导致
  EI_int 非单调"：**预检构造成立、正式语料未复现**（见 §9），只作预检观察保留，不推广。
- **实现期陷阱（B17/B18）**：状态索引约定与乘积分布表列序不一致（块对角机制会假阳性
  报出 EI_int=1.82）；TE 分子漏掉对非目标位的边际化（会算出负 TE）。见 §10。
- 若 P6 得出"宏观不优于微观"，登记 B 条目并在 LAYERS.md 标注 S3 为"已验证但该层不必要"。

---

## 8. 与 E25 的关系（接口继承图）

```text
C1 语料（E24）→ L0 统计 → 谱聚类划分（E24）→ 模块宏观状态 V（E25）→ 双稳/滞回（E25）
C1' 语料（E26）→ L0 统计 → 谱聚类划分（E26 重算并冻结）→ 同样的 V 定义 → EI_macro vs EI_micro（E26）
```

两代实验共用同一套"L0 统计 → 划分 → 宏观状态"接口；E26 只在其上加了一个
**信息论读出**，不新增任何动力学规则。

---

## 9. 实测结果（2026-09-11，P1–P7 全 PASS）

| 项 | 结果 |
| --- | --- |
| P1 接口回归 | E24 语料重现 ARI=1.000、3 模块、ρ=0.0019（接口未改动） |
| P2 语料构造核查 | C1' 密度 0.51（24/47 对）、跨模块块均值 37.9、ρ=0.273；C2' 跨模块非零对 = 0；划分覆盖 100% |
| P3 剂量响应 | **10/10 种子严格单调**；EI_int 均值 = 0.0000 / 0.0118 / 0.0560 / 0.1021 / **0.1474**；min(EI_int@65) = 0.1317 ≥ 0.01 |
| P4 零耦合对照 | EI_int = −1.3e−15（浮点零，容差 1e−9） |
| P5 必要性消融 | 未消融 0.1403 → 置零跨模块边后 −1.3e−15（精确归零） |
| P6 因果涌现 | EI_micro_density 0.395 vs EI_macro_density 0.515，差 +0.121 → **C：宏观不劣于微观** |
| P7 无越权 | L0/L1 跑前跑后 (12, 43, 12) 一致；EI 只读 |
| 辅助：经验曲线 | 跨模块事件 0/1/5/20/100 次 → EI_int = 0 / 0.0001 / 0.0032 / 0.0406 / 0.1462（随经验长出来） |
| 辅助：结构方向性 | C1' 的 asym = 0.720（有序事件使 L1 方向不对称，"非成对事件"这个决定真的起作用了） |
| C3' 探索性 | EI_int = 0.166；asym = 1.058；TE(S1→T)=0.091、TE(T→S1)=0.156、TE(S2→T)=0.034、TE(T→S2)=0.135 |

**P6 的方向与我预注册时写的预期相反**（当时按预检推测会落在 B 侧，实测落在 C 侧）。
如实记录：按预注册的密度口径，宏观整合不劣于微观。

**C3' 的探索性发现（如实记录）**：结构口径有明确方向性（asym=1.058，因为语料只植入
S→T 的有序事件），但**功能口径没有前向优势**——TE(T→S1)=0.156 反而大于 TE(S1→T)=0.091。
原因：主实验机制 W 由 L0 共现计数构成，是**对称**的，信息在机制层面双向流动；
方向性只存在于 L1 统计口径，不存在于 L0 机制。这与 §3.3 预注册的提醒同向
（当时写"可能接近相等"，实测是后向更大；结论性质不变：**对称机制不产生方向性优势**）。

**B16 的修正（如实记录）**：预检构造下 w=91 的 EI_int 非单调（0.0447 < 0.0609），
但**正式语料没有复现**——w=91 在 10/10 种子上仍然高于 w=65（0.141–0.183）。
因此该饱和区结论只对预检构造成立，不推广；B16 只保留"整片等权稠密耦合会让
跨模块权重重排退化为恒等变换"这一结构性部分。

## 10. 实现期发现的两个陷阱（B17 / B18）

**B17：状态索引约定必须与乘积分布表的列序一致。** `_product_table` 用倍增法展开
2^n × 2^n 的乘积分布表：第 i 轮拆出 node i 的 ±1 两支，所以 node i 对应索引第 i 位
（**LSB-first**）。若状态枚举写成 MSB-first（看起来更"顺眼"），行标签与列标签就会错位，
**块对角机制也会算出 EI_int = 1.82**（本应为 0）。这个不一致不会报错，只会给出一个
"看起来合理"的正数，极难靠数值直觉发现。自查办法：块对角语料上 EI_int 必须精确为 0。

**B18：TE 的分子要对非目标位求和。**
TE = Σ P(v_T,v_S,v'_T) log2[ P(v'_T|v_T,v_S) / P(v'_T|v_T) ]。
若把分子写成整个下一步状态的 P(v'|v)（漏掉对非目标位的边际化），会算出**负的 TE**
（本实现首轮得到 −0.78）。互信息不可能为负——这是最好的自检信号。
