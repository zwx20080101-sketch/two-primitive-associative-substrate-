# 研究提案（RESEARCH-PROPOSAL）

日期：2026-09-06　状态：经用户确认（方向 = 科学验证）

## 0. 一句话课题

> **用最简单的 token 输入与"共现 + 扩散"链接，能否在受控实验中产生
> 可验证、可复现、且区别于朴素基线的行为效果？**

价值预设（用户明确）：不预设价值大小；验证优先。若能上量并达到
Transformer 级强度，那是远期问题，不在本课题承诺内，逐步解决。

## 1. 研究问题（可证伪）

Q1 结构是否从经验自组织？（无预置知识，仅共现）
Q2 频率/时间是否调制检索？（N、Δt 的作用）
Q3 方向不对称是否来自经验统计？（正走顺/倒走费力）
Q4 "语义"是否表现为共享情境拓扑？（翻译/同义绑定）
Q5 组块与位置记录能否承担"双写/整词"类任务？
Q6 角色能否由"输入标记/语序 + 绑定"承担？
Q7 强反馈是否不需要基底写侧调制？
Q8 系统能否做到"无中生有"闭包？

每问对应一组已注册判据的实验（E0–E19）。

## 2. 主假设与判定方式

主假设 H0：

> 在受控 token 输入下，"共现绑定 + 激活扩散 + 最小上层表示
> （方向/上下文/组块）+ 外部消费层"足以**确定性地**复现一组
> 联想/时序/语言式/价值式现象，且这些行为显著区别于
> 随机连接、纯计数（无扩散）、无方向、无组块等朴素基线。

判定方式（三件套）：

1. **机制演示**：现象 + 预期数值 + 判据（已有 E0–E19）；
2. **对照/消融**：去掉某机制后现象消失或显著退化（待补系统化指标）；
3. **稳健性**：同构现象在小→中规模（10×/100×）仍成立（gate 目标 C）。

本系统是确定性 toy 系统：结论形态是"在输入族 X 下必然产生行为 Y"
（充分性/必然性演示），不是人群统计显著性。若需要统计口径，
则对随机输入分布抽样，报告"成功率/命中率 vs 基线"。

## 3. 范围与终止线

### 范围（做）

- 受控 token 流：同步集合、有序流、带边界单元；
- 现象族：记忆/补全/方向/时间/翻译绑定/组块/角色/叠词外推/拼写/价值反馈；
- 对照与消融；小→中规模稳健性。

### 不做（显式排除，写进论文限制）

- 真实感知/运动接入（token 由实验给定）；
- 完整语法递归与系统性泛化证明；
- 人脑等同性主张（只报告"与若干现象一致"）；
- Transformer 级规模与效果对比（远期，不在本课题）。

### 终止线（报告一 = 可发表单元）

当满足以下全部条件时，本课题第一阶段结束：

1. 每个主假设有：演示实验 + 对照/消融指标 + 判据记录；
2. 规模一致性（gate C）至少在中规模复现关键现象；
3. 复现包完备：代码、README、判据表、结果 JSON、随机种子说明
   （当前系统确定性，无需种子；抽样实验需固定种子）；
4. 论文包：架构图 + 公式 + 实验表 + 限制声明 + 预印本/仓库链接。

## 4. 当前收获（可写入报告一）

- 架构：L0 冻结基底 + L1/L2/L4 + 外部 stub + 读取端；
- 19 个带判据的实验（E0–E19）全部 PASS，确定性可复现；
- 公式冻结（FORMULAS/INTERFACE/PARAMETERS）、复杂度基线（BENCHMARK）；
- 边界宪章与功劳记账（BOUNDARY）——方法学贡献；
- 明确证据等级：充分性演示，非人脑结论。

## 5. 待补证据（发布前缺口）

| 缺口 | 说明 | 对应工作 |
| --- | --- | --- |
| 系统化对照/消融 | 把现有"隐含对照"升级为统一指标表：命中率/消融前后比值 vs 基线 | E20 消融套件 |
| 规模一致性 | 关键现象在更大规模下重跑 PASS | 规模一致性指标（原 gate 目标 C） |
| 统计口径 | 确定性之外的抽样成功率（如需） | 报告时补充 |
| 论文包 | 图表、复现脚本、限制声明 | 写作阶段 |

## 6. 里程碑（映射现有 ROADMAP）

| 阶段 | 内容 | 出口 |
| --- | --- | --- |
| M0–M3 | 宪章、M1、M2、M3 四件套 | 已完成 |
| V1 验证补强 | 消融/对照套件 + 规模一致性 + 统计口径 | 证据缺口清零 |
| V2 发表 | 论文包 + 预印本/仓库 | 发布报告一 |
| S-上量（远期工程，原 M4） | 上量、真实输入、Transformer 级对比 | 另立课题 |

命名约定：M0–M3 = 工程准备（已完成）；V1/V2 = 科学验证与发表（当前主线）；
S-上量 = 远期工程上量（原"M4 上量"，编号已改以避免歧义）。

### V1 展开计划（下一步）

| 步骤 | 内容 | 出口 |
| --- | --- | --- |
| V1.1 | E20 消融/对照套件：有/无各机制 + 随机/纯计数基线，统一指标表 | 已完成(exp20_ablation_suite, 八行全 PASS) |
| V1.2 | 规模一致性：关键现象在 10×/100× 规模复跑 PASS | 已完成(exp21_scale_consistency, 六现象全 1.0) |
| V1.3 | 统计口径：随机输入族抽样的成功率（如需） | 已完成(exp22_sampling_statistics, Wilson 95% CI) |
| V2 | 论文包写作与发布 | 待启动（V1 证据缺口已清零） |

每个步骤动手前先列判据并经确认；不改 L0 冻结基线。

## 7. 一句话收束

我们不证明"这能造出大脑"，只证明：**极简的 token 链接机制，
在受控条件下能产生可验证、可复现、区别于朴素基线的行为——
这件事的证据值得被写出来、发出去。**
